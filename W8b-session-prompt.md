# W8b session prompt — Precons surfaces (`/precons` + share-page chrome + Start from this precon)

Pull latest, then run W8b — the ninth Wave-2 package. **`WAVE2.md` is the contract**; read its W8b section (the steps ARE the build order), **D7 (the page you are building — the ASCII sketch is the layout contract)**, and the W8 row of the pin matrix (`site-header.test.tsx` arrays + `access.test.ts` are already-updated W8a pins; do not regress them). W1–W8a are live (`e40342d`, `ce9d7c2`, `0da07cc`, `077401a`, `34a5d41`, `2c8a755`, `e1b94c1`, `1b1a7c4`+`89035ab`). W8a made the data exist; W8b renders it: the `/precons` index, the share-page precon chrome, the hub shelf, and **Start from this precon**.

Pre-flight, in order. (1) **Nightlies green** (`gh run list --workflow=nightly-ingest.yml`) — the precons step now runs nightly; its Sunday/dispatch FULL sweep saying `unchanged: 175` is healthy, not drift. (2) **A warm beta signal outranks a package**: owner posted / feedback / a stranger's 429 / a new issue → run `P2.9-session-prompt.md` (MTG) or `P4.7-session-prompt.md` round 3 (OP) first (2026-09-20 census baseline: 25 user decks — 24 MTG + 1 OP — 1 user, likes 1 · bm 0 · folders 1; **precons are NOT census**, `kind='user'` only). (3) Working tree clean at or after W8a's docs commit. (4) State your baseline: **884 tests / 114 files / 6 pre-existing `no-unused-vars` warnings / 0 errors** (measured 2026-09-20 on `89035ab`); db:size 266.4 MB (alert 350) — W8b adds no data, no before/after needed.

## What W8b is NOT (scope fence)

NOT One Piece starter decks (the `/precons` OP pill renders DISABLED with the D7 hint — the reason lives in `LATER.md` row "One Piece starter decks", not in UI copy), NOT pretty `/precons/<slug>` URLs (`/d/p_<slug>` is the address — LATER row exists), NOT hand-written blurbs (LATER row), NOT Collector's Edition products (LATER row, W8a skipped all 16), NOT autofill/doors (W9), NOT edits to the ingest or migration (W8a is closed; if a data defect surfaces, fix data in a W8a-shaped follow-up, not here). Anything else → `LATER.md` with a trigger.

## Facts you inherit (verified 2026-09-20 on `1b1a7c4` — re-grep lines, trust the shapes)

- **The data**: 175 `kind='precon'` deck rows ↔ 175 `precon_products` rows (2011–2026). `precon_products` = `deck_id` (PK, FK CASCADE), `code` (UNIQUE — MTGJSON fileName, e.g. `BreedLethality_C16`), `slug` (UNIQUE), `set_code`, `release_date` (date, nullable), `product_name`, `blurb` (**NULL everywhere** — reserved, see decisions), `source_hash`. `public_id = 'p_' + slug`. All precons: `visibility='public'`, owner/claim/folder/fork columns NULL (CHECK-enforced), `created_at = updated_at =` release date, `leaderIds`/`ciMask` denorms real, `decks.description` carries the generated factual blurb (already feeds og:description).
- **Test fixtures to use** (live in prod AND dev — same DB): `p_breed_lethality_c16` (Atraxa, C16, 2016-11-11, 83 rows / 100 cards — the D7 example URL) and `p_timey_wimey_who` (partner pair: The Tenth Doctor + Rose Tyler, WHO, 2023-10-13, 94 rows). Era sweep if needed: `p_counterpunch_cmd` (2011) … `p_blight_curse_ecc` (2026).
- **`kind` is already on the wire** (`deckMetaJson`) and every community surface already filters `kind='user'` (home rail, `loadHubDecks`, combos-smoke). The hub's "Precons led by {commander}" shelf is NEW read surface: `loadHubPrecons` per WAVE2 (public + `kind='precon'` + `leader_ids @>`), renders ONLY when non-empty, ABOVE "Decks with this commander". Atraxa is the live test: 2 precon leads (C16 + CM2 anthology) vs 1 user deck.
- **Share page seam**: `src/app/(site)/d/[publicId]/page.tsx` loads the deck; join `precon_products` there and pass a `precon` prop to `DeckShareView` (badge beside the format chip, meta line "Preconstructed deck · {Set} ({CODE}) · Released {Mon YYYY}", the D7 generated summary, byline swap to "Official product list · data via MTGJSON", **Start from this precon** beside Fork). Fork of a precon already works and mints `kind='user'` (W3 menus and engagement need no changes).
- **Seeding a draft**: `DeckEditor` must seed entries **directly from the precon endpoint into state with no `markDirty`** — NOT through `applyImport`: its merge (`src/lib/decks/import.ts:148-155`, re-grep) pushes `{cardId, zone, qty, tags}` and **drops `printingId`**, which would throw away the precon's own printings. The W4 `?leader=` latch + late-response guard in `deck-editor.tsx` is the pattern for a draft-safe `from=` param; `new-deck-chooser.tsx` latches `from` like it latches game/leader (sessionStorage pick-intent precedent). Acceptance is exact: opening the seeded draft fires **no `POST /api/decks`**; the FIRST edit produces exactly one POST and one 100-entry PUT (the `deck-editor.test.tsx` create-count pins are the guard — they must not move).
- **API**: `GET /api/precons/[code]` — public, `s-maxage=86400` (state the caching intent in the route comment, house rule), returns meta + `DeckCardWire[]`, **precon rows only** (a user deck's id/publicId → 404). Wire/cache-header precedents: `src/app/api/cards/[id]/printings/route.ts` (W5) for edge-cache shape, `src/lib/cards/wire.ts` does not exist yet (it's a W9a extraction) — the share page's card-loading path is the wire source to reuse. **Vercel rewrites API Cache-Control — trust `x-vercel-cache: HIT`, not the echoed header** (W5 lesson).
- **`/precons` page**: ONE static page (`revalidate = 86400`, no `searchParams` — client-side filter island), all 175 tiles in the HTML grouped by year, images lazy. `DeckTile` gains an optional `badge` prop (it renders the home rail and hub shelves — badge must be additive, existing snapshot/test pins untouched). Est. price on tiles per D7: cheapest-printing sum is per-deck aggregate data — decide the source honestly (see decisions).
- **Nav + sitemap pins**: `BROWSE_LINKS` at `src/components/site-nav.tsx:36-40` (3 entries today; Precons becomes the 4th); `site-header.test.tsx` has TWO arrays to update (desktop assertions ~:53-59 region, mobile menu array ~:77-84 — the WAVE2 pin's :70-74/:95-101 numbers have drifted, re-grep both); `/precons` into `src/app/sitemap.ts` staticPages (:53, re-grep). `seo-smoke:172-181` robots rows must stay green.
- **ISR gotchas**: hub-pages memory — prove ISR locally via `s-maxage`/`x-nextjs-cache`, on Vercel via `x-vercel-cache HIT`; hidden-pane tabs never hydrate until painted (screenshot or read HTML for the filter island; R6 memory).

## Verify-first list (never from memory)

1. **The D7 sketch against the real tile data** before building the filter island: pull the 175 rows once (year spread 2011–2026, 16 years → year groups), confirm color pills cover the ci range (colorless precons exist? check `ciMask=0` count) and that Sort options (Newest/Oldest/Name?) match what D7 shows.
2. **Start from this precon, E2E on dev**: seeded draft shows 100 cards WITH the precon printings (spot-check a Breed Lethality row's printing id against `deck_cards.printing_id`), zero POST until first edit, then exactly one POST + one PUT (Network tab or DB_LOG); Suggestions populates immediately; Undo/autosave behave like any draft.
3. **Unknown `from=`**: seeds nothing AND says so (visible copy, not a silent no-op).
4. **Share-page chrome on both fixtures**: badge, meta line, summary, byline, CTA — and an OP deck + a normal MTG user deck show NONE of it (`p_` prefix is not the test; `precon_products` join is).
5. **Hub shelf**: Atraxa hub shows "Precons led by" with 2 tiles above 1 community deck; a no-precon commander (any 2026 user-deck commander) shows no empty shelf.
6. **Home rail / recent decks / community counts unchanged** (they were proven clean in W8a; re-prove the rail after DeckTile gains `badge`).
7. **`pnpm check` + `smoke:hubs` + `smoke:seo`** green; header tests updated in lockstep with the nav.

## Design decisions to make explicitly (disclose + pin each)

- **`[code]` param of `/api/precons/[code]`**: `precon_products.code` (fileName, stable, ugly) vs `slug` (URL-ish, also unique). Pick one, 404 the other — do not accept both.
- **Est. price on tiles**: live sum over current cheapest prices (query cost on a 175-tile static page is one aggregate at revalidate time — fine) vs omitting until W8b's share summary needs it anyway. D7 draws it; if you ship without it, say so in the ship note and the tile design must not look amputated.
- **The D7 share-page summary** (colors, commander, curve, est. price today, five priciest) is computed at render — decide what comes from `decks.description` (the W8a factual sentence) vs computed live, and whether `precon_products.blurb` finally earns data or stays NULL (W8a reserved it; NULL is a fine answer).
- **Filter island state**: URL-less client state (D7 says no server searchParams — but decide whether filters reflect into `location.hash`/`replaceState` for shareability or stay ephemeral).

## Deployable outcome

`pnpm check` green and deployed; `/precons` live and in the nav + sitemap; both fixture share pages carry the chrome; Start from this precon proven E2E (no-POST acceptance exact); hub shelf live on Atraxa; nothing regressed on home/hubs/OP surfaces. Docs in the same package: `WAVE2.md` tracker W8b ticked with the sha; dated ship note in this file; LATER rows for anything fenced off; memory updated; **`W9a-session-prompt.md` written** the way this one was (it builds the autofill engine — carry the endpoint shape you shipped and the `wireSelect`/`toWire` extraction point `src/app/api/cards/resolve/route.ts:50-86` forward). Nothing posted; the owner's beta announce remains their call.

## Session notes (environment, updated by W8a)

- PATH per command: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node/pnpm + gh); psql/aws absent — DB probes via scripts/.tmp tsx (dotenv + postgres, db-size.ts pattern; delete probes after). `source .env.local` breaks on unquoted `&` in OAuth secrets — extract vars via dotenv, never source.
- **Dev shares the ONE Neon DB with prod** — precon rows are LIVE data; read freely, never mutate them by hand. QA drafts you create get deleted after (census 25 re-proven).
- `client.json()` crashes on the createDb (drizzle-wrapped) postgres client — `${JSON.stringify(x)}::jsonb` instead (W8a lesson; scryfall/spellbook's bare-client idiom doesn't transfer).
- Dev server: `preview_start {name: "dev-log"}` after `lsof -nP -iTCP:3000 -sTCP:LISTEN`; stop it before `pnpm build` (shared `.next`). `next dev` re-adds the AGENTS.md block — commit it with your work if it appears.
- Pane quirks: hidden pane stalls Base UI exits/toasts and never hydrates hidden tabs (R3/R6); signed-in and preview-dependent editor flows are RTL-only (pane signed out everywhere, W3); Base UI menus in `deck-editor.test.tsx` only open after the file's first test has rendered (LATER row — full-file order is fine; use `pollFor`, findBy* hangs under that file's fake timers).
- Prod curls need `--compressed`; deck-create budget 10/h + 30/day per IP — `pnpm counters:reset` if a QA battery trips it (never retry into a 429).
- Deploy verification: GitHub commit status context "Vercel", **the sha from `git rev-parse HEAD`, never retyped**.

## Context, not tasks

Sequence after W8b: W9a autofill engine → W9b review sheet → W9c doors → W10 tournaments. P2.9 (MTG) and P4.7 round 3 (OP) standing triggers remain live beside the W-series. Owner decisions of 2026-09-19 stand: ownerless official precons, evidence-based starter shell, plain buy links. Bandai/Azuki posture unchanged; OP ambient art stays off. Cold-start rule holds: precons render AS product lists — release-date timestamps, never "recent", never community counts. Premium never gates card data or prices.
