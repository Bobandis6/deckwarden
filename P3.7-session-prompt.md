# P3.7 session prompt

Pull latest, then kick off P3.7 from deckwarden-build-plan.md — M3's next
package (~line 208): Collection import v1 — ManaBox CSV (it carries
Scryfall IDs → exact-printing matching; competitors' importers are
notoriously buggy, do it cleanly) + Moxfield CSV → owned badges in the
builder + "you own N/100 of this deck, missing cost $Y"; feature-level
reference is deck-building-website-feature-plan.md's "Collection Mode"
section (~line 241) plus the "Owned versus missing" lines (~212, ~329).
Know what it is NOT before designing: it is NOT the full collection
manager UI (LATER row, post-M5 — no browse/edit/sort grid, no per-card
add-to-collection buttons, no public collections, no "cards assigned to
other physical decks"), not a mobile scanner (LATER: never), not price
history, not the "build only from my collection" MODE with a purchase
budget (the feature plan's Collection Mode is the full product; v1 ships
the DATA + the two read surfaces named in the plan line), and NOT the
tournament ranking-signal aggregate — both PASSED-gated rows in LATER.md
still wait for "the next M3 intelligence package slot" and P3.7 is a
data-import package; leave them alone. READ THIS FIRST — the schema head
start the plan promised does not exist: Appendix A (~line 413) sketches
`collections(user_id, printing_id, finish, quantity)` as "schema now, UI
later" and scripts/backup-user-tables.sh already lists `collections` in
USER_TABLES, but grep src/db/schema.ts and drizzle/*.sql — there is NO
collections table anywhere (verified 2026-09-01). Two consequences: (1)
this is the first user-data migration since M1 — `drizzle-kit generate`,
eyeball the SQL before applying, apply to Neon deliberately, commit the
migration with the package; (2) the R2 backup script would fail on a
missing table the day the R2 secrets are configured — the migration
closes that latent bug, but say so in the commit and re-check the list
against the real table name you choose. Design decisions to make
explicitly, each disclosed in code and test-pinned: (1) the table:
follow Appendix A's shape — PK (user_id, printing_id, finish), quantity
smallint, updated_at; user_id FK ON DELETE CASCADE (account delete must
take the collection — extend src/lib/profile/delete-account.ts's
transaction comment and the account smoke); printing_id FK to
card_printings (NO cascade — ingest never hard-deletes, is_removed rows
stay owned); one index by user. Collections are ACCOUNT-ONLY by
construction (user_id NOT NULL) — consistent with the P3.6 fork call and
the plan's "schema now": guests get a sign-in prompt, never a
localStorage collection. (2) Neon math out loud BEFORE the migration: a
row is ~60 bytes + two btree entries ≈ 150 bytes on disk; a 10k-card
ManaBox export is ~1.5MB; the table grows with users × collection size,
the second growing-with-usage table after deck_versions — cap rows per
user in the import (20k printings is a serious paper collection; disclose
the cap in the UI when hit), state the growth model in the schema comment,
and read db:size before and after (209.0MB on 2026-09-02, alert 350).
(3) Parsing is a PURE core (src/lib/collection/parse.ts): detect
ManaBox vs Moxfield by header row, tolerant of column order/BOM/quoted
commas/CRLF, output normalized rows {scryfallId?, name, setCode?,
collectorNumber?, finish, quantity, language?} plus per-line rejects
with reasons — vitest suite with REAL header rows (the plan's resource
line links ManaBox's guide + the StepKie/MtgCsvHelper community header
reference; Moxfield's export header is documented on their site — read
both live, no training-data guesses about column names). Resolution is a
second pure-ish step: ManaBox rows resolve by Scryfall ID directly
(card_printings.id IS the scryfall id — src/lib/cards/images.ts says so;
verify one real id from a real export round-trips), Moxfield rows resolve
by (set code, collector number) first and name second — which means the
setHint LATER row (P1.6 resolves by name only; the resolve route ignores
set codes) FIRES HALF-WAY: build the set+number resolver as a shared
function and note in LATER that the deck-import route can adopt it later
(don't widen scope to the paste importer — record it). Unresolved rows
are reported honestly (count + first N examples + a downloadable/
copyable reject list), never silently dropped; foil/etched/nonfoil
finishes map to card_printings.finishes and unknown finishes fall back
to "nonfoil" with a disclosed count. (4) Import semantics: decide
MERGE (upsert quantities, the default — re-importing a fresh export
should not double-count, so upsert SETS quantity per (printing, finish),
it doesn't add) vs REPLACE (wipe-then-insert, offered as an explicit
checkbox), one transaction, batched inserts (a 10k-row single VALUES
statement is fine on Postgres; chunk at ~2k to keep the request small),
rate-limited by user id (src/lib/rate-limit.ts precedent: session-only
routes key on user id; imports are rare — 10/hour is generous) and
body-size-capped in zod (a CSV of 20k lines is ~2MB; state the limit;
consider POSTing the parsed rows JSON rather than the raw CSV so parsing
stays client-side and the server validates a typed array like
/api/cards/resolve does). (5) The read side — two surfaces, both
account-gated and honest about identity-vs-printing: "owned" for a deck
entry means the user owns ANY printing of that identity (the deck
references identities; the chosen printing is cosmetic), so the owned
set is a per-user SET of card_identity_ids derived by joining
collections → card_printings — one query, cached per request; badge in
the editor deck list (a small "owned" mark, no layout shift) and the
share page? — decide: the share page is public and the viewer's
collection is theirs, so an "you own N/100 · missing ≈ $Y" line for the
SIGNED-IN viewer only, computed server-side like viewerEngagement;
missing cost = sum over unowned entries of qty × identity cheapest_usd
(the same price column the Cut Coach uses — disclose "cheapest printing
price" and that NULL-priced cards are excluded from the sum with a
count). (6) The recommendation engine's owned hook is the one contract
already waiting: src/lib/recommend/queries.ts `ownedCardIds` is INERT
and test-pinned as "the P3.7 contract" (queries.test.ts) — an empty set
means an empty pool, undefined means hook off. Wire it as an explicit
OPT-IN query param on GET /api/decks/[id]/recommendations
(?owned=1, session required to mean anything; a guest or a user with no
collection gets the hook OFF, never an empty pool) and a toggle in the
Suggestions panel ("only cards I own"), with the evidence payload
unchanged — this is the cheapest real slice of Collection Mode and the
feature plan's "prefer cards already in the user's collection" line;
smoke:recommend's contract extends to prove both states. API shape:
/api/collection (session-only, plain handlers + zod, dynamic no-store):
GET summary {rows, printings, identities, updatedAt}, POST import
{rows[], mode} → {inserted, updated, unresolved[], capped?}, DELETE
(wipe, confirmed in UI); the owned-set helper lives in
src/lib/collection/owned.ts and is consumed by the editor's GET
/api/decks/[id] (add `owned: string[]` of identity ids for the signed-in
owner ONLY — the guest path returns []), the share page, and the
recommendations route. UI: an Import collection section on /account
(file picker → client-side parse → preview counts + rejects → confirm →
POST; progress + result line), the owned badge in DeckListPane rows and
the "N/100 owned · missing ≈ $Y" line in the deck-list header next to the
size label (the P3.4 "Over by N" CTA precedent shows header wiring), the
Suggestions toggle, and the share-page line for signed-in viewers.
Deployable outcome = migration applied to Neon and eyeballed; ManaBox
and Moxfield exports parsed by the pure suite with real headers; an
import of a REAL export file round-trips against prod under a minted
session (you don't have the owner's exports — build the fixture CSVs
from real Scryfall ids fetched via the search API, plus at least one
deliberately-unresolvable line, and prove the reject path); the editor
shows owned badges + the N/100 line for a fixture deck; the Suggestions
"only owned" toggle restricts the pool and returns evidence; the
share-page line renders for the signed-in fixture user and NOT for a
signed-out request; account delete cascades the collection (extend
smoke:account); a new smoke:collection covering parse-error 400s,
merge-vs-replace, the cap, the owned set, and the recommendations
opt-in; pnpm check green, deployed, existing smokes green against prod
(mind the per-IP deck-create limiter — see notes).

Session notes:

* Toolchain: node/pnpm at ~/.local/node22/bin — non-login shells need
  export PATH="$HOME/.local/node22/bin:$PATH". `source .env.local` FAILS in
  zsh (unquoted `&` on line 6) — pull single vars:
  export DATABASE_URL="$(grep '^DATABASE_URL=' .env.local | cut -d= -f2- | tr -d '"')".
  gh and psql are NOT installed — GitHub REST via curl (token via `git
  credential fill`); ad-hoc SQL via a node one-liner with the repo's
  `postgres` package. Deploy verification: Vercel posts a commit status
  (context "Vercel") on the pushed sha; actions/runs?head_sha= needs the
  full 40-char sha. Scratch tsx scripts must live INSIDE the repo (a
  gitignored/untracked scripts/.tmp name) — the scratchpad can't resolve
  node_modules.
* Pre-flight: pnpm check on main AND the nightly Action. P3.6's session
  found the first unattended Topdeck night red on a transient upstream
  500 and fixed it (afdee33: fetchChunk retries 5xx/network errors with
  backoff; the db:size / purge / pg-client / backup steps carry
  `if: !cancelled()` so they run even when an ingest fails). Verify the
  latest scheduled run: all steps green, topdeck stats ~15-day window with
  small inserted counts, db:size under 350MB. A red step is triage BEFORE
  P3.7 work.
* What P3.6 shipped (2026-09-01, commit 2326f85 — not in the plan doc):
  deck_versions / decks.current_version / forked_from_deck_id are live.
  src/lib/decks/versions.ts (named versions, cap 50, row-locked writers,
  pre-restore safety snapshot, restore through the ONE card writer
  src/lib/decks/save-cards.ts which the editor PUT also uses — any new
  writer of deck_cards MUST go through writeDeckCards); src/lib/decks/
  diff.ts is the pure diff (zones × cards × qty; tags/printings ignored by
  design); src/lib/decks/forks.ts (account-only forks, v1 = frozen
  upstream baseline, credit states linked/private/none, and
  detachForksAndDeleteDecks — EVERY deck delete path goes through it:
  single DELETE, account delete, the purge script). API: /api/decks/[id]/
  versions (+/[version], +/restore), /fork, /upstream. UI: History dialog
  from the editor header, fork credit line (editor + share page), Fork
  button on share pages. scripts/versions-forks-smoke.ts (smoke:versions,
  47 checks) is the round-trip proof. Versions are owner-only (reasoning
  in LATER).
* Neon: 209.0MB on 2026-09-02 (alert 350, free ~512); deck_versions is
  empty in prod (real user actions only). P3.7 ADDS the first user-data
  table since M1 — eyeball the generated SQL, apply it once, commit it.
* LATER rows for THIS package: "Full collection manager UI" stays (its
  trigger is post-M5 retention; P3.7 lands only import + owned badges, as
  the row itself says); the setHint row fires HALF (set+number resolver
  built for Moxfield CSV, paste importer adoption recorded as the
  remainder); the two tournament-signal rows stay PASSED-gated. Add rows
  for whatever Collection Mode slices you leave out (budget mode,
  shopping list export, printing preference) with triggers.
* Rate limiter interplay learned in P3.6: the per-IP deck-create bucket
  is 10/hour and forks consume it too. Running every smoke back to back
  against prod exhausts it — run smoke:collection and smoke:versions
  first, then the others; a burst of "deck create failed" mid-run is the
  limiter (fixed hourly windows reset at :00 UTC), confirm with one curl
  (429 + Retry-After) rather than debugging a phantom regression.
* Smoke rules against prod: BASE_URL=https://deckwarden.gg pattern;
  smoke:recommend creates/deletes its own guest deck; smoke:combos and
  smoke:hubs are DB-connected; smoke:seo's ONE known failure is "core
  sitemap lists the public deck" (revalidate lag) — anything else is
  real; engagement/account/versions smokes mint signed sessions
  (BETTER_AUTH_SECRET pattern in scripts/engagement-smoke.ts) — the
  collection smoke must too (session-only routes). Guest-deck cleanup:
  x-deck-token DELETE, local AND prod.
* Prod verification: the owner is mid-seeding REAL featured decks and has
  NO collection imported yet — never import into, wipe, or read the
  owner's account as a fixture. Mint your own throwaway user (the P3.6
  scratch fixture pattern: minted session → cookie set via document.cookie
  on the prod origin → editor/account pages render signed-in → DOM reads
  via javascript_tool), and delete the user (cascade takes the collection
  — prove it) when done.
* Browser pane: hidden pane swallows computer clicks silently — drive via
  javascript_tool, verify via DOM/API reads; file inputs can't be driven
  by the pane, so the import UI's parse step should be a pure function the
  smoke and a JS-driven fetch can exercise without a real <input
  type=file>.
* Context, not tasks: beta announcement still deliberately deferred;
  permission emails to Bandai + Azuki Labs out 2026-08-30; cold-start rule
  stands — no simulated activity (no seeded collections, no fake "you own
  N" numbers).
* Protocol as usual: one package, ends deployed and check-green; scope
  leaks to LATER.md with a trigger.
