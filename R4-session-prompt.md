# R4 session prompt — Responsive builder (the `wide:` three-pane tier, the `md`–`wide` two-pane tier with the tools Drawer, the phone Deck / Search / Tools tabs with every pane kept mounted, the Add cards action, the card sheet on explicit inspection, `DrawerVirtualKeyboardProvider`, 44 px targets, safe-area insets, the footer off the editor)

Pull latest, then run R4 — the seventh R-series package and the last one before the closing R6 pass. **`REDESIGN.md` is the contract (read §1 Mechanics — game accent, motion, primitives, and the Breakpoints line: Tailwind defaults plus `--breakpoint-wide: 75rem` for a `wide:` variant; §2 "Desktop builder" in full for the pane order that must survive, and "Responsive structure" in full — the three tiers, the phone list, `DrawerVirtualKeyboardProvider`, bottom safe-area insets, 44 px targets, the footer hidden on the editor route; §3 for R2's ambient layer and chip, which must survive pane stacking; §4 "Interaction language" (everything `motion-safe:`) and the deferral list; the R4 row in §5 with its "done when"; §6 — the R3 block (the pane widths, the 1024 px deck-pane squeeze it left for you, the sticky blocks, `keepMounted` panels, the hidden-pane stalls), the R2 block (the chip's sticky row and why it is bottom-left), the R5b block's environment notes, the smoke-pin ledger and the LATER-row ledger (nothing fires here; the ledger's "row numbers are historical identifiers" note applies); §7 Functional checks — "Mobile tab changes and responsive transitions preserve editor state" — and the Visual checks)**; build plan §6b makes R-packages legal sessions under the CLAUDE.md protocol. R4 is the responsive builder: the same editor, the same keyboard script, the same draft contract, laid out in three tiers — 1200 px and above (`wide:`) three panes, about 320 px search · flexible deck · about 320 px tools; 768–1199 px (`md:`) search and deck side by side with the tools in a Base UI Drawer; below 768 px one active pane under persistent Deck / Search / Tools bottom tabs with every pane kept mounted, opening on Deck with an obvious **Add cards** action leading to Search, search usable for repeated additions without switching tabs after each card, card details opening in a bottom sheet (a Drawer) only on explicit inspection and never while typing, Tools holding analytics, the sample hand and whichever coaching panels the adapter supports, query / scroll position / tool state preserved across tab changes, `DrawerVirtualKeyboardProvider` for the software keyboard, bottom safe-area insets, touch targets of at least 44 px, and the site footer hidden on the editor route. Expect ZERO migrations, ZERO new dependencies, ZERO new URLs (the route table diff is EMPTY — `/decks/new` stays `○`, `/decks/[id]/edit` stays `ƒ`, the 55 glyph lines unchanged), ZERO API or wire changes, no ingest or data work, the keyboard script unchanged at every tier that has a keyboard (`/`, `4 Sol Ring`, arrows wrapping, Enter, Ctrl+Enter, Escape, `?`), the first real edit still creating exactly ONE deck and nothing else ever creating one (a tab change, a drawer, a sheet, a resize, a theme or appearance change — none of them a POST and none of them a dirty mark), and the (site) pages unchanged except the footer's new home. The owner's decisions are settled (§0 and §6): the sequence R4 → R6, the restrained fun ceiling, One Piece ambient art off until Bandai answers, mascot = shield mark only, the "1200 px" tier is a custom `wide:` variant (not a Tailwind default), the phone gets tabs (never a stacked page). Do not relitigate them.

Pre-flight, in order. (1) Nightlies since 2026-09-11 all green (`gh run list --workflow=nightly-ingest.yml` — from the shell, NOT from a python `subprocess` with a stripped `env`: `gh` needs HOME, and an env-stripped call fails auth and drops a stray `./.local/state/gh/` into the tree; cron 10:37 UTC drifting ~13:30–17:40Z; 2026-09-10's ran 14:37Z green; 2026-09-11's is the FIRST nightly on R5b code — R5b changed no ingest and reads nothing the ingest writes differently, so a red run is P4.7 branch F and takes precedence: fix or report it as P4.7, and R4 waits for its own session). (2) Working tree clean at or after `9776761` (the R5b docs commit; R5b's code is `15fa66f`) plus this prompt's commit. (3) db:size baseline stated (254.6 MB on 2026-09-10 before and after R5b's dev pass; Neon alert 350 MB) — R4 adds no rows by hand; the pane's drafts are the only rows you mint, through the editor's own first-edit path, and every one is deleted through Details → Delete deck… (or `DELETE /api/decks/:id` with its claim token) and confirmed gone by query; the API smokes mint and delete their own. (4) QA fixtures unchanged, read from the DB (R5b's readout at `15fa66f`): the FIVE public decks — `/d/q9j9uxphwp9n` Queza (MTG, ci 7, "Queza, Augur of Agonies", 100 / 100, description "Esper draw-combo · High power · Intended for Commander Bracket 4"), `/d/uwvrnv2pv4t6` Atraxa Superfriends (MTG, ci 23), `/d/4c5_9yd9ewdy` BOROS EQUIPMENT BURN (MTG, ci 9, Eivor), `/d/end97r37znuv` Nelson & Murdock (MTG, ci 9, Daredevil — the one FILED deck, in the owner's unlisted folder `veunykz3yuje` "custom decks"), `/d/jhr5ax43ewx7` "Six DON!!, Endless Thunder" (One Piece, ci 32 Purple, Enel OP15-058, 50 / 50, the only like) — all on the owner's account (`bobandis6`, the only user), the owner's three unlisted "Untitled" decks (`28vsuc6j54pf`, `avzq42djrda7`, `2un8xrmq5wrs`), seven anonymous decks (four private, three unlisted — old walk fixtures, not yours), 16 decks in all, 1 like, 0 bookmarks, 0 collection rows — never yours to touch. The browser pane is signed out on prod AND on dev and holds no claim token for any of them, so **the pane's editor fixtures are the drafts it mints itself** (`/decks/new?game=mtg`, `/decks/new?game=optcg&leader=OP15-058`); the owner's decks are never opened in the editor by you. The owner's open looks — the `/account` sections, the signed-in deck header, the owner's `/f/` view (R5b), the signed-in home and `/account` tiles (R5a), the Background art toggle (R2), the Danger zone — leave no trace; state whether the owner did them (R5b could not tell: the only evidence is `auth:…|/get-session` rows in `rate_limit_counters`, which any visitor produces). (5) The deck-create limiter (10/h + 30/day per IP; `deck-create:ip:::1` on dev) read BEFORE the pass and the mints planned: every draft's first edit is one POST; `smoke:decks` 2, `smoke:versions` 2 (+ 5 forks, which consume the same limiter), `smoke:optcg-deck` 1, `smoke:collection` 1 (run it first — its budget note); `smoke:seo` DELETES the local `deck-create:*:::1` counters at its start (its line 107), which is why the counter under-counts after a seo run — read it right before you rely on it. (6) `pnpm check` green at the start (684 tests in 94 files at `9776761`, the same 6 pre-existing `no-unused-vars` warnings, 0 errors) and `pnpm build`'s route table saved before and after (the glyph lines with sizes stripped — python over the build log, the R5a/R5b method; 55 lines including the legend): the diff must be EMPTY. No `shadcn add` is expected (`drawer.tsx`, `sheet.tsx`, `tabs.tsx`, `scroll-area.tsx`, `collapsible.tsx` all exist under `src/components/ui/`); never `shadcn init`.

What R4 is NOT (scope fence)

NOT R6 (C9 `loading.tsx` skeletons, G6 analytics bars, G9 the curve ghost, F4 the dealt hand's stagger, the six-width audit in both themes, reduced motion, contrast, focus order, the `/cards` restyle) — R4 makes the layout right at 375 / 768 / 1024 / 1200 / 1440 and measures it; R6 audits everything. NOT a change to the keyboard script, the search reducer (`src/lib/decks/search-state.ts`), the autosave engine, `ensureDeck` / the draft contract, `applyEdit`, the toast + Undo, the leader replace, the hotkey guard, the tag editor, import / export / history / share / details (they stay `Modal` dialogs; their phone fit is R6's audit), or any API (`git diff --stat src/app/api src/lib/decks/editor-state.ts src/lib/decks/search-state.ts src/components/editor/use-autosave.ts src/components/editor/use-editor-hotkeys.ts` is empty). NOT the share page, the hubs, home, `/account`, `/u`, `/f`, the 404 / error pages beyond the footer relocation (R5b's), NOT the OG images, NOT `SiteHeader` (the editor never renders it). NOT a new persisted preference — the active phone tab is session state, not a `localStorage` key; the View / Group / Sort prefs stay under `deckwarden:deck-view`, the index view under `deckwarden:index-view`, appearance under `deckwarden:appearance`. NOT card-stacks (LATER's card-stacks row, line 61 today), NOT multi-step undo (line 60), NOT quick-add by One Piece card number (line 62), NOT ⌘K, draggable panels, extra theme families, a Warden illustration. NOT One Piece art anywhere (R2's gate stands; the One Piece editor keeps the gradient). NOT a change to R2's `AmbientArt` beyond an inset for its sticky chip row (a prop, no behavior change — its test stays green as is), NOT to `SurfaceHeader`, `CardNamePreview`, the tiles. NOT `global-error.tsx`. NOT a Next, React, Tailwind or Base UI upgrade. NOT any P4.7 branch unless pre-flight (1) forces it, and then R4 is not this session.

Facts you inherit (verified 2026-09-10 against `9776761` — trust these, spot-check nothing)

* LATER row numbering. "Row N" labels are HISTORICAL identifiers; today's lines: multi-step undo 60, card-stacks 61, OP quick-add 62, OP null-cost 63, the claim nudge 64, the `/c/` CTA row (FIRED in R5b) 65, the R1b ISR row 71, the r2.dev row 51, the goldfish row 14 (sample-hand polish adds no draw-next — R6's, and untouched here). Name rows by content in everything you write. R4 fires nothing on the ledger; the R3 block's "the 1024 px deck-pane squeeze is R4's" is the one debt you pay.
* The editor root (`src/components/editor/deck-editor.tsx`, 1016 lines). `<div className="relative isolate flex min-h-dvh flex-col lg:h-dvh" data-game={load.adapter.id}>` (818) — ONE breakpoint, `lg` (1024): below it the three sections stack and the document scrolls; from it the root is a fixed-viewport `h-dvh` and each section scrolls itself. `<Toaster timeout={TOAST_MS} viewportProps={{ "data-game": load.adapter.id }} />` (822; `viewportProps` is `ToastPrimitive.Viewport.Props & Record<data-…>`, so `className` / `style` pass through). `EditorHeader` (823–835). The dialogs under `ModalFinalFocus.Provider` (837–889; `dialog` state 190, `dialogFromMenu` 194, `moreRef` 195). The grid `<div className="min-h-0 flex-1 gap-0 lg:grid lg:grid-cols-[minmax(20rem,26rem)_minmax(0,1fr)_minmax(16rem,22rem)]">` (891): `<section aria-label="Card search" className="min-h-0 border-b lg:overflow-y-auto lg:border-r lg:border-b-0">` (892–904) holding `<SearchPane ref={searchRef} … onAdd={handleAdd} onPreview={showCard} />`; `<section aria-label="Deck list" className="min-h-0 border-b lg:overflow-y-auto lg:border-b-0">` (905–924) holding `DeckListPane … onOpenCuts={load.adapter.recommend?.cuts ? openCuts : undefined} onChooseLeader={focusSearch}`; `<section ref={rightPaneRef} aria-label="Card detail and suggestions" className="min-h-0 lg:overflow-y-auto lg:border-l">` (925–1010): when `tabbed` (812 — `recommend || capabilities.combos`, i.e. Magic) the R3 Base UI `Tabs value={rightTab} onValueChange` (934–938) with a `h-10` strip row (939–951: `TabsList variant="indicator" aria-label="Right pane view"`, `TabsIndicator`, triggers Card / Suggestions / Combos / Cuts) and four `TabsContent keepMounted` panels (952–997) — `CardDetailPane`, `RecommendationsPanel`, `ComboRadarPanel`, `CutCoachPanel`, each gated by `active={rightTab === …}` (their fetch effects bail on `!active`: recommendations-panel 105 / 140, combo-radar-panel 82 / 110, cut-coach-panel 93 / 121 — results survive a switch because the panels never unmount); One Piece (`tabbed` false) renders a plain "Card" `h2` in an `h-10` row above `CardDetailPane` (1003–1008). `AmbientArt` is the LAST child (1013). The math at 1024 px: 416 + 352 px of outer maxima leave the deck pane 256 px (the R3 block's measurement) — the squeeze R4 owns. State you will route: `rightTab` (206, `RightTab = "card" | "suggest" | "combos" | "cuts"`), `preview` (189), `showCard = setPreview + setRightTab("card")` (556–559 — called by the search pane's previews AND adds and by every deck-row click, so today there is NO distinction between a passive preview and an explicit inspection), `openCuts = setRightTab("cuts") + rightPaneRef.current?.scrollIntoView({ behavior: "smooth", block: "start" })` (627–630, "on stacked mobile, bring the right pane into view" — the stacked page is going away), `focusSearch = searchRef.current?.focus()` (514), `useEditorHotkeys({ enabled: dialog === null, onSlash: focusSearch, onHelp: openShortcuts })` (523), `handleAdd` (561–600: `setCards`, `showCard(card)`, the leader replace, `applyEdit`, the toast with Undo), `handlePanelAdd` (605–615: quiet, no tab flip), `ensureDeck` (237–270: single-flight `POST /api/decks` on the FIRST flush with something to say, `history.replaceState` to `/decks/[id]/edit`), `applyEdit → markDirty` (496–506), `hydrate` (305–361), the leader seed (370–403), the load states (793–808: "Loading deck…" in a `min-h-screen` main). The docblock's line "Mobile (<lg): panes stack and the page scrolls; the header wraps to two rows" (815–817) describes what you replace.
* The header (`src/components/editor/editor-header.tsx`, 172 lines): `<header className="flex shrink-0 flex-wrap items-center gap-x-2 gap-y-1.5 border-b px-3 py-2 lg:h-14 lg:flex-nowrap lg:py-0">` (72); the mark (73–79), the chip (80–82), the name input `order-last min-w-0 basis-full … lg:order-none lg:flex-1 lg:basis-auto` (83–90 — its own row below `lg`), the fork credit (92–97), the right cluster `<div className="ml-auto flex items-center gap-1.5 lg:ml-0">` (98–129): `SaveIndicator` (`w-36`, 140–172), Share (`canShare`), the More menu (Details · Import · Export · History when `canHistory` · Keyboard shortcuts `?`), `AppearanceMenu`. `editor-header.test.tsx` (134 lines) pins the mark → `/`, the chip, the input, exactly one appearance control and no site nav, the fixed-width slot across four states with Retry outside the menu, Share only with a row, the More items; keep every one.
* The search pane (`src/components/editor/search-pane.tsx`, 289 lines): `<div className="flex h-full flex-col">` (146); the sticky block `bg-background sticky top-0 z-10 px-3 pt-3 pb-1` (149) with the combobox `<input … autoFocus … role="combobox" aria-controls="quick-add-results" aria-activedescendant …>` (150–166), the keycap hint (167–176), the live line (177–185); the listbox (188–274) with rows `group/row flex cursor-pointer items-center gap-2 rounded-md px-2 py-1 text-sm` (203–206), `onClick` → `select` + `onPreview(card)` (207–210), `onDoubleClick` → add (211), the thumbnail box (216–224), the actions span `hidden shrink-0 gap-1 group-aria-selected/row:flex group-hover/row:flex pointer-coarse:flex` (243 — the ONE `pointer-coarse:` in the tree, pinned by `search-pane.test.tsx` 152) with `Add` and `{leaderNoun}` `size="xs"` buttons (244–269); `add()` refocuses the input after a successful add (117 — the "repeated additions" behavior already exists on desktop); the debounced fetch previews `cards[0]` on every response (96) and `moveSel` previews on arrows (124) — both PASSIVE and both through the one `onPreview` prop (52). `search-pane.test.tsx` (191 lines: fake timers `toFake: ["setTimeout", "clearTimeout"]`, `fetch` stubbed, `renderPane`, `type`, `settle`) pins the combobox contract, wrap-around, Enter / Ctrl+Enter, "No cards match" only after a response, Retry, the live line, the active row's actions, the thumbnail box, keycaps, no success line.
* The deck pane (`src/components/editor/deck-list-pane.tsx`, 245 lines): `<div className="p-3">` (125); the summary row (127–155: `h2 "Deck"`, the over-limit `Over by N — rank cuts` button 132–140, the ownership line, `{sizeLabel} cards`, `CompletionRing`); `ValidationPanel` (157–162); `LeaderZone … onChooseLeader` (164–173); the three `Segmented`s persisted through `saveViewPrefs` (176–204; `view-prefs.ts` line 23 `KEY = "deckwarden:deck-view"`); the empty state `<EmptyState className="mt-4" title="No cards yet" hint="Add them from Search." mark />` (214, with R1b's comment "a phone has no 'left' — R4 puts Search in a tab"); `DeckTextView … stickyHeaders pop` (216–226) / `DeckGridView … stickyHeaders` (228–235); `<AnalyticsPanel blocks={analytics} />` (238 — a CLOSED `Collapsible`, analytics-blocks.tsx 164–180); `<SampleHand … />` (242 — an OPEN `Collapsible`, sample-hand.tsx 51). Rows (`src/components/deck/deck-text-view.tsx`): `li.group/row … px-1 py-0.5 text-sm` (128 — about 28 px tall), steppers and remove as `icon-xs` ghost Buttons (24 px; 134 / 154 / 179) revealed only on hover or focus-within (`REVEAL_CLASS`, 35–36) — invisible to a finger; the name button (R5b's `preview` opt-in is off in the editor). Grid cards (`deck-grid-view.tsx`) and the leader zone's card buttons (`leader-zone.tsx` 77–97) are `onPreview` clicks.
* The card detail pane (`src/components/editor/card-detail-pane.tsx`, 159 lines): the empty text "Search or click a card to see it here." (40–45), `CardImage … mx-auto w-full max-w-72` (51–57), name · pips · `{idBadge} · {subtitle} · {statLine}`, body text, the `TagEditor` with an `<input aria-label="Add tag">` (124–140 — the one text field a phone sheet will host, i.e. the software-keyboard case `DrawerVirtualKeyboardProvider` exists for), the price + "Card page" link.
* Layout, footer, viewport. `src/app/layout.tsx`: `<body className="min-h-full flex flex-col">` → `ThemeProvider` → `{children}` + `<SiteFooter />` (53–58) — the footer renders on EVERY page, the editor included ("on app-like full-height pages (the editor) it sits below the fold, which is intentional" — its docblock, `src/components/site-footer.tsx`, 45 lines: the Scryfall / Fan Content line and Legal · Privacy · Contact · GitHub). `src/app/(site)/layout.tsx` (18–29) renders `<SiteHeader />{children}` only — plain and synchronous ON PURPOSE (no `headers()`; `/c/`, `/l/`, `/cards/[id]` are ISR). The editor routes: `src/app/decks/[id]/edit/page.tsx` (`force-dynamic`, `<DeckEditor deckId={id} />`, no site header — 21–26) and `src/app/decks/new/page.tsx` (a static shell: `Suspense` + `NewDeckChooser`); the chooser (`new-deck-chooser.tsx`, 93 lines) renders `<SiteHeader />` itself in the picker branch (58) and `<DeckEditor deckId={null} draftGame draftFormat draftLeaderKey />` in the editor branch (44–49). The root `not-found.tsx` and `error.tsx` render `SiteHeader` themselves since R5b (they sit outside `(site)`); `(site)/not-found.tsx` renders the body alone under the group layout. NO `viewport` export exists anywhere in `src/app` (grep), so Next emits its default `width=device-width, initial-scale=1` and `env(safe-area-inset-*)` reads 0 on a notched phone until `viewport-fit=cover` — `export const viewport: Viewport = { … viewportFit: "cover" }` is legal from a `layout.tsx` OR a `page.tsx` (Next 16.3.2 docs, `generate-viewport.md`).
* Breakpoints and CSS. NO `--breakpoint-wide` exists: `src/app/globals.css` has `--container-reading / browse / wide` (39–43) and nothing else named wide, so `wide:` is not a variant yet — add `--breakpoint-wide: 75rem` to the `@theme inline` block (Tailwind 4 derives the `wide:` variant from `--breakpoint-*`; verify the compiled CSS carries `@media (width >= 75rem)` before trusting a `wide:` class). `src/lib/theme/tokens.test.ts` parses only the `:root` and `.dark` blocks (16–37) — unaffected. The reduced-motion backstop (172–180) zeroes every animation and transition; `motion-safe:` on anything decorative you add. `min-h-dvh` / `h-dvh` are already the editor's units (818); `Modal` panels are `max-h-[85dvh]`; the Drawer primitive sizes itself in `dvh`.
* Primitives installed and unwired (R5b's list, unchanged): drawer, sheet, scroll-area, tooltip, skeleton, input, select, toggle (only through toggle-group), separator, progress, popover, navigation-menu. `src/components/ui/drawer.tsx` (208 lines) wraps Base UI Drawer: `Drawer` (root; `modal` default true, `showSwipeHandle`, `snapPoints`, `swipeDirection` default `"down"`), `DrawerTrigger`, `DrawerClose`, `DrawerContent` (= Portal + Backdrop when modal + Viewport + Popup + optional `DrawerSwipeHandle` + `Drawer.Content`; the popup is `fixed z-50`, `data-[swipe-axis=x]:[--drawer-content-width:75%] … sm:[--drawer-content-width:24rem]` for left / right drawers and `max-h calc(100dvh - 6rem)` for bottom ones, with `bg-popover` and the swipe transforms), `DrawerHeader / Footer / Title / Description`. Base UI 1.7.0 root props (`node_modules/@base-ui/react/drawer/root/DrawerRoot.d.ts`): `open` / `defaultOpen`, `modal: boolean | 'trap-focus'` (true = focus trapped, page scroll locked, outside pointer disabled; `'trap-focus'` = focus trapped, scroll and outside pointer free; false = nothing trapped), `onOpenChange`, `onOpenChangeComplete`, `disablePointerDismissal` (and for non-modal drawers no close on focus leaving), `actionsRef`, `handle` / `triggerId`, `swipeDirection`, `snapPoints: (number | string)[]` (0–1 = fractions of the viewport height, > 1 = px, or `'px'` / `'rem'` strings), `snapToSequentialPoints`, `snapPoint` / `defaultSnapPoint` / `onSnapPointChange`. `Drawer.Portal` takes `keepMounted` and `container`; `Drawer.SwipeArea` is an invisible edge area that OPENS the drawer on a swipe (direction defaults to the opposite of the root's); `DrawerVirtualKeyboardProvider` ("keyboard-aware focus and scroll handling for bottom-sheet drawers with form fields", children only); `DrawerProvider` coordinates indent / background effects (not needed). `ui/sheet.tsx` is a Dialog-based side panel (R3 passed on it for the shortcut sheet); `ui/tabs.tsx` is R3's (`TabsList variant="indicator"`, `TabsIndicator` on `--active-tab-left/-width`, `TabsContent`); `Segmented` sits on toggle-group. Base UI's Tabs list / tab, the Drawer popup, its snap points and the keyboard provider reference `ResizeObserver` — jsdom has neither it nor `matchMedia`; no test stubs `ResizeObserver` today, `ambient-art.test.tsx` and `use-count-up.test.tsx` stub `matchMedia`, and NO RTL test renders Base UI Tabs or `DeckEditor` yet.
* Two things that ride the viewport's bottom edge: the toast viewport (`src/components/ui/toast.tsx` 32: `pointer-events-none fixed inset-x-4 bottom-4 z-50 mx-auto w-auto max-w-sm … sm:right-4 sm:left-auto sm:mx-0 sm:w-full` — centred 16 px up on phones, bottom-right from `sm`) and R2's credit chip (`src/components/deck/ambient-art.tsx` 167–170: a `pointer-events-none sticky bottom-0 z-20 h-0` row at the surface's END with the `<p>` at `bottom-2 left-3`, bottom-LEFT because toasts own bottom-right). A fixed bottom tab bar covers both unless they are raised by its height plus the safe-area inset.
* Hotkeys and dialogs: `useEditorHotkeys` (`use-editor-hotkeys.ts`, 47 lines) — one window listener, `enabled = dialog === null`, `isTextTarget` skips INPUT / TEXTAREA / SELECT / contenteditable and anything inside `[role="dialog"], [role="alertdialog"], [role="menu"]` (30) — a Base UI Drawer popup is a `dialog`, so keys pressed inside one are inert already; `dialog` state names only the six `Modal` dialogs, so an open Drawer or sheet does not switch `/` and `?` off by itself. `ModalFinalFocus` returns focus to the More trigger for menu-opened dialogs (R3). The `?` sheet is a `Modal` (R3 passed on `Sheet` for it, `shortcuts-sheet.tsx` 3–9).
* Tests: 684 green at `9776761` (94 files). Precedents: `editor-header.test.tsx`, `search-pane.test.tsx`, `use-editor-hotkeys.test.tsx`, `use-leader-art.test.tsx`, `use-count-up.test.tsx` (matchMedia stubbed), `src/components/deck/deck-share-view.test.tsx` (mocks `next/navigation`'s `useRouter`, renders a full 100-card deck, fake timers), `ambient-art.test.tsx` (matchMedia via `vi.stubGlobal`, `fireEvent.load`), `site-header.test.tsx` (auth client mocked; Base UI menus open on the pointer sequence). Lint: `react-hooks/refs` and `react-hooks/set-state-in-effect` are errors (the previous-render pattern; setState inside a timer or a subscription callback is fine); every raw `<img>` needs `CardImage` or the eslint-disable line; a raw `<a>` to an internal path fails `@next/next/no-html-link-for-pages`.
* Smokes: none reads the editor's DOM (client-rendered; `/decks/[id]/edit` SSRs "Loading deck…"). API-level smokes R4 must not break: `smoke:decks` (deck-api-smoke; 2 creates), `smoke:versions` (2 creates + 5 forks), `smoke:optcg-deck` (1), `smoke:collection` (1, run first). `smoke:seo` and `smoke:engagement` read the home / share / hub HTML, which R4 does not touch except through the footer (seo-smoke pins "Scryfall" on `/cards` and hubs-smoke on hubs — both have their own credits; the footer's Legal / Privacy links are not pinned) — run `smoke:seo` once anyway after the footer move (it mints 2). Prod: the editor is provable signed out ONLY as a draft (`/decks/new?game=mtg`, `?game=optcg&leader=OP15-058`) with zero creates — never a first edit on prod.
* Corpus and environment: 16 decks / 1 user / 1 folder; 4,023 slugged Magic leaders, 142 One Piece leaders. `.claude/launch.json` (gitignored) has `dev-log` (`DB_LOG=1`; `preview_logs search:"[db]"` counts statements — the editor's loads: `GET /api/decks/[id]` for a real deck, none for a draft); dev output is isolated in `.next/dev`, so `pnpm build` runs beside `next dev`; a stale dev server from a finished session may hold port 3000 (`lsof -nP -iTCP:3000 -sTCP:LISTEN`, root `…/Claude.app/Contents/Helpers/disclaimer -- /bin/zsh -c … pnpm dev`) — kill it and `preview_start {name: "dev-log"}`. Route table: 55 glyph lines at `9776761`.

Verify-first list (never from memory)

1. The keyboard script, unchanged, on DEV at 1440 px (`wide:`) in a guest draft (`/decks/new?game=mtg`): `/` focuses search from the deck pane; type `4 Sol Ring` (the pane's `Return` does not reach the combobox — dispatch `new KeyboardEvent("keydown", { key: "Enter", bubbles: true })` on the input); ArrowDown / ArrowUp wrap and preview into the Card tab; Enter adds 4× and toasts with Undo; Ctrl+Enter (dispatched with `ctrlKey: true`) adds a leader; Escape clears; `?` opens the sheet and pauses the keys. Exactly ONE `POST /api/decks` for the whole draft (count with `performance.getEntriesByType("resource")` — `read_network_requests` sometimes records nothing) and the URL swapped in place to `/decks/<id>/edit` with the search input keeping focus; the save slot reads "Saved" after the debounce. Then delete the draft through Details → Delete deck… and confirm the row is gone by query.
2. Tiers, measured with `getBoundingClientRect` (1024 / 1200 / 1440 screenshots fail in the hidden pane; 375 and 768 screenshot fine): at 1440 three panes with the search and tools sections about 320 px and the deck section the rest (state each width); at 1200 the same three panes (the boundary is INCLUSIVE — `wide:` fires at ≥ 75rem); at 1199 and 1024 two panes (search | deck) with NO tools section inline, the deck pane wider than 256 px (state it), and a Tools control in the header; at 768 the same two panes; at 375 one pane with the bottom tab bar. No horizontal overflow at any width (`document.documentElement.scrollWidth === innerWidth`). `pointer-coarse:` styles: check `matchMedia("(pointer: coarse)").matches` under the pane's mobile preset (it emulates touch — verify rather than assume; if false, prove the coarse-pointer rules by class presence and a `resize_window`-independent forced check, and say so).
3. The phone flow (mobile preset, then RELOAD — the preset switches the UA): the editor opens on Deck; the tab bar shows Deck · Search · Tools with the Deck count; **Add cards** switches to Search and focuses the box; add three different cards (type, dispatch Enter each time) WITHOUT leaving Search — the toast confirms above the bar each time (measure: the toast viewport's bottom sits above the bar's top), the Deck tab's count climbs; switch to Deck: the three rows are there; switch back to Search: the query, the results and the list's scroll position are exactly as left (scroll the results before switching; read `scrollTop` after returning); Tools shows Analytics, the Sample hand and the Card / Suggestions / Combos / Cuts strip (Magic) or the single Card panel (One Piece); the whole flow makes exactly ONE `POST /api/decks` (the first add) and zero more across every tab change, drawer, sheet, theme or appearance toggle.
4. Panes stay mounted: on the phone, all three sections are in the DOM at once (count `section[aria-label]` — three — and check the inactive ones carry `hidden`); switching tabs never remounts them (hold a reference to the search input element before the switch; after two switches `document.querySelector('input[role=combobox]') === held`); the tools panels keep their fetched results across a tab switch (the Suggestions fetch count does not grow when you leave and return). The same on `md` for the Drawer: open Tools, switch to Suggestions (one fetch), close, reopen — no refetch.
5. Explicit inspection only: on the phone, typing in Search previews into the Tools tab's Card panel SILENTLY (the sheet stays closed — `document.querySelector('[data-slot=drawer-popup]')` null, the search input still focused); tapping a search result's name opens the card sheet with that card; so does tapping a deck row, a grid card, the leader card, a validation chip, a sample-hand card; the sheet closes on the Close control, on Escape (dispatched keydown), and — where the pane can — on backdrop press; it never opens on Add, on `/`, on a tab change, or on load. On `md`, explicit inspection opens the Tools drawer on the Card tab, `openCuts` opens it on Cuts, and clicking deck rows while it is open updates the card without closing it (if you choose the non-modal drawer — decision below). At `wide:` nothing changes: no sheet, no drawer, `openCuts` flips the inline tab.
6. State survives a RESIZE across a tier boundary: with a query typed, a card previewed, Suggestions selected and two cards added, resize 1440 → 1024 → 375 → 1440 (`resize_window` custom widths; reload NOT required — a live resize): the deck rows, the search query and results, the preview card, the selected tool tab and the save status ("Saved", no dirty mark) all survive; state which parts remount on a crossing (the tools panels' fetched results, the dealt hand — decision below) and that the remount costs at most one refetch. Zero `/api/decks` requests during the resizes.
7. Sticky blocks and the ambient layer survive stacking: on the phone's Search tab the search block sticks at the pane's top while results scroll (`getBoundingClientRect().top` unchanged after scrolling the list); on Deck the group headers stick; R2's chip (`[data-slot=art-credit]`) is on screen with a crop on a Magic draft with a leader (Background art On), sits ABOVE the tab bar (measure), and is absent on the One Piece draft; the ambient crop paints under the active pane at every tier.
8. Touch targets on coarse pointers: the tab bar triggers, the search rows, the deck rows' steppers and remove (visible without hover on coarse pointers), the segmented items, the Add cards action, the drawer / sheet close controls — each ≥ 44 px on the smaller axis (measure a sample of each in the mobile preset; state the numbers). Desktop rows keep today's density (the hover reveal, `py-0.5`) — the coarse rules must not change the `wide:` look.
9. Safe area and the software keyboard: the tab bar's bottom padding reads `env(safe-area-inset-bottom)` (assert the class / computed `padding-bottom` — 0 in the pane, which has no inset); the two editor pages export `viewport` with `viewportFit: "cover"` (grep the HTML `<meta name="viewport"` for `viewport-fit=cover` on `/decks/new` and `/decks/<id>/edit`, and its ABSENCE on `/`); `DrawerVirtualKeyboardProvider` wraps the sheet (or the editor root) — provable only by structure in the pane (the provider in the tree, the tag input inside the sheet); an OPTIONAL real proof: if an iOS simulator boots (`mcp__Claude_Code_iOS_Simulator__control` `attach` — it errors harmlessly without one; never fix Xcode for this), open Safari at the Mac's dev URL, run the phone flow with a real touch keyboard and a real safe-area inset, and screenshot it; skip without comment if nothing boots.
10. The footer: absent on `/decks/new?game=mtg` and `/decks/<id>/edit` (no `<footer`), present on `/`, `/cards`, `/c/atraxa-praetors-voice`, `/d/q9j9uxphwp9n`, `/account`, the `/decks/new` PICKER (no `?game=`), `/nonsense` and `/c/zz-no-such-commander-zz` (the root and group 404s) and the error page in RTL; the editor carries its compact attribution line instead (decision below) — for the One Piece draft the ©BANDAI posture line, for Magic the Scryfall / Fan Content line; `(site)/layout.tsx` still reads no request data (grep for `headers(`, `cookies(` — none); `/c/`, `/l/`, `/cards/[id]` still `●` in the build.
11. The `wide:` variant compiles: grep the dev CSS (or the build's) for `width >= 75rem` (or `min-width: 75rem`); the R1a tokens test stays green; `pnpm check` green; the route table diff EMPTY.
12. Accessibility: the tab bar is a `tablist` with three `tab`s controlling three `tabpanel`s (Base UI wires `aria-controls`); the active tab is `aria-selected`; the bar is reachable by keyboard and arrow keys move between tabs; the sheet and the drawer are `role="dialog"` with a name (`DrawerTitle` — visible or `sr-only`), trap focus when modal, return focus on close, close on Escape; the Add cards button has a name; the Deck count is text ("Deck · 34"), never color-only; focus rings in the game accent on every new control (`data-game` on the editor root already provides `--ring`); the hidden panes are `hidden` (not merely off-screen), so nothing invisible is tabbable.
13. Both themes at 375 and 768 (screenshots) and the measured widths above; the One Piece draft at 375 (the single Card panel, the posture line, no art request); reduced motion untouched (every new animation `motion-safe:`).
14. Deploy verification via the Vercel commit status on the full 40-char sha (from the shell); then the prod pass in §Deployable outcome.

Design decisions to make explicitly (disclose + test-pin each)

* The tier model and where it is decided. CSS decides the LAYOUT (`md:` and `wide:` grid classes on the editor root and sections — no JavaScript for widths); ONE hook decides the two things CSS cannot — where the tools content mounts and what an explicit inspection opens: `useTier()` (`src/components/editor/use-tier.ts` or inside the layout component) = `useSyncExternalStore` over `matchMedia("(min-width: 48rem)")` and `matchMedia("(min-width: 75rem)")` → `"phone" | "md" | "wide"`, with a stable server snapshot (the editor is client-only on both routes — `/decks/new` bails to the client under `useSearchParams`, `/decks/[id]/edit` SSRs the loading main — so the snapshot never paints, but it must exist; say which value). The tools content — the R3 Tabs (Card / Suggestions / Combos / Cuts) or the One Piece single panel — renders inline in the third grid cell at `wide`, inside the Drawer at `md`, inside the Tools tab panel on the phone; a portal target change is a remount in React, so crossing 1200 px or 768 px REMOUNTS the tools panels (their fetched results refetch once when their tab is active; `preview`, `rightTab`, the deck, the search all live in `DeckEditor` and survive). Disclose it as the one thing a resize resets; if you find a cheap way to keep the panels mounted across the boundary (lifting their fetched state into `DeckEditor` is NOT cheap — say no), take it and pin it.
* The layout component: `EditorLayout` (`src/components/editor/editor-layout.tsx`, "use client") owning the tier plumbing, the phone Tabs, the md Drawer, the sheet, the tab bar and the bottom inset — `DeckEditor` hands it `header`, `search`, `deck`, `tools`, `sheet` content and the state it needs (`activePane` + setter, `toolsOpen` + setter, `sheetOpen` + setter, the Deck count, `onAddCards`), and keeps composing the panes as today. RTL renders `EditorLayout` with stub panes under a stubbed `matchMedia` (+ `ResizeObserver`) per tier; a second test renders `DeckEditor` in DRAFT mode (`deckId={null}`, `next/navigation` mocked, `fetch` stubbed to answer the search and the resolve) and pins the "done when": tab changes, drawer / sheet open-close and a tier flip (change the `matchMedia` stub's `matches` and fire its listeners) call `fetch("/api/decks", POST)` ZERO times and leave the save slot at "Saved"; the search input's value and the preview survive the flip; the first Enter creates exactly one deck (one POST).
* Pane widths: `wide:grid-cols-[minmax(18rem,20rem)_minmax(0,1fr)_minmax(18rem,20rem)]` ("about 320 px" sides — state the measured widths at 1200 and 1440) and `md:grid-cols-[minmax(18rem,22rem)_minmax(0,1fr)]` (search | deck; state the deck width at 768 and 1024). `lg:` leaves the editor root entirely (the header's `lg:h-14 lg:flex-nowrap` one-row form may stay `lg:` — measure whether 768 fits one row with the new Tools control; if not, keep the wrap and say so). The root stays `h-dvh` at EVERY tier (per-pane scroll containers everywhere — the sticky search block and group headers then stick inside their pane at every tier exactly as on `lg` today, and the phone's document never scrolls under a fixed bar); the active phone pane is the scroll container.
* The phone container: Base UI `Tabs` (controlled `value={activePane}`), `TabsList` rendered as the bottom bar — `fixed inset-x-0 bottom-0 z-40 border-t bg-background pb-[env(safe-area-inset-bottom)]`, three triggers of `min-h-11` with a lucide icon over a label (Deck with the live count as text, Search, Tools), `TabsIndicator` in the accent along the bar's TOP edge (`aria-label="Editor panes"`) — and three `TabsContent keepMounted` panels wrapping the existing sections. Base UI's `keepMounted` panels get the `hidden` attribute when inactive, and a `display: none` element DROPS its scroll position, so save each pane's `scrollTop` when it deactivates and restore it in a layout effect when it reactivates (a `Map` in a ref keyed by pane) — pinned in RTL (set `scrollTop` on the scroller, switch away and back, read it). The tools Tabs nest inside the Tools panel (two independent Tabs roots — fine). The Deck tab opens first; the bar hides from `md` (`md:hidden`).
* "Add cards": a primary `Button` in the deck summary row on the phone only (`md:hidden`) — and the empty state's action beside its "Add them from Search." hint — that sets the active pane to Search and calls `focusSearch()` (a user gesture, so the keyboard may open); a plain tab tap to Search does NOT focus the box (no keyboard pop on browsing). The search pane's `autoFocus` (158) must not fire into a hidden panel on load and must keep firing on `md` / `wide` — decide (a `focusOnMount` prop from the layout, or leave `autoFocus` and prove the phone never focuses a hidden input); after each add the pane refocuses its box as today (117) — that IS the repeated-adds behavior.
* Explicit versus passive: split the one `onPreview` channel. `SearchPane` gains `onInspect(card)` for the row's tap / click (207–210) while the response and arrow previews (96, 124) stay `onPreview`; `DeckEditor` routes `showCard(card, { explicit })` — passive: `setPreview` + `setRightTab("card")` silently at every tier; explicit: the same PLUS open the sheet on the phone or the Tools drawer on `md`. Deck-row clicks, grid cards, the leader card, validation chips and sample-hand cards are explicit (they are clicks). `handleAdd`'s `showCard` call (567) is PASSIVE (an add never opens the sheet). `openCuts` (627–630): phone → active pane Tools + `rightTab = "cuts"`; md → open the drawer on Cuts; wide → the inline tab as today (drop the `scrollIntoView` or keep it for wide only — say).
* The phone card sheet: `Drawer` (bottom — the default `swipeDirection="down"`, `modal`, `showSwipeHandle`, no snap points unless the content needs them — say) rendering `CardDetailPane` for `preview` with `tagging` (a SECOND instance of the pane; the Tools tab's Card panel keeps showing the same card — no conflict, no fetch); `DrawerTitle` = the card's name (visible, above the image, or `sr-only`); `DrawerVirtualKeyboardProvider` around the editor root (or the sheet) so the tag input scrolls clear of the software keyboard; closes on swipe, backdrop, Escape and its Close control; unmounts when closed (no `keepMounted` — the pane is cheap). Never opened by typing, adds, `/`, tab changes or load.
* The `md` Tools drawer: `Drawer swipeDirection="right"` (docked right, the primitive's 24rem from `sm`), `modal={false}` with `disablePointerDismissal` so the deck stays interactive beside it and explicit inspections update its Card tab without closing it (the alternative `modal='trap-focus'` traps focus and would make every deck-row click reopen — say which you chose and why), a `DrawerTitle` ("Tools", `sr-only` or visible), a Close control, Escape closes; opened by a "Tools" `Button` in the header's right cluster (`hidden md:inline-flex wide:hidden`, `aria-label` or visible text), by explicit inspection, by `openCuts`. Optionally a `Drawer.SwipeArea` along the right edge — only if it never steals the deck pane's scroll; say.
* The Tools panel's contents by tier: `wide` and `md` = the R3 Tabs exactly as today; the phone = Analytics and the Sample hand ABOVE the tool Tabs (the contract puts them in Tools on phones), so `DeckListPane` gains a prop (`extras?: boolean`, default true) that the layout sets false below `md`, and the Tools panel renders `AnalyticsPanel` + `SampleHand` itself — a remount on crossing 768 (the dealt hand resets; disclose). The compact attribution line (the footer's replacement — next decision) ends the Tools content at every tier.
* The footer: move `<SiteFooter />` out of `src/app/layout.tsx` into `src/app/(site)/layout.tsx` (after `{children}`) and render it in the three headerless shells that stand outside the group — the chooser's PICKER branch (beside its `SiteHeader`), the root `not-found.tsx`, `error.tsx` — so every non-editor page keeps it and the editor routes lose it; `global-error.tsx` never had it. The editor gets a compact per-game attribution at the end of its Tools content instead (Magic: "Card data and images courtesy of Scryfall · unofficial Fan Content, not endorsed by Wizards of the Coast", `text-[0.65rem] text-muted-foreground` with the Scryfall link; One Piece: `OptcgPostureLine` with a compact className) — the One Piece editor gains the ©BANDAI line it never had (P4.1 / P4.6 require it wherever One Piece card images render; the editor was the gap — say so). Prove `(site)/layout.tsx` stays request-free and the ISR routes stay `●`.
* The bottom inset: the editor root sets `--editor-bottom-inset` (the bar's height + `env(safe-area-inset-bottom)` below `md`, `0px` from `md`); the `Toaster`'s viewport takes `viewportProps={{ style: { bottom: "calc(var(--editor-bottom-inset, 0px) + 1rem)" } }}` (or a class — tailwind-merge resolves `bottom-*`), and `AmbientArt` gains ONE optional prop for its sticky row's bottom offset (a class or style — no behavior change; the existing test stays green) that the editor passes; the share page passes nothing.
* Touch targets: `pointer-coarse:` rules only — search rows `pointer-coarse:min-h-11`, deck rows `pointer-coarse:min-h-11` with the steppers and remove `pointer-coarse:opacity-100` (the hover reveal cannot work on a finger) and `pointer-coarse:size-9` (or the `icon-sm` size), the segmented items and the leader-zone buttons ≥ 44 px, the tab bar `min-h-11` + inset; the `wide:` mouse density unchanged (measure both). If the pane's mobile preset does not report `pointer: coarse`, the RTL class pins plus a forced check carry it, and you say so.
* `viewport`: `export const viewport: Viewport = { width: "device-width", initialScale: 1, viewportFit: "cover" }` on the two editor `page.tsx` files (page-level, so the (site) pages are untouched; the root layout keeps no viewport export). Say whether `interactiveWidget` ("resizes-content") helps the keyboard case or conflicts with the provider — read the Next doc, decide, pin.
* Hotkeys: unchanged; decide whether `/` should still focus search while the `md` drawer (non-modal) is open (recommend yes — harmless) and confirm `?` is inert inside the sheet (it is a `dialog`; `isTextTarget` covers it); the phone's tab bar must not intercept `/` or `?`.
* Budget statement for §6: 0 DB, 0 API, 0 wire; the editor's request count per action unchanged (search GETs, the one POST, the PUT autosaves, the panels' GETs); +1 refetch per tools panel per tier crossing while active. Route table diff empty.
* LATER rows: none fire; add a row only for something real you defer (a `Drawer.SwipeArea` for the tools, a persisted active tab, snap points on the sheet — each only if a trigger exists).

Deployable outcome
`pnpm check` green and deployed; verified on DEV: the keyboard script at 1440 with exactly one deck created and the draft deleted; the three tiers measured at 375 / 768 / 1024 / 1199 / 1200 / 1440 with the pane widths stated and no horizontal overflow; the phone flow (Deck → Add cards → three adds without leaving Search → the badge count → Deck → Search with query, results and scroll intact → Tools with analytics, the sample hand and the strip) with one POST total and zero on tab / drawer / sheet / theme / appearance / resize; the sheet only on explicit inspection, never on typing; the `md` drawer from the header, from inspection and from `openCuts`; panes mounted once (element identity held across switches; no refetch on return); the resize survival with the remount disclosed; the sticky blocks and R2's chip above the bar; the toast above the bar; touch targets measured; the footer gone from the editor and present on the site pages, the picker, the 404s and the error page (RTL); the compact attribution on both games' editors; `viewport-fit=cover` on the two editor pages only; both themes at 375 / 768; the One Piece draft at 375; `smoke:collection`, `smoke:decks`, `smoke:versions`, `smoke:optcg-deck` and `smoke:seo` green on dev (spaced against the deck-create limiter — read the counters first, remember seo resets them); the route-table diff empty; verified ON PROD signed out with no edits and no creates: `/decks/new?game=mtg` and `/decks/new?game=optcg&leader=OP15-058` at 375 (the tabs, Add cards, the seeded Enel, no `/api/decks`), 768 (two panes + Tools drawer), and 1440 (three panes); no footer on either; `/` and `/c/atraxa-praetors-voice` with the footer and `x-vercel-cache` MISS → HIT on the hub; the `<meta name="viewport">` with `viewport-fit=cover` on the editor and without it on `/`. Docs in the same package: `REDESIGN.md` progress tracker R4 ticked with the sha; the R4 decisions block under §6 (the tier model and the hook, the remount disclosure, the pane widths measured, the scroll model and the scrollTop restore, the tab bar, Add cards, explicit vs passive, the sheet, the drawer's modality, the Tools composition by tier, the footer move and the attribution line, the bottom inset, touch targets, the viewport export, the dev and prod passes, the mints and their deletion); the smoke-pin ledger's R4 row (none new, or the ones you added); the unwired-primitives list updated (drawer wired; say what else); db:size stated; nothing minted beyond the drafts, every draft deleted and stated. Nothing posted, seeded, or simulated; no prod deck created.

Session notes (environment, unchanged from R5b unless noted)

* PATH: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node / pnpm + gh); psql / aws absent — ad-hoc SQL via tsx in `scripts/.tmp/` (gitignored but LINTED; delete only what you add). **A zsh `for` loop in the Bash tool loses PATH after its first iteration** — R5b's stash-window loop lost `git` mid-loop and left the tree STASHED until `/usr/bin/git stash pop`; orchestrate multi-step shell work in python with absolute paths (`/usr/bin/git`, `/usr/bin/curl`, `/bin/cat` — `/usr/bin/cat` does not exist) or single commands. `sed` / `cat -A` mangle the build log's glyphs — python for the route table. Prod curl bodies arrive gzip-encoded: `--compressed`, or every string check reads false.
* `.env.local`'s `DATABASE_URL` is the Neon pooler prod uses: dev fixtures are real rows; the five public decks ARE the home rail and `/u/bobandis6` on prod too. Never mint on prod. The deck-create limiter counts the pane's creates (`deck-create:ip:::1`, 10/h and 30/day); read `rate_limit_counters` before the pass; `smoke:seo` deletes those counters at its start.
* Dev server: `preview_start {name: "dev-log"}`; first hits compile routes (seconds) — poll `/api/health`. `pnpm check` before `pnpm build` (both write `.next`; dev lives in `.next/dev`). Next 16 dev logs server fetches only with `logging: { fetches: { fullUrl: true } }` in `next.config.ts` (R5b switched it on for one restart and reverted before committing) — not needed here.
* Browser pane: `document.visibilityState` stays "hidden" — Base UI exit animations and timers stall (toasts never auto-dismiss, Tabs panels keep an unsettled `hidden`, Drawer close animations may linger with `data-ending-style`; front the tab or assert state through attributes, not visibility), streamed Suspense HTML can sit un-revealed, long scripts hit the 45 s tool timeout (keep pane scripts short); menus and toggle items open from a PLAIN JS `click`; the editor's combobox needs a dispatched `KeyboardEvent("keydown", { key: "Enter", bubbles: true })` (and `{ ctrlKey: true }` for the leader add); hover needs dispatched `pointerover` / `pointerenter` / `mousemove` events (`pointerType: "mouse"`); programmatic `focus()` never matches `:focus-visible` in the hidden document; `navigator.clipboard` rejects; lazy images may never load (assert `complete && naturalWidth > 0`, or `scrollIntoView` first); `read_network_requests` buffers ~250 requests and sometimes records nothing — `performance.getEntriesByType("resource")` in the page is the reliable list; `resize_window` `preset: "mobile"` (375×812, touch emulation and an Android UA — reload after switching; check `matchMedia("(pointer: coarse)")`), `preset: "tablet"` (768×1024) and custom `width` / `height` (1024 / 1199 / 1200 / 1440 — measure with `getBoundingClientRect`; screenshots at those sizes came back empty in R5a), then `preset: "desktop"` to reset; the Drawer's swipe gestures cannot be driven in the pane (`left_click_drag` is a mouse drag — Base UI's swipe listens to pointer events, so try it once and fall back to the Close control; `touch_path` is the iOS simulator's); the theme is `localStorage.theme` (`"light"` / remove for the dark default) — clear `theme`, `deckwarden:appearance`, `deckwarden:index-view`, `deckwarden:deck-view` on both origins when done, and delete every claim token (`deckwarden:deck-token:<deckId>` — `PREFIX` in `src/lib/decks/token-store.ts` line 13) after deleting the drafts.
* Prettier reflows scripted-edit anchors: re-grep after `pnpm format`. `*.md` is prettier-ignored. React splits `{"text "}{n}` into two text nodes with a comment between — render one template string wherever a test or smoke reads the HTML.
* Deploy verification: `gh api repos/Bobandis6/deckwarden/commits/<full sha>/status` from the shell → the "Vercel" context (R5b's went green within the first poll); `gh run list --workflow=nightly-ingest.yml` for nightlies. ISR is real on prod only; `/decks/new` is static (`x-vercel-cache: PRERENDER`) and `/decks/[id]/edit` dynamic — neither needs a cache proof.
* `pnpm check` takes about a minute; `smoke:seo` mints fixture decks (dev only).

Context, not tasks
Sequence after R4: R6 (C9 skeletons, G6 / G9 analytics, F4 the dealt hand, the `/cards` restyle R5a assigned there, the audit at 390 / 768 / 1024 / 1200 / 1440 / 1920 in both themes, reduced motion, contrast, focus order, touch targets, status never color-only — the closing pass), with its own prompt written the way this one was, after R4 ships. P4.7's standing triggers still exist beside the R-series (the `img.deckwarden.gg` flip — LATER's r2.dev row, line 51, which unlocks the One Piece thumbnails, tiles, shelf images and the `/leaders` grid —, the Limitless key, feedback, red nightlies) — their own P4.7 round, never a slice of an R-session. Bandai and Azuki permission emails remain unanswered — posture unchanged; One Piece ambient art stays off (REDESIGN.md §3) and the posture line stays on every One Piece surface (the editor joins that list in R4). Cold-start rule holds everywhere: no simulated decks, posts, or metrics, ever. Premium never gates card data. The owner's open items: the Danger-zone look, the signed-in editor look, the Background art toggle, the signed-in home and `/account` tiles, the signed-in deck header and the owner's `/f/` view, the `/account` sections — and, new after R4, the phone builder on a real device (the only thing this session cannot fully prove from the pane). R5b's observations stand: the seo smoke resets the local deck-create counters; the in-group 404 hydrates client-side; `:focus-visible` never matches in the hidden pane; the OP promo Nami P-117 has a null cost (LATER's null-cost row, line 63); OP quick-add by card number is name-only (line 62). The smoke-pin and LATER-row ledgers in REDESIGN.md §6 remain the map.
