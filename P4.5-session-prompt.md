# P4.5 session prompt — Limitless OP tournament ingest + hub finishes shelf

Pull latest, then run P4.5 — the OTHER half of the plan's line-216 bullet: "Limitless tournament API if key granted (they grant to public-facing projects)." The premise was CORRECTED 2026-09-05 (75edf5c, LATER row 58, verified live): the tournament endpoints are KEYLESS — the key only buys rate headroom and the /games deck-RULES endpoint — so this package does NOT wait on the grant. Build the nightly Limitless ingest for OP and put the first community signal on the P4.4 hub surface: a source-attributed "Top finishes" shelf on /l/[slug]. The OP beta announcement stays the package AFTER this one, and is owner-deferred besides. Expect ZERO migrations (verified — see facts), one new ingest script + workflow step, one adapter capability addition (tests), one hub shelf, smoke extensions.

Pre-flight, in order. (1) Last scheduled nightly green (cron 10:37 UTC, drifts to ~14:30–17:40). Historical note if reading old runs: the 2026-09-05 nightly was the FIRST with mapper v2 + the overlay applier — a one-time `mapper_version: 2` full remap (identities updated ≈2,785) plus `legalities_overlay {entries:10, …}` is expected THERE, near-zero after; that remap is not drift. `tournaments_aggregated` > 0 continues (Topdeck drain). (2) Limitless key status: the owner sent the request 2026-09-05 and was told to expect days — ASK whether it was granted. Granted → add to `.env.local`, `gh secret set LIMITLESS_API_KEY`, wire the workflow env, send as `X-Access-Key`; row 58 gains "granted <date>". Not granted → build keyless, it is NOT a blocker (verified: 50 req/5min). (3) db:size baseline (246.2MB on 2026-09-05; Neon alert 350MB).

## What P4.5 is NOT (scope fence)

NOT any MTG/Topdeck change — topdeck.ts, its aggregate, and the /c/ shelves are a parallel source sharing the same tables; mirror its patterns, do not refactor it. NOT the P3.8-style leader×card AGGREGATE for OP — that is its own later package once OP decklist shapes are proven at volume; add a LATER row with that trigger. NOT pairings/matchup surfaces, NOT the webhook system (nightly pull is the pattern), NOT archetype pages (Limitless auto-assigns `deck` objects — note what you see, decide storage, build no new surface). NOT OP recommendations or Cut Coach (engine stays dark). NOT deck shelves on OP hubs — the cold-start counter still runs; tournament finishes are source-attributed EXTERNAL data, the same distinction /c/ hubs draw, and never a substitute for real user decks. NOT the /games deck-categorization-rules endpoint (key-gated; a later conversation even with a key). NOT the beta announcement (owner's, next). NOT img.deckwarden.gg (LATER row stands).

## Facts you inherit (verified live 2026-09-05 — trust these, spot-check nothing)

- API base `https://play.limitlesstcg.com/api`. Keyless endpoints: `/tournaments` (query params game/format/organizerId/limit/page; default 50 newest), `/tournaments/{id}/details`, `/tournaments/{id}/standings`, `/tournaments/{id}/pairings`, `/games`. A key adds headroom + the /games …/decks rules endpoint only; sent as `?key=` or `X-Access-Key` header (prefer the header — keeps it out of logs).
- Keyless rate limit OBSERVED in response headers: `ratelimit-policy: "50-in-5min"` with a `ratelimit` countdown header. A nightly OP pull (list + details + standings for a handful of new events) fits with big margin at Topdeck-style pacing; honor the headers anyway.
- `game=OP` is live: `{"id":"OP","name":"One Piece Card Game","formats":{"STANDARD":"Standard","EXTRA":"Extra"},"platforms":{"CAM":"Webcam & Discord","SIM":"Simulator"},"metagame":true}`. 14 games on the platform.
- Volume sampled 2026-09-01→04: 8 OP events, 4–64 players. NOTE: `format` came back null/absent on those list rows despite the docs listing it — handle absent format honestly (verify-first 2 decides the mapping; optcgStandard is our only OP format row).
- Documented response shapes (docs.limitlesstcg.com/developer/tournaments): list = {id, game, format, name, date, players}; details adds organizer{id,name,logo}, platform, `decklists` bool, `isPublic` bool, isOnline, phases[{phase,type,rounds,mode}], bannedCards, specialRules; standings = {player, name, country, placing, record{wins,losses,ties}, decklist (GAME-SPECIFIC shape, undocumented for OP), deck{id,name,icons} auto-assigned, drop}. No explicit "finished" flag is documented anywhere on list/details.
- Schema fits with ZERO migrations (read 2026-09-05): `tournaments` has gameId/formatId/`source` (comment literally says `'topdeck' (M4: 'limitless')`)/externalKey with unique (source, external_key)/startDate/playerCount/topCut/cardsAggregatedAt; `tournament_standings` has tournamentId/placement (unique per event)/playerName/leaderIds uuid[] under GIN/decklistUrl/wins/draws/losses. Lean-row rule: card-level lists are NOT stored (schema comment forbids it; the aggregate row above covers the future).
- OP leader resolution is id-first by construction: OP decklists carry card codes ("OP01-003" ≙ `external_key`, unique per game) — stronger than MTG's name path. The P4.2 resolve-route precedent (exact external_key pass) is the pattern. But verify the actual shape first (verify-first 1).
- /l/[slug] hubs exist (P4.4), ISR hourly, cold-start card-data-only, Bandai footer already on the page — the shelf adds no new attribution footer, but every rendered stat needs the Limitless label + link (the P3.1/P3.8 hard rule; MTG renders it via `adapter.capabilities.tournaments` meta — optcg ships `capabilities: {}` today, so adding `tournaments` meta for OP is an ADAPTER change, in scope, test-pinned).
- Env state (P4.4 audit): R2_PUBLIC_IMAGE_BASE now in .env.local AND .env.example (local punk-records runs are safe); TOPDECK_API_KEY local+Actions; DATABASE_URL everywhere; no Limitless key anywhere yet.
- Existing MTG constants: MIN_EVENT_PLAYERS + TOP_PLACEMENT live in `src/lib/games/mtg/topdeck-map.ts`; the /c/ shelf reads `loadTopFinishes` (src/lib/tournaments/queries.ts). How game-scoped those are is verify-first 3 — do not assume.

## Verify-first list (never from memory)

1. THE OP decklist shape: fetch ONE real standings response for a `decklists: true` OP event BEFORE designing the resolver — the leader marker (OP lists have exactly one leader), the card-code format, what `decklist` holds when submission was off, and whether `deck` archetype objects are populated for OP.
2. Settledness: no documented "finished" flag — find the honest signal (standings present + date past + a trailing margin?) and mirror Topdeck's settled-once discipline (TRAILING_REFETCH_DAYS shared-const pattern) so events are ingested exactly once, idempotently. Also decide the absent-`format` mapping (all OP → optcgStandard? record why).
3. loadTopFinishes + the /c/ shelf composition: what is game-scoped vs MTG-pinned (queries.ts, topdeck-map constants, the /c/ page render) — reuse the leaderIds-containment query pattern for /l/, don't fork blindly.
4. The public event URL shape for deep links (docs don't state it — find a real tournament's public page URL, e.g. via a standings page) before writing the eventUrl builder.
5. Terms/attribution: look for a ToS/terms link on docs or the platform; the Showcase page implies blessed public-facing use; attribution + linkback regardless (hard rule).
6. `isPublic: false` events — default lean+respectful is SKIP entirely; verify the field exists in practice and record the decision.
7. Volume over a longer window (page the list keylessly, politely): events/day and size distribution over ~90–180 days → picks OP's MIN players threshold honestly (observed events run 4–64; a 4-player webcam weekly is not signal) and estimates row growth for db:size.

## Design decisions to make explicitly (disclose + test-pin each)

- OP `capabilities.tournaments` meta: label "Limitless" + eventUrl builder from verify-first 4 — adapter change with adapter tests (P4.4 was zero-adapter; this package is not).
- OP MIN_EVENT_PLAYERS (MTG uses 16 on Topdeck; pick from verify-first 7's data, disclose in the constant's comment) + TOP_PLACEMENT for OP (top-16 mirrors Topdeck; smaller events may need "placement ≤ topCut" honesty — decide with data).
- Ingest shape: `scripts/ingest/limitless.ts`, workflow step after punk-records (shared advisory lock, own `ingest_runs` source 'limitless', stats JSON), window + settled-once + idempotent merge mirroring topdeck.ts WITHOUT touching it. Keyless pacing well under 50/5min; real User-Agent; optional LIMITLESS_API_KEY → X-Access-Key with zero other behavior change.
- Storage decisions: skip drops? store record (wins/draws/losses — columns exist)? playerName = public display name only (Topdeck precedent); decklistUrl = the source's public standings/list URL per verify-first 4.
- Shelf composition on /l/[slug]: placement · event name · players · date · link, Limitless label+link, rendered ONLY when rows exist (cold-start rule — hubs without data change NOT ONE PIXEL); pin presence-when-data AND absence-when-none in smoke (hubs-smoke or seo-smoke, wherever the /c/ precedent lives).

## Deployable outcome

Nightly workflow runs the Limitless step green (prove via manual `workflow_dispatch` before relying on cron), OP tournaments + standings rows exist with the settled-once discipline (re-run = no dupes — pin it), /l/ hubs WITH data render the attributed finishes shelf and hubs without data are byte-identical to before, OP adapter capabilities.tournaments test-pinned, smoke extended and green against dev, `pnpm check` green, deployed, verified in the browser ON PROD (a real /l/ hub showing real finishes with the Limitless link — drive via javascript_tool, hidden-pane rule), db:size stated with a growth estimate per 180 days (standings are lean rows; expect small). LATER.md: row 58 updated with key status; new row for the OP leader×card aggregate with its trigger; any deferred decision gets its row.

## Session notes (environment, unchanged from P4.4 unless noted)

- PATH: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node/pnpm + gh); psql/aws absent — ad-hoc SQL via tsx in scripts/.tmp/ (tsconfig-excluded but LINTED; delete scratch when done — a stale one broke `pnpm check` in P4.4).
- Deploy verification: Vercel commit status (context "Vercel") on the full 40-char sha; `gh run list --workflow=nightly-ingest.yml` for nightlies.
- Browser pane: hidden pane swallows computer clicks AND read_page — drive via javascript_tool, verify via DOM/API reads.
- Prettier reflows scripted-edit anchors: after `pnpm format`, re-grep before exact-string edits.
- ISR surfaces lag revalidate windows in prod — assert fresh behavior against dev, existence/reachability against prod.
- All Limitless requests: real `User-Agent: Deckwarden/1.0 (https://deckwarden.gg)` — same courtesy as Scryfall/punk-records.

## Context, not tasks

Limitless key: requested by the owner 2026-09-05, approval pending — a reminder is set for the owner to check ~2026-09-08; pre-flight (2) handles both outcomes. Bandai/Azuki permission emails still unanswered — posture unchanged; the finishes shelf lives on /l/ pages that already carry it. The OP beta announcement (plan line 217) is the next package and owner-deferred. Premium never gates card data. No simulated or seeded decks, ever.
