# P3.5 session prompt

Pull latest, then kick off P3.5 from deckwarden-build-plan.md — M3's fifth
package (~line 207): Topdeck.gg ingestion → tournament results on hubs
(credit + link rendered), "seen in top-X lists" as a ranking signal. Know
what it is NOT before designing: this is the first package since P2.5
whose product is INGEST + SSR shelves, not an editor panel — no new client
fetch loop, no new rate-limited route unless you can justify one next to
the two read routes that exist. It is not EDHTop16 scraping (LATER row:
that fires only if Topdeck proves insufficient), not Meta Lens (needs
corpus depth plus real usage — leave its row), not deck-badge tournament
verification (feature plan's label system, later milestone), and not a
community shelf: tournament data is REAL data — this is exactly the
"Topdeck upgrades hubs in M3" promise the cold-start mitigation table
made, so the cold-start rule is satisfied by it, never worked around with
it. TWO things before any schema design: (1) the API key —
TOPDECK_API_KEY exists in neither .env.local, .env.example, nor (verify)
the Actions secrets; it is self-serve per the plan's resource line
(https://topdeck.gg/docs/tournaments-v2, credit required) but account
creation is the OWNER's action, not yours — check in the first hour and if
absent say so immediately and loudly (minting takes minutes; build the
mapper against the documented shape while waiting), because the package
cannot end "deployed with data" without it and must NEVER end with
simulated tournaments (cold-start rule: no faked activity, ever); (2) the
contract — read the LIVE docs and make one real authenticated request
BEFORE designing anything: response columns, whether standings carry
commander names / a decklist URL / embedded card lists (deckSnapshot?),
partner-pair encoding, pagination, and their actual published rate limit
(the plan's "respect 100 req/min" is a politeness floor, not a verified
number) — any training-data memory of this API is hearsay until curled.
The ingest follows the spellbook.ts precedent, named part by part: a PURE
mapper module (src/lib/games/mtg/topdeck-map.ts) with skip-reasons and a
vitest suite like spellbook-map.test.ts, a thin IO script
(scripts/ingest/topdeck.ts, pnpm ingest:topdeck) using the direct
non-pooler URL, the shared INGEST_LOCK_KEY advisory lock, an ingest_runs
row (source 'topdeck') with real stats, temp-table staging + idempotent
change-skip merge, and a nightly-ingest.yml step AFTER spellbook (names
resolve against card_identities), hard-fail visible like its siblings,
before the db:size gauge. One deliberate difference to design, not
inherit: Scryfall/Spellbook re-read a full bulk export nightly; Topdeck is
a paginated API of dated events — decide the incremental watermark (window
derived from the last succeeded ingest_runs row vs a stored cursor,
re-fetching a trailing window since results can settle late) and keep
initial backfill and nightly refresh ONE parameterized code path like the
siblings. Schema decisions, all with the Neon math done out loud (DB
measured 203.9MB of the ~350MB alert line, collection import P3.7 still to
land): tournaments + standings tables carrying game_id like everything
else (M4's Limitless line wants the same shape), source-scoped external
keys for dedupe and the event deep link, standings holding the commander
as a uuid[] of card_identities ids (the decks.leader_ids + GIN decks_hub
index precedent — partner pairs are arrays, and a partner-pair event is a
required mapper test case), placement, event date/name/size, decklist URL
if present (text, nullable). Commander names resolve via normalizeCardName
against name_norm EXACTLY — never trgm-fuzzy at ingest (a wrong-card match
poisons every shelf silently); unresolved names skip WITH a counted
skip-reason, spellbook-style. Decide and DISCLOSE the bounds like the
popularity floor and scan cap before it: top-X-only storage (X named — the
plan's own phrase is "top-X lists"), a minimum-event-size threshold, and
the backfill window, each with its row-count arithmetic stated. The plan's
Topdeck line also says "archive raw responses to R2" — R2 is live (the
restore-drill workflow restores from it; backup-user-tables.sh has the env
pattern): implement it or flag the conflict with reasoning in the commit,
not silent drift. Surfacing: the hub page (/c/[slug], ISR revalidate 3600
— state that intent holds: data changes nightly, hourly is already right)
gets a "Top finishes" shelf that renders ONLY with real rows (that page's
own shelf rule), hubs only — the commanders index and card pages are out
of scope. Gate it on adapter.capabilities.tournaments, which is still the
P3.1 placeholder `tournaments?: boolean` that nothing sets — P3.3's combos
precedent says make it DECLARATIVE ({sourceLabel, sourceHref,
eventUrl(externalKey)}): adapters declare, core renders, and the visible
"Topdeck.gg" credit + link wherever tournament data appears is a hard
attribution rule from the plan's risk table, footer included. The ranking
signal is the package's honest conditional: "seen in top-X lists" needs
CARD-level data, and standings may carry only a commander plus a decklist
LINK — fetching third-party decklists (Moxfield etc.) is a second external
dependency with its own ToS and scale problems and is NOT this package. If
the verified response reliably embeds card lists, do the Neon math for a
LEAN aggregate (never 100 raw card rows per standing) and ship the signal
through the P3.1 seam — one more evidence source with its own honest name,
same payload shape (LATER row 32's exact words: "the engine's
RecommendMeta seam is built for it"), weights in rank.ts, phrasing +
sources entry in recommend.ts, smoke:recommend contract extended; if not,
the signal half lands in LATER with the empirical finding recorded. EITHER
WAY the co-occurrence LATER row fires NOW — its trigger literally names
"Topdeck.gg ingestion (M3, tournament lists)": decide whether what you
actually ingested is a mineable deck corpus, fire or re-record with
reasoning and a real next trigger — decisions, not a second session of
inertia. Deployable outcome = the nightly Action green WITH the topdeck
step having run for real (rows in the tables, a succeeded ingest_runs row,
db:size still under the line), the Top finishes shelf live on a prod
commander hub with real events, dates, placements, and working Topdeck
links + credit, capability declared and rendered through it, the mapper
vitest suite green (partner pair, unresolved skip, dedupe/idempotence
pinned), hubs smoke extended (or a new DB-connected smoke) covering the
shelf query, the ranking-signal decision recorded in code or LATER,
deployed and check-green, and the existing smokes still green against
prod. If the key never arrives in-session, the honest end state is
everything buildable-without-live-data built, check-green, deployed
dormant (empty tables render nothing by the shelf rule), and the blocking
dependency stated as the first line of the handoff — never a fixture.

Session notes:

- Toolchain: node/pnpm at ~/.local/node22/bin — non-login shells need
  export PATH="$HOME/.local/node22/bin:$PATH". .claude/launch.json
  (gitignored) wraps pnpm dev for the preview runner. `source .env.local`
  FAILS in zsh (unquoted `&` in a URL on line 6) — pull single vars:
  export DATABASE_URL="$(grep '^DATABASE_URL=' .env.local | cut -d= -f2-
  | tr -d '"')".
- Before starting: pnpm check on main AND the nightly Action's recent runs
  (two consecutive greens). gh is NOT installed — GitHub REST API via
  curl. Deploy verification: Vercel posts a commit status (context
  "Vercel") on the pushed sha; actions/runs?head_sha= needs the FULL
  40-char sha.
- What P3.4 shipped (2026-08-31, commit bf33070 — not in the plan doc):
  - Right pane is Card | Suggestions | Combos | Cuts; the Cuts tab gates
    on adapter.recommend?.cuts — RecommendMeta grew an optional `cuts`
    block (CutsMeta in games/types.ts) because cut phrasing scopes to the
    sibling declarations it reuses; sources map grew role-template +
    price. STAPLE_RANK / WIDELY_PLAYED_RANK are exported shared tier
    constants in games/mtg/recommend.ts — one boundary vocabulary for
    both directions.
  - The ranker is PURE CLIENT (src/lib/recommend/cuts.ts): rankCuts +
    completeCombosByCard, CUT_WEIGHTS {popularity .45, curve .25, role
    .2, price .1}, CutEvidence = RecommendationEvidence + side
    "cut"|"keep". Evidence order IS presentation order: combo warnings →
    cut-side lines → keep tail. Complete-combo members group-sort AFTER
    all non-members (an ordering rule, not a weight — test-pinned);
    template combos never warn (deckComboStatus carried over);
    evidence-less cards are dropped and the count disclosed.
  - Combo membership reuses GET /api/decks/[id]/combos under the same
    panel fetch policy plus an over-limit gate; completeness is
    re-verified client-side against current entries so a just-cut partner
    demotes its warning between fetches. Degradation is disclosed
    (leaderless / pending / failed), never silent.
  - A cut is a DECREMENT via the editor's own setQty(qty−1) (last copy
    removes the row); leader zone never ranked; gate = deckSizeCount vs
    FormatDef.deckSize.max (replaced hasLeader for this panel only).
    Deck-list header got an "Over by N — rank cuts" CTA (onOpenCuts prop)
    that flips the tab and scrollIntoViews the right pane on stacked
    mobile. ConfidenceChip + sourceMeta are exported from
    recommendations-panel.tsx for panel reuse.
  - No new route, no rate-limit change, no smoke:recommend change —
    coverage is cuts.test.ts (16 machine tests) + recommend.test.ts cut
    phrasing, plus browser-driven prod proof. That split (pure mapper +
    vitest, thin surface, prod proof) is also the ingest pattern P3.5
    should mirror.
- LATER triggers for THIS package: row 32 (co-occurrence signal) — its
  trigger names Topdeck ingestion; take the call explicitly. Row 21
  (EDHTop16) and row 28 (Meta Lens) stay unless findings change them; the
  forks/single-deck-DELETE row belongs to the versioning package (P3.6) —
  leave it. P3.4 re-recorded producedMask with a produced-color-surface
  trigger — not this package either.
- Neon: db:size read 203.9MB on 2026-08-31 (alert 350MB, free ~512MB).
  Every new table needs its row math stated; ingest_runs.stats records
  db_size_bytes per run — keep that in the topdeck stats too.
- smoke rules against prod: BASE_URL=https://deckwarden.gg pattern;
  smoke:recommend 27 checks (creates/deletes its own guest deck);
  smoke:combos and smoke:hubs are DB-connected (need DATABASE_URL via the
  grep pattern above); smoke:seo's ONE known failure is "core sitemap
  lists the public deck" (revalidate lag) — any other failure is real;
  engagement/account smokes mint signed sessions (BETTER_AUTH_SECRET
  pattern in scripts/engagement-smoke.ts).
- Browser pane (memory file has details): hidden pane swallows computer
  clicks — drive via javascript_tool, verify via DOM/API reads; the hub
  shelf is SSR, so curl-grepping the prod HTML is the reliable check.
  Guest-deck cleanup pattern unchanged (x-deck-token DELETE, local AND
  prod) if the ranking signal needs a deck fixture.
- Prod verification target: pick a commander that actually appears in
  recent Topdeck results (cEDH staples — Kinnan, or a Tymna/Thrasios pair
  to prove the partner-array path end to end on a real hub).
- Context, not tasks: owner is mid-seeding (featured brews with primers);
  the beta announcement is deliberately deferred; permission emails to
  Bandai + Azuki Labs went out 2026-08-30. Cold-start rule stands — no
  simulated activity, no waiting on content.
- Protocol as usual: one package, ends deployed and check-green; scope
  leaks to LATER.md with a trigger.
