# P4.7 session prompt — beta response (state-gated)

Pull latest, then run P4.7 — the first session with NO predetermined package.
M4's code is done (P4.6, dc27213 + the 8b29bc8 docs follow-up); M4 itself
exits when the owner posts the announcement, and M5 (Azuki) stays
permission-gated. This session's scope is decided by a state probe, not a
plan bullet: run the pre-flight, fire ONLY the branches whose triggers read
true, and if none fire, report the state honestly and stop. **"Nothing to do
yet" is a valid, complete outcome; invented work is not.** The standing
triggers this session exists to catch: LATER row 51 (img.deckwarden.gg
flip), row 58 (Limitless key), beta feedback, M4-exit recording, red
nightlies, and the cold-start probes (rows 52/59, claim-nudge).

## Pre-flight, in order

1. **Nightlies since 2026-09-07 all green** (`gh run list
   --workflow=nightly-ingest.yml`; cron 10:37 UTC drifting ~13:30–17:40).
   The 09-07 run was the first on dc27213 (no ingest surface changed in
   P4.6 — expect routine). Steady state: punk-records sha-skip or small
   update; Limitless keyless ~15d window mostly merging 0/0, occasional
   small inserts as events settle. ANY red run → branch F, before
   everything else.
2. **`dig NS deckwarden.gg +short`** — four `porkbun.com` hosts = runbook
   not executed (branch A dormant). Two `cloudflare.com` hosts = Part 1
   done → also `curl -sI
   https://img.deckwarden.gg/optcg/images/OP15-058.png`: 200 = Part 2
   done and **branch A fires** (the flip half was explicitly promised
   back to a session; runbook step 11 in `P4.6-op-beta-launch.md`).
3. **`gh issue list` first, then ASK the owner** one informed question
   set: (a) announcement posted? where? links to the live thread(s);
   (b) any feedback anywhere beyond those threads; (c) Limitless key —
   granted / declined / still Pending Approval (keys are exception-only
   since the 09-07 banner; they may have asked in the Limitless Discord);
   (d) if NS still Porkbun: moving DNS during this session, or still
   deferred?
4. **db:size** (252.5MB on 2026-09-07; alert 350) and, in the same
   scratch query: decks by format and `rate_limit_counters` (`deck-create:%`).
   The deck counts are the real-usage probe — prod ended P4.6 with ZERO
   OP decks by design (walk decks deleted; the owner seeds real ones
   pre-announcement).

## Branches, in priority order

Each fired branch ends deployed + `pnpm check`-green + verified on prod, or
explicitly documented. If multiple heavy branches fire (A plus a thick C),
do A plus C's fix-now set, and spin the remainder into a named follow-up
prompt — one work package per session still holds.

**F. Red nightly** — diagnose from the run logs before touching anything;
a signal that pattern-matches a known failure may have a different cause.
Known-benign shapes are in memory (P4.3: overlay stats + mapper-v2 remap
look like drift and aren't).

**A. img.deckwarden.gg flip** (fires on pre-flight 2; owner-gated until
then). Read `.env.local`'s current `R2_PUBLIC_IMAGE_BASE` for the exact
path shape FIRST (r2.dev host + `/optcg/images` suffix today — verify,
don't trust this file). Only after the test URL serves: set the new value
(same suffix, `img.deckwarden.gg` host) in `.env.local` AND `gh secret set
R2_PUBLIC_IMAGE_BASE`, then ONE `gh workflow run nightly-ingest.yml` —
punk-records' skip-check tuple includes `image_base`, so the dispatch
re-points all ~4.8k `image_override`s (the proven P4.1 mechanism). Watch it
green, then verify: prod hub art `src` host flipped (ISR note: /l/ pages
revalidate hourly, so assert freshness against dev and
reachability/eventual flip against prod; old r2.dev URLs keep working —
Cloudflare keeps the dev URL enabled alongside, so there is no breakage
window), `cf-cache-status` on the new host as a nicety, seo-smoke green
against dev. Then: LATER row 51 rewritten as FIRED (keep the throttling
lesson), the checklist item in `P4.6-op-beta-launch.md` checked off with
the date (the P2.8 precedent), memory updated.

**B. Limitless key wiring** (fires on owner "granted"). `.env.local` +
`gh secret set LIMITLESS_API_KEY` — never echo the key; workflow env
already wired, sent as `X-Access-Key` (re-verify that header name against
docs.limitlesstcg.com before flipping — they changed key policy once
already). One manual dispatch to prove no regression. Record the grant
date in row 58. Declined/withdrawn → one-line row 58 note, stop tracking.
The deck-rules endpoint stays its own later conversation either way.

**C. Feedback triage** (fires on owner links / open issues). Read every
named thread and issue, then sort into exactly three piles: **fix-now**
(the P4.6 paper-cut bar: funnel-critical, small, no new surfaces — the
Limitless-paste fix is the calibration example), **LATER** (features and
anything with design questions — file the row with the feedback quoted as
its trigger evidence), **won't-do** (say why in the session summary).
Walk any reported cut on prod yourself before fixing it (budget rules
below). If a thread deserves a substantive technical reply, DRAFT it in a
file for the owner — the session never posts.

**D. M4 exit recording** (fires on "posted"). Memory only, the m3-exit
precedent: a dated m4-exit note (where posted, links, launch-day state) +
MEMORY.md line. The plan document doesn't move.

**E. Cold-start probes** (fires on real OP decks existing). Count real
user decks/claims from the DB. These rows need REAL usage, not a good
week: row 52 (/l/ deck shelves) and the claim-nudge row fire as **proposed
next packages with data attached**, never as work smuggled into P4.7 —
shelf design (which decks, ordering, thresholds) is its own session. Row
59 (OP aggregate) is months of volume away; just note the
`ingest_runs` source='limitless' trend.

**(none fired)** → commit nothing code-side; deliver a short status note:
nightly state, DNS state, key state, deck/usage counts, db:size, and the
null-cost check below. Done is done.

## Standing hygiene (fold into the summary regardless of branches)

- Null-cost events: `SELECT count(*) FROM card_identities WHERE game_id=2
  AND attrs->>'category'='event' AND cost_value IS NULL` — 23 at P4.6. A
  drop means punk-records backfilled upstream and the "–" bucket healed
  itself (LATER row updates); do NOT build the overlay unless users
  complained (that row's trigger).
- If the owner posted: spot-read the announcement thread for factual
  claims that drifted from what shipped — flag, never reply.

## What P4.7 is NOT (scope fence)

- NOT posting, sending, or seeding ANYTHING — replies, decks, Showcase
  requests: drafts and checklists only, the owner sends (unchanged since
  P2.8).
- NOT M5/Azuki work (permission unanswered) and NOT the Bandai follow-up
  (posture unchanged; no draft may imply permission).
- NOT feature-building in response to feedback — the paper-cut bar or a
  LATER row, nothing between.
- NOT /l/ deck shelves, the OP aggregate, or the claim nudge as
  implementations (probe → propose, branch E).
- NOT premium/affiliate/monetization, NOT the Vercel→VPS move
  (first-affiliate-dollar trigger), NOT a home redesign, NOT /c/ title
  changes (the two-game widening is done and pinned).
- NOT the Limitless deck-rules endpoint, key or no key.

## Facts you inherit (verified in P4.6 — trust these, spot-check nothing)

- The funnel is whole and pinned: Limitless Copy-to-Clipboard exports
  resolve 17/17 via trailing-(CODE) (was 0/17); pasted leader lines route
  to the leader zone (`importZoneFor`); re-import of the card filling a
  capped zone is a silent no-op; `/l/` CTA latches via `?leader=` with the
  draft contract intact (no row until first real edit); printed-id badges
  disambiguate (two Enel LEADERS: OP05-098 vs OP15-058); OP placeholders;
  5-card sample hand; artless game-true deck OG (`deckOgLabels`,
  test-pinned); ©BANDAI posture line on OP deck share pages; /legal
  credits Topdeck + Limitless.
- Two-game identity live: title/og "Deckwarden — Commander & One Piece
  deck builder" ("Commander deck builder" kept as substring), both games
  in description + hero; per-page MTG titles verified unmoved. All
  seo-smoke-pinned in both directions.
- Limitless keys are exception-only (account banner, 2026-09-07); the
  09-05 request may sit as Pending Approval forever; NOTHING is gated.
- CTA hub of record: `/l/enel-op15-058` (230 finishes/86 events at P4.6;
  Luffy op13-001 the recognizability alternate at 110).
- 23/410 OP events carry `cost_value NULL` because punk-records upstream
  has `"cost": null` (El Thor OP15-075 raw verified) — the DON "–" bucket
  renders it honestly; the nightly heals it automatically if upstream
  backfills.
- r/OnePieceTCG (~107k, no rival sub): self-promo allowed once/3 days
  (Rule 8), text post + "Original Content" flair, AutoMod holds ~half of
  fan-tool posts (modmail, never repost). Discords: discord.gg/optcg
  (~107k), TCG Match Making (~55k), Egman's (competitor's house),
  Limitless Online (data questions + Showcase).
- seo-smoke now creates TWO fixture decks (MTG + OP, deleted after) — run
  it against dev only; against prod it would spend 2 of the 10/h
  deck-create budget.
- Deck-delete UI uses a native confirm that scripted clicks can't drive —
  walks delete via API + claim token.
- StrictMode lesson: never pair an abort-on-cleanup with a run-once ref
  guard (the abort kills the only allowed attempt).

## Verify-first (never from memory)

1. Branch A: the CURRENT `R2_PUBLIC_IMAGE_BASE` value and path suffix
   from `.env.local`, and the test image on the new domain, BEFORE any
   env/secret write.
2. Branch B: the auth header name in Limitless's live docs.
3. Branch C: reproduce each reported cut on prod yourself before editing.
4. Branch E: real-deck counts and claim rates from the DB, not
   impressions from a thread.
5. Any nightly diagnosis: the run's own logs, not the memory of past
   shapes.

## Deployable outcome

State-dependent, with a floor: every fired branch deployed, check-green,
and browser-verified on prod (hidden-pane rules); LATER.md rows moved (51
fired or untouched; 58 dated or closed; new C-rows filed with quoted
triggers); `P4.6-op-beta-launch.md` checklist items checked off as the
owner completes them (with dates); memory updated (m4-exit if posted; the
p46 "Next" pointer superseded); db:size stated. If nothing fired: the
status note IS the deliverable — no code commit, main stays green, and the
session says exactly which triggers it checked and found cold.

## Session notes (environment, unchanged from P4.6 unless noted)

- PATH: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"`
  (node/pnpm + gh); psql/aws absent — ad-hoc SQL via tsx in scripts/.tmp/
  (tsconfig-excluded but LINTED; delete scratch when done).
- Deploy verification: Vercel commit status (context "Vercel") on the
  full 40-char sha; `gh run list --workflow=nightly-ingest.yml` for
  nightlies.
- Browser pane: hidden pane swallows computer clicks AND read_page AND
  scroll/screenshot state — drive via javascript_tool, verify via
  DOM/API reads.
- Prettier reflows scripted-edit anchors: after `pnpm format`, re-grep
  before exact-string edits.
- ISR surfaces lag revalidate windows in prod — assert fresh behavior
  against dev, existence/reachability against prod; check a route's own
  `revalidate` before asserting freshness anywhere (/l/ hubs: 3600).
- PROD deck-create budget 10/h + 30/day per IP: read
  `rate_limit_counters` before any prod walk that creates a deck; never
  run engagement/versions smokes against prod.

## Context, not tasks

Bandai and Azuki permission emails remain unanswered — posture unchanged.
Cold-start rule holds everywhere: no simulated decks, posts, or metrics,
ever. Premium never gates card data. After P4.7, the next NUMBERED package
is whichever trigger arrives first: the /l/ deck-shelves package (row 52,
carried by real decks), M5 (carried by Azuki permission), or another
beta-response round if feedback volume warrants one — each gets its own
prompt written the way this one was.
