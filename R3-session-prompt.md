# R3 session prompt — Desktop builder (header hierarchy, search state machine, Tabs, Toast, the Warden moment, the completion ring)

Pull latest, then run R3 — the third R-series package. **`REDESIGN.md` is the contract (read §1 Mechanics — motion, icons, primitives, game accent; §2 "Desktop builder" for the header / search / deck pane / tool pane specs and the responsive table you are NOT building; §4 for the interaction language, the fun moments F1 F2 F3 F7 F9 F10 F14 and the catalog rows C5 C12 C13 C15 F6 F11 plus the unnumbered "builder fixes" row; the R3 row in §5; the ledgers and the R1a / R1b decisions in §6 — the R1b `AppearanceRow` note is yours to retire; and §7 first)**; build plan §6b makes R-packages legal sessions under the CLAUDE.md protocol. R3 is the desktop builder: the editor's own header (mark, game/format chip, name, a fixed-width save slot, Share primary, a More menu), the search state machine with tiny full-card thumbnails and actions on the active row, `useEditorHotkeys` (`/` and `?` inert while a dialog is open), keycaps and the `?` shortcut sheet, sticky search input and group headers, steppers on hover / focus-within with pips in a fixed column, the One Piece badges (`idBadge` on rows / grid corners / leader zone, Life on the leader caption, Power and Counter as a muted mono suffix), the tool pane on Base UI Tabs with a sliding indicator and View / Group / Sort on ToggleGroup, the add toast with Undo, the save-indicator micro-animation, the post-import count-up, the completion ring on Meter in the game accent, "The Warden approves this deck ✓" on the false→true zero-issue transition, and the three builder fixes (leader replace with Undo for max-1 zones, "Choose commander / leader" focusing search, the single-panel tool-pane heading). Expect ZERO migrations, ZERO new dependencies, no new URLs, no API changes (every edit still flows through `/api/decks`, `/api/decks/[id]/cards`, `/api/cards/search` and `/api/cards/resolve` exactly as today), no data or ingest work, and no visible change to the (site) pages beyond the shared deck components (`DeckTextView`, `DeckGridView`, `LeaderZone`, `Segmented`) that the share page also renders. The owner's decisions are settled (REDESIGN.md §0 and §6): the keyboard script is unchanged, the fun ceiling is restrained (no confetti, sounds, streaks), OP ambient art is off (R2's anyway), one appearance control per page, and the first real edit creates exactly one deck. Do not relitigate them.

Pre-flight, in order. (1) Nightlies since 2026-09-09 all green (`gh run list --workflow=nightly-ingest.yml`; cron 10:37 UTC drifting ~13:30–17:40; 2026-09-08's ran 14:45Z green; 09-09's is the FIRST nightly on R1a + R1b code and had not fired when R1b closed at 06:24Z on 09-09). A red run is P4.7 branch F and takes precedence — fix or report it as P4.7, and R3 waits for its own session (one package per session). (2) Working tree clean at or after `a21a99f` (the R1b docs commit) plus this prompt's commit. (3) db:size baseline stated (253.4MB on 2026-09-09; Neon alert 350MB) — R3 adds no rows except the one anonymous draft deck the dev-side first-edit check mints, which you delete before the session ends (state its id and its deletion). (4) QA fixtures unchanged: the OP deck "Six DON!!, Endless Thunder" at `/d/jhr5ax43ewx7` and ALL FOUR public MTG decks (`/d/q9j9uxphwp9n` Queza — most recently updated, `/d/uwvrnv2pv4t6` Atraxa Superfriends, `/d/4c5_9yd9ewdy` Boros Equipment Burn, `/d/end97r37znuv` Nelson & Murdock) belong to the owner's account (read from the DB 2026-09-09). The owner's signed-in look: Queza and the Enel deck were saved from the owner's account at 03:44Z / 04:13Z on 2026-09-09, after R1a shipped, so the signed-in editor look evidently happened; the `/account` Danger-zone look leaves no trace and is unconfirmed — state whether the owner did it. The browser pane is signed out on prod and OAuth cannot be typed, so R3's own editor pass is a GUEST DRAFT on DEV with real edits (`/decks/new?game=mtg|optcg`) and, on prod, draft mode with NO edits (deck-create budget). (5) `pnpm check` green at the start (520 tests at `a21a99f`, 6 pre-existing `no-unused-vars` warnings, 0 errors) and `pnpm build`'s route table identical to R1b's under Facts — R3 touches no route files, so nothing may move; state it anyway. No `shadcn add` is expected (tabs, toast, tooltip, toggle-group, collapsible, meter, sheet, skeleton, badge are installed); never `shadcn init`; if an add turns out necessary, the R1a notes in REDESIGN.md §6 tell you what the CLI does to `cn` and `button.tsx`.

What R3 is NOT (scope fence)

NOT the responsive builder: no `wide:` breakpoint token, no tablet Drawer, no phone Deck / Search / Tools tabs, no card sheet, no `DrawerVirtualKeyboardProvider`, no 44 px audit — R4. The grid stays `lg:grid-cols-[minmax(20rem,26rem)_minmax(0,1fr)_minmax(16rem,22rem)]`, `lg` stays the only breakpoint, and below it the three panes still stack full-width. NOT the art resolver, `/api/cards/[id]/art`, `capabilities.ambientArt`, the ambient layer, or the appearance menu's "Background art" entry (R2 — it lands on the header you build). NOT the share page's artwork header, hover / focus previews, the Copy-decklist ripple, or the leader glow ring (R5b) — the share page changes only through the shared components and must keep working read-only (no steppers, no remove, no Choose action when the mutation props are absent). NOT the homepage, the hubs, the indexes, `/account` (R5a / R5b), and NOT the site header (R1b's — unchanged; the editor still renders no `SiteHeader`, and the `/decks/new` picker keeps rendering it). NOT `loading.tsx` skeletons, the analytics grow-in, the dealt-hand stagger F4, or the target-curve ghost G9 (R6) — `SampleHand` changes only by gaining its collapsible wrapper. NOT the claim nudge (row 62 stays trigger-gated), NOT ⌘K (a LATER row only when a user asks), NOT multi-step undo (the single Undo ships; write its LATER row with the trigger), NOT the card-stacks grid view (write its LATER row when you touch the grid badges), NOT draw-next in the sample hand (row 14). NOT API or route changes, NOT a change to `RESULT_LIMIT` (20), the 200 ms search debounce, the 1 s autosave debounce, `MAX_QTY` (99), or the wire shapes. NOT a Next, React, Tailwind, or Base UI upgrade. NOT any P4.7 branch unless pre-flight (1) forces it, and then R3 is not this session.

Facts you inherit (verified 2026-09-09 against `a21a99f` — trust these, spot-check nothing)

* Editor root (`src/components/editor/deck-editor.tsx`, 933 lines). `<div className="flex min-h-dvh flex-col lg:h-dvh" data-game={adapter.id}>` → `<header className="flex shrink-0 flex-wrap items-center gap-x-3 gap-y-1.5 border-b px-4 py-2 lg:h-14 lg:flex-nowrap lg:py-0">`: a `Link` to `/` (`ArrowLeftIcon` + "Deckwarden", `aria-label="Back to home"`), the name `<input aria-label="Deck name" placeholder="Untitled — click to name your deck" maxLength={120} className="… order-last min-w-0 basis-full … lg:order-none lg:flex-1 lg:basis-auto">` (name edits `markDirty()` through `metaRef`), `ForkCreditLine` when `forkedFrom`, then a right cluster of outline `xs` Buttons Details · Import · Export · Share (only once `share` is set, i.e. a server row exists) · History (only with `liveDeckId`) · `SaveIndicator` — "Saved" / "Saving…" / "Unsaved…" in `text-sm tabular-nums` with `aria-live="polite"`, or "Save failed" plus a destructive `xs` Retry that calls `autosave.flush()`. Then the dialogs, conditionally mounted on `dialog: "import" | "export" | "share" | "details" | "history" | null` (`DetailsDialog`, `ImportDialog`, `ExportDialog`, `HistoryDialog` — `wide`, `ShareDialog`; all on `Modal`, a Base UI Dialog since R1a: focus trap, Escape, focus restore). Then the grid `min-h-0 flex-1 gap-0 lg:grid lg:grid-cols-[…]` of three `<section>`s — `aria-label="Card search"` (`min-h-0 border-b lg:overflow-y-auto lg:border-r lg:border-b-0`), `"Deck list"`, `"Card detail and suggestions"` (`ref={rightPaneRef}`; `openCuts` scrolls it into view on stacked layouts) — each its own scroll container on `lg`, which is what sticky positioning must be relative to. After the root div, R1b's `<AppearanceRow />` (rendered by `src/app/decks/[id]/edit/page.tsx` and by the chooser's editor branch in `src/app/decks/new/new-deck-chooser.tsx`) sits above the site footer; its docblock says R3 retires it.
* Editor state and the pure layer. `entries` + `entriesRef` (the ref is what saves read), `cards` map, `preview`, `share`, `forkedFrom`, `owned` / `hasCollection`, `rightTab: "card" | "suggest" | "combos" | "cuts"`, `liveDeckId`. `applyEdit(result: EditResult)` sets entries and `markDirty()`s, returning the error; `handleAdd(card, zoneId, qty)` (search) returns the error string; `handlePanelAdd(card)` adds one to the main zone WITHOUT flipping the tab; `handleSetQty`, `handleRemove`, `handleImport(outcome)` (whole-list swap + `markDirty`), `showCard` (sets the preview AND flips `rightTab` to "card"), `openCuts`, `handleDeleteDeck` (guest → `/`, account deck → `/account`). `src/lib/decks/editor-state.ts` (tested): `parseQuickAdd` (`/^\s*(\d{1,2})\s*[xX]?\s+(.*\S)/`, qty clamped 1..`MAX_QTY` 99), `addCard` (an existing (zone, card) entry increments; a zone maximum returns `error: "${zone.label} is full (max N card(s))"`), `setQty` (≤ 0 removes; the maximum is re-checked), `removeCard`, `setTags`, `deckSizeCount`, `toSavePayload`. `src/lib/decks/panel-view.ts` (`deckStateKey`, `hasLeader`; tested) is the panels' refetch policy. `src/lib/decks/view-prefs.ts` persists View / Group / Sort under `deckwarden:deck-view`.
* Draft contract (P2.8). `deckId === null` is draft mode; `ensureDeck()` is single-flight and POSTs `/api/decks` `{ game, format, website: "" }` on the FIRST flush that has something to say, stores the claim token (`token-store.ts`), `history.replaceState`s to `/decks/[id]/edit` with no remount, and sets `liveDeckId` / `share`. `use-autosave.ts`: `markDirty()` → 1 s trailing debounce → `flush()`; statuses `saved | dirty | saving | error`; a failure keeps the state dirty and Retry is `flush()`; `pagehide` / unmount flush with keepalive. The theme provider sits OUTSIDE the editor tree (REDESIGN §1), so a theme change touches nothing that calls `markDirty`; R1b proved on prod that a theme switch in a draft leaves the URL on `/decks/new` and sends no `/api/decks` request. The hub CTA seam (`draftLeaderKey` → `/api/cards/resolve` → a leader-zone entry that is state only, never a save) must survive: `seo-smoke` ~342 pins the `/decks/new?game=optcg&leader=<KEY>` href on `/l/` hubs.
* Search (`src/components/editor/search-pane.tsx`, 257 lines). `raw` → `parseQuickAdd` → 200 ms debounced GET `/api/cards/search?game&format&name&limit=20`; `results`, `sel`, `message: { text, tone: "ok" | "err" }`. The input: `role="combobox"`, `aria-controls="quick-add-results"`, `aria-activedescendant="search-result-{i}"`, `aria-label="Card search"`, `autoFocus`, placeholder from `adapter.display.searchPlaceholder` ("Add cards — try “4 Sol Ring”" / "Add cards — try “4 OP01-025”"). Results: `ul#quick-add-results role="listbox"` of `li role="option" aria-selected` rows (`group/row`), each with the name, the `idBadge` chip (OP), the in-deck `×N`, `CostPips`, and Add / `{leaderNoun}` secondary `xs` Buttons revealed by `group-hover/row:flex` — hover only, never on the active row or on touch (the spec's `group-aria-selected/row:flex` fix). Keys: ArrowDown / ArrowUp wrap and preview, Enter adds `qty` to the main zone, Ctrl / Cmd+Enter adds 1 to the leader zone, Escape clears; double-click adds. After an add: message "Added 4× Name" (" as Leader" for the leader zone), input cleared and refocused. The hint line is plain text: `↑↓ select · Enter add · Ctrl+Enter add as {leaderNoun} · “4 Name” sets quantity`. "Search failed — check your connection." exists with no Retry. The two bugs the spec names, confirmed: "No cards match “{query}”." renders whenever `query && results.length === 0`, including while the request is still in flight; `message` never clears on new input (only on the next add or error). The `/` shortcut is a `window` keydown listener INSIDE SearchPane that skips only INPUT / TEXTAREA targets — a Base UI dialog's focused button is neither, so `/` inside a dialog focuses the search box behind it; there is no `?` today. No thumbnails: `EditorCard.image` (the display URL — Scryfall `normal` for MTG, the pub-…r2.dev mirror for OP) is on every result; `toSmallImage()` in `src/lib/cards/images.ts` rewrites only the `cards.scryfall.io/normal/` pattern to `small` and passes other URLs through untouched; `thumbnailUrl()` does not exist yet (`images.test.ts` has a `toSmallImage` block to extend).
* Deck pane (`src/components/editor/deck-list-pane.tsx`, 203 lines). Header row: `<h2 className="text-sm font-semibold">Deck</h2>` + a right cluster — the over-limit button "Over by N — rank cuts" (only with `onOpenCuts` and `total > max`; MTG only, since OP declares no `recommend`), `"{total} / {max} cards"` (`tabular-nums`), the ownership line (`data-testid="ownership-line"`, only with a collection). Then three `Segmented` controls (View Text / Grid, Group Type / Cost / Tags, Sort Name / Cost / Price — `src/components/deck/segmented.tsx`: `role="group"` + `aria-label`, `aria-pressed` buttons, shared with the share page), an `aria-live="polite"` error line for rejected quantity edits, `ValidationPanel`, `AnalyticsPanel` (a hand-rolled disclosure in `analytics-blocks.tsx`: `aria-expanded` button "Analytics", closed by default), `LeaderZone` (R1b `EmptyState` "No commander yet" / "No leader yet" + hint "Ctrl+Enter on a search result adds one." — the hint only when `onRemove` is set; occupied: `w-40 max-w-[45%]` `CardImage frame` inside a button with the severity ring and a remove `XIcon`), the R1b `EmptyState` "No cards yet" / "Add them from Search." with the mark, then `DeckTextView` or `DeckGridView`, then `SampleHand` (always rendered, no collapse; "Draw sample hand" deals 7 / 5 `CardImage`s at 146×204 — F4 (R6) restyles it).
* `DeckTextView` rows (`src/components/deck/deck-text-view.tsx`): `li.group/row` → steppers ALWAYS visible (`−` / `+` ghost `icon-xs` Buttons, `aria-label="One fewer/more {name}"`, around a `w-6` quantity span), the name button (severity dot, truncating name, ✓ owned mark), `CostPips` (`shrink-0 text-xs` — not a fixed column), the remove `XIcon` button at `opacity-0 group-hover/row:opacity-100 focus-visible:opacity-100`. Group headers `<h3 className="text-muted-foreground border-b pb-1 text-xs font-medium tracking-wide uppercase">` are not sticky. Rows are plain list items (not a listbox), so `focus-within` works there. `DeckGridView` (R1b `CardImage frame`) overlays `×N` top-right and the owned badge top-left — top corners only, on purpose.
* Validation. `ValidationPanel` (`src/components/deck/validation-panel.tsx`): zero issues → `<p className="mt-2 … text-emerald-700 dark:text-emerald-400"><span aria-hidden>✓</span> Legal {formatLabel} deck</p>` (no `role="status"`); otherwise a summary button "N problems · M warnings" that expands the list with card chips. Both adapters emit a `DECK_SIZE` error whenever the count is under the minimum (`src/lib/games/mtg/validate.ts` ~158: "Commander decks are exactly 100 cards (has N)."; `src/lib/games/optcg/validate.ts` ~110 plus the `LEADER_*` codes), so an EMPTY deck never has zero issues — F1's "never on an empty deck" is already enforced by the data; F1 needs only the false→true transition guard and `role="status"`.
* Zones. MTG Commander (`src/lib/games/mtg/formats.ts`): `commander` min 1 / max 2 (`isLeaderZone`, `countsTowardSize`, copy limit 1), `main`; `deckSize` 100 / 100; hand 7. OP Standard (`src/lib/games/optcg/adapter.ts` ~50): `leader` max 1 (`isLeaderZone`, does NOT count), `main` 50 / 50 (`defaultCopyLimit` 4); hand 5. So the "full max-1 zone" that gets replace-with-Undo is the OP leader zone only; the MTG commander zone at two keeps `addCard`'s "Commander is full (max 2 cards)". `display.leaderNoun` is "Commander" / "Leader".
* Tool pane. `load.adapter.recommend || load.adapter.capabilities.combos` → a hand-rolled `div role="tablist" aria-label="Right pane view"` of `RightTab` buttons (`role="tab" aria-selected`) Card · Suggestions (`adapter.recommend`) · Combos (`capabilities.combos`) · Cuts (`recommend.cuts`), with `div role="tabpanel" hidden={…}` panels that stay MOUNTED — each panel's `active` prop gates its fetching and `saveStatus` (the autosave status) makes it refetch once per settled burst. Else (OP declares neither) the bare `<CardDetailPane>` with no heading at all. Base UI `Tabs.Panel` has `keepMounted` (verified in `node_modules/@base-ui/react/tabs/panel/TabsPanel.d.ts`); `Tabs.Indicator` exposes `--active-tab-left / -right / -top / -bottom` CSS vars; the installed `src/components/ui/tabs.tsx` (Tabs / TabsList / TabsTrigger / TabsContent, a `line` variant with an `after:` underline) has no Indicator part yet.
* Card detail pane (R1b: `CardImage` without frame, `mx-auto w-full max-w-72`): name, `CostPips`, `{idBadge} · {subtitle} · {statLine}`, body text, the P2.7 tag editor, the price and a "Card page" link. `adapter.display`: `costHtml`, `subtitle`, `bodyText`, `statLine?` (OP: "5000 Power" / "+1000 Counter" / "5 Life" parts joined with " · "), `defaultGroupBy`, `leaderNoun`, `searchPlaceholder`, `idBadge?` (OP → `externalKey`, e.g. "OP15-058"; MTG none). `idBadge` renders today only in search rows and the detail pane — not on deck rows, grid badges, or the leader zone (C13). `.don-cost` is the OP cost chip (R1a); `CostPips` renders whatever `costHtml` emits.
* Primitives installed and unwired for R3: tabs; toast (`src/components/ui/toast.tsx`: `Toaster` = Provider + Portal + Viewport + list, a module-level `toast = createToastManager()`, `toast.add({ title, description, type, timeout, actionProps })` with `actionProps: ButtonProps` rendered by `ToastAction` as an outline `sm` Button, `ToastClose` labelled "Close toast" — and `<Toaster>` is mounted NOWHERE yet, grep confirms 0 uses); tooltip (Provider / Tooltip / Trigger / Content); toggle-group (`ToggleGroup` with `multiple` false = single value array, `ToggleGroupItem`); collapsible (Root / Trigger / Panel); meter (`src/components/ui/meter.tsx`, hand-written: Root / Track / Indicator / Label / Value — a LINEAR track; `MeterPrimitive.Root` takes `value / min / max / getAriaValueText / format` and provides `role="meter"` + `aria-valuenow`, so a ring is Root plus a custom SVG indicator, exactly what its docblock says R3 does); sheet; skeleton; badge. There is no `kbd` primitive — keycaps are plain `<kbd>` markup. dropdown-menu is wired (R1b) including `DropdownMenuLinkItem`; `AppearanceMenu` takes `side`.
* Motion and tokens. `motion-safe:` on every decorative animation; the reduced-motion backstop in `globals.css` zeroes durations; 120 ms controls, 250 ms crossfades, ≤ 400 ms one-offs; tw-animate-css classes (`animate-in`, `zoom-in-95`, …) are available. `--accent-game` comes from `[data-game]` on the editor root; portaled content (toasts, dialogs, menus) renders OUTSIDE that root, so `--accent-game` falls back to the brand there unless the portal's element carries `data-game` too.
* Copy pins. No smoke reads the editor's DOM (it is client-rendered; `/decks/[id]/edit` SSRs only "Loading deck…"). API-level smokes R3 must not break: `smoke:decks` (create returns the claim token once, PUT / DELETE contract), `smoke:versions` (versions, forks, restore), `smoke:optcg-deck` ("PUT cards → 200 (issues reported, never a rejection)"), `smoke:collection` ("You own 1/3" on the share page for the collection holder, no "You own" for guests). `seo-smoke` pins the hub CTA href above. Every `Modal` label and dialog button name is free to change EXCEPT the Danger-zone strings on `/account` (not the editor's).
* Tests: 520 green at `a21a99f`. RTL precedents: `src/components/ui/modal.test.tsx`, `src/components/auth/delete-account.test.tsx` (mocks `next/navigation`), `src/components/site-header.test.tsx` (opens Base UI menus with the pointerDown → mouseDown → click sequence — that works in jsdom, unlike the hidden pane), `src/components/cards/card-image.test.tsx`. Pure precedents: `editor-state.test.ts`, `view-prefs.test.ts`, `panel-view.test.ts`, `sample-hand.test.ts`, `images.test.ts`.

Verify-first list (never from memory)

1. The keyboard script, unchanged, on DEV in a guest draft (`/decks/new?game=mtg`): `/` focuses search from the deck pane; type `4 Sol Ring`; ArrowDown / ArrowUp wrap and preview; Enter adds 4× to Main deck, clears and refocuses the input; Ctrl+Enter on a legendary creature adds it as Commander; Escape clears. Drive it with `KeyboardEvent`s dispatched on the focused element via javascript_tool (React listens at the root — `bubbles: true`) or with `computer` on a fronted tab; assert via the DOM (`aria-activedescendant`, deck rows, the toast) and `read_network_requests`.
2. The first edit creates exactly ONE deck: one `POST /api/decks` (count it), the URL swaps to `/decks/<id>/edit` with no remount (the search input keeps focus), then `PUT /api/decks/<id>/cards`; a second edit adds no POST. Delete that deck from Details → "Delete deck…" at the end and confirm the row is gone (tsx in `scripts/.tmp/`, delete only what you add).
3. Theme and appearance never mark dirty: with the editor idle at "Saved", switch the theme through the header's control → the slot still says "Saved", no `/api/decks` request; repeat in the OP draft.
4. `useEditorHotkeys`: with any dialog open (Details, Import, Export, History, Share, the `?` sheet), `/` and `?` on a focused dialog button do nothing (focus stays inside); the pinning component test renders the editor's hook host with a dialog open and fires the keys.
5. The search state machine: the reducer test covers idle → typing → searching → results / empty / failed and clear; "No cards match “q”." never renders before a response; the notice clears on the next input; "Search failed — check your connection." shows a Retry that refetches the same query.
6. Thumbnails: `thumbnailUrl()` returns the `small` Scryfall rendition for `cards.scryfall.io/normal/...` URLs and null for anything else (the OP mirror included — row 51); rows render a sized (no shift) full-card thumbnail, lazy, never cropped; OP rows keep row alignment without an image. Pinned in `images.test.ts`.
7. Active-row actions: Add / Commander (Leader) buttons are visible on the `aria-selected` row and on coarse pointers, not only on hover; Enter still adds without them.
8. Tabs: the tool pane on Base UI Tabs with every panel `keepMounted` — switch Suggestions → Combos → Card and back; results survive with no refetch (count `/api/decks/<id>/recommendations` and `/combos` requests); the indicator slides `motion-safe:` only; `showCard` and `openCuts` still drive the tab; the OP draft shows the plain "Card" heading and no tablist.
9. Toast + Undo: "Added 4× Sol Ring" toasts with Undo; Undo goes through `applyEdit` (a real edit → the next autosave PUTs it) and restores the PREVIOUS quantity, not zero; rejected adds (zone full) and search failures stay on the pane's live line and never toast — nothing announces twice; the `Toaster` mounts once; the Undo button focuses and rings in the game accent.
10. Leader replace (OP): with Enel in the zone, Ctrl+Enter or the Leader button on another leader REPLACES it and toasts "Replaced Enel with …" + Undo (Undo puts Enel back and removes the other); in the MTG draft a second commander still adds (max 2) and a third still returns "Commander is full (max 2 cards)". The pure helper is unit-tested; the behavior is checked in the pane.
11. The Warden approves (F1): on the false→true zero-issue transition the line reads "The Warden approves this deck ✓" with `role="status"` and the shield settles once, ≤ 400 ms, `motion-safe:`; never on an empty deck (a fresh draft shows the DECK_SIZE problem, not the line); not re-triggered by a name edit, a tag edit, a View toggle, or a theme switch; the line stays until issues return. Prove the transition in a component test with a fake adapter whose `validate` flips.
12. Completion ring (F2): `role="meter"` with `aria-valuenow` and `getAriaValueText` "N of M cards", drawn in `--accent-game`; the glow fires once at exactly max (`motion-safe:`); over the limit the ring turns destructive beside the existing "Over by N — rank cuts" action; a null `deckSize.max` renders no ring (no format has one today — keep the branch).
13. Save slot (F9): fixed width — measure the slot's box across "Saved", "Saving…", "Unsaved…", "Save failed" + Retry; nothing in the header shifts; Retry stays outside the More menu.
14. Count-up (F10): an import with delta > 5 animates the total (`motion-safe:`), ±1 edits never animate, reduced motion is instant; the final number is always exact.
15. Keycaps (F14) and `?` (F7): the hint line renders `<kbd>` elements; `?` opens the shortcut sheet (focus-trapped, Escape closes, focus returns to what had it); `?` typed INSIDE the search input still types "?" (text-field targets excluded, like `/`).
16. Sticky (C12): the search input sticks to the top of the search section and group headers stick inside the deck section while those sections scroll on `lg`; nothing overlaps the summary row; below `lg` (stacked, page scroll) nothing breaks.
17. C13 / C15 on the OP draft (`/decks/new?game=optcg&leader=OP15-058`): the seeded Enel shows "OP15-058" and "5 Life" on the leader caption; deck rows show the card number and "5000 · +1000" (Power · Counter) as a muted mono suffix, and `.don-cost` on every cost surface (search rows, deck rows, detail pane); grid badges sit in the top corners only.
18. Header hierarchy: the mark → `/` (the `BrandMark`, F13 tilt, NOT the R1b `SiteHeader`), the game · format chip, the name input (same `aria-label` and placeholder), the save slot, Share primary once `share` exists, More (a Base UI Menu) holding Details · Import · Export · History (History only with `liveDeckId`), the fork credit line kept, the appearance control folded into the header and `AppearanceRow` deleted from both mount sites — exactly one appearance control in the DOM of every page (the R1b header test keeps passing for the (site) pages; add the same assertion for the editor).
19. Share-page regression: `/d/q9j9uxphwp9n` and `/d/jhr5ax43ewx7` still render read-only (no steppers, no remove, no Choose action; View / Group / Sort on the new ToggleGroup work; the ownership line untouched); `pnpm smoke:collection`, `smoke:versions`, `smoke:decks`, `smoke:optcg-deck` green against dev, never prod.
20. Deploy verification via the Vercel commit status on the full 40-char sha; then the prod pass in §Deployable outcome.

Design decisions to make explicitly (disclose + test-pin each)

* Editor header composition. `BrandMark` link to `/` (`size-7`, the same F13 tilt classes as the site header, `aria-label="Deckwarden"`) · a `Badge` chip `{adapter.name} · {format.label}` (`data-game` stays on the root — the chip sets nothing) · the name input unchanged in contract · `SaveIndicator` in a fixed-width `tabular-nums` slot (`aria-live="polite"`, the four states never shift the row; the R1a `Button` keeps "Retry" destructive) · Share as the primary Button once `share` is set — in a draft hide it (nothing exists to share) and disclose · More: a `DropdownMenu` with an `EllipsisIcon` trigger `aria-label="More"` and items Details · Import · Export · History (History only with `liveDeckId`), each opening the existing dialog · the appearance control: `AppearanceMenu` placed right of More (icon-only below `sm` already), `AppearanceRow` and its two mount sites deleted · the fork credit line stays on the name row · the `← Deckwarden` text link goes (the mark is the way home). Under `lg` the header still wraps to two rows as today.
* `useEditorHotkeys` (`src/components/editor/use-editor-hotkeys.ts`): ONE window keydown listener owned by `DeckEditor`, `{ enabled, onSlash, onHelp }` with `enabled = dialog === null && !shortcutsOpen`; ignores INPUT / TEXTAREA / contenteditable targets and anything inside `[role="dialog"]` / `[role="alertdialog"]`; `SearchPane` exposes focus through a forwarded ref (or an `inputRef` prop) so the hook and the Choose action can call it. SearchPane's own `/` listener is deleted.
* Search state (`src/lib/decks/search-state.ts`): a pure reducer over `{ status: "idle" | "typing" | "searching" | "results" | "empty" | "failed", query, results, sel, notice }` with actions input / request / response / failure / move / added / clear (and a request id so a stale response never wins); the component becomes a thin shell over `useReducer` with the same 200 ms debounce, the same `/api/cards/search` params, `RESULT_LIMIT` 20. "No cards match “q”." only in `empty`; "Search failed — check your connection." + Retry only in `failed`; `notice` clears on the next input.
* Thumbnails: `thumbnailUrl(image: string | null): string | null` in `images.ts` = `toSmallImage` for `cards.scryfall.io` URLs, null otherwise; rows render `CardImage` (width 146, height 204, lazy, no frame) constrained to about `h-9`; null renders no `<img>` but keeps a same-width spacer so MTG and OP rows align and the row-51 flip changes no layout — disclose the spacer.
* Toasts: mount `<Toaster>` once inside `DeckEditor` (not the root layout — nothing else toasts yet); give the viewport `data-game={adapter.id}` (extend `Toaster` with a viewport-props passthrough) so the Undo button rings in the game accent; ~5 s timeout; one toast per successful add ("Added 4× Sol Ring", "Added Sol Ring as Commander"), replacing the pane's success line; errors stay on the pane's `aria-live` line. Undo = `applyEdit(setQty(entries, format, zone, cardId, previousQty))`.
* Leader replace: `replaceLeader(entries, format, zoneId, cardId)` in `editor-state.ts`, valid only for `isLeaderZone && max === 1`: returns `{ entries, replaced: previousCardId | null }`; the editor toasts "Replaced Enel with Nami" + Undo (restore the previous, remove the new); MTG partner zones (`max: 2`) are untouched — `addCard`'s full message stands.
* "Choose commander / leader": `LeaderZone` gains an optional `onChooseLeader` prop; when set (editor only), the empty state's action is a Button "Choose {leaderNoun}" that focuses the search input and keeps the hint "Ctrl+Enter on a search result adds one."; the share page (prop absent) keeps the plain EmptyState.
* Single-panel heading: when the adapter declares neither `recommend` nor `combos`, render a "Card" heading in the same row height as the tablist above `CardDetailPane`, so both games' panes align.
* Tabs: `src/components/ui/tabs.tsx` gains `TabsIndicator` (Base UI `Tabs.Indicator` positioned with the `--active-tab-*` vars, `motion-safe:transition-[left,width] motion-safe:duration-150`); the tool pane uses `Tabs` controlled by `rightTab` with `TabsContent keepMounted` for every panel; the panels' `active` props stay derived from `rightTab`.
* ToggleGroup: `Segmented` reimplemented over `ToggleGroup` (single value, roving focus, `aria-label` kept) with a CSS-slid active background (`motion-safe:`); its `label / options / value / onChange` contract stays so the editor and the share view do not change their call sites.
* Completion ring: `src/components/deck/completion-ring.tsx` — `MeterPrimitive.Root` + an SVG circle (`size-8`, `stroke: var(--accent-game)`, `stroke-dashoffset` with `motion-safe:transition-[stroke-dashoffset] motion-safe:duration-250`), `getAriaValueText` "N of M cards"; over the limit `stroke: var(--destructive)`; at exactly max a one-shot glow keyframe (≤ 400 ms, `motion-safe:`); the count text stays beside it in `tabular-nums`; `deckSize.max === null` → no ring.
* The Warden line: `ValidationPanel` renders the zero-issue line as `<p role="status">` "The Warden approves this deck ✓" (build plan §10 wording) with the `BrandMark` beside the check; a ref tracks the previous zero-issue state and the settle animation (`motion-safe:animate-in zoom-in-95 duration-300`, once) plays only on the false→true transition; the share page shows the same line statically (no animation on mount) — disclose.
* Sticky: the search input `sticky top-0 z-10 bg-background` inside its section; group `<h3>`s `sticky top-0 bg-background` inside the deck section; decide whether the deck summary row sticks too — if it does, the group headers' `top` offsets by its height, and measure it in both games.
* Steppers and pips: the quantity always visible; `−` / `+` `opacity-0 group-hover/row:opacity-100 group-focus-within/row:opacity-100`; `CostPips` in a fixed right column (measure the widest MTG cost in the fixtures — Atraxa's six-pip costs — and pick the width); OP rows: the `idBadge` chip after the name and a `font-mono text-xs text-muted-foreground` "5000 · +1000" suffix built from the adapter's `statLine` parts (Life belongs to the leader caption only).
* Keycaps and the sheet: a tiny `Kbd` (`src/components/ui/kbd.tsx`, a styled `<kbd>`) used by the hint line and the shortcut sheet; the sheet is a `Modal` titled "Keyboard shortcuts" listing `/`, ↑ ↓, Enter, Ctrl+Enter, Escape, `4 Name`, `?` — the Dialog already traps focus; disclose if you choose the Sheet primitive instead.
* Count-up: `useCountUp(value, { threshold: 5, durationMs: 400 })` on the deck total, instant when `matchMedia("(prefers-reduced-motion: reduce)")` matches or the delta is ≤ 5; the hook is unit-tested with fake timers.
* Collapsibles: `AnalyticsPanel` and `SampleHand` wrapped in the Collapsible primitive with labelled triggers ("Analytics", "Sample hand"), analytics closed by default as today, the sample hand open by default (its Draw button is the content), state not persisted.
* LATER rows to write when this ships (with triggers): multi-step undo (trigger: the single Undo proves demand — a user asks for more), card-stacks grid view (trigger: grid-view usage); ⌘K only if a user asks. Row 14 untouched; the sample hand adds no draw-next.

Deployable outcome
`pnpm check` green and deployed; verified on DEV in guest drafts of both games — the keyboard script, first edit → exactly one deck (then deleted, id stated), Undo, leader replace, `?`, Tabs with surviving panel state, the ring, the Warden line, sticky headers, keycaps, thumbnails, C13 / C15, one appearance control, no site header; verified ON PROD in both themes with NO edits — `/decks/new?game=mtg` and `/decks/new?game=optcg` show the new header (mark, chip, name, save slot, More, appearance), the MTG tablist and the OP "Card" heading, keycaps, the `?` sheet opening and closing with focus restored, the R1b empty states, and the share pages `/d/q9j9uxphwp9n` and `/d/jhr5ax43ewx7` still read-only with the new controls; smokes `decks`, `versions`, `collection`, `optcg-deck` green on dev (verify-first 19, never against prod). Docs in the same package: `REDESIGN.md` progress tracker R3 ticked, every decision above that deviated from §2 / §4 recorded under §6 open items, the unwired-primitives list updated (tabs, toast, tooltip if used, toggle-group, collapsible, meter, badge now wired), the two LATER rows written with triggers, `AppearanceRow`'s retirement noted against the R1b decision; db:size stated; the dev deck's deletion stated. Nothing posted, seeded, or simulated; no prod deck created.

Session notes (environment, unchanged from R1b unless noted)

* PATH: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node / pnpm + gh); psql / aws absent — ad-hoc SQL via tsx in `scripts/.tmp/` (tsconfig-excluded but LINTED; delete only what you add).
* `.env.local`'s `DATABASE_URL` is the same Neon pooler prod uses: dev fixtures are real rows, and the draft deck you mint on dev is a real anonymous row — delete it through the editor (Details → Delete deck…) and confirm with a query. Never mint on prod (10/h + 30/day per IP; read the counters before any prod run).
* A dev server may already be listening on 3000 from ANOTHER chat rooted in this checkout (R1b found one, 1h36m old, HMR-serving the current tree): `preview_start {name: "dev"}` will refuse the port — check `lsof -nP -iTCP:3000 -sTCP:LISTEN` and, if its cwd is this directory, reuse it via `preview_start {url: "http://localhost:3000"}` and run the smokes against it. `pnpm build` (Turbopack) coexists with `next dev`; `next start -p 3111` beside it for any cache-header proof, killed after.
* Browser pane: a hidden pane swallows `computer` clicks, read_page and scroll state — drive via javascript_tool, verify via DOM / API reads; screenshots work on the fronted tab (`tabs_select` first); Base UI menus and toasts open from a PLAIN `click` `MouseEvent` in the hidden pane (the pointer path waits on requestAnimationFrame, which a hidden document pauses) — jsdom tests need the pointerDown → mouseDown → click sequence instead; Escape is a `keydown` on `document.activeElement`; keyboard flows are `KeyboardEvent`s dispatched on the focused input (`bubbles: true`). `read_network_requests` buffers only the last ~250 requests — read right after the action. `resize_window` for 1024 / 1440 checks; the pane is SIGNED OUT on prod and OAuth cannot be typed. Clear `localStorage.theme` and reset the viewport when done.
* Prettier reflows scripted-edit anchors: re-grep after `pnpm format`. `*.md` is prettier-ignored.
* Deploy verification: `gh api repos/Bobandis6/deckwarden/commits/<full sha>/status` → the "Vercel" context; `gh run list --workflow=nightly-ingest.yml` for nightlies. ISR is real since R1b: hub and card pages lag up to an hour on prod — assert existence on prod, freshness on dev.
* `pnpm check` takes about a minute; `smoke:collection` has a budget note (3 users, 1 deck — run it first among the smokes).

Context, not tasks
Sequence after R3: R2 (the art resolver and ambient layer land on this header and summary surface; the appearance menu gains "Background art") → R5a → R5b → R4 (the `wide:` token, the tablet Drawer, the phone tabs — on R3's panes) → R6, each with its own prompt written the way this one was, after the previous package ships. P4.7's standing triggers still exist beside the R-series (img.deckwarden.gg flip, Limitless key, feedback, red nightlies) — their own P4.7 round, never a slice of an R-session. Bandai and Azuki permission emails remain unanswered — posture unchanged; OP ambient art stays off (REDESIGN.md §3). Cold-start rule holds everywhere: no simulated decks, posts, or metrics, ever. Premium never gates card data. The owner's open item: the two-minute signed-in look at `/account`'s Danger zone, and — new after R3 — the signed-in editor on `/d/q9j9uxphwp9n` with its Share primary and History in the More menu. The smoke-pin and LATER-row ledgers in REDESIGN.md §6 remain the map.
