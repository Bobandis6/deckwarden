# W4 session prompt — Choose-commander flow (leader zone → browse → back, + `/commanders?q=`)

Pull latest, then run W4 — the fourth Wave-2 package. **`WAVE2.md` is the contract**; read its W4 section (steps 1–6 are the build order), D3 (the ASCII spec — it IS the design), the W4 row of the pin matrix, and REC-2. W1–W3 are live (`e40342d`, `ce9d7c2`, `0da07cc`); W4 turns the leader zone's dead end into a round trip: empty zone → **Browse commanders** → `/commanders` (now name-filterable) → `/c/<slug>` → back into the SAME deck with the commander applied and Undo. **No API or schema work**: `/decks/new?game=…&leader=<key>` draft seeding already exists (P4.6) — W4 adds the *saved-deck* round trip via a sessionStorage pick intent, never a new endpoint.

Pre-flight, in order. (1) **Nightlies green** (`gh run list --workflow=nightly-ingest.yml`). A red run preempts everything. (2) **A warm beta signal outranks a package**: owner posted / feedback / a stranger's 429 / a new issue → run the `P2.9-session-prompt.md` round (MTG) or `P4.7-session-prompt.md` round 3 (OP) as its own session first (2026-09-19 census baseline: 25 decks — 24 MTG + 1 OP, the owner's — 1 user, likes 1 · bm 0 · folders 1). (3) Working tree clean at or after W3's docs commit (feat is `0da07cc`). (4) State your baseline: **799 tests / 106 files / 6 pre-existing `no-unused-vars` warnings / 0 errors** (measured 2026-09-19 on `0da07cc`), and `pnpm db:size` (262.8 MB on 2026-09-19, alert 350).

## What W4 is NOT (scope fence)

NOT printings (W5/W6), NOT an in-editor picker dialog (LATER row — the owner asked for the browse page), NOT One Piece random/autofill (no data — cold-start rule), NOT any change to `/api/cards/resolve` or `/api/decks`, NOT new deps. The `?leader=` DRAFT path (`/decks/new`) stays as P4.6 built it — W4's editor work is only the SAVED-deck apply. `/l/` gets the same treatment as `/c/` (both games in lockstep, the P4.4 rule) but `/leaders` popularity data stays as is. Anything else → `LATER.md` with a trigger.

## Facts you inherit (verified 2026-09-19 against `0da07cc` — re-grep lines, trust the shapes)

- **Adapter display**: `src/lib/games/types.ts:443` is the `display:` block in `GameAdapter` — add optional `leaderBrowse?: { href: string; label: string }` there (NOT under `hub`: hub is absent for One Piece and gates template rendering). Implement at `mtg/adapter.ts:122` ("/commanders" · "Browse commanders") and `optcg/adapter.ts:159` ("/leaders" · "Browse leaders"); both `display.test.ts` files exist — extend them.
- **Leader zone**: `src/components/deck/leader-zone.tsx` — `onChooseLeader?` at :47, today's empty state renders "No commander yet", "Ctrl+Enter on a search result adds one.", and a "Choose commander" button calling the callback (:78–83). D3 replaces this with a primary styled `<Link>` (the link-not-button pattern — `account-slot.tsx` guest branch :57–63) + a ghost "Search by name" button that keeps calling the unchanged callback. `leader-zone.test.tsx:36-52` is the test to rewrite. Wire-through: `deck-list-pane.tsx:67,93,197` passes `onChooseLeader`; `deck-editor.tsx:1101` binds it to `focusSearch` (:547).
- **Pick intent** (new `src/lib/decks/leader-pick-intent.ts`): sessionStorage + `useSyncExternalStore`, pure helpers unit-tested. Written on Browse click ONLY when a saved deck id exists (draft mode keeps plain navigation); shape `{deckId, deckName, game, at}`; 30-minute TTL; cleared on apply and on the banner's Cancel. The store precedent is `src/lib/hub/index-view.ts` — note its deliberate server-snapshot choice and copy the discipline: the HUB islands' server snapshot must be **null** (server HTML byte-identical; smokes grep it), so the CTA/banner appear only after hydration. **Never `useSearchParams` on hub pages** (they are ISR ●), never set-state-in-effect (both react-hooks rules are errors).
- **Hub CTAs**: `/c/[slug]/page.tsx:196-207` — "Build with this commander" → `/decks/new?game=mtg&leader=${leader.externalKey}`; `/l/[slug]/page.tsx:213-215` same with "leader". Both move into the new client `hub-build-cta.tsx` whose server render is today's EXACT anchor; a fresh same-game intent ADDS the primary "Use for '{name}'" → `/decks/<id>/edit?leader=<key>` above it. Pinned by `hubs-smoke:103-108` (OP CTA latch, entity-tolerant) and `seo-smoke:339-343`; index hrefs by `hubs-smoke:75-84`.
- **Editor apply**: `deck-editor.tsx:398-431` is the shape to copy — `seededLeaderRef` one-shot, draft-only guard, `/api/cards/resolve` POST (pass 0 resolves id-shaped tokens — an external key resolves without names), `isLeaderCandidate` check, empty-zone check, then state writes inside the async IIFE (the `combo-radar-panel.tsx:84-86` discipline). W4's saved-deck variant applies ONLY with a matching un-expired intent and an empty leader zone, goes through **`handleAdd`** (Undo toast built in — do NOT hand-roll entry pushes like the draft seeder; the deck must autosave), then `history.replaceState` strips `?leader=` and the intent clears. `/decks/[id]/edit/page.tsx` is `force-dynamic` and currently passes only `deckId` — add `searchParams`.
- **REC-2**: `/commanders` is already ƒ and already takes `searchParams` (ciMask + page, `commanders/page.tsx:47-59`); `loadLeaderIndex` is `src/lib/hub/queries.ts:269`. Add `q` normalized through `src/lib/cards/normalize.ts` → `name_norm LIKE %q%` (trgm index exists); keep popularity order; GET form left of the color chips; empty → `EmptyState` "No commanders match “…” · Clear filter". Remember DFC name_norm has NO "//" (P3.9).
- **Toasts**: the editor mounts its own `Toaster`; `handleAdd`'s notify (deck-editor.tsx:584-597) already gives the "Added X · Undo" toast for free.

## Verify-first list (never from memory)

1. **Server HTML byte-stability**: after wrapping the CTA in an island, `curl` the hub pages on dev and diff the anchor markup against prod's — the smokes grep exact text + href. Same for `/commanders` server HTML with no `q`.
2. **`?q=` normalization**: run the actual normalize function against "Krenko", "atraxa, praetors" and a DFC name; check the query plan uses the trgm index (`DB_LOG=1` + explain if in doubt), and that `q` combines with a ciMask filter rather than fighting it.
3. **Intent invariants on dev**: a crafted `/decks/<id>/edit?leader=<key>` with no intent changes nothing; a stale (>30 min) or wrong-deck intent changes nothing; a non-empty zone changes nothing and says why on the live line; apply fires exactly one `handleAdd` and the URL loses `?leader=` without a history entry.
4. **No deck row before first edit** on the fresh-draft path (the P2.8 contract): watch `POST /api/decks` in the network log while walking Browse → hub → Build with this commander.
5. **ISR intact**: `/c/<slug>` second local hit `x-nextjs-cache: HIT`; `pnpm build` route table — `/c/` `/l/` stay ●, `/commanders` stays ƒ, zero route-type changes.
6. **Flush-before-navigate**: Browse from a saved deck with unsaved edits must flush autosave before leaving (D3 "flushes autosave") — find the editor's existing flush/beforeunload path and reuse it; verify the edit survives the round trip.
7. **Sokka canary** (locale collator) if `q` sorting touches any collator path; popularity order must not regress to alphabetical.
8. **Browser-pane caveats** (unchanged): hidden pane stalls Base UI exits and freezes CSS transitions — verify islands via DOM/curl/jsdom; pane Enter needs a dispatched keydown for GET forms.
9. **Smokes on dev**: `smoke:hubs`, `smoke:seo`, `smoke:optcg` (+ `counters:reset` when the battery fills local counters — creates 10/h before versions; never retry into a 429). Deploy via the GitHub commit status (context "Vercel", full sha; MISS ×2 then HIT on ISR reads). Prod pass read-only: hub CTAs render, `/commanders?q=krenko` filters, **zero decks minted on prod** (budget 10/h + 30/day per IP).

## Design decisions to make explicitly (disclose + pin each)

- **Banner island placement** on `/commanders` and `/leaders`: above the grid, one shared component; server snapshot null. Record where it sits relative to the new `q` form.
- **Ghost "Search by name"** vs today's primary "Choose commander": D3 makes Browse primary — the demotion is deliberate; record it beside the leader-zone test rewrite.
- **`q` + pagination**: does `q` reset `page`? Default: yes (a new filter restarts at page 1) — record it.
- **One Piece parity**: `/leaders` gets the banner + `q` in the same commit (lockstep rule) even though OP traffic is one deck; record if you split it out.

## Deployable outcome

`pnpm check` green and deployed; verified ON PROD read-only (signed-in editor flows dev/RTL-covered — the standing disclosure): D3 complete — leader-zone Browse link (adapter-driven label/href), pick intent with TTL + Cancel, hub "Use for '{name}'" CTA, editor apply with Undo through `handleAdd`, param stripped, `/commanders?q=` filtering with EmptyState. Acceptance walk from WAVE2 §W4 done on dev end-to-end for BOTH the fresh-draft and saved-deck paths. Route table: **zero type changes**. Tests: `leader-zone.test.tsx:36-52` rewritten; new `leader-pick-intent` unit tests (TTL, wrong-deck, clear-on-apply); both `display.test.ts` extended; `hubs-smoke:75-84,103-108` + `seo-smoke:339-343` green untouched. Docs in the same package: `WAVE2.md` tracker W4 ticked with the sha; a dated ship-note in this file; memory updated; **`W5-session-prompt.md` written** the way this one was. db:size stated. Nothing posted, seeded, or simulated.

## Session notes (environment, unchanged from W3 unless noted)

- PATH per command: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node/pnpm + gh); psql/aws absent. Census via a scripts/.tmp tsx probe if signals need checking (tsconfig-excluded, delete after).
- Dev server: `preview_start {name: "dev-log"}` after `lsof -nP -iTCP:3000 -sTCP:LISTEN`; stop it before `pnpm build` (shared `.next`). `next dev` re-adds the AGENTS.md block — commit it with your work.
- The pane is signed out everywhere (localhost included — W3 confirmed). Editor flows are testable in DRAFT mode signed-out (the fresh-draft path needs no session); the saved-deck path needs a deck the browser owns — create a guest draft on dev, edit once (claim token in localStorage), then walk the round trip on it. Never mint prod rows.
- Prettier reflows scripted-edit anchors after `pnpm format`; `*.md` is prettier-ignored. Base UI submenu triggers ignore mouse clicks (W3 lesson) — irrelevant here unless a menu grows a submenu.
- Prod: curls need `--compressed`; deck-create budget 10/h + 30/day per IP — this session creates no prod decks.

## Context, not tasks

Sequence after W4: W5 printings (+ API + REC-1) → W6 editor printings → W7 buy links → W8a/b precons → W9a/b/c autofill → W10 tournaments. P2.9's and P4.7's standing triggers still exist beside the W-series and get their own round, never a slice of a W-session. Owner decisions of 2026-09-19 stand. Bandai and Azuki emails unanswered — posture unchanged. Cold-start rule holds: no simulated decks, posts, or metrics. Premium never gates card data.
