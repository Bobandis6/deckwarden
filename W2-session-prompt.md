# W2 session prompt — Brand completion (OG wordmark + font, icons, themeColor, the audit)

Pull latest, then run W2 — the second Wave-2 package. **`WAVE2.md` is the contract**; read its W2 section, D0's "Brand details worth adding", the W2 row of the pin matrix, and the tracker's W1 tick (the implementation decisions there bind W2 — especially the favicon/qlmanage note). W1 (`e40342d`) landed the tokens, the vector crest/shield, the favicon and Literata titles; W2 finishes the brand everywhere the site is seen from outside: OG unfurls get the crest + a real Literata wordmark, phones get home-screen icons and a `themeColor`, `SurfaceHeader` gets its gold rule and the generic band goes green→charcoal, and the re-key is **audited**, not assumed. No layout moves.

Pre-flight, in order. (1) **Nightlies green** (`gh run list --workflow=nightly-ingest.yml`). A red run preempts everything. (2) **A warm beta signal outranks a package**: owner posted / feedback / a stranger's 429 / a new issue → run the `P2.9-session-prompt.md` round (MTG) or `P4.7-session-prompt.md` round 3 (OP) as its own session first. (3) Working tree clean at or after W1's docs commit (`git log --oneline -2 -- W1-session-prompt.md` shows the ship-note commit; feat is `e40342d`). (4) State your baseline: **790 tests / 105 files / 6 pre-existing `no-unused-vars` warnings / 0 errors** (measured 2026-09-19 on `e40342d`), and `pnpm db:size` (262.9 MB on 2026-09-19, alert 350).

## What W2 is NOT (scope fence)

NOT layout, route, schema, API or feature work — the account menu is W3, the choose-commander flow W4. NOT One Piece ambient art (off until Bandai answers) and NOT One Piece OG artwork — unfurls there stay artless (P4.6 decision). NOT a re-key of any token value D0 fixed — W1 shipped D0 verbatim; if the audit finds a failing pair, fix the *consumer* (a class, an alpha), and only touch a token with the measured ratio recorded in the REDESIGN.md addendum. NOT new dependencies (`ImageResponse`/satori are in Next; the Literata TTF is an asset, not a package). NOT the buy/precon/autofill work. Anything else goes to `LATER.md` with a trigger.

## Facts you inherit (verified 2026-09-19 against `e40342d` — re-grep lines, trust the shapes)

- **The reserved indigo set** (the whole point of W2): `og.accentGeneric: "#a5b4fc"` at `src/lib/theme/tokens.ts:107` (used by `ogAccent()` at :117), its test literal at `tokens.test.ts:96` inside the test renamed by W1 to "…generic keeps the pre-W2 OG indigo", and `src/lib/og/labels.test.ts:30` (`accent: "#a5b4fc"`). Lines 12/21 of that file hold `#b5a2ff` / `#62d6c5`, which do not change. `src/lib/og/elements.tsx` carries an indigo comment. After W2 the checklist grep for indigo over `src/` must return **zero** hits.
- **Gold exists**: `--gold` / `gold` token `#c9a96a` dark, `#ba9b61` light, `--color-gold` exposed; OG always paints dark, so `og.accentGeneric` becomes `#c9a96a` (= `dark.gold`, assert it that way, not as a new literal).
- **Eight `ImageResponse(` call sites** in four routes: `src/app/(site)/cards/[id]/opengraph-image.tsx:44,67`, `c/[slug]/opengraph-image.tsx:48,73`, `d/[publicId]/opengraph-image.tsx:43,63`, `l/[slug]/opengraph-image.tsx:48,64` — every one (including the fallback branches) must receive `fonts`, or unfurls will mix Literata and Inter.
- **Satori takes static TTF only** — no woff2, no variable fonts, so the `next/font` Literata files are unusable there. Plan: **(new)** `assets/fonts/Literata-SemiBold.ttf` + its `OFL.txt` (download the static instance from the googlefonts/literata GitHub release; commit both), **(new)** `src/lib/og/fonts.ts` — `readFile(join(process.cwd(), "assets/fonts/Literata-SemiBold.ttf"))` with the path a **literal** so standalone output tracing copies the file; cache the promise at module level; try/catch → return no fonts (Inter) so an OG route can never 500.
- **The mark geometry**: crest = 96-box in `src/components/brand-mark.tsx` (stroke 3), shield = its own `11 24.5 74 74` crop (stroke 5), `src/app/icon.svg` is the shield. For raster icons: `qlmanage -t -s <px> -o <dir> file.svg` renders exact-size PNGs (proven in W1); **`pnpm dlx png-to-ico` is a trap** (emits a 285 KB four-size ICO) — W1 hand-assembled the ICO; for 192/512 PNGs qlmanage output is committed as-is. Apple touch icons are composited on white when transparent — give `apple-icon` an opaque `#0f1314` (or ivory) background, not transparency.
- **Viewports**: the root layout exports `metadata` only (no `viewport` export yet — add one with `themeColor: "#0f1314"`, single value; the theme is class-based with a dark default, so a `prefers-color-scheme` pair would be wrong). The two editor pages export their **own** `viewport` (R4: `viewport-fit=cover`, `interactive-widget=resizes-content`) — the themeColor meta must appear there too; verify in served HTML, then decide one shared constant.
- **`SurfaceHeader`** (`src/components/surface-header.tsx`): `SURFACE_BAND` heights at :49, the band container at :68 (`rounded-2xl` + gradient/art), the visible credit at :94–103. `SurfaceSkeleton` (`surface-skeleton.tsx`) renders the same band — the gold rule and the generic green→charcoal gradient must appear in both or loading flashes the old look. Hub/deck bands with game accents keep their accent gradients; only the **generic** (no-game) band changes.
- **No manifest, no apple icon exist** (`src/app/manifest.ts`, `src/app/apple-icon*` absent). `manifest.ts` is static by default — say so in its docblock (caching-intent rule). Adding them changes the route table (**expected additions**: `/manifest.webmanifest`, `/apple-icon`); every pre-existing route must keep its type — state the diff.
- **The audit's thin pairs, measured 2026-09-19**: amber-700 on ivory 4.57:1 (passes, thin); gold light `#ba9b61` is decorative-only (2.4:1); focus rings 2.95 dark / 2.57 light at 50 %. W1's re-measure table for the R6 fixes is in the REDESIGN.md addendum — extend that table, don't fork it.
- **`--font-heading` stays `var(--font-sans)`** and is pinned by `tokens.test.ts` ("the display face is exposed…, and headings stay sans (W1)") — if you decide modal titles go serif, that test moves WITH the decision, in the same commit, and the decision lands in the REDESIGN.md addendum either way.
- **OG pins that must not move**: `seo-smoke`'s OG checks; OG images repaint through `og.bg/fg/muted/panel/raised` (asserted equal to dark tokens); `revalidate = 86400` on all four routes, so prod unfurls linger up to a day — assert freshness on dev, existence on prod. One Piece unfurls stay artless.

## Verify-first list (never from memory)

1. **Satori font loading** against the installed `next/dist/docs` + `@vercel/og` types before writing `fonts.ts`: the `fonts` option shape (`{ name, data, weight, style }`), and that `name: "Literata"` must match the `fontFamily` used in the JSX styles.
2. **Standalone tracing**: after `pnpm build`, confirm the TTF is in `.next/standalone`'s traced files (or that the OG route works from `next start`) — a dynamic path silently ships without the file and the fallback hides it.
3. **The Inter fallback**: temporarily rename the TTF locally → every OG route still returns 200 (test this BEFORE styling depends on Literata metrics).
4. **The wordmark footer**: current OG footer text/shape in `src/lib/og/elements.tsx` (the "deckwarden.gg" line) — D0 wants small crest + "Deckwarden" in Literata with ".gg" in the surface accent. The crest in satori: inline the SVG as a data-URI `<img>` or re-draw with satori-safe elements — verify what elements.tsx already does for images and follow its pattern.
5. **`themeColor` in served HTML** on a site page AND both editor pages (they export their own `viewport`).
6. **iOS home-screen icon**: `/manifest.webmanifest` served with the 192/512 entries; `apple-icon` answering `image/png` with an opaque background. (No iOS device drive available — verify the served bytes and sizes, disclose the rest.)
7. **The audit itself, at 390 / 768 / 1200 / 1440, both themes**, over: home, `/cards`, a card page, a hub per game, a deck share page, `/account`, the editor per game, one dialog — every text pair ≥ 4.5:1 (≥ 3:1 large), focus visible on every control; measure with the WCAG formula in the pane (W1's helper in the ship-note session works verbatim) and record the table in the REDESIGN.md addendum.
8. **Route table** before/after: only the two expected additions.
9. **Smokes on dev**: `smoke:seo` (mints fixtures — never prod), `smoke:hubs`, `smoke:optcg`, `smoke:engagement`; `pnpm counters:reset` if the battery fills local counters.
10. **Deploy** via the GitHub commit status (context "Vercel", full sha); prod pass read-only, zero creates.

## Design decisions to make explicitly (disclose + pin each)

- **Serif modal titles** (`--font-heading` → `var(--font-display)`?): try it in the dev pass on the editor's dialogs/sheets/drawer; record yes or no in the REDESIGN.md addendum; the tokens.test pin moves with the answer.
- **OG footer lockup**: small crest + "Deckwarden" (Literata 600) + ".gg" in the surface accent, per D0. Keep the byte budget sane — the crest is a few hundred bytes of SVG.
- **`og.accentGeneric` assertion style**: set it to `dark.gold` (a reference, not a new literal) and update the two test literals to `#c9a96a`; rename the W1 test name ("…generic keeps the pre-W2 OG indigo" → gold).
- **Icon set**: 192/512 PNGs from the shield via qlmanage (committed binaries) vs `ImageResponse` routes. W1's precedent is committed binaries for the favicon; either is fine — disclose which and why.

## Deployable outcome

`pnpm check` green and deployed; verified ON PROD read-only: each OG route (one per family — deck, `/c/`, `/l/`, card) returns a PNG carrying the crest and the Literata wordmark **on dev** (prod asserts 200s; unfurls lag a day); the indigo grep over `src/` returns **zero** hits; `/manifest.webmanifest` and the apple icon served; `themeColor` in the HTML of a site page and both editors; the gold rule under `SurfaceHeader` bands (and the skeleton's) in both themes; the generic band green→charcoal; the audit table recorded in the REDESIGN.md addendum with every failing pair fixed. Docs in the same package: `WAVE2.md` tracker W2 ticked with the sha; a dated ship-note in this file; memory updated; **`W3-session-prompt.md` written** the way this one was. Route-table diff stated (exactly the two additions). db:size stated. Nothing posted, seeded, or simulated.

## Session notes (environment, unchanged from W1 unless noted)

- PATH per command: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node/pnpm + gh); psql/aws absent. W2 needs no SQL.
- Dev server: `preview_start {name: "dev-log"}` after `lsof -nP -iTCP:3000 -sTCP:LISTEN`; stop it before `pnpm build` (they share `.next`). `next dev` re-adds the AGENTS.md block — commit it with your work.
- Browser pane: hidden pane pauses rAF and stalls Base UI exits; verify via DOM reads; OG images load fine by direct URL, but the og:image URL carries a hash suffix — read it from the page HTML first (W1 hit this).
- Prettier reflows scripted-edit anchors after `pnpm format`; `*.md` is prettier-ignored.
- Prod: curls need `--compressed`; ISR reads MISS twice post-deploy before HIT; deck-create budget 10/h + 30/day per IP — this session creates no prod decks.

## Context, not tasks

Sequence after W2: W3 account menu + deck actions → W4 choose-commander → W5 printings (+ API + REC-1) → W6 editor printings → W7 buy links → W8a/b precons → W9a/b/c autofill → W10 tournaments. P2.9's and P4.7's standing triggers still exist beside the W-series and get their own round, never a slice of a W-session. Owner decisions of 2026-09-19 stand (plain buy links, ownerless precons, evidence-based shell, full re-key with dark default). Bandai and Azuki emails unanswered — posture unchanged. Cold-start rule holds: no simulated decks, posts, or metrics. Premium never gates card data.
