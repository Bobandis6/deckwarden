# P3.6 session prompt

Pull latest, then kick off P3.6 from deckwarden-build-plan.md — M3's next
package (~line 206): deck versioning (named versions, card-by-card diff,
restore) + forks with credit and upstream-diff; the feature-level reference
is deck-building-website-feature-plan.md's "Deck Versions and Forks"
section (~line 471). Know what it is NOT before designing: it is not
collaborative brewing (invite editors, live presence — the feature plan's
next section, LATER row), not "follow a deck for future updates" (that
line in the feature section is a community/notification feature — record
it in LATER with a trigger, don't build it), not comments, not markdown
notes (existing LATER row stays), not public "deck evolution" timelines
unless you deliberately decide history visibility (default owner-only;
whatever you pick, record the reasoning), and NOT the tournament
ranking-signal aggregate — P3.5's gate read 99.97% embedded lists and both
signal rows in LATER.md are recorded PASSED waiting for "the next M3
intelligence package slot"; P3.6 is a product/storage package, leave those
rows alone. The schema head start: deck_versions, decks.current_version,
and decks.forked_from_deck_id have been provisioned since P1.1 exactly so
this package needs no migration — read the schema.ts comments first;
frozen shape is JSONB [{cardId, zone, qty, tags, printingId}], and TODAY
nothing writes any of them (verify by grep: only delete-account touches
forked_from_deck_id, only cascades touch deck_versions, current_version is
0 everywhere). One LATER row FIRES as a hard in-scope item: single-deck
DELETE vs forked_from_deck_id — the self-FK has no ON DELETE, so the
DELETE handler in src/app/api/decks/[id]/route.ts 500s on the first deck
that has a live fork; src/lib/profile/delete-account.ts steps 2–3 (null
dependents' pointers in the same transaction, then delete) are the exact
precedent to mirror. AND this session found the same latent bug in
scripts/purge-anon-decks.ts — it deletes candidate anon decks with no
detach, so the nightly purge 500s the day a purged guest deck has a fork;
fix every delete path (single DELETE, account delete already correct,
purge) in this package and pin delete-with-fork in a smoke, regardless of
which forkability scope you choose. Design decisions to make explicitly,
each disclosed in code and test-pinned: (1) when a version is born —
manual "save version" with the schema's note field (the plan's phrase is
NAMED versions) plus an automatic pre-restore safety snapshot so restore
is always reversible; unique(deck_id, version) is the concurrency guard
and current_version the counter. (2) Restore is ONE transaction: replace
deck_cards from the frozen JSONB, recompute the leader_ids/ci_mask denorms
via the SAME code path the editor save uses (find it and reuse it — a
second denorm implementation is an instant drift bug), bump version state;
guard dangling printingId (printings can churn across ingests — fall back
to the identity's default printing, count it, disclose it) and remember
is_removed identities still resolve by design. (3) Fork semantics: decide
who may fork (account-only vs guests — guests exist by design but weigh
the spam surface; src/lib/rate-limit.ts + the deck-create limiter is the
precedent if guests get it) and what copies (current deck_cards + name +
forked_from_deck_id; never likes/versions/claim state); visibility default
follows create (unlisted); credit renders on /d/[publicId] AND the editor
("Forked from <upstream>" linking its public page), with the
upstream-gone (pointer nulled) and upstream-private (credit without link?)
states decided and rendered honestly, never a broken link. (4)
Upstream-diff: "changes since you forked" needs a fork-time baseline and
the schema has NO forked_from_version column — strongly consider writing
the UPSTREAM's list as the fork's version 1 at fork time (note "forked
from <name>") instead of a migration: zero schema change, the baseline is
frozen forever even if upstream churns or vanishes, and the package's own
diff machinery renders it; diff vs upstream's CURRENT list can be a second
view if cheap. Whichever you choose, say why in code. (5) The diff itself
is a PURE core function (src/lib/decks/diff.ts): added / removed /
qty-changed / zone-moved per zone, operating on the frozen shape,
adapter-agnostic (zone ids in data, labels from FormatDef at render), with
a vitest suite like cuts.ts — the P3.4 split (pure machine + vitest, thin
surface, browser-driven prod proof) is the pattern again. (6) Neon math
out loud even with no migration: a 100-card frozen snapshot is ~6–8KB of
JSONB, so cap versions per deck in zod (~50 is plenty) and surface the cap
honestly in the UI when hit; DB read 218.99MB on 2026-09-01 (alert 350) —
version rows are USER data, the first growing-with-usage table, so state
the growth model, don't hand-wave it. API shape follows the established
sub-resource pattern under /api/decks/[id]/ (bookmark/combos/
recommendations siblings): versions GET/POST, a single-version GET for
diff payloads, restore POST, fork POST — plain handlers + zod, owner-gated
like PATCH (claim-token guests included for their own decks), caching
intent stated (these are dynamic no-store APIs; /d/[publicId] keeps its
existing mode — check it before touching). UI: versioning is a deck-level
concern, not a fourth right-pane tab — a History affordance in the deck
header near the P3.4 "Over by N" CTA precedent (onOpenCuts shows how
header → pane wiring works if you do use a pane), listing versions with
note/date/count, diff view, restore with confirmation; share page gets the
fork button + credit line. Deployable outcome = versions save/diff/restore
proven against prod with a fixture deck (create → version → edit → diff
shows the exact delta → restore reverts it → pre-restore snapshot exists),
a fork of a public fixture deck live with working credit link AND
upstream-diff rendering, every delete path fork-safe and smoke-pinned, the
pure diff suite green, a new or extended smoke covering the version+fork
API round trip (mint a session via the engagement-smoke pattern if
account-only), pnpm check green, deployed, and the existing smokes still
green against prod.

Session notes:

* Toolchain: node/pnpm at ~/.local/node22/bin — non-login shells need
  export PATH="$HOME/.local/node22/bin:$PATH". `source .env.local` FAILS in
  zsh (unquoted `&` on line 6) — pull single vars:
  export DATABASE_URL="$(grep '^DATABASE_URL=' .env.local | cut -d= -f2- | tr -d '"')".
  gh is NOT installed — GitHub REST via curl (token via `git credential
  fill`). Deploy verification: Vercel posts a commit status (context
  "Vercel") on the pushed sha; actions/runs?head_sha= needs the full
  40-char sha.
* Pre-flight: pnpm check on main AND the nightly Action — specifically,
  the most recent scheduled run is the FIRST fully unattended night of the
  topdeck step (cron 10:37 UTC; went live 2026-09-01 with backfill + one
  dispatched proof run). Verify it: step green, stats show a ~15-day
  window, small inserted/updated counts, stale_deleted ~0, db:size gauge
  under 350MB. A red or weird topdeck step is triage BEFORE P3.6 work.
* What P3.5 shipped (2026-09-01, commits 9d6569b + 56d898f — not in the
  plan doc): tournaments + tournament_standings tables (source-scoped
  external keys, standings.leader_ids uuid[] + GIN, top-16-only at
  16+-player events, bounds disclosed in the shelf UI);
  adapter.capabilities.tournaments is now DECLARATIVE {sourceLabel,
  sourceHref, eventUrl(externalKey)} (P3.3 combos precedent); Top finishes
  shelf on /c/[slug] via src/lib/tournaments/queries.ts (ISR 3600
  unchanged); smoke:hubs auto-flips to with-rows assertions;
  nightly-ingest.yml gained the topdeck step (after the card jobs, before
  db:size) + an R2 raw-archive step (topdeck/raw/<date>/); the mapper
  (topdeck-map.ts, 16 tests) carries a WindowBounds guard because the API
  returned a future-dated test event in live probing — never remove it.
  ingest_runs rows (source 'topdeck') carry window + skip-reason stats.
* Neon: 218.99MB post-backfill 2026-09-01 (alert 350, free ~512). Every
  migration eyeballed — though P3.6 should need NONE (provisioned schema);
  if you do add a column (e.g. forked_from_version), that's a deliberate
  decision against the v1-baseline alternative, argued in the commit.
* LATER rows for THIS package: forks/single-deck-DELETE row FIRES (see
  above — plus the purge-anon-decks instance found late in P3.5). The two
  tournament-signal rows (co-occurrence + seen-in-top-16) are PASSED-gated
  for the intelligence slot — leave them. Add a deck-follow row (feature
  plan names it; community trigger). Collaborative brewing already has a
  row. Markdown-notes row unchanged.
* Smoke rules against prod: BASE_URL=https://deckwarden.gg pattern;
  smoke:recommend creates/deletes its own guest deck; smoke:combos and
  smoke:hubs are DB-connected (DATABASE_URL via the grep pattern);
  smoke:seo's ONE known failure is "core sitemap lists the public deck"
  (revalidate lag) — anything else is real; engagement/account smokes mint
  signed sessions (BETTER_AUTH_SECRET pattern in
  scripts/engagement-smoke.ts). Guest-deck cleanup: x-deck-token DELETE,
  local AND prod.
* Prod verification: the owner is mid-seeding REAL featured decks — never
  version, fork, or delete the owner's decks as test fixtures. Create your
  own fixtures (guest or minted-session), including your own PUBLIC
  upstream to fork; delete them when done, and prove the detach path
  deliberately (delete the upstream first in the local run and show the
  fork survives with a nulled pointer).
* Browser pane: hidden pane swallows computer clicks silently — drive via
  javascript_tool, verify via DOM/API reads (memory file has details).
  Editor surfaces are client-heavy; API smokes + targeted DOM reads beat
  screenshot-driving.
* Context, not tasks: beta announcement still deliberately deferred;
  permission emails to Bandai + Azuki Labs out 2026-08-30; cold-start rule
  stands — no simulated activity (forks/versions are real user actions
  only; no seeded fork counts, ever).
* Protocol as usual: one package, ends deployed and check-green; scope
  leaks to LATER.md with a trigger.
