# P2.9 session prompt — MTG beta response (state-gated; run a round after the owner posts, or on any MTG-side signal)

> **Round 1 ran 2026-09-17 on `21be020`: every trigger cold** — the owner hasn't posted (the phone re-run is still their next move), so this round is the baseline-confirmation pass. Pre-flight: nightlies green through TODAY's run (09-17 `35239279079` success, watched to completion in-round, after 09-16 5m28s / 09-15 6m7s / 09-14 5m14s); tree clean at this prompt's commit, `pnpm check` re-measured green (764 tests / 104 files, the same 6 pre-existing warnings, 0 errors); db:size 261.2 MB (alert 350); census identical to the baseline in pre-flight 4 — 25 decks / 1 user / 13 owned public (12 Commander + the Enel OP deck) / 12 anon (4 private + 8 unlisted), newest deck row still the owner's 09-15 seeding, and **likes 1 · bookmarks 0 · folders 1** now recorded numerically for future deltas; counters show only loopback `::1` at 8/h+8/day (the P4.8-day dev battery) plus the two known owner IPs (`172.56.150.50` at 1 — the observation pre-flight 5 already carries; `162.252.181.30` stale 09-15 windows) — no stranger IPs, nobody near a cap (branch B cold); issues still the 1-closed baseline (branch A cold); pre-flight 7's grep answered once for all rounds: Sentry IS code-wired (`src/instrumentation.ts` + `src/instrumentation-client.ts` + the error boundaries, P1.8, DSN-gated, error-monitoring only) but this lineage has no programmatic dashboard access, so error input stays owner-pasted (branch D cold); C/E/F all gate on a post that doesn't exist yet. Shipped: nothing — so the `counters:reset` opening chore (`c628688`) stays queued for the first warm round that ships code. Armed: this prompt for round 2 (owner's post or any MTG signal); P4.7 stays armed for round 3 on the OP side.

Pull latest, then run a P2.9 round. This is the r/EDH-side sibling of `P4.7-session-prompt.md`: not a feature package but a RESPONSE protocol, runnable repeatedly. P2.8b (2026-09-16, `1a43ee4`) cleared the announcement's code gate — the owner's paste imports 80/80 with the commander guessed, the header workaround collapses to one warning, single-slash DFCs resolve, and the phone's Commander button returns to the Deck pane — so the MTG post (r/EDH + 2–3 Commander Discords, drafts in `P2.8-beta-launch.md`) is the owner's next move, behind their own phone re-run. Each P2.9 round checks the trigger map below in order and acts ONLY on what is warm; a fully cold round ends with the dated round note and NOTHING else — the cold-start rule holds (no simulated decks, posts, or metrics, ever; sessions never post — the owner posts). Warm branches are paper-cut-sized: a fix ships **deployed and `pnpm check`-green** in the round or it goes to `LATER.md` with a trigger. A red nightly outranks everything here: it is P4.7 branch F — diagnose from the run's own logs under THAT prompt's rules, and P2.9's round is over.

**Round notes:** append a dated note under this title after every round (the P4.7 pattern) — triggers checked, what was warm, what shipped (shas), the census/db numbers, and what stays armed.

**Relationship to P4.7:** One Piece signals (Limitless key, the `img.deckwarden.gg` DNS flip, OP feedback, the OP cold-start probes) belong to `P4.7-session-prompt.md` round 3 — never absorb its branches. If both games are warm, run P4.7's round as its own session first (it is older and gates M4's exit).

## The opening chore (fires only if this round will run a smoke battery)

If any branch ships code (⇒ the full battery runs), FIRST build the newest "Deferred infrastructure" row (`c628688`): `scripts/reset-dev-counters.ts` + a `"counters:reset"` package.json script. It deletes ONLY rows matching `deck-create:%:::1` from `rate_limit_counters` (both hour and day keys — the exact deletion `smoke:seo` performs at its start), prints each deleted key+count, and refuses any argument or wider pattern by construction (no CLI args; the pattern is a literal). Then the battery runs back-to-back: `counters:reset` → collection → decks → optcg-deck → engagement → profile → versions → seo → account → recommend (+ hubs/combos/optcg, zero-create, any time; `smoke:optcg` is still the ONLY smoke allowed against prod). Mark the LATER row FIRED with the sha. Ledger rules that stay true even with the script (measured 2026-09-17): creates land at smoke COMPLETION time, a BLOCKED create still increments (`consumeRateLimit` upserts before checking `count <= max`), so never retry into a 429.

## Pre-flight, in order

1. **Nightlies green** (`gh run list --workflow=nightly-ingest.yml --limit 3`; PATH below). Last probed: 2026-09-16 run 35113363181, success, 5m28s (09-15 and 09-14 also green). Red → P4.7 branch F takes precedence; P2.9 waits.
2. **Tree clean at or after this prompt's commit.** At 2026-09-17 the last code commit is P4.8's `8508830`; docs through `c628688`. `pnpm check` = 764 tests in 104 files, 6 pre-existing `no-unused-vars` warnings, 0 errors. Re-measure and state it.
3. **db:size stated** — 261.2 MB on 2026-09-17 (alert 350; the checklist's launch-week baseline of 259.4 predates the P2.8b/P4.8 session).
4. **Census by query — the BASELINE, not a fixture freeze.** At 2026-09-17: 25 decks, 1 user (`bobandis6`), 13 owned public (12 Commander with primers + the Enel OP deck `jhr5ax43ewx7`; demo deck `uwvrnv2pv4t6`), 12 anonymous (4 private, 8 unlisted). **After a post, growth is the DATA, not drift**: count new users, new decks (anon vs claimed), likes, bookmarks, and folders since this baseline and put the deltas in the round note. The 13 public decks and the demo deck remain untouchable fixtures; new users' decks are theirs — read-only, never edited, never deleted.
5. **Counters read** (`rate_limit_counters` where key like `'deck-create:%'`): note every NON-loopback IP and how close it sits to 10/h / 30/day. Known context: `162.252.181.30` (owner's phone, 14/day on 2026-09-15 while seeding) and `172.56.150.50` (owner, 1 on 09-17) — IPs are dynamic, so treat "owner's IP" as a dated observation, not a fact.
6. **GitHub issues** (`gh issue list --state all`): baseline is 1 closed issue (launch-week note in `P2.8-beta-launch.md`). The owner files friend-watch stalls as issues — new ones are branch-A input.
7. **Error surface**: the checklist names Sentry for launch-week watch, but this session lineage has NEVER verified programmatic Sentry access — `grep -ri sentry src/ next.config.ts package.json` first to learn what is wired before promising anything; otherwise stack traces arrive as owner-pasted input.

## Trigger map — check in order; act only on evidence

- **A. Funnel breakage reported** (import fails, editor breaks, claim/sign-in trouble — from Reddit replies, Discord, issues, or the owner). Highest priority. For an import failure: get the EXACT paste verbatim, add it as a fixture beside `MOXFIELD_TLA_PASTE` (`src/lib/games/mtg/test-fixtures.ts`), watch it fail at the right level (tokenizer / resolve / guess), fix or LATER. Known disclosed miss to recognize before calling it a bug: a commander that sorts FIRST alphabetically is deliberately not guessed (`import-guess.ts` — the honest failure is the pre-P2.8b behavior); if real users hit it repeatedly, that is a LATER row proposing a widened rule, not a hotfix.
- **B. Real-user 429s** (pre-flight 5 shows a stranger's IP at the cap, or a report). Gather the evidence (key, window, what they were doing — 10 imports/hour is a plausible genuine burst) and put a concrete cap recommendation in the round note. **Do not ship a limit change without the owner's explicit go** — it is standing abuse-surface config; the owner said "revisit when a real user trips it," not "auto-raise."
- **C. Claim-nudge trigger (LATER row 64)**: query anon decks created since the post vs claims. Row 64's own fence applies when it fires: one quiet line in the editor header for a guest owner, never a banner. Genuinely ambiguous numbers (e.g., 2 anon decks) → note it, don't fire.
- **D. Errors** (pre-flight 7 surfaced something new): diagnose from the stack; paper cut → fix; bigger → issue + LATER row.
- **E. Feedback triage** (non-breakage: requests, confusion, praise): convert to LATER rows with triggers or GitHub issues; ship nothing speculative. Existing rows that feedback may fire: multi-step undo (60), card-stacks (61), md drawer width (72), null-cost curve complaints (63 — OP-side, hand to P4.7).
- **F. Growth/watch numbers** (always, even when everything else is cold *if* the post is up): deltas from pre-flight 4, db:size, nightlies — one paragraph in the round note. Hobby-plan bandwidth/ISR anomalies get a sentence if the owner reports a Vercel email (LATER's infra row is the escalation path).
- **G. All cold** (owner hasn't posted; no signals): round note saying so, prompt stays armed, stop. Do not "improve" anything while waiting — that is exactly what LATER.md is for.

## What P2.9 is NOT (scope fence)

- NOT P4.7's OP branches (its prompt owns them), NOT M5/Azuki (contingent on outreach the owner hasn't sent — the email itself is the owner's, optional), NOT a redesign revival (R-series closed at `fe0fa40`).
- NOT posting, replying, or voting anywhere as the session; drafts for the owner's replies are fine if asked in-round.
- NOT editing/deleting ANY user-created row (owner's 13 public decks included); the anon purge owns abandoned drafts.
- NOT prod rate-limit changes without the owner's explicit go (branch B); NOT analytics (Cloudflare Web Analytics stays off — "no analytics" is in the announcement's promise).
- NOT `normalize.ts`, NOT `/api/cards/resolve` context changes, NOT schema migrations — a warm branch needing any of those is by definition bigger than a paper cut → LATER + round note.

## Facts you inherit (verified 2026-09-17 at `c628688` — trust these)

- **P2.8b behaviors live on prod** (`1a43ee4`): `importLeaderGuess` hook (types.ts, beside `importZoneFor`); MTG rule in `src/lib/games/mtg/import-guess.ts` (positional, shape-gated, `Intl.Collator("en")`, k=1 then 2; pins in `import-guess.test.ts` incl. the verbatim owner paste); review-step disclosure line "Commander: <name> — from the first line of the list"; add-mode yield (same card = silent skip, different occupant = quiet main-deck landing, checked against PRE-import occupants); spill collapse "<Zone> is full — moved N cards to <Zone> (first three, …)" with single-move wording unchanged; tokenizer widens ` / ` → ` // ` and reads Archidekt `[Commander…]` categories as hints (live-verified export); phone leader-add switches to the Deck pane (`handleAdd`, tier-gated; plain Add stays on Search).
- **P4.8 behaviors live on prod** (`8508830`): `searchIdPrefix` in `src/lib/cards/resolve-token.ts` (OP only, `^[A-Z]+\d*-\d*$` uppercased, no-hyphen → name search, Magic null); the search route swaps the translator for one bound `external_key LIKE '<prefix>%'` ordered by key when it fires (`sort`/`dir` ignored there, docblocked; miss = honest empty, no trgm); `parseQuickAdd` takes `4xOP01-025` (alternation order: spaced-x before attached-x before whitespace); prod-verified: OP01-025 exact, lowercase, OP01-02 → ten ascending, OP99-999 empty, OP06-116 legality by number, luffy byte-identical.
- **The full smoke battery is green** on this tree (2026-09-17), canned p95 335 ms dev-RTT, route table 58 extracted lines and diff-empty across both packages.
- **The announcement's remaining halves are the owner's** (`P2.8-beta-launch.md`): phone re-run with both Moxfield buttons (Toph lands, 0 warnings expected), r/EDH self-promo lane (check the sub's CURRENT rules in-round if asked; lead with a hub like `/c/atraxa-praetors-voice`), 2–3 Discords, watching 3–5 friends build (stalls → issues), Azuki email optional and unsent.
- **Deploy verification**: `gh api repos/Bobandis6/deckwarden/commits/<full 40-char sha>/statuses` → context "Vercel" (code deploys went green in ~2–4 min this week; ISR hubs read MISS twice post-deploy before HIT).

## Verify-first (never from memory)

1. Re-run pre-flight 1–7 and state the numbers even when cold — the round note IS the deliverable of a cold round.
2. A warm branch's fix follows the house pattern: failing pin first (fixture verbatim), fix, `pnpm check` green with the test count stated, battery per the opening chore, route-table diff empty, deploy verified on the full sha, prod-verified with zero creates (resolve/search curls only; never import on prod — an import mints a deck).
3. Anything shipped updates its LATER row / checklist line / memory in the same round, with the sha.

## Deployable outcome

A cold round: the dated round note committed under this title, nothing else touched. A warm round: the branch's fix deployed and `pnpm check`-green, its pins in the tree, its rows FIRED, the round note recording trigger → evidence → decision → sha, census deltas and db:size stated, every dev draft deleted and confirmed by query. Either way the prompt stays armed for the next round; M4 still exits on the owner's OP post (P4.7's ledger), and the MTG beta "exits" into routine watch when a round finds real users building without funnel breaks.

## Environment (carried from the P2.8b/P4.8 session, 2026-09-17)

- PATH per command: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"`; `gh` in `~/.local/bin`; a zsh `for` loop in the Bash tool can lose PATH after its first iteration.
- psql/aws absent — ad-hoc SQL via tsx in `scripts/.tmp/` (gitignored but LINTED; wrap in `async function main()` — top-level `await` fails under CJS compile; `dotenv` with `.env.local`, whose `DATABASE_URL` is prod's Neon pooler: dev rows are REAL rows; delete what you add, confirm by query).
- Dev server: `lsof -nP -iTCP:3000 -sTCP:LISTEN` (kill stale), `preview_start {name: "dev-log"}` (`DB_LOG=1`; `preview_logs search:"[db]"` counts statements; the write-count authority).
- Browser pane: hidden tabs don't hydrate until painted; Base UI menus from plain JS clicks; typed input = native value setter + bubbling `input`; Enter = dispatched `KeyboardEvent("keydown", …)` (`ctrlKey` for leader adds); guest deck deletion = `DELETE /api/decks/:id` with the `x-deck-token` header from localStorage `deckwarden:deck-token:<id>`.
- Prod curls need `--compressed`. Prettier reflows scripted-edit anchors (`*.md` is prettier-ignored; re-grep after `pnpm format`).
- Rate-limit ledger: completion-time landing, blocked-attempts-increment, `smoke:seo` deletes the `:::1` counters at its start — superseded by `counters:reset` once the opening chore ships.

## Context, not tasks

P2.8b's known-miss disclosure, the partner-second-commander trade-off ("Add cards" again), and the `/cards` placeholder staying name-y are all deliberate, documented decisions — recognize them in feedback before reclassifying them as bugs. The Limitless key stays pending (re-submitted 2026-09-12, exception-only, nothing gated). Bandai and Azuki posture unchanged: ambient OP art off, ©BANDAI line on every OP surface, hotlinks impossible (CORP), images via the public R2 bucket. Premium never gates card data or prices. Vercel Hobby is non-commercial: the first affiliate dollar triggers the VPS runbook, not a bigger Hobby footprint.
