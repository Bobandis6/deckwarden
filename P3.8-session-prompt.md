# P3.8 session prompt

Pull latest, then kick off P3.8 from deckwarden-build-plan.md — M3's next
INTELLIGENCE package, the one both PASSED-gated rows in LATER.md have been
waiting for ("the next M3 intelligence package slot"): the lean commander×card
aggregate mined from Topdeck's top-16 lists, shipped as evidence through the
P3.1 seam. It completes plan line ~207 ("Topdeck.gg ingestion → tournament
results on hubs (credit + link rendered), **'seen in top-X lists' as a
ranking signal**" — P3.5 shipped the shelf, P3.8 ships the signal) and plan
line ~202's "co-occurrence once corpus exists". Feature-plan references:
"Whether the recommendation comes from tournament, community, or curated
data" (~88), "Recent tournament usage" (~118), "Tournament usage" (~213), and
the Meta Lens section (~216) for what this is NOT. Know what it is NOT before
designing: it is NOT the Meta Lens (archetypes, matchups, meta share over
time, "common opposing leaders" — LATER row, needs depth + usage), not
EDHTop16, not per-standing card rows (schema.ts's tournaments comment: lean
rows, never ~100 raw card rows per standing — the aggregate exists precisely
so that never happens), not fetching third-party decklists (decklist_url is
never followed; the corpus has 0 URL-only standings anyway), not mining USER
decks for co-occurrence (cold-start ban; 12 user decks exist — real
tournament data is the allowed source), not a hub "most played cards with
this commander" table (that's Meta Lens's "frequently used tech cards" — the
new table makes it one query, which is exactly why it goes to LATER with a
trigger instead of leaking in), and not the flagged natural-language search
(still "optional, behind a flag, costs money" — a later package or never).
READ THIS FIRST — the corpus facts, measured 2026-09-02 against the LOCAL raw
archive (`.topdeck-raw/`, gitignored: 18 gzipped chunk files, 87.2MB gz /
403MB JSON — the 2026-09-01 180-day backfill, 2026-03-05 → 2026-09-01; the
measurement was a scratch script that gunzips each file, runs
`mapTournament` from src/lib/games/mtg/topdeck-map.ts, and walks each kept
standing's `deckObj` Mainboard keys — re-derive it, it isn't committed):
2,917 events seen → 1,894 kept (≥16 players) → 20,768 kept standings
(placement ≤ 16) → 20,760 carry a full mainboard (99.96%); median mainboard
98 distinct names (min 45, max 99); 1,993,316 card entries; 15,679 distinct
cards; 990 distinct commander SETS (933 distinct single leaders; 386 sets
have exactly one list, 325 have ≥5); **170,729 distinct (commander set, card)
pairs** — 81,323 with ≥2 lists, 56,954 with ≥3 — or 177,180 if keyed by
single leader instead of exact set. Top sets: Kraum + Tymna 1,659 lists,
Kinnan 1,474, Thrasios + Rograkh 1,121, Silas Renn + Rograkh 926, Sisay 771;
Talrand (the recommend smoke's fixture commander) has ZERO standings — the
smoke needs a Kinnan-class fixture for the new source. Live DB:
tournaments 1,894 rows (544KB), tournament_standings 20,606 (4.3MB), whole
database 212.3MB (alert 350). Two storage facts that shape the design: (a)
the backfill's raw files exist ONLY on this machine — ingest_runs row 26 ran
locally; R2 holds just the nightly windows from 2026-09-02 on (the Action's
archive step logged "archived 2 file(s) → s3://…/topdeck/raw/2026-09-02/";
R2 secrets live in Actions, not in .env.local, and `aws` is not installed
locally) — so the initial aggregate build reads `.topdeck-raw/` locally (or
the session first makes the backfill durable — say how); (b) the nightly
re-fetches a TRAILING_REFETCH_DAYS = 14 overlap (results settle late), so a
naive "count every list I see" double counts. Design decisions to make
explicitly, each disclosed in code and test-pinned: (1) the table(s):
`commander_card_stats(leader_ids uuid[] sorted, card_identity_id, lists int,
top4 int, first_seen date, last_seen date)` with PK (leader_ids,
card_identity_id) plus a tiny denominator table
`commander_stats(leader_ids, lists, last_seen)` (990 rows) — the exact
commander SET is the honest key ("with your commander(s)"); it must match
decks.leader_ids — and those are NOT sorted: leaderDenorm
(src/lib/decks/cards.ts:53) keeps command-zone entry order while the Topdeck
mapper sorts, so sort the deck side at query time (never compare the arrays
raw); a per-leader partner fallback is a LATER note unless it's
trivial. Neon math out loud BEFORE the migration: a row is ~90B on the heap
(23B header + a 1–2 uuid array ≈ 24–40B + uuid 16B + two int4 + two dates)
plus ~60B in the PK btree ≈ 150B all-in → 170k pairs ≈ 26MB (a ≥2-list floor
halves it to ~12MB but silences the 386 one-list commanders, whose single
list is real data that should simply read low-confidence — decide, disclose);
the aggregate is NOT time-windowed by construction even though the ingest
is, so state the growth model (~26MB per 180 days ≈ 50MB/year all-time) and
the pruning mechanism (a rebuild from the R2 raw archive — LATER row with a
size trigger), and read db:size before/after. (2) Increment semantics — the
crux: aggregate a tournament EXACTLY ONCE, when it is SETTLED (start_date
older than TRAILING_REFETCH_DAYS, so the window never re-fetches it), tracked
by a `tournaments.cards_aggregated_at timestamptz` column; extend the pure
mapper to emit per-standing resolved card-id lists (it already walks the
Mainboard for `hasCardList`; MIN_LIST_CARDS = 40) and roll them up IN MEMORY
per (leader set, card) → upsert `lists = lists + excluded.lists` — nothing
per-standing touches disk; the initial build is the SAME roll-up code over
the local raw files for tournaments settled as of the run (through ~2026-08-19),
and each nightly adds the day that just settled (the window stretches on a
failed night — `min(BACKFILL, daysSinceLastSuccess + 14)` — so nothing ages
out unaggregated); disclose the 14-day lag in the evidence ("settled events")
and add `tournaments_aggregated` / `card_lists_aggregated` /
`list_cards_unresolved` to ingest_runs.stats; standings that change after
aggregation are accepted drift, documented. Verify the deckObj Mainboard
VALUES are counts (the measurement used keys only). (3) List-card
resolution: 2,247 of 1.99M entries (0.11%, 216 distinct names) failed EXACT
name_norm — almost all double-faced cards written as ONE face ("Shatterskull
Smashing" is a BACK face; "Birgi, God of Storytelling", "Wandering Archaic",
"Hydroelectric Specimen", "Pinnacle Monk", "Invasion of Ikoria" are fronts),
plus Topdeck's "________ Goblin" (8 underscores vs Scryfall's 5) and the
reversible-name bug below — so the list resolver needs the resolve route's
front-face pass AND a back-face pass (attrs.faces carries every face name —
build one face-name → identity map per run), exact only, never trgm, with the
unresolved COUNTED. Use the full identity map, not the ingest's leaders-only
map. (4) An ingest data bug found while measuring — fix it FIRST, as its own
commit (it is a correctness bug the signal depends on, not scope creep): 12
identities have doubled canonical names "X // X" — Scryfall's
`reversible_card` layout puts both faces in the top-level `name`, and
scryfall-map.ts `mapIdentity` takes `card.name` verbatim (SKIPPED_LAYOUTS
covers only token/emblem/art_series) — including 7 LEADER candidates
("Chulane, Teller of Tales // Chulane, Teller of Tales", Jinnie Fay, Norin the
Wary, Okaun, Eye of Chaos, …) and 5 triple names ("Marang River Regent //
Coil and Catch // Marang River Regent"). Effects today: doubled titles on
those card pages and hubs, doubled names in search, and exact-name resolution
failing everywhere the DFC pass isn't run — the Topdeck leader map is exact,
so the backfill logged 271 `unresolved_commander` skips, some of them Chulane
lists. Fix in the mapper (derive the identity name from the oracle faces when
the layout is reversible_card; dedupe identical faces; keep the real "A // B"
for a DFC that also has a reversible printing), pin it with a fixture in
scryfall-map.test.ts, and let the nightly's hash-guarded re-ingest apply it
(changed names update); cardSlug already takes the front face, so hub URLs
don't move (chulane-teller-of-tales exists). (5) The seam, additive only:
RecommendMeta gains `tournaments?: { source: "topdeck-top16",
evidence({ commanderNames, lists, ofLists, share, top4 }) → { why, howOften } }`
and `sources["topdeck-top16"] = { label: "Topdeck.gg", href:
"https://topdeck.gg" }` — the API's visible-credit requirement is met
because the panel renders the source label + link with every evidence entry
(same label/href as the hub shelf's capability); core queries.ts gets
`loadTournamentSignals(leaderIds, candidateIds)` (one query on the new table
for the deck's exact leader set + the denominator) AND a third candidate
source `loadTournamentCandidates(leaderIds, filter, limit)` — commander-
specific tech that the global edhrec top-300 pool never surfaces is the whole
point — re-checked through the SAME deterministic filter (legality / CI /
budget / owned / exclude); rank.ts rebalances WEIGHTS with a `tournaments`
weight (sum 1 — decide the split with reasoning: measured deck-specific play
beats global play; e.g. popularity .30 / tournaments .30 / combos .25 /
curve .15), scores the share with a sample-size shrink (share × n/(n+k)),
and sets confidence by n (n<5 low, 5–19 medium, ≥20 high) with the numbers
IN the evidence: why "Played in 62% of top-16 lists with Kinnan, Bonder
Prodigy", howOften "58 of 94 top-16 lists at 16+ player events on Topdeck.gg,
settled events since 2026-03" — both LATER rows fire through ONE evidence
source (co-occurrence IS the share; "seen in top-16 lists" is the same
dataset — a separate top-4 flavor only if it stays honest and cheap). A deck
whose commander set has no rows gets no tournament evidence and no
tournament candidates — honest absence, never a fabricated neutral score,
and the other signals are unchanged (P3.1 tests stay green untouched).
(6) Cut Coach: the same table answers the cut side ("0 of 94 top-16 Kinnan
lists play it") through cuts meta — ship it only if it's a few lines through
the existing seam, else LATER. (7) No new UI: the Suggestions panel already
renders arbitrary sources with label + link; the panel's "only cards I own"
and budget filters compose with the new candidate source for free. Deployable
outcome = the reversible-name fix committed with a test; migration
(new table(s) + `tournaments.cards_aggregated_at`) eyeballed and applied to
Neon; the initial aggregate built from `.topdeck-raw/` locally with its
counts logged and reconciled against the numbers above (pairs, lists,
unresolved); the nightly increment wired into scripts/ingest/topdeck.ts with
new stats fields and proven by ONE manual `workflow_dispatch` run (settled-
day increment, no double count — assert `commander_stats.lists` for a top
commander before/after); the signal live on /api/decks/[id]/recommendations
with Topdeck.gg-credited evidence; smoke:recommend's SOURCES contract
extended (present for a Kinnan-class fixture, absent for a commander with no
lists); smoke:hubs and smoke:combos still green; pnpm check green; deployed;
db:size stated before/after; LATER rows 32/33 rewritten as Done with the
shipped facts.

Session notes:

* Toolchain: node/pnpm at ~/.local/node22/bin — non-login shells need
  export PATH="$HOME/.local/node22/bin:$PATH". `source .env.local` FAILS in
  zsh — pull single vars: export DATABASE_URL="$(grep '^DATABASE_URL='
  .env.local | cut -d= -f2- | tr -d '"')". gh, psql, aws are NOT installed —
  GitHub REST via curl (token via `git credential fill`); ad-hoc SQL via a
  tsx one-off with the repo's `postgres` package; scratch tsx scripts live
  in scripts/.tmp/ (gitignored since P3.7 — inside the repo so node_modules
  resolve). macOS `cat` has no -A (use `cat -v`). Deploy verification: Vercel
  posts a commit status (context "Vercel") on the pushed sha;
  actions/runs?head_sha= needs the full 40-char sha.
* Pre-flight: pnpm check on main AND the nightly Action. The 2026-09-02
  10:37 UTC scheduled run is the FIRST unattended scheduled run after
  afdee33's retry fix (the 2026-09-01 scheduled run was the Topdeck-500 red;
  the two green runs since were manual dispatches) AND the first with the
  `collections` table: verify every step green, the backup step's log line
  lists collections ("backed up: users deck_versions deck_cards decks
  deck_folders deck_bookmarks deck_likes collections" — the 03:47 run listed
  seven tables because the table didn't exist yet), topdeck stats ~15-day
  window with small inserted counts, db:size under 350. A red step is triage
  BEFORE P3.8 work.
* What P3.7 shipped (2026-09-02, commit 7082c80 — not in the plan doc): the
  `collections` table (drizzle/0012_collections.sql; PK user+printing+
  finish, user FK cascades, printing FK doesn't; empty in prod — the owner
  has no collection); src/lib/collection/ (parse.ts pure CSV parser pinned
  to REAL ManaBox/Moxfield headers, plan.ts fold/cap/finish, ownership.ts
  the one "You own N/M · missing ≈ $Y" function, resolve.ts id → set+number
  → name, store.ts one-transaction merge/replace, owned.ts reads);
  src/lib/cards/resolve-printings.ts (the shared set+number resolver — half
  of the setHint LATER row); /api/collection GET/POST/DELETE +
  /api/collection/owned; GET /api/decks/[id] now returns `owned` +
  `hasCollection`; /api/decks/[id]/recommendations takes `?owned=1`
  (`owned.applied` / `owned.reason` in the response — the P3.1 ownedCardIds
  hook is live); editor header line + ✓ marks, share-page line for signed-in
  viewers, Suggestions "Only cards I own" toggle, /account Collection
  section; smoke:collection (52 checks, 3 minted users, 1 deck); LATER got
  five Collection Mode remainder rows.
* Neon: 212.3MB on 2026-09-02 after P3.7 (includes dead tuples from the
  smoke's 20k-row cap test — autovacuum reclaims; the collections table is
  empty). Alert 350, free ~512. P3.8 adds the third growing table.
* LATER rows for THIS package: rows 32 (co-occurrence) and 33 ("seen in
  top-16 lists") FIRE — rewrite them as Done with the shipped facts (table,
  settled-once rule, weights, the reconciled counts). Add rows with triggers
  for whatever is left out: the Meta Lens hub table ("most played with this
  commander" — one query now), partner/per-leader fallback, rolling-window
  pruning via R2 rebuild (size trigger), the Cut Coach tournament consumer
  if skipped, top-4/placement-weighted flavor if skipped.
* Rate limiter / smoke budget learned in P3.7: POST /api/decks is 10/hour
  AND 30/day per IP (fixed windows: :00 UTC hourly, 00:00 UTC daily). On
  2026-09-02 the daily bucket hit 31/30 (P3.6's prod smokes at 03:00–04:00
  UTC plus P3.7's), so versions/engagement/decks were run only against the
  local dev server for P3.7 — re-run them against prod when the budget
  allows. Read the counters first: `select key, window_start, count from
  rate_limit_counters where key like 'deck-create:%' order by window_start
  desc`. Order: collection → recommend → versions → account, then the rest.
  A 429 mid-smoke is the limiter, not a regression.
* Smoke-script bug (pre-existing, P2.2): scripts/profile-folders-smoke.ts
  crashes in its `finally` when a deck create 429s (`sql(cleanup.deckIds)`
  with an undefined id → UNDEFINED_VALUE) and leaves `@smoke.invalid` users
  + "Smoke Deck" rows in prod — cleaned twice on 2026-09-02. Hardening it
  (guard the pushes / filter undefined ids, in every smoke that pushes ids)
  is a legitimate standalone chore commit at session start — separate from
  the package, not a LATER row. After any red prod smoke: `select * from
  users where email like '%@smoke.invalid'` and delete strays (decks first —
  decks.user_id has no cascade).
* Smoke rules against prod: BASE_URL=https://deckwarden.gg pattern;
  smoke:recommend creates/deletes its own guest deck; smoke:combos and
  smoke:hubs are DB-connected; smoke:seo's ONE known failure is "core
  sitemap lists the public deck" (revalidate lag) — anything else is real;
  engagement/account/versions/collection smokes mint signed sessions
  (BETTER_AUTH_SECRET pattern in scripts/engagement-smoke.ts); on prod a
  minted cookie must be set under BOTH names — `better-auth.session_token`
  AND `__Secure-better-auth.session_token` (https) — setting only the first
  silently makes you a guest (P3.7 learned it the hard way).
* Prod verification: never import into, wipe, or read the owner's account as
  a fixture. Mint throwaway users (scripts/collection-smoke.ts's mkUser
  pattern), cookie via document.cookie on the prod origin with the Secure
  prefix, DOM reads via javascript_tool, delete the deck THEN the user when
  done. The recommendations fixture must be a commander WITH lists (Kinnan,
  Bonder Prodigy: 1,474; Sisay, Weatherlight Captain: 771) — Talrand has none.
* Browser pane: hidden pane swallows computer clicks silently — drive via
  javascript_tool, verify via DOM/API reads.
* Topdeck contract facts (P3.5, verified live): POST /v2/tournaments with
  columns name/decklist/wins/draws/losses returns `deckObj` alongside
  `decklist`; `deckObj.Commanders` keys are names, the Mainboard key matches
  /^main/i; placement = `standing` or index+1; the API can return events
  outside the requested window (the mapper re-checks); ≥1.1s between
  requests, 429 honors Retry-After; visible "Topdeck.gg" credit + link
  wherever the data appears is the hard attribution requirement (the
  adapter's capabilities.tournaments carries label/href/eventUrl).
* Context, not tasks: beta announcement still deliberately deferred;
  permission emails to Bandai + Azuki Labs out 2026-08-30; cold-start rule
  stands — the aggregate is REAL tournament data (allowed); deriving
  anything from seeded or simulated decks stays banned.
* Protocol as usual: one package, ends deployed and check-green; scope
  leaks to LATER.md with a trigger.
