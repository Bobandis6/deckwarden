# LATER.md — Deliberately Deferred

The scope-discipline file. Anything that threatens a milestone's package list lands here
with one line on when it earns its way back. Nothing on this list is a "no" — it's a
"not until the trigger fires." (See [deckwarden-build-plan.md](deckwarden-build-plan.md).)

## Deferred features

| Item | Revisit when |
| --- | --- |
| Deck comments (incl. per-card comments) | There's a community to moderate — real daily actives, post-M3. Brings moderation, reports, spam; a standing tax. |
| Full collection manager UI — browse/edit/sort grid, per-card add-to-collection buttons, public collections, "cards assigned to other physical decks". **P3.7 (2026-09-02) landed the data + the two read surfaces only**: the `collections` table (the plan's "schema now" had never actually been written — P3.7 was the first user-data migration since M1), ManaBox/Moxfield CSV import on /account (merge/replace, honest reject list), owned badges + "You own N/100 · missing ≈ $Y" in the editor and on share pages for the signed-in viewer, and the Suggestions "only cards I own" toggle. | Editor retention proven (post-M5). |
| Price history / trend charts | Someone asks twice. Needs the weekly or deck-referenced-only side table — never per-printing-per-day rows. |
| Full goldfish playtester (turns, battlefield, draws beyond the opener, London bottoming) | Sample-hand widget shipped in P2.7 (2026-08-30) on share page + editor — now it's a demand sensor: revisit when people actually draw hands. Big surface, incumbents are mediocre here — could be a differentiator later. P2.7 deliberately kept mulligans plan-literal (full redraw of 7, no bottoming) and shipped no draw-next-card button — both are playtester scope. |
| PWA + offline deck viewing | After M3. Cheap win for paper events; needs a service-worker pass over share pages. |
| Email digests ("new cards fit your decks") | An email provider exists (Resend). First real retention push, post-M3. |
| Natural-language search via Claude API | Behind a feature flag whenever; costs money per query — needs either budget or premium gating. |
| Meilisearch/Typesense | Postgres FTS measurably fails (p95 > 300ms warm or fuzzy quality complaints). Not before. |
| Scryfall `/cards/manifest` incremental sync | Nightly full-bulk job time or Neon compute hours become a measured problem. |
| Self-hosting MTG images on R2 | Traffic makes hotlinking impolite, or Scryfall asks. Planned-for via `image_override` + manifest timestamps. |
| EDHTop16 integration | Topdeck.gg data proves insufficient; requires Discord contact for current API terms. |
| Card Kingdom buy-buttons | Affiliate switch-on. Contact-based partnership; URL structure already supports `partner=` params. |
| Collaborative brewing (live editors, suggestions) | Strong community signal post-M4. Real-time infra is a step change. |
| Mobile card scanner | Never, probably — ManaBox import covers 90% of the value at 1% of the cost. |
| Other MTG formats (Standard/Modern/etc.) | Commander experience is clearly winning and users ask. Legality model already supports it. |
| Native mobile apps | PWA proves insufficient. |
| Deck Passport / QR export page | Nice-to-have polish after OG images (M2) prove the share loop. |
| Matchup guides / Meta Lens deep pages | Requires tournament corpus depth from Topdeck (M3+) plus real usage. |
| Local playgroup spaces | Community features prove retention first. |
| Marketplace, proxy generator, tournament organizer, AI-chat-first interface | Original doc's postpone list — reaffirmed. Proxy tools carry real IP risk (especially One Piece). |
| Smarter mana-source detection: P1.5's `producedMask` counts conditional producers like Command Tower as all five colors; fetches/treasure-makers not counted. **The row's other half — role-tag-consuming analytics — FIRED in P3.4 (2026-08-31)**: the Cut Coach is the first deck-vs-template consumer (role counts vs `adapter.hub.roles` over user tags, case-insensitive exact match, qty-weighted, tagged cards only — "13 of your cards are tagged Ramp; the template suggests ~10" — plus deck-wide curve-bucket surplus vs the curve template). **P3.4 took the producedMask call: still deferred, with reasoning.** The Coach's land advice is deliberately count-vs-template via the Lands role tag, not produced-color analysis — reading produced colors would mean inferring roles for untagged lands, which the Coach's own no-inference rule bans, and color-sufficiency advice is a different product (which colors the mana base casts) than cut ranking (how many slots the deck frees). Nothing shipped reads `producedMask` beyond P1.5's disclosed-heuristic sources table. | The first surface that renders produced-color advice — a mana-base coach or Swap Lab's land swaps (M3+ feature plan) — or user feedback that the Mana sources table misleads. |
| Co-occurrence as a recommendation ranking signal — **FIRED in P3.8 (2026-09-02), shipped as the commander×card aggregate.** `commander_card_stats` (exact sorted commander set × card: lists / top4 / first–last seen; PK on the pair) + `commander_stats` (the denominator, 933 sets) rolled up IN MEMORY at ingest from the raw deckObj mainboards — never a per-standing card row. Aggregated EXACTLY ONCE per tournament when SETTLED (start_date strictly older than `TRAILING_REFETCH_DAYS` = 14, shared const so the fetch window and the cutoff can't disagree), gated by `tournaments.cards_aggregated_at` in one transaction with the upserts; the initial build (`scripts/ingest/topdeck-aggregate-backfill.ts` over the local 180-day raw archive, 2026-09-03) reconciled against the corpus measurement: 2,917 events seen (exact), 1,891 kept, 20,614 kept standings (99.97% with lists; the measurement's 20,768 resolved commanders against a FULL identity map where production is leaders-only by design), 1,979,200 mainboard entries with **0 unresolved** — Topdeck's deckObj values carry the Scryfall oracle id, so resolution is id-first, then exact name, then exact face name, never trgm. Settled build: 1,756 events → 19,191 lists → 159,457 pair rows, 32MB (DB 212.3 → 245MB, alert 350). Growth ≈ 26MB/180 days; the nightly adds each day as it settles (`tournaments_aggregated` et al. in ingest_runs.stats). | Done. |
| "Seen in top-16 lists" as a recommendation ranking signal — **FIRED in P3.8 (2026-09-02) through the P3.1 seam, one evidence source carrying both LATER rows** (co-occurrence IS the share). `RecommendMeta.tournaments` + `sources["topdeck-top16"]` = { Topdeck.gg, https://topdeck.gg } (the hard attribution rule — the panel renders label + link with every evidence entry); `loadTournamentSignals` + `loadTournamentCandidates` (a THIRD candidate source: commander-specific tech through the same deterministic filter; deck `leader_ids` sorted at query time — leaderDenorm keeps entry order, the aggregate sorts); WEIGHTS rebalanced to popularity .30 / tournaments .30 / combos .25 / curve .15 (sum 1; measured deck-specific play ties global play, P3.1 tests untouched); score = share × n/(n+10), confidence n<5 low / 5–19 medium / ≥20 high, raw numbers in the words ("Played in 62% of top-16 lists with Kinnan, Bonder Prodigy" / "58 of 94 top-16 lists at 16+ player events on Topdeck.gg, settled events since 2026-03"). A commander set with no rows gets NO tournament evidence and no tournament candidates — honest absence. smoke:recommend pins both directions (Kinnan present, Talrand absent), green against prod 2026-09-03. | Done. |
| Meta Lens hub table — **FIRED in P3.10 (2026-09-17, `52c2c47`)**: "Most played with {commander}" on /c/[slug], directly after staples + curve. Design calls, made with numbers: union across every set CONTAINING the leader (Tymna = 18 sets / 2974 lists, "(any pairing)" worded only when >1 set; solo collapses naturally); ranked by RAW share — a global-lift "tech" ordering measured degenerate (saturates at total/commander for commander-exclusive one-ofs, its top 15 was one-list singletons); 25 rows; union floor ≥5 lists → honest absence (Atraxa at 4 lists is the live example); `top4` a DISCLOSED column, never ranked on (row 38 fence held). One statement per hourly ISR render: commander_stats scan → pair-PK prefix join, 1.2ms/286 buffers (the flat `@>` scan measured 21ms/2507). shareLabel honesty pins: 99.66% renders "99%", never a false "100%"; a real share never shows 0%. Footer Topdeck credit ORs the new section. | Done. |
| Per-single-leader tournament fallback: the aggregate keys by EXACT commander set (honest — "with your commander(s)"), so a Tymna+Kraum deck never sees Tymna+Thrasios data, and a set with 1 list reads low-confidence rather than borrowing a partner's corpus. A leader-keyed view (177,180 pairs if keyed by single leader) would give partner decks a broader denominator at the cost of the exact-set claim in the evidence words. | A real deck hits an empty exact set whose single leaders have rich data, and the evidence phrasing can stay honest ("with Tymna the Weaver in any pairing"). |
| Aggregate pruning / rolling window: `commander_card_stats` is all-time by construction (~26MB per 180 days ≈ 50MB/year) while the ingest window rolls. The rebuild tool already exists — `scripts/ingest/topdeck-aggregate-backfill.ts` pointed at a directory of raw chunks: truncate the two tables, NULL `cards_aggregated_at`, re-run over the window you want. Raw coverage: R2 holds nightlies from 2026-09-02 on; the 180-day backfill chunks (2026-03-05 → 2026-09-01, 18 files, 87MB gz) are archived with SHA256SUMS + README in the owner's iCloud Drive at `deckwarden-archive/topdeck-raw-backfill-2026-09-01/` (2026-09-02 — private on purpose: raw Topdeck responses, public repo). | DB crosses ~300MB, or the Neon 350MB alert fires. |
| Cut Coach tournament consumer — **FIRED in P3.11 (2026-09-19, `d2309db`)**, all four named parts: (1) IO — the combos route (a GET; card ids are loaded server-side, not posted) grew an ADDITIVE `tournaments` block over the same in-deck ids via `loadTournamentSignals`, gated on the game's `recommend.tournaments` meta (OP skips the reads entirely and answers `{context: null, byCard: {}}`), preserving the one-fetch-per-edit policy. `byCard` presence IS the honesty contract: a measured card may honestly read `lists: 0` (the "0 of N" line — live example: Frostbite Pyromental, 0 of 1,473 Kinnan lists), an absent id was never measured (mid-edit adds), and the LEADERS are excluded — commanders have no self-rows at ingest, so a zero there would be fabricated. (2) `rankCuts` scores `(1 − share) × n/(n+TOURNAMENT_SHRINK_K)` (the add direction mirrored), denominator-banded confidence, keep-side lines trail via the popularity keepTail; absent context → zero evidence, zero contribution, and no extra floor beyond context existence (rank.ts's precedent, not the hub's ≥5 — shrink + confidence carry small samples). Share tiers are the adapter's (`TOURNAMENT_STAPLE_SHARE` .5 keep "measured staple" / `TOURNAMENT_PLAYED_SHARE` .15 keep "sees real measured play" / below cut with raw counts), pct honest per the P3.10 clamp (never a false 100%), `top4` disclosed in howOften, never ranked on (row 38 held), exact-set wording in every sentence (row 35 held). (3) `CUT_WEIGHTS` reseated at popularity .30 / tournaments .25 / curve .20 / role .15 / price .10 (sum 1); every P3.4 pin survived; combo membership stays weightless ordering — a measured-zero combo piece still sorts after all non-members (pinned). (4) 7 new pins + the widened `recommend-smoke` (Talrand honest-empty block, Kinnan measured context + byCard semantics); `topdeckHowOften` extracted so the add/cut scope sentences can't drift. Keep-side live example: The One Ring, 59% of 1,473 Kinnan lists (870, 258 top-4s), sorts last. Original note — "0 of 94 top-16 Kinnan lists play it": the same table answering the cut side; NOT a few lines (phrasing block, weights rebalance, IO, test surface). | Done. |
| Placement-weighted tournament scoring: `top4` is stored per pair and DISCLOSED in the evidence ("17 placed top 4") but does not score — lists is the ranking signal. A top-4-share flavor (or win-weighted) only if it stays honest about sample sizes half the size of the list counts. | Users ask why a top-4 staple ranks under a mere top-16 staple, with a real example. |
| Commander-resolution coverage in the Topdeck ingest (leaders-only map, exact name): three measured gaps, ~240 of 20,850 top-16 standings (1.2%) skipped as `unresolved_commander` across the 180-day corpus. **(a) FIRED 2026-09-03 (2eb2351)**: legendary Vehicles + P/T-boxed Spacecraft flag as leader candidates per CR 903.3 verified verbatim from MagicCompRules 20260227 — NARROWER than "all of them": Spacecraft need a power/toughness box, so The Eternity Elevator stays false (pinned in `scryfall-map.test.ts` alongside Shorikai/Hearthhull/Copter shapes). Rollout verified by manual `workflow_dispatch`: 37 leader slugs assigned in the post-pass (0 flagged-but-unslugged after), the nightly window's `unresolved_commander` fell to 8/1498, hubs render (/c/shorikai-genesis-engine 200), and the idempotent 7-day standings merge already landed Shorikai ×5 / Hearthhull ×3. History does NOT retroactively heal: settled events never re-fetch and their tournaments are already `cards_aggregated_at`, so pre-fix standings/aggregate rows recover only at the next full rebuild (the pruning row's backfill machinery); an optional `TOPDECK_WINDOW_DAYS=180` manual ingest would re-fetch standings for the shelves (~18 polite requests, no aggregate effect) — owner's call, skipped. Also verified: "Kavaero, Mind-Bitten" (×4) does not exist on Scryfall — not this gap, some other unresolved cause. **(b) FIRED 2026-09-17 (163108d, P3.9)**: `buildLeaderResolver` (pure, 5 pins) — full names populate BEFORE per-face names so a full name always beats a colliding face, popularity-ordered first-wins, leaders-only stays, same-id repeats (reversible cards) aren't collisions; shared `loadLeaderResolver(sql)` feeds the nightly AND `topdeck-aggregate-backfill.ts` (found in-session duplicating the inline map), so the next full rebuild heals pre-fix DFC history too. Manual dispatch proven (run 35246412908): map 4083 → 4381 names (+298 = 149 DFC leaders × 2 faces, collisions still 1), `unresolved_commander` 19 → 9 on a comparable window, and +10 standings healed in an UNCHANGED 149-event window (1511 → 1521; Ral 22 → 25 in-window, no front-face Kefka in the current 15 days — the ×11 was the 180-day corpus). `TOPDECK_WINDOW_DAYS=180` wider re-fetch again skipped, owner's call. Original note — DFC commanders written as their front face ("Ral, Monsoon Mage" ×17, "Kefka, Court Mage" ×11): the face-name pass P3.8 built for LIST cards, applied to the leader map. (c) Background pairs ("Clan Crafter" ×26): the background is not a leader candidate and pairs need their own hub semantics; the remaining ~9/run unresolved ≈ (c) + garbage names. | Someone's commander has no hub / no tournament shelf and they say so, or M4's ingest work touches the leader map anyway. (a)+(b) done; (c) remains. |
| Markdown (or any formatting) in deck notes — P2.7 ships notes as plain text with pre-wrap line breaks | Users paste markdown primers or ask for headings/links. Rendering is the easy half; sanitization and a preview toggle are the real cost. |
| Real 403 status on private-deck/folder denial shells (they still serve HTTP 200). **P2.6 (2026-08-30) took the fired call: noindex the shells, keep 200 + the client gate.** Reasoning: (1) the crawler concern — the actual trigger — is fully answered by `robots noindex` metadata, which is also what Next itself injects on real 403s; (2) `forbidden()` requires the experimental `authInterrupts` flag on our pinned Next; (3) `forbidden.tsx` receives no props, so it can't render `PrivateShareGate` (needs deckId) — a real 403 would break anonymous claim-token owners viewing their own private deck at /d/…; (4) the data was never in the HTML. Status-code purity re-earns a look when authInterrupts goes stable AND the owner gate can move fully server-side. | authInterrupts stabilizes in a Next upgrade, or crawlers observed indexing the shells despite noindex. |
| setHint-aware import resolution + set codes in export (P1.6 resolves by name only, so two distinct cards sharing an exact printed name — Mystery Booster "Counters" — merge on import; the parser already captures `setHint`, the resolve route ignores it). **HALF-FIRED in P3.7 (2026-09-02)**: the (set code, collector number) → printing resolver now exists as a shared function (`src/lib/cards/resolve-printings.ts`, a VALUES join on cp_by_set, exact match only) because Moxfield CSVs carry no Scryfall ids. The remaining half is adoption: /api/cards/resolve still ignores `setHint`, and the paste importer still resolves by name. **The OP half FIRED in P4.1 (2026-09-03)** — but as its own pass, not via resolve-printings: OP import resolves by CARD ID (an exact `external_key` pass for game=optcg in the resolve route; "OP01-025" tokens hit identity directly, which is stronger than a set hint). The MTG setHint adoption remains untouched. | A real MTG user hits the name collision — then it's wiring the existing (set, collector) resolver into the resolve route. |
| Single-deck DELETE (/api/decks/[id]) vs `forked_from_deck_id`: the self-FK has no ON DELETE, so deleting a deck that a live fork points at would 500. **FIRED in P3.6 (2026-09-01).** Every delete path now goes through one helper (`src/lib/decks/forks.ts` `detachForksAndDeleteDecks`): the single DELETE, account deletion (already correct, now shared), and the nightly anon purge (`scripts/purge-anon-decks.ts`, which had the same latent bug — a purged guest deck with a fork). Pinned by `smoke:versions` (DELETE with a live fork → 204, pointer NULLed; purge helper likewise). | Done. |
| "Follow a deck for future updates" (feature plan, Deck Versions and Forks section) — a community/notification feature, not storage: needs a follows table, a digest or in-app inbox, and unfollow/privacy semantics. P3.6 (2026-09-01) shipped versions + forks and deliberately left this out; forks already get "their changes since you forked" on demand via the frozen v1 baseline, which covers the pull side without any notification infra. | Real daily actives asking for it, together with the email-digest row (Resend exists) — one notification surface, not two. |
| Deck diff over tags and printings — P3.6's diff (`src/lib/decks/diff.ts`) compares zones × cards × quantities only; a version whose sole change is a tag or an alt-art swap reads "no card changes" (the UI says so). **W6 (2026-09-20) made printing swaps a first-class autosaved edit** (the pane's "Use this printing in deck"), so a version whose only change is a printing choice now happens in normal use, not just via import edge cases — the "no card changes" reading is user-visible. | A user asks why their printing swap doesn't show in history (now reachable from the pane, not just re-tags), or printing provenance matters for collections. |
| Version history visibility — P3.6 kept versions OWNER-ONLY (the feature plan's "view the original deck's evolution" is a public timeline). Reasoning: history is a working scratchpad (safety snapshots, half-finished brews) that the owner never chose to publish; the share page publishes the CURRENT list only, and forks carry their own frozen baseline. | Users ask to publish a changelog; then it's an opt-in per-version "public" flag, not a default flip. |
| **FIRED 2026-09-08 — R1a (`8f69064`):** dark is the default via next-themes (Light / System in the footer appearance menu until the R1b header), `color-scheme` follows, no flash; editor, hubs, share pages and OG unfurls verified in both themes. Original note — Dark mode default (§10 brand hook): globals.css carries full `.dark` tokens but nothing ever sets the class — the site ships light-only. Flipping the default (or honoring prefers-color-scheme) is a visual QA pass over editor/hubs/share/OG surfaces, not a toggle. Spotted during P2.8's home-page work; deliberately not smuggled into it. | Fired by the R-series brand pass ([REDESIGN.md](REDESIGN.md) §6). |
| Spellbook popularity floor (≥ 1 or NULL) **stays** — P3.3 (2026-08-31) fired the scoped revisit and kept it. Reasoning: DB measured 204MB against the ~350MB free-tier alert line with Topdeck ingestion (P3.5) and collection import (P3.7) still to land; +38MB would buy detection only of combos EDHREC has seen in zero decks, and the Radar now **discloses** the bound in its panel footer ("covers combos with recorded play, plus unranked new ones") instead of implying completeness. Unranked (NULL) combos are kept and rendered honestly ("no tracked plays yet"). The row's other half — template-combo semantics — is settled and test-enforced (`deckComboStatus` in `src/lib/combos/view.ts`: a template combo is never "complete"; every surface renders "Also needs …", the engine's phrasing). Lifting the floor remains one constant in `spellbook-map.ts` + a re-ingest — fully recoverable. | Neon paid tier / VPS move (the 38MB stops mattering), or a user reports a real combo the Radar misses that Spellbook lists at zero popularity. |
| `artist` column on `card_printings` (~1.5MB). P2.6's OG images render art_crop + the required artist/© line by fetching artist from the Scryfall API at render time (data-cached daily per printing, og/scryfall.ts) — chosen over a migration because adding the field means touching the hash-guarded ingest the same week its spellbook step first runs unattended, plus a full backfill run. A stored artist would drop the OG path's runtime Scryfall-API dependency. **Correction (P4.1, 2026-09-03): punk-records v2.0 carries NO artist field anywhere** — the earlier "punk-records carries artist too" claim was wrong, so OP was never a second consumer for this column; the trigger stands on the MTG reasons alone. | OG unfurl latency measurably hurts, or Scryfall asks us to cut API traffic. |
| OG share images for folders (/f) and profiles (/u) — P2.6 shipped the plan-scoped three (decks/hubs/cards); folders/profiles unfurl with site defaults. | Folder-link sharing shows real usage, or the Deck Passport polish item gets picked up. |
| **OP image serving flip to R2 — FIRED same session (P4.1, 2026-09-03).** Bandai hotlinks are IMPOSSIBLE — en./asia-en.onepiece-cardgame.com serve `Cross-Origin-Resource-Policy: same-site`, so every browser refuses cross-site embeds (verified against live headers); `embeddablePrintingImageUrl` gates every wire so a non-embeddable URL becomes an honest null, never a broken img. Owner created the images-only PUBLIC bucket `deckwarden-public` (the main bucket `deckwarden-backups` holds pg/ dumps and must never get a public domain — public access is per-bucket), enabled its r2.dev Public Development URL, and widened the R2 token scope; secrets `R2_PUBLIC_IMAGE_BASE` + `R2_PUBLIC_BUCKET` set; one dispatch re-pointed all 4,843 `image_override`s (the ingest's skip-check includes image_base). Mirror lessons paid for in runs 33714359501/33721036663: per-file `aws s3 cp` ≈ 1s so the backfill must stage locally + upload once recursively, and the mirror step needs its own `timeout-minutes` — a JOB-timeout cancellation kills the `!cancelled()` safety steps. **Remaining half: r2.dev is rate-limited and "not for production"** — the real fix is `img.deckwarden.gg` as a bucket custom domain, which requires the deckwarden.gg DNS zone on Cloudflare. **P4.6 (2026-09-07): the OP-beta trigger FIRED; owner deferred the DNS move** — the full console runbook (Cloudflare zone + record checklist incl. the load-bearing Porkbun MX/SPF, grey-cloud-the-Vercel-records per Vercel's own KB, bucket attach, env+secret+one-dispatch flip) is the TOP owner item in `P4.6-op-beta-launch.md`. Standing rule: **announce only after the flip, or knowingly accept r2.dev throttling on launch traffic.** **R5a (2026-09-10) gates three more surfaces on this flip:** the deck tiles' One Piece images (home rail, Continue building, `/account`, the `/c/` shelf — the slot paints the color gradient meanwhile), the home One Piece shelf images (gradient slots with the printed id), and the `/leaders` image view (no List / Grid toggle there until `img.deckwarden.gg` serves — 142 r2.dev images on one page is exactly this throttling); the gate is `thumbnailUrl()` in every case, so the flip changes no layout. | Owner executes the runbook (any day, ~20 min active); the flip half (step 11) can be handed back to a session once `img.deckwarden.gg` serves a test image. |
| OP leader hubs + /c/ routing design — **the routing/slug half FIRED in P4.4 (2026-09-04).** Decision (recorded in hub/queries.ts's header): each game keeps its own hub root — /commanders → /c/[slug] stays MTG-only and none of its 4,012 URLs move; OP gets /leaders → /l/[slug]. Grounds: vocabulary ("commander" is MTG-speak; OP players say "leader") and SEO stability — NOT collision fear, which the slug shape kills structurally: OP slugs are `cardSlug(name)-external_key` ("monkey-d-luffy-op01-003", `leaderHubSlug` in normalize.ts) because 142 leaders share 77 names (17 Luffys) — the name alone never identifies a leader. assign-leader-slugs.ts grew an OP pass (punk-records post-pass + standalone backfill), same append-only/NULL-only contract, external_key order, collision-proof by construction (shape test-pinned; backfill run twice against prod: 142 then 0). Hubs shipped in their designed cold-start form (adapter contract: no `hub` capability = card data only): art, colors, life, traits, effect text, namesake cross-links, color/trait browse links into /cards, build CTA — NO staples/budget/combos/tournament/deck sections (every one an MTG-only signal today; the cold-start rule bans empty shelves). **The deck-shelf half HOLDS.** | Real OP decks accumulating → community shelves on /l/ hubs (routing + slugs are done; never seed or simulate decks to get there). |
| **Real OP banlist + overlay — FIRED in P4.2 (2026-09-04, 89a357d).** `data/optcg/legalities.json` is the live authority (Bandai's page retrieved 2026-09-04: 5 bans + 3 pairs effective 2026-04-10), applied as dated rows sourced `bandai:2026-04-10` by the nightly (unconditionally, before punk-records' sha-skip exit) and `pnpm ingest:optcg-legalities`. Design note vs the provision: Bandai's pair bans turned out SYMMETRIC card+card ("Card A and Card B cannot be included in the same deck" — the OP07-115+EB04-058 pair has no leader side), so the shipped condition is `{type:"banned_with",cardIds:[external_keys]}` mirrored on both partners; the never-used `banned_with_leader` shape was dropped from types.ts. Maintenance: on a Bandai announcement, hand-edit the overlay (keep pairs mirrored — the parser fails one-sided edits), and rows land within a nightly. | (fired — row kept for the maintenance note) |
| OP cards in sitemaps — **FIRED in P4.4 (2026-09-04).** The row's trigger was "an OP browse/search surface exists to link them," and P4.4 built exactly that (/cards?game=optcg, /leaders, /l/ hubs) — so the widening landed the same session, one package earlier than the code comment's "OP beta package" guess (the row's trigger wins; the comment was updated to say so). cards/sitemap.ts + robots.ts widened to MTG+OP in lockstep (shared WHERE mirrored, chunk count now covers ~40k URLs), OP lastModified = seen_at exactly like MTG; new l/sitemap.xml lists the 142 hub URLs (thin-content call written in its header: distinct real content, tiny corpus, card pages already indexed with less framing — escape hatch is noindex, not URL removal). | Done. |
| OP printing release dates: punk-records v2.0 has NO dates anywhere, so `sets.released_at` and `card_printings.released_at` are NULL for optcg (default printing = the base-id variant instead of newest-first). A hand-maintained pack→date map is ~60 rows if something ever needs to sort on it. | An OP surface needs release-date ordering (set browser, "newest cards"). |
| OPTCG names carry HTML entities from ingest — **FIRED 2026-09-04 (fec9173)**: scope-checked live (33 card names + 2 pack titles, `&amp;` only; oracle_text/trigger_text/type_line/traits all clean, legalities.json ids untouched). `decodeHtmlEntities()` in punk-map.ts at the mapping boundary (name/effect/trigger/traits/attributes/pack titles, single pass, unknown entities verbatim), MAPPER_VERSION → 2, forced re-ingest updated exactly the 33 identities + 2 set names; live trgm search resolves the decoded names ("ace & sabo" → OP13-007). | Done. |
| Other OP languages (english-asia, japanese, …): punk-records carries seven language trees; we ingest `english/` only — same call as MTG's English-only rule. | A user asks; it's a card-corpus decision (non-English identities), not an ingest tweak. |
| Limitless TCG tournament data for OP (the Topdeck analogue; dev API at docs.limitlesstcg.com) — **the ingest half FIRED in P4.5 (2026-09-05), keyless.** Nightly `pnpm ingest:limitless` (after punk-records): client-side date windowing over the newest-first list (no date filter exists), /details + /standings per candidate, ~6.7s pacing under the observed keyless "50-in-5min", mapper `src/lib/games/optcg/limitless-map.ts` (leader = `decklist.leader` set-number → external_key id-first; dropped players have `placing: null` and are skipped, never index-synthesized; `decklists:false` events skipped before the standings request; `format` null/STANDARD → optcgStandard, labeled EXTRA/CUSTOM skipped counted; `isPublic:false` skipped). Same tables, source='limitless'; MIN 16 players / top-16 kept (sampled 143d: 2.8 events/day, median 16 players). /l/ hubs render the attributed "Top finishes" shelf (adapter `capabilities.tournaments` = Limitless + /tournament/{id} permalink, test-pinned). Auto-archetype `deck` objects observed: for OP, `deck.id` IS the leader card code (dupes leader_ids) and `{}` when no decklist — NOT stored, no archetype surface. **Key half: CLOSED 2026-09-20 — no key needed (see row end); nothing was ever gated.** Requested 2026-09-05; as of 2026-09-07 the account API page banners that access keys "are not required anymore and only given out in specific cases" and the request sits as Pending Approval — it may never move, which is fine: the ingest is keyless by design and proven unattended, and a key would only raise the observed ~50-per-5-min pacing ceiling and unlock the deck-rules endpoint (its own later conversation). Their docs publish no required credit wording (verified 2026-09-07) — our voluntary credit already exceeds it; when public, ask about their developer-docs Showcase listing (checklist item in P4.6-op-beta-launch.md). **Key half CLOSED 2026-09-20** — staff answered the 09-12 re-submitted application in their Discord: the API "was opened earlier this year" and "most use cases will not require a key." The application is moot (the row's withdrawn arm): keyless is the *sanctioned* path, not a pending grant. The optional `LIMITLESS_API_KEY` → X-Access-Key wiring stays in the script + workflow as a never-case rate-headroom valve. | Done (closed 2026-09-20 — no key needed; row kept for the ingest-design notes). A policy reversal would show up as the nightly's Limitless step going red — the X-Access-Key wiring is already in place. |
| OP leader×card aggregate — the P3.8 `commander_card_stats` analogue mined from Limitless standings decklists (the full lists are in the standings responses already; card codes resolve id-first via external_key, stronger than MTG's name path). NOT stored per-standing (lean-row rule); the roll-up must happen at ingest time from the live window like P3.8's, or grow a raw-archive step first — limitless.ts deliberately archives nothing today, unlike topdeck.ts. Prereq before designing thresholds: months of settled OP events at real volume (observed ~9 kept events/week vs MTG's far larger corpus) so shares aren't noise. | OP decklist volume proven at scale (a few months of nightly ingest — check `ingest_runs` source='limitless' stats), or Cut Coach / recommendations want an OP tournament signal. |
| Multi-step undo in the editor: R3 (2026-09-09) shipped ONE Undo per add — the toast's Undo restores the previous quantity (or swaps the previous leader back) through `applyEdit`, so it autosaves like any edit — and nothing for removals, steppers, imports or restores (History covers whole-list restores). A real undo stack means a reducer over every edit kind with its own keyboard binding (⌘Z fights the browser in text fields) and a clear story for edits that already saved. | The single Undo proves demand — a user asks to step back further, or to undo a removal / an import. |
| Card-stacks grid view (Moxfield-style stacked columns per group — cards overlapping so 100 fit on one screen): R3 kept the flat image grid (P1.3's) and added only the top-corner id badge (C13). Stacks need their own hover/keyboard model (the covered card must be reachable) and a badge placement that never covers the artist / © line. | Grid-view usage — the `deckwarden:deck-view` preference showing `grid` in real sessions, or a user asks for stacks. |
| **FIRED 2026-09-17 — P4.8 (`8508830`):** the id pass shipped PREFIX-wide, not exact-only — `searchIdPrefix` beside the classifier in `resolve-token.ts` (One Piece only; uppercased; `^[A-Z]+\d*-\d*$`, so "OP" without a hyphen stays a name search and the [A-Z0-9-] charset makes the LIKE escaping-free; Magic null), and the route swaps the translator's conditions for one bound `external_key LIKE '<prefix>%'` ordered by key (the `(game_id, external_key)` unique index range-scans it under C.UTF-8 — 0.06–0.07ms measured with the exact query shape; `sort`/`dir` ignored on this path, docblocked). "4 OP01-025" → the one Zoro row and Enter adds 4; "OP01-02" browses the ten OP01-02x in key order; a bare miss is honestly empty with NO trgm arm (the R5b rule); `format` legality rides along (pinned live on OP06-116); the name path is byte-identical (luffy diffed dev-vs-prod); `/cards?game=optcg` inherits the pass ("10 cards" for OP01-02) with its placeholder deliberately left name-y. `parseQuickAdd` widened for the sim-style "4xOP01-025" (the "4xenagos"→"enagos" edge disclosed); trailing-"(CODE)" stays a name search. Pins: resolve-token.test, editor-state.test, search-pane wire pin, optcg-smoke exact/case/prefix/miss/legality. Original note — Quick-add by One Piece card number: the search box's placeholder says "try “4 OP01-025”", but `/api/cards/search` matches names only (trgm over `name_norm`), so "OP01-025" returns "No cards match" — the id-first pass lives only in `/api/cards/resolve` (import, the hub CTA seed). Seen during R3's dev pass (2026-09-09); the placeholder predates R3 and the row exists because the R3 fence forbids API changes. The fix is one exact `external_key` pass in the search route for games whose `display.idBadge` exists, or a placeholder that stops promising it. | Fired by the owner's P4.7-round-2 call (question (e)); the editor box now keeps its own promise. |
| OP null-cost events: 23/410 event cards carry `cost_value NULL` because **punk-records upstream has `"cost": null`** (verified against the raw OP15-075 "El Thor" JSON, 2026-09-07 — the OP15 Enel-package events among them, so the flagship meta deck shows 20/51 cards in the DON curve's "–" bucket). The dashboard renders the bucket honestly (P4.3's design anticipated null costs) and characters/stages/leaders are 100% correct. The fix would be a ~23-row hand-maintained cost overlay merged at ingest — or nothing, since the nightly picks up any upstream backfill automatically (mapper promotes `cost` on every run). | punk-records fixes the field upstream (check the "–" bucket shrinking, launch-week watch item), or beta users call the curve misleading — then it's the overlay, kept as lean as the legalities one. |
| Claim-nudge visibility for anonymous editors: the walked funnel (P4.6) found no visible "sign in to keep this deck" affordance in the editor or share page for a guest owner — the claim MECHANISM works (server-side token → claimed on sign-in, P3.6-covered), but nothing advertises it beyond the home-page copy. A nudge is a conversion surface with nag-design questions, not a paper cut — deliberately not smuggled into P4.6. | Real anon decks accumulating with a low claim rate, or a beta user asks "how do I keep this?" — then it's one quiet line in the editor header, not a banner. |
| **FIRED 2026-09-10 — R5b (`15fa66f`):** /c/ hubs carry "Build with this commander" → `/decks/new?game=mtg&leader=<oracle id>` (hidden on banned / not-legal hubs), and `/api/cards/resolve` pass 0 takes Magic uuid-shaped tokens through `classifyResolveToken` (`src/lib/cards/resolve-token.ts`) — the same seam /l/ has used since P4.6; the chooser and the editor changed by zero lines. Original note — "Build with this commander" CTA on MTG /c/ hubs: /l/ hubs got the latched build CTA in P4.4/P4.6 (`?leader=` external key → draft-seeded leader zone, the P4.6 seam in deck-editor.tsx), but /c/ hubs have NO build CTA at all — an odd parity gap now that the seam exists and is game-agnostic (MTG would seed by oracle-id external key through the same resolve pass-0… which skips MTG today; it would need a `key:` variant or slug resolution). | Fired in R5b (the next MTG-surface package touched /c/). |
| Collection Mode beyond P3.7's slice (feature plan "Collection Mode"): **build only from my collection** as a builder MODE with a purchase budget ("owned cards plus a chosen purchase budget" — needs a mode switch that gates search, suggestions AND validation, not just the Suggestions filter shipped in P3.7); a **missing-card shopping list** export (the "You own N/100" line already knows the missing set — the export is text/CSV plus buy links, which belongs with the Card Kingdom affiliate row); **printing preference** (auto-pick the OWNED printing for a deck entry, alt-art aware); **quantity-aware ownership** (P3.7 is per identity: one Forest in the collection makes all 10 in the deck "owned" — the line's tooltip says so); **cards assigned to other physical decks**; and "prefer cards already in the user's collection" as a RANKING signal rather than a hard filter (deliberately not done: the evidence payload would need an honest "you own it" source, and owning a card is not a reason it fits). | Real imported collections in prod (the owner has none yet) and a user asking for any one of these; budget mode first if the paper-player beta cohort materializes. |
| More collection formats (Archidekt, Deckbox, Dragon Shield, TCGplayer app, Delver Lens CSVs) — P3.7's parser recognizes exactly ManaBox and Moxfield by header and refuses everything else with the header it saw, rather than guessing a mapping. Each format is one header map + a finish/quantity vocabulary in `src/lib/collection/parse.ts`, test-pinned with a REAL header row captured live (never from memory). | Someone pastes a refused header into an issue. |
| Owned badges outside the deck views: the search pane ("owned" mark on results), card pages ("in your collection"), hub staples tables, and an "owned only" search filter. P3.7 kept "owned" to the deck list, the share page and the Suggestions toggle; the search pane would need a per-result lookup on every keystroke (a POST /api/collection/owned per result page, or the owned set shipped to the client once). | The Suggestions toggle sees real use, or a user asks "does search know what I own?". |
| Owned set as a SQL subquery instead of a materialized IN list — `?owned=1` materializes the user's owned identity set once per request and passes it through the P3.1 `ownedCardIds` contract (an IN list; ~10k params for a 20k-printing collection, under Postgres's 65,535 limit). Fine at today's scale; an `EXISTS (select 1 from collections join card_printings …)` condition would be the zero-transfer form. | A collection at the cap makes the recommendations route measurably slow (Sentry / the 30/min limiter never being the bottleneck). |
| Language-specific printings in collections — ManaBox writes ONE Scryfall id per printing regardless of the Language column (verified against real exports), and we store English printings only, so a Japanese Sol Ring imports as the English printing of the same card. Correct for "do I own this card"; wrong the day someone wants their collection's languages back. | A user asks; it would mean storing non-English printings (a card-corpus decision, not a collections one). |
| **FIRED 2026-09-09 — R1b (`d0b25de`):** `/c/[slug]`, `/l/[slug]`, `/cards/[id]` and their three `opengraph-image` routes export `generateStaticParams() → []` beside `revalidate` (commented at every site so nobody deletes an "unused" export). Proof: the build lists all six under `dynamicRoutes` with ● glyphs while `/legal`, `/privacy`, `/decks/new`, robots and every sitemap kept theirs; a local `next start` answers MISS then HIT with `s-maxage=3600, stale-while-revalidate`; on prod an untouched hub, card page and OG image go `x-vercel-cache: MISS → HIT → HIT` with `x-nextjs-prerender: 1` and a climbing `age` (Vercel rewrites the browser-facing Cache-Control of every prerendered route, static pages included, to `public, max-age=0, must-revalidate` — the edge holds the hour). Force-dynamic pages still say `private, no-cache, no-store`. Hub and card pages now lag up to an hour behind a deploy or an ingest: assert existence on prod, freshness on dev. Original note — ISR that never was: `/c/[slug]`, `/l/[slug]`, `/cards/[id]` (and their `opengraph-image` routes) export `revalidate` but have no `generateStaticParams`, so Next renders them dynamically on every hit — prod serves `cache-control: private, no-cache, no-store` with `x-vercel-cache: MISS`, and `.next/prerender-manifest.json` lists none of them under `dynamicRoutes`. Present since P2.6: verified at `d4c21e5` (pre-R1a) and `b0e1fcb` on 2026-09-08 with scratch production builds, so not an R1a regression. Fix verified the same way: `export function generateStaticParams() { return []; }` puts the route in the manifest, flips the build glyph to ●, and serves `s-maxage=3600, stale-while-revalidate` with `x-nextjs-cache: HIT` on the second request (Next docs, generate-static-params.md: "You must return an empty array from generateStaticParams … in order to revalidate (ISR) paths at runtime"). | Fired in R1b with its yardstick (`R1b-session-prompt.md`, verify-first 1). |
| The `md` tools drawer (768–1199 px) is 384 px wide and docks over the deck pane, which is 416 px at 768 — all but 32 px of the deck is covered while it is open (R4, 2026-09-12; at 1024 the deck keeps 288 px). A narrower `--drawer-content-width` under 900 px, or a peek snap point, would fix it. | A tablet builder reports losing the deck behind the drawer, or the phone tabs prove preferable below ~900 px. |
| **FIRED 2026-09-16 — P2.8b (`1a43ee4`):** all three breaks fixed against the owner's verbatim paste (now `MOXFIELD_TLA_PASTE` in `mtg/test-fixtures.ts`). **(a)** landed as `importLeaderGuess`, an optional adapter hook (`types.ts`, beside `importZoneFor`): MTG's `mtg/import-guess.ts` guesses the first k lines (k=1 then 2) only when no line hints the leader zone, lines 0..k-1 resolve commander-eligible (the exported `isEligibleCommander` — one rule, no duplicated exception set), line k-1 breaks the order, and the unhinted remainder is non-decreasing under `Intl.Collator("en")` (code-unit compare fails the Sokka canary; pinned by the real fixture). Known miss, disclosed: a commander that sorts first (Aang before Abandoned) is not guessed. The review step shows "Commander: <name> — from the first line of the list" before apply; `guessed` items in ADD mode yield to an occupied commander zone (same card = idempotent silent skip, different card = quiet main-deck landing; empty zone or replace mode applies). Dev-verified: the paste imports "80 of 80", Toph commands, 100/100, zero warnings. **(b)** spills collapse per zone: the typed-`Commander` workaround now emits ONE warning — "Commander is full — moved 78 cards to Main deck (Abandoned Air Temple, Airbending Lesson, Appa, Steadfast Guardian, …)" (Toph+Aang fill the max-2 zone); a single move keeps the old wording byte-for-byte. **(c)** the tokenizer widens ` / ` → ` // ` after the set hint (adjacent slashes can't match), so Kyoshi resolves via the exact pass; `normalize.ts` untouched. Bonus, live-verified 2026-09-16 on archidekt.com (deck 7031486: `1x Xyris, the Writhing Storm (dmc) 175 [Commander{top}]`): a `[Commander…]` category is now a commander hint, and the Archidekt fixture pins `commanders: ["Atraxa…"]`. Original note — Moxfield import fidelity — the P2.8 phone dry run (2026-09-14, owner, signed out, a real Toph deck) found three breaks in paste import. **(a) The commander never lands in the command zone.** Moxfield's "Plain text" export and its "Copy for Moxfield" button emit the SAME shape: the commander as the FIRST line, unmarked (no `Commander` header, no `*CMDR*`, no blank line), then the other 99 sorted alphabetically — so `parseMtgDecklist` gives it no zone hint and it imports as a main-deck creature. `decklist.test.ts`'s "Moxfield text export" fixture has exactly this shape and pins `commanders: []`, so the gap is enshrined, not accidental. The tokenizer can't know legality, so the fix is resolution-time: when no line carries a commander hint and the commander zone is empty, a first line (first two, for partners) that breaks the alphabetical order of the rest AND resolves to a commander-eligible card routes to the command zone — a positional sibling of P4.6's `importZoneFor` hook. **(b) The workaround floods warnings.** Typing `Commander` above the paste latches every following line to the commander zone (no `Deck` header follows), so `applyImport` spills 79 cards back to Main with one "Commander is full — moved X to Main deck" warning each; the deck comes out right, the dialog is a wall. Collapse the spills into one summary line (or end a header-less Commander section once the zone is full). **(c) Single-slash double-faced names miss.** Moxfield writes "The Legend of Kyoshi / Avatar Kyoshi"; `normalizeCardName` folds only `//`, so the line is "Not found" (the same card resolved when typed with `//`). Fix it in the tokenizer (` / ` → ` // ` on the name), not in `normalize.ts` — changing the shared normalizer means a re-ingest. Add both real pastes as fixtures. | Fired in P2.8b — the announcement's gate is code-cleared; the owner's phone re-run (both Moxfield buttons) is the human half still pending. |
| **FIRED 2026-09-16 — P2.8b (`1a43ee4`):** `handleAdd` switches a phone to the Deck pane on BOTH successful leader-zone paths (the OP max-1 replace and the plain add) — zone-based, so One Piece's Leader button behaves identically; plain Add still stays on Search (dev-verified: input keeps focus for the next add). The search pane's post-add refocus no-ops once the pane hides (focus falls to body — R4's hidden/inert holds, no iOS keyboard risk). Disclosed trade-off: a partner deck's SECOND commander needs "Add cards" again. md/wide untouched (pins beside the openCuts tier tests). Original note — Add commander on a phone should return to the deck list. The search row's leader-noun button (Commander; Leader for One Piece) runs the same `handleAdd` as Add, which never changes `activePane`, so a phone user stays on Search after choosing a commander (P2.8 dry run, owner, 2026-09-14). Owner's spec: after a SUCCESSFUL leader-zone add at `tier === "phone"`, switch to the deck pane; plain Add keeps staying on Search for repeated adds; md and wide are unchanged (the deck is already on screen). | Fired with the Moxfield row above (same package). |
| On-site tournament decklists (the "view deck lists" half of Wave-2 idea 8 — **flagged: part of an explicit owner request**, deferred in `WAVE2.md` §F): card lists are deliberately not stored (Neon budget). Recommended design: write each top-16 list as a small JSON sidecar to the **public** R2 bucket at ingest (backfill from the raw Topdeck archive, row 36) and render with the read-only deck view + "Open as draft" — zero Neon bytes. Topdeck's terms for rendering full lists on-site are unverified. | W10 is live and someone asks for lists, **and** the terms allow it; ideally after `img.deckwarden.gg` (row 51) replaces r2.dev. |
| One Piece starter decks in Precons (**flagged**, `WAVE2.md` §F): no source with quantities exists — punk-records has set membership only, so W8's MTGJSON path has no OP analogue. | You hand-enter ~30 lists into `data/optcg/starter-decks.json`, or a source with quantities appears. |
| One Piece autofill (**flagged**, `WAVE2.md` §F): no popularity signal, no leader×card aggregate; faking either breaks the cold-start rule. W9's doors are adapter-gated, so OP simply renders none. | Row 59 (OP leader×card aggregate from Limitless lists) fires. |
| Affiliate buy links (TCGplayer via Impact; Card Kingdom by direct contact — row 22): Vercel Hobby is non-commercial, so W7 ships **plain** links and `NEXT_PUBLIC_TCGPLAYER_PARTNER_BASE` stays empty. Known cost for later: an Impact `?u=` wrapper double-encodes the Mass Entry list, so long decks will hit W7's copy-list path more often. Row 66's missing-card shopping list partially ships as W7's "Only cards I'm missing" menu item. | The VPS/Pro move (row 80); then one env var + `rel="sponsored"` + disclosure lines. |
| Exact-printing buy links (`tcgplayer_id` on `card_printings`): a migration + mapper change + full re-map for a marginal gain over TCGplayer's name search. | W6 usage shows people actually choose printings, or buy-link conversion starts to matter. |
| "Public profile ↗" in the account menu: the client session carries no `username`; needs Better Auth `additionalFields` + a session refetch after the username form (cookie cache ≤ 300 s stale). | After W3 ships, if the owner wants it — S. |
| In-editor commander picker dialog (no navigation): the owner asked for the browse page; W4 delivers it with a pick-intent way back. | Players bounce on the round trip, especially on phones. |
| Indexable tournament event pages; pretty `/precons/<slug>` URLs: event pages are thin pages carrying player names (W10 ships them `noindex`); `/d/p_<slug>` is already readable. | Search Console shows demand. |
| Autofill variants: "chaos mode", total-deck budget, client-side reroll, theme hints (tribal/keyword matching): the owner chose the evidence-based shell (2026-09-19); per-card budget tiers match Suggestions; theme hints are inference from card text, adjacent to the no-role-inference rule. Client-side reroll has a designed escape hatch — `buildShell` is pure, so the ranked window could be returned once and rerolled locally. | Someone asks twice / shell-quality feedback / Neon compute from rerolls shows up. |
| Bulk multi-select on `/account`: one-at-a-time (W3's ⋯ menu) covers today's deck counts (cap 100). | A user manages dozens of decks. |
| Hand-written precon blurbs: generated factual summaries ship first (W8); WotC marketing copy is never copied. | The owner wants editorial voice on the newest products. |
| Maskable manifest icons (`purpose: "maskable"`): W2 ships 192/512 with `purpose: "any"` only — declaring maskable promises an 80 % safe zone nobody verified on a device (the shield sits at 65.6 % of the canvas, likely fine, unproven). Same qlmanage pipeline, one padding tweak + a device/Lighthouse pass. | An Android install shows a cropped shield, or Lighthouse PWA audit is run in anger. |
| ⋯ deck actions on the home "Your decks" rail: W3 scoped the quick-actions island to `/account` (the contract's call, recorded 2026-09-19) — home-rail tiles gain nothing until `AccountDeckTile` wraps them too, and the rail mixes guest decks (no PATCH rights without the claim token wired through). | The owner reaches for a home-rail tile's actions and finds none, or a beta user asks. |
| Printings-gallery long-tail edges (W5, 2026-09-20): a `?printing=` deep link to a printing beyond the API's 250-row cap silently falls back to the default (only basic lands have >250; the cap IS the pagination), and "Filter sets…" searches loaded rows only — a set outside the inline 100 needs "Show all" first (the empty state says "No **loaded** sets match"). | A player shares a beyond-the-cap deep link and reports it, or filter confusion shows up in feedback. |

## Deferred infrastructure

| Item | Trigger |
| --- | --- |
| Vercel Hobby → Pro or Hetzner+Coolify | First affiliate dollar / any commercial use, or first overage email. Runbook lives in the repo. |
| Neon free → paid or Postgres-on-VPS | `pg_database_size()` alert at ~350MB, or compute-hour cap pain. |
| Redis / queues / background workers | A measured problem that cron + Postgres can't absorb. Not speculatively. |
| Dev counter reset script — **FIRED in P3.10 (2026-09-17, `52c2c47`)**: `scripts/reset-dev-counters.ts` + `pnpm counters:reset`, built as the session's opening chore per spec — literal `deck-create:%:::1` pattern only, refuses any argument by construction (tested), prints each deleted key + window + count. Proven in anger the same session: the battery's own creates fill 10/h BEFORE `smoke:versions` (12 by then — and a diagnostic re-run into the 429 itself increments, exactly as the ledger note warns), so the script also fires MID-battery: reset → continue, zero waiting. The limiter itself untouched; prod limits still cold-start-gated. | Done. |
