# R2 session prompt — Deck artwork (the art resolver, the art endpoint, the capability flag, the color-identity fallback, the background-art preference, the ambient layer behind the builder and the share page)

Pull latest, then run R2 — the fourth R-series package. **`REDESIGN.md` is the contract (read §1 Mechanics — motion, game accent, tokens; §2 "Site shell" for the appearance menu's "Background art after R2" line and the "Desktop builder" diagram's "Faint leader artwork (or color gradient) behind the workspace" row; §3 in full — Behavior, Sources / fallbacks / the One Piece decision, Interface additions; §4 G7 and the "animated ambient backgrounds" deferral; the R2 row in §5; the ledgers plus the R1a / R1b / R3 decisions in §6 — the R3 note that "R2 lands on this header and summary surface" and the unwired-primitives list are yours to retire; and §7 Artwork checks first)**; build plan §6b makes R-packages legal sessions under the CLAUDE.md protocol. R2 is the signature feature: `fetchScryfallArtMeta(printingId) → { artCropUrl, artist } | null` refactored out of the OG-only `fetchOgArt`, the shared nullable `CardArt` descriptor, `GET /api/cards/[id]/art?printingId=`, `capabilities.ambientArt` (Magic declares `art_crop`, One Piece declares nothing), `display.colorSwatches?(mask)`, the "Background art: On / Off" preference under `deckwarden:appearance` in the appearance menu, and the ambient layer — art_crop at about 8 % opacity in dark and 4 % in light behind a gradient veil, an attribution chip whenever cropped art is on screen, a 250 ms crossfade after the replacement image has loaded, a request-id guard so a stale request never wins, the color-identity gradient for every fallback — behind the editor's three-pane workspace and the share page's `<main>`. Expect ZERO migrations, ZERO new dependencies, exactly ONE new URL (the GET art endpoint — nothing else moves in the route table), no change to any existing wire shape (`/api/decks`, `/api/decks/[id]`, `/api/decks/[id]/cards`, `/api/cards/search`, `/api/cards/resolve` untouched), no ingest or data work, no `artist` column (LATER row 49 stands: artist comes from the Scryfall API at request time, data-cached daily per printing), no stored art URLs, no image optimizer, and no visible change to the (site) pages beyond the share page's ambient layer, its chip, and the appearance menu's new item. The owner's decisions are settled (REDESIGN.md §0 and §6): One Piece ambient art is OFF until Bandai answers (the flag is undeclared; flipping it later is one adapter line you do NOT write), the fun ceiling is restrained, the keyboard script and the draft contract are untouched, one appearance control per page, and theme / appearance changes never mark dirty and never create a deck. Do not relitigate them.

Pre-flight, in order. (1) Nightlies since 2026-09-09 all green (`gh run list --workflow=nightly-ingest.yml`; cron 10:37 UTC drifting ~13:30–17:40Z; 2026-09-08's ran 14:45Z green; 2026-09-09's is the FIRST nightly on R1a + R1b + R3 code and had not fired at 12:03Z on 09-09 when this prompt was written). A red run is P4.7 branch F and takes precedence — fix or report it as P4.7, and R2 waits for its own session (one package per session). (2) Working tree clean at or after `3617f3a` (the R3 docs commit) plus this prompt's commit. (3) db:size baseline stated (254.3 MB on 2026-09-09 after R3's dev smokes; Neon alert 350 MB) — R2 adds no rows except the one anonymous draft deck the dev-side leader-art check mints, which you delete before the session ends (state its id and its deletion). (4) QA fixtures unchanged: the OP deck "Six DON!!, Endless Thunder" at `/d/jhr5ax43ewx7` and ALL FOUR public MTG decks (`/d/q9j9uxphwp9n` Queza — a legal 100, `/d/uwvrnv2pv4t6` Atraxa Superfriends, `/d/4c5_9yd9ewdy` Boros Equipment Burn, `/d/end97r37znuv` Nelson & Murdock) belong to the owner's account (read from the DB 2026-09-09); the two "Untitled" decks on the owner's account from 05:42Z / 05:45Z on 09-09 are the owner's, never yours to touch. The owner's open looks — the `/account` Danger zone, and the signed-in editor on `/d/q9j9uxphwp9n` with its Share primary and History in the More menu (R3) — leave no trace; state whether the owner did them. The browser pane is signed out on prod and OAuth cannot be typed, so R2's editor pass is a GUEST DRAFT on DEV with real edits (`/decks/new?game=mtg`, and `/decks/new?game=optcg&leader=OP15-058` for the fallback) and, on prod, draft mode with NO edits plus the public share pages (deck-create budget 10/h + 30/day per IP — read the counters before any prod run; R2's prod pass needs no create at all). (5) `pnpm check` green at the start (581 tests at `3617f3a`, the same 6 pre-existing `no-unused-vars` warnings, 0 errors) and `pnpm build`'s route table: R2 ADDS exactly `ƒ /api/cards/[id]/art` and moves nothing else — save the table before and after and diff them (the R3 prompt's method: grep the glyph lines, strip sizes). No `shadcn add` is expected (`DropdownMenuCheckboxItem` exists in `src/components/ui/dropdown-menu.tsx`); never `shadcn init`.

What R2 is NOT (scope fence)

NOT the hub banners: `/c/[slug]` and `/l/[slug]` keep their R1b heroes untouched (G3 with the art-crop banner and credit chip is R5b's — it will CONSUME your resolver); `src/lib/hub/queries.ts`'s `loadDefaultPrinting` may be reused, never changed. NOT the share page's artwork HEADER (title, leaders, author, format, legality, share actions — R5b): R2's share-page change is the ambient layer, its chip, and `ShareDeckMeta` growing the two fields the wire already carries. NOT the homepage, tiles, indexes, `/account`, `/u`, `/f` (R5a / R5b). NOT an `artist` column, a stored art URL, or any ingest change (LATER row 49 and the lean-row rule). NOT `next/image` optimization anywhere (hotlink the Scryfall CDN, `<img>` or `unoptimized`). NOT One Piece art: the adapter declares no `ambientArt`, `/api/cards/[id]/art` answers `{ art: null }` for OP cards without ever calling Scryfall, and OP gets the gradient — do not add a flag, an env toggle, or a "soon" comment that reads as a plan. NOT the OG images beyond the refactor's wrapper: the three `opengraph-image.tsx` routes (`d/[publicId]`, `c/[slug]`, `cards/[id]`) keep calling `fetchOgArt` and render exactly as before (`seo-smoke` pins that each og:image answers 200 `image/png` above 10 000 bytes — run it against dev only, it mints fixture decks). NOT the responsive builder (R4), NOT skeletons / stagger / the analytics grow-in (R6), NOT the card page hero (it stays the full card with `priority`). NOT a change to the keyboard script, autosave, the draft contract, `RESULT_LIMIT`, the toast, the tabs, or any R3 surface except adding the layer beneath them. NOT a rate-limit policy change for existing routes (the new GET gets one only if you decide it needs one — a design decision below). NOT a Next, React, Tailwind, or Base UI upgrade. NOT any P4.7 branch unless pre-flight (1) forces it, and then R2 is not this session. NOT ⌘K, NOT animated ambient backgrounds, NOT custom artwork uploads, NOT a per-face art choice for double-faced cards (front face, §3) — LATER rows only if a user asks.

Facts you inherit (verified 2026-09-09 against `3617f3a` — trust these, spot-check nothing)

* The OG-only mechanism. `src/lib/og/scryfall.ts` (60 lines): `fetchOgArt(printingId): Promise<OgArt | null>` → `GET https://api.scryfall.com/cards/{printingId}` with `User-Agent: Deckwarden/1.0 (https://deckwarden.gg)` + `Accept: application/json` and `next: { revalidate: 86400 }`; reads `image_uris.art_crop ?? card_faces[0].image_uris.art_crop` and `artist ?? card_faces[0].artist` (the front face for double-faced cards, already); a missing art OR artist returns null (the attribution rule — never unattributed art); then fetches the art bytes (`Accept: image/*`, same revalidate) into `{ dataUri: "data:image/jpeg;base64,…", artist }`; any throw → null. `OgArt` is consumed by `OgFrame({ art })` in `src/lib/og/elements.tsx` ~26–90 (art at 560×630 with a left-to-right gradient blend into the text column) and `OgAttribution` ~93 renders `Art: ${artist} · ™ & © Wizards of the Coast` INSIDE the image. Callers: `src/app/(site)/d/[publicId]/opengraph-image.tsx` (`force-dynamic`; `deckOgLabels(deck.gameId).fetchArt && deck.commanderId ? loadDefaultPrintingId(commanderId) : null` → `fetchOgArt`; OP unfurls are artless by P4.6 decision 2), `c/[slug]/opengraph-image.tsx`, `cards/[id]/opengraph-image.tsx`. `src/lib/og/data.ts` ~125: `loadDefaultPrintingId(cardIdentityId)` (`is_default = true`). The OG tests are `curve.test.ts` and `labels.test.ts` only; nothing mocks `fetch` for the OG path today.
* Printings and images. `card_printings` (`src/db/schema.ts` ~170): `id` (the Scryfall id for MTG; a v5 uuid for OP), `card_identity_id`, `image_override` jsonb `{ front, back }` (rare MTG mismatches stored at `normal`; EVERY OP printing points at the R2 mirror `pub-….r2.dev`), `is_default`, `is_removed`, `released_at`. `src/lib/cards/images.ts`: `scryfallImageUrl`, `printingImageUrl` (override-aware), `isEmbeddableImageUrl` (Bandai hosts refused — CORP same-site), `embeddablePrintingImageUrl`, `toSmallImage`, `thumbnailUrl` (R3). `src/lib/hub/queries.ts` ~48: `loadDefaultPrinting(cardIdentityId) → { id, imageOverride } | null` (drizzle, importable from a route handler). Art for R2 comes from the Scryfall API by PRINTING id, not from a derived CDN URL, so `image_override` never applies to art and the blocked-host gate is moot for Magic — say so in the resolver's docblock.
* The deck wire. `fetchDeckCardsWire(deck)` (`src/lib/decks/deck-cards-wire.ts`, 114 lines) → per entry `{ cardId, zone, qty, tags, printingId: string | null, card: CardWire }` — `printingId` is the CHOSEN alt-art printing (null = default; the editor has no printing picker, so chosen printings arrive only through `PUT /api/decks/[id]/cards` bodies, e.g. a version restore or an API client); `card.image` derives from the chosen or default printing. The editor hydrates from `GET /api/decks/[id]` (`src/app/api/decks/[id]/route.ts` ~42, the same wire) and its `EditorEntry` carries `printingId?` (`deck-editor.tsx` ~319); `EditorCard` carries no printing id. Search results (`/api/cards/search`) carry `image` from the default printing — the printing id is embedded in the Scryfall URL for MTG, but the endpoint resolves the default server-side, so nothing parses URLs.
* Leaders and masks. `splitLeaderEntries(entries, format)` in `src/lib/decks/view-model.ts` (it takes the FORMAT — REDESIGN §3 abbreviates the call) → `leader` in persisted entries order; the first is the art leader. `leaderDenorm(entries, format, ciMaskByCard)` in `src/lib/decks/cards.ts` writes `decks.leader_ids` (entries order) and `decks.ci_mask` (OR of the leaders' `ci_mask`) on every save; `deckMetaJson` (`src/lib/decks/serialize.ts`) puts `leaderIds` and `ciMask` on every deck-meta wire including the share page's `deck` prop, but `ShareDeckMeta` (`src/components/deck/deck-share-view.tsx` ~50) omits both in its type — add them. Every card carries `colorsMask` and `ciMask` (`CardData`, `types.ts` ~70); for OP `ci_mask == colors_mask` by ingest contract (`punk-map.ts` ~32). `src/lib/games/colors.ts`: `COLOR_BIT` (W1 U2 B4 R8 G16 C32), `COLOR_ORDER`, `maskToLetters`, `ciPipsHtml`; `--mana-w/u/b/r/g/c` live in `globals.css` ~187, identical in both themes. `src/lib/games/optcg/colors.ts`: `OPTCG_COLORS` (Red #d32f2f, Green #2e7d32, Blue #1565c0, Purple #6a1b9a, Black #37474f, Yellow #f9a825 in Bandai display order; bits Yellow 1 · Blue 2 · Black 4 · Red 8 · Green 16 · Purple 32) and `maskToOptcgColorNames`. Enel OP15-058 (the hub-CTA seed) is a Yellow leader.
* Adapters. `GameAdapter.capabilities` (`src/lib/games/types.ts` ~440): `combos?`, `tournaments?` — MTG declares both (`mtg/adapter.ts` 161–181), OP declares `tournaments` only (`optcg/adapter.ts` ~200). `display` (~397–425) is `costHtml · subtitle · bodyText · statLine? · rowStats? (R3) · defaultGroupBy · leaderNoun · idBadge? · searchPlaceholder · importPlaceholder`. `getAdapter(gameId)` / `listAdapters()` in `registry.ts`; `GAME_ID` (mtg 1, optcg 2) and `gameCodeById` in `src/db/seed-data.ts`. Per-game display tests: `src/lib/games/optcg/display.test.ts` (R3); MTG has no adapter/display test file yet (`analyze / decklist / recommend / scryfall-map / spellbook-map / topdeck-map / validate` only).
* Surfaces. Editor root `src/components/editor/deck-editor.tsx` ~772: `<div className="flex min-h-dvh flex-col lg:h-dvh" data-game={load.adapter.id}>` → `<Toaster timeout={TOAST_MS} viewportProps={{ "data-game": … }} />` → `<EditorHeader …/>` → the dialogs inside `<ModalFinalFocus.Provider>` → the grid ~845 `min-h-0 flex-1 gap-0 lg:grid lg:grid-cols-[minmax(20rem,26rem)_minmax(0,1fr)_minmax(16rem,22rem)]` of three `<section>`s (`aria-label` "Card search" / "Deck list" / "Card detail and suggestions"), each `lg:overflow-y-auto`, none with a background of its own (the body paints `bg-background`). Opaque strips on top of that: the search pane's sticky block (`search-pane.tsx` ~150, `bg-background sticky top-0 z-10`), the deck pane's sticky group headers (`STICKY_HEADER_CLASS` in `deck-text-view.tsx`, `bg-background sticky top-0 z-10`), the header's `border-b`. State: `entries`, `entriesRef`, `cards` (a `ReadonlyMap<string, EditorCard>`), `preview` (the detail pane — MUST never drive art), `load.format`, `liveDeckId`, `autosave.status`; `applyEdit` → `markDirty` is the ONLY dirty path. Share page: `deck-share-view.tsx` ~183 `<main className="max-w-browse mx-auto w-full flex-1 px-4 py-6" data-game={adapter.id}>`, a client component fed by the server page `src/app/(site)/d/[publicId]/page.tsx` (`force-dynamic`, `fetchDeckCardsWire`, then `<DeckShareView deck cards author viewer forkedFrom ownership owned />`; private decks render `PrivateShareGate` — no art may leak through it) plus `OptcgPostureLine` under OP decks. `/decks/[id]/edit/page.tsx` is `force-dynamic`; `/decks/new` is static with `useSearchParams` behind Suspense.
* Appearance. `src/components/theme/appearance-menu.tsx` (54 lines): a Base UI `DropdownMenu` (`Button ghost sm` trigger, `SunMoonIcon` + "Appearance" with `max-sm:sr-only`) → `DropdownMenuContent align="end" side={side} aria-label="Appearance"` → `DropdownMenuRadioGroup` Dark / Light / System with `closeOnClick` (R1a decision: a theme is a one-shot choice); rendered by `SiteHeader` and `EditorHeader`; `site-header.test.tsx:50` and `editor-header.test.tsx:53` pin exactly one "Appearance" button. `ThemeProvider` (`theme-provider.tsx`: next-themes, `attribute="class"`, `defaultTheme="dark"`, `enableSystem`, `enableColorScheme`, `disableTransitionOnChange`) sits in the root layout outside the editor tree; `useTheme()` exposes `resolvedTheme`. localStorage precedents: `src/lib/decks/token-store.ts` (`deckwarden:deck-token:` prefix, try/catch around every access, SSR-guarded) and `src/lib/decks/view-prefs.ts` (`deckwarden:deck-view`, `parseViewPrefs` validating field by field, `loadViewPrefs` / `saveViewPrefs`); `useSyncExternalStore` precedent for a client-only read: `deck-share-view.tsx` ~105 (`noopSubscribe`, server snapshot null, the claim token).
* Routes. GET conventions: `src/app/api/cards/options/route.ts` — `export const dynamic = "force-dynamic"`, zod `QUERY.safeParse(Object.fromEntries(request.nextUrl.searchParams))` → 400 `{ error, issues }`, 404 `{ error }`, `NextResponse.json(body, { headers: { "Cache-Control": "public, s-maxage=86400, stale-while-revalidate=86400" } })`. Dynamic segments type as `RouteContext<"/api/decks/[id]">` (`api/decks/[id]/route.ts:42`) — `next typegen` (inside `pnpm typecheck`) generates the type for the new path once the file exists. Rate limiting (`src/lib/rate-limit.ts`, Postgres-backed, fail-open): `RATE_LIMITS` covers writes only (deck create, cards PUT, meta write, card resolve); no GET is limited; `clientIp(request.headers)` lives in `src/lib/decks/access.ts`. Route handlers have no test files; lib modules do.
* Motion and tokens. `motion-safe:` on every decorative transition; 250 ms crossfades; the reduced-motion backstop in `globals.css` zeroes durations; `--accent-game` from `[data-game]`; `.dark` on `<html>` is the theme switch, so `opacity-[0.04] dark:opacity-[0.08]` needs no JavaScript. `src/lib/theme/tokens.ts` exports `light`, `dark`, `og`, `ogAccent`, `rgba` — the OG frame's blend uses `rgba(BG, …)`.
* Tests: 581 green at `3617f3a`. Precedents: `src/components/editor/search-pane.test.tsx` (`vi.stubGlobal("fetch", mock)` + fake timers + `advanceTimersByTimeAsync`), `use-count-up.test.tsx` (fake timers), `validation-panel.test.tsx` and `use-count-up.ts` (the "previous render" state pattern), `modal.test.tsx` (Base UI Dialog in jsdom), `site-header.test.tsx` and `editor-header.test.tsx` (menus open on the pointerDown → mouseDown → click sequence in jsdom); pure: `images.test.ts`, `colors.test.ts`, `optcg/colors.test.ts`, `view-model.test.ts`, `cards.test.ts` (`leaderDenorm`), `og/labels.test.ts`. Lint: `react-hooks/refs` and `react-hooks/set-state-in-effect` are ERRORS in this tree — no ref reads or writes during render, no synchronous setState in an effect body (fetch in the effect, setState in its callbacks, or a reducer).
* Copy pins. No smoke reads the editor's DOM. Share-page pins R2 must not break: `smoke:collection` ("You own 1/3" for the collection holder, no "You own" for guests), the OP posture line and `©BANDAI` on OP surfaces (R5b's ledger), `seo-smoke`'s og:image checks above (dev only). Every `Modal` label is free EXCEPT the Danger-zone strings on `/account`.

Verify-first list (never from memory)

1. The resolver, unit-tested with a mocked `fetch`: a single-faced card → `{ artCropUrl, artist }`; a double-faced card → the FRONT face's art and artist; a card with art but no artist → null; a non-OK response → null; a thrown fetch → null; the call carries the `User-Agent` and `Accept` headers and `next: { revalidate: 86400 }`. The OG wrapper `fetchOgArt` still returns `{ dataUri, artist }` on top of it (pin the data-URI prefix), and the three OG routes are unchanged: on dev, `curl -sI` `/d/q9j9uxphwp9n/opengraph-image`, `/c/<a slug>/opengraph-image`, `/cards/<queza's id>/opengraph-image` → 200 `image/png`, and the deck image's byte size before and after the refactor within a few percent (record both numbers; the pixels are the same art).
2. The endpoint: a malformed `id` or `printingId` → 400; an unknown or removed card → 404; a `printingId` that belongs to ANOTHER card → the status you chose (404 recommended — it does not exist for this card); no `printingId` → the default printing; an OP card → 200 `{ art: null }` with no Scryfall call (extract the decision into a pure helper and unit-test it; on dev, the OP call answers in milliseconds and the dev server log shows no api.scryfall.com fetch); a Magic leader → `{ art: { url, layout: "art_crop", artist, credit } }`; `Cache-Control: public, s-maxage=86400, stale-while-revalidate=86400` on every 200 (decide and state what the 404s carry).
3. The editor's ambient layer on DEV in a guest draft (`/decks/new?game=mtg`): before any leader → neutral (no `<img>`, no chip, no gradient); Ctrl+Enter a commander → exactly ONE `/api/cards/<id>/art` request (`read_network_requests`), the art fades in only after the image loaded (`img.complete && naturalWidth > 0` before the opacity class flips), at computed opacity 0.08 in dark and 0.04 in light; the chip reads `Art: {artist} · ™ & © Wizards of the Coast` and is inside the viewport whenever the art is; hovering / arrowing through search results and clicking deck rows changes NOTHING (request count unchanged, `preview` is not an input); adding, stepping and removing main-deck cards changes nothing; swapping the commander (remove + add, Undo) → one new request each and the art follows the FINAL leader when two swaps race (the guard is unit-tested with two responses resolving out of order); removing the last commander → neutral again; two partners → the FIRST leader entry's art, and the share page agrees (`leaderDenorm` order = `splitLeaderEntries` order — unit-test that equality); a chosen alt printing → `printingId=` on the request (prove it by PUTting the dev deck's commander entry with a real alternate printing id of the same card through `/api/decks/<id>/cards` with the claim token via curl, then reloading the editor).
4. Autosave independence: no art event calls `markDirty` — the save slot reads "Saved" across a preference toggle, a theme switch, a load failure and a leader-art fetch; no PUT / PATCH from art; the first real edit still creates exactly one deck (count the POST); in a draft, theme and Background-art toggles leave the URL on `/decks/new` with zero `/api/decks` requests.
5. The preference: the appearance menu gains "Background art" (checked by default); Off removes the art and the chip together at once (and the gradient? — a design decision below), On brings the art back WITHOUT a refetch (count requests; the browser cache or your state holds it); the choice persists across reload under `deckwarden:appearance` (validated JSON like `view-prefs`); a corrupt value falls back to On; exactly one "Appearance" button on every page (the two existing tests keep passing; add the checkbox item to both).
6. The fallback (G7): the OP draft `/decks/new?game=optcg&leader=OP15-058` never requests art (zero `/api/cards/*/art`), and shows Enel's Yellow gradient behind the workspace with no chip; Magic with art Off, and Magic when the endpoint answers `{ art: null }` or the image fails to load (point the dev deck at a printing whose art 404s by mocking, or assert the failure branch in the component test) → the `--mana-*` gradient from the commander's identity; a colorless commander (mask 0) → the neutral `--mana-c` gradient; partners OR their identities; equal visual weight for both games (same opacity policy).
7. Both themes with a populated deck (import Queza's list as R3 did) at 1440 × 900 and 1024 × 768: screenshots in dark and light with art On, then Off; the text panes stay readable (the veil); the sticky search block and the sticky group headers over the art look intentional (your decision below — measure that nothing overlaps the chip).
8. The share page on dev: `/d/q9j9uxphwp9n` renders the art server-resolved (ZERO `/api/cards/*/art` requests from the client) with the chip, and Off hides it after hydration; `/d/jhr5ax43ewx7` shows the Yellow gradient and no chip, the posture line and `©BANDAI` untouched; the read-only contract from R3 holds (no steppers, no remove, no Choose); private decks: `PrivateShareGate` renders no art and no leader data.
9. Attribution audit: `grep -rn "art_crop" src/` at the end lists only the resolver, the `CardArt` layout literal, the OG path and the ambient layer; every render of cropped art has the chip in the same component; `CardImage` never receives an art_crop URL.
10. A double-faced commander on dev (search a transforming legendary the corpus has — e.g. "Jace, Vryn's Prodigy" is not a commander; pick a legendary DFC and say which) → front-face art in the editor and the same art in that deck's OG unfurl.
11. Keyboard, dialogs, toasts: `/`, `?`, Enter, Ctrl+Enter behave as R3 left them; the ambient layer is `aria-hidden` and `pointer-events-none`, sits under every dialog, menu and toast (portaled), and the chip is not focusable.
12. Reduced motion: with `prefers-reduced-motion: reduce` emulated (`resize_window` cannot; use `matchMedia` override in a component test) the crossfade is instant and the art still appears.
13. Deploy verification via the Vercel commit status on the full 40-char sha; then the prod pass in §Deployable outcome.

Design decisions to make explicitly (disclose + test-pin each)

* Module layout: `src/lib/cards/art.ts` exports the `CardArt` type (`{ url, layout: "art_crop" | "full_card", artist?: string, credit: string }`), `artCredit(artist)` (the ONE builder of "Art: {artist} · ™ & © Wizards of the Coast" — `OgAttribution` reads it too), and `fetchScryfallArtMeta(printingId)`; `src/lib/og/scryfall.ts` keeps `fetchOgArt` as a wrapper (bytes → data URI) so the OG routes change by zero lines. `card_faces[0]` = the front face; the API's `image_uris.art_crop` is the URL you serve (the Scryfall CDN, hotlinked, `<img>` — the CLAUDE.md image rule).
* Adapter contract: `capabilities.ambientArt?: { kind: "art_crop" | "full_card" }` — MTG `{ kind: "art_crop" }`, OP nothing; `display.colorSwatches?(mask: number): string[]` — MTG: `maskToLetters(mask & ~COLOR_BIT.C)` mapped to `var(--mana-x)`, empty → `["var(--mana-c)"]`; OP: `OPTCG_COLORS` hexes in display order, empty → a neutral grey you name. Unit-test both (`src/lib/games/mtg/display.test.ts` is new; extend `optcg/display.test.ts`).
* The endpoint `src/app/api/cards/[id]/art/route.ts`: `force-dynamic`; zod on `id` (uuid) and `printingId` (optional uuid); the card's `game_id` → `gameCodeById` → `getAdapter` → no `ambientArt` → `{ art: null }` before any network; the printing must belong to the card and not be removed (one query; `loadDefaultPrinting` when omitted); `{ art: CardArt | null }`; the day-long `Cache-Control` on 200 (decide 404s); no rate limit by default — a GET bounded by real printing ids and edge-cached is not the resolve route's shape — unless you choose a generous per-IP limit (say why); the Scryfall fetch is data-cached per printing for a day regardless of who asks.
* The client hook `useLeaderArt` (`src/components/editor/use-leader-art.ts`, or a pure reducer in `src/lib/decks/ambient-art.ts` + a thin hook): inputs `{ enabled, cardId, printingId }`; a request id per change, `AbortController` on change / unmount, fetch only when `(cardId, printingId)` changes — never on `preview`, never on main-deck edits; preloads the image (`new Image()` + `decode()`) before committing so the crossfade starts after load; `enabled` false (preference Off, adapter without the flag) → no fetch and state cleared. Test the stale guard with two out-of-order responses.
* `AmbientArt` (`src/components/deck/ambient-art.tsx`): the editor root gains `relative isolate`; the layer is `absolute inset-0 -z-10 overflow-hidden pointer-events-none` with `aria-hidden`; the art `<img alt="" decoding="async" fetchpriority="low" className="h-full w-full object-cover opacity-[0.04] dark:opacity-[0.08] motion-safe:transition-opacity motion-safe:duration-250">` (two stacked images, or a keyed image with `data-loaded`, for the crossfade); the veil `bg-linear-to-b from-background/70 via-background/30 to-background/90` (Tailwind v4 gradient syntax) on top of the art; the gradient fallback `radial-gradient` from `colorSwatches` at the same opacity policy — decide whether the gradient also hides when the preference is Off (§3 lists "preference off" under fallbacks, so the gradient STAYS with art Off — state it). The chip: a `<p>` at `absolute right-3 bottom-2 text-[0.65rem] text-muted-foreground` inside the layer with `pointer-events-auto` for text selection — decide its corner so it never sits under the toast viewport (bottom-right on `sm+`) or the footer; top-right under the header is the alternative. On the share page the layer lives inside `<main>` (which becomes `relative isolate`), so the site header and footer stay outside it.
* The opaque strips: the sticky search block and sticky group headers are `bg-background`; over faint art they read as solid bands. Either keep them (disclose) or move them to `bg-background/90` with `supports-backdrop-filter:backdrop-blur-sm`; screenshot both themes and decide by readability, not by taste.
* Preference store `src/lib/theme/appearance.ts`: `{ backgroundArt: boolean }` under `deckwarden:appearance`, `parseAppearance` (validated), `loadAppearance` / `saveAppearance` (try/catch), `useAppearance()` over `useSyncExternalStore` with a module-level listener set plus the `storage` event (cross-tab); the SERVER snapshot is `null` ("unknown") so the layer renders nothing until hydration — no flash for Off readers, and On readers see the 250 ms fade they would see anyway — disclose. The menu item: `DropdownMenuCheckboxItem` "Background art" under a `DropdownMenuSeparator` below the theme radios, shown on EVERY page (the setting is global; a page without an ambient surface still owns the setting) — or only on `data-game` surfaces; decide and pin in both header tests. `closeOnClick` for the checkbox: keep the menu open (a toggle invites a second look) or close it (consistent with the radios) — decide.
* The share page: `page.tsx` resolves art server-side — the first leader from `deck.leaderIds[0]`, the entry's `printingId` from the wire when set, else `loadDefaultPrinting` — with the same adapter gate, and passes `art` + `swatches` (or the mask) as props; `DeckShareView` renders `AmbientArt` from props with no client fetch; private decks never reach that code.
* Partner order: the editor uses `splitLeaderEntries(entries, format).leader[0]`; the share page uses `deck.leaderIds[0]`; both are entries order — pin with a unit test that runs both over the same list.
* Loading behavior: the art request fires on leader change only, after the search debounce and independent of autosave; a slow Scryfall answer never blocks an edit; an in-flight request during unmount aborts.
* LATER rows to write only if you defer something real (per-face art for DFCs, a "which printing" picker in the editor — that one is a feature, not a fix). The §4 deferrals (animated backgrounds, uploads) are already recorded.

Deployable outcome
`pnpm check` green and deployed; verified on DEV in a guest draft of each game — neutral → art after one request → chip → partners → replace / Undo → removal → neutral, the OP gradient with zero art requests, both themes at 1440 and 1024 with art On and Off, the preference persisting, autosave untouched, the first edit → exactly one deck (then deleted, id stated), the OG routes answering as before (byte sizes recorded), the alt-printing path proven; verified ON PROD in both themes with NO edits — `/decks/new?game=mtg` neutral with the new menu item, `/decks/new?game=optcg&leader=OP15-058` showing the Yellow gradient with zero art requests (the seed is state only, still no save), `/d/q9j9uxphwp9n` and `/d/uwvrnv2pv4t6` with server-resolved art and the chip (Off hides both, On restores), `/d/jhr5ax43ewx7` with the gradient and no chip, `curl -sI` of the three OG images → 200 `image/png`; `smoke:seo` green on dev (it mints — never prod), `smoke:collection` and `smoke:optcg-deck` green on dev. Docs in the same package: `REDESIGN.md` progress tracker R2 ticked with the sha, every decision above that deviated from §3 recorded under §6 open items, §3 "Interface additions" annotated as landed (do not rewrite the contract), the R3 "R2 lands on this header" note retired, the unwired-primitives list updated if you wired anything (tooltip is the likely one, for the chip — say if not); db:size stated; the dev deck's deletion stated. Nothing posted, seeded, or simulated; no prod deck created.

Session notes (environment, unchanged from R3 unless noted)

* PATH: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node / pnpm + gh); psql / aws absent — ad-hoc SQL via tsx in `scripts/.tmp/` (tsconfig-excluded but LINTED; delete only what you add).
* `.env.local`'s `DATABASE_URL` is the same Neon pooler prod uses: dev fixtures are real rows, and the draft deck you mint on dev is a real anonymous row — delete it through the editor (More → Details → Delete deck…, with `window.confirm` overridden to `() => true` in the pane) and confirm with a query. Never mint on prod. The dev limiter did NOT count the pane's creates for `::1` in R3 while the tsx smokes did (8 in one hour window) — read `rate_limit_counters` before the smokes if you plan more than a couple of creates.
* A dev server may already be listening on 3000 from ANOTHER chat rooted in this checkout (`lsof -nP -iTCP:3000 -sTCP:LISTEN`; R3 reused one); reuse it via `preview_start {url: "http://localhost:3000"}` and run the smokes against it. First hits compile API routes (seconds) — poll, don't assume. `pnpm build` coexists with `next dev`.
* Browser pane (R3's lessons): `document.visibilityState` stays "hidden" even after `tabs_select`, so Base UI EXIT animations and toast timers stall (menus linger in `data-ending-style`, toasts never auto-dismiss, Tabs panels keep an unsettled `hidden`), streamed Suspense HTML can sit un-revealed for a minute, and long scripts hit the 45 s tool timeout because timers throttle — keep pane scripts short (≤ 2 s of sleeps, few poll iterations), re-probe after a `tabs_select`, and treat jsdom tests plus fronted-tab checks as the evidence. Base UI menus open from a PLAIN `click` `MouseEvent`; jsdom needs the pointerDown → mouseDown → click sequence. React state after a dispatched `KeyboardEvent` needs ~50 ms before the DOM reflects it. Base UI toasts render `role="dialog"` (`aria-labelledby` = the title) — filter `[role="dialog"]` by name. Lazy images may never load in the hidden pane; the ambient `<img>` is eager, so assert `complete && naturalWidth > 0`, and screenshot from the fronted tab. `read_network_requests` buffers ~250 requests — read right after the action. `resize_window` for 1024 / 1440; reset with `preset: "desktop"`; clear `localStorage.theme` AND `deckwarden:appearance` on both origins when done.
* Prettier reflows scripted-edit anchors: re-grep after `pnpm format`. `*.md` is prettier-ignored.
* Deploy verification: `gh api repos/Bobandis6/deckwarden/commits/<full sha>/status` → the "Vercel" context (poll every 20 s; R3's deploy took ~4 minutes); `gh run list --workflow=nightly-ingest.yml` for nightlies. ISR is real since R1b: hub and card pages lag up to an hour on prod — assert existence on prod, freshness on dev. Share pages and the editor are `force-dynamic`, so R2's prod checks are immediate.
* `pnpm check` takes about a minute; `smoke:collection` has a budget note (3 users, 1 deck — run it first among the smokes); `smoke:seo` mints fixture decks (dev only).

Context, not tasks
Sequence after R2: R5a (home, tiles, `ColorChip`, index image view — tiles will want your `CardArt` for leader images, but that is `small` full-card renditions, not art_crop) → R5b (hub art-crop banners with the credit chip on your resolver; the share artwork header; `/c/` build CTA) → R4 (responsive builder on R3's panes; your ambient layer must survive pane stacking, which R4 verifies) → R6, each with its own prompt written the way this one was, after the previous package ships. P4.7's standing triggers still exist beside the R-series (img.deckwarden.gg flip — LATER row 51 —, the Limitless key, feedback, red nightlies) — their own P4.7 round, never a slice of an R-session. Bandai and Azuki permission emails remain unanswered — posture unchanged; OP ambient art stays off (REDESIGN.md §3) and the OP posture line stays on OP share pages. Cold-start rule holds everywhere: no simulated decks, posts, or metrics, ever. Premium never gates card data. The owner's open items: the two-minute signed-in look at `/account`'s Danger zone, the signed-in editor on `/d/q9j9uxphwp9n` (Share primary, History in More), and — new after R2 — the Background art toggle in the signed-in header. R3's observations stand: OP quick-add by card number is name-only (LATER row 72), the promo Nami P-117 has a null cost (row 61), and the 1024 px deck-pane squeeze is R4's. The smoke-pin and LATER-row ledgers in REDESIGN.md §6 remain the map.
