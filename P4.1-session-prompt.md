# P4.1 session prompt

Pull latest, then kick off P4.1 from deckwarden-build-plan.md — M4's OPENER,
the One Piece DATA package (plan line ~213: "punk-records (static versioned
JSON) as primary, optcgapi.com as cross-check; card images mirrored to R2;
honest gray-zone statement"). M3 is exited: its criterion ("every suggestion
shows its evidence") shipped with P3.8, and the last M3 bullet — flagged
natural-language search — stays deliberately unshipped ("optional, costs
money, ship only behind a flag… or never"). P4.1 ends with the English OPTCG
card corpus live in prod behind the EXISTING game-scoped seams: searchable
via /api/cards/search?game=optcg, card pages rendering with image and ©
line, the nightly re-ingesting on upstream change, and the gray-zone
statement published. Know what it is NOT before designing: NOT the real
adapter (validate/analyze stay stubs returning [] — leader-color legality,
banned pairs, and the 4-copy rule are M4's second package; the overlay file
`data/optcg/legalities.json` ships EMPTY-or-not-at-all this package, decide
with a LATER row either way); NOT the OP dashboard (DON curve etc.); NOT OP
Leader hubs or a /c/ decision (cold-start rule applies — 0 OP decks exist;
and /c/[slug] carries no game segment while the slug unique index is
per-game (schema.ts ci_slug), so two games CAN mint the same slug — hub
routing is a REAL design question that goes to LATER with the adapter
package as trigger, not answered casually here); NOT the deck editor game
picker (decks/new/page.tsx hardcodes draftGame="mtg" — the adapter package
flips it when validation is real); NOT Limitless tournaments; NOT the OP
beta. READ THIS FIRST — the upstream facts, all verified live 2026-09-03
against buhbbl/punk-records@main (manifest: version "2.0", source
"vegapull", generated_at 2026-08-28; no GitHub releases — "versioned" means
automated data-update commits to main every 3–7 days, so the ingest PINS A
COMMIT SHA and re-ingests when the pinned-vs-latest sha differs, the
topdeck.ts:220 last-succeeded-run pattern; fetch via
raw.githubusercontent.com/buhbbl/punk-records/<sha>/english/…): 60 packs
(english/packs.json — a DICT keyed by numeric pack_id like "569001", value
{id, raw_title, title_parts:{label:"ST-01", prefix, title}}; NO release
dates anywhere — printings.released_at will be NULL or a hand map, decide);
4,843 card entries in english/index/cards_by_id.json (1.69MB) = 2,785 base
ids + 2,058 `_pN` parallel-art entries; base categories: 142 Leader, 2,185
Character, 410 Event, 48 Stage; six colors Red/Green/Blue/Purple/Black/
Yellow (501/472/486/481/460/453 base occurrences — the stub's mask TODO at
optcg/adapter.ts:12 must finalize Purple and Yellow bits); id prefixes
OP01–OP17 (~119–123 each), ST01–ST36 (5–19), EB01–EB04 (~61), PRB01–PRB02,
and P-xxx promos (106, pack-less); 1,615 DUPLICATE names across distinct
base ids ("Monkey.D.Luffy" many times over — identity is the CARD ID, never
the name; schema.ts:117 already documents external_key = 'OP01-001'); 338
base names contain dots, 79 apostrophes (normalizeCardName folds MTG's " // "
— harmless here — but does NOT touch dots; decide whether OP name matching
needs a dot rule and remember normalize.ts's header: changing it forces a
re-ingest); leader `cost` is almost certainly LIFE (75 of 142 leaders cost 5
all mono-color, 59 of 68 dual-color leaders cost 4 — the Bandai site reuses
the cost slot; VERIFY against a few known cards before mapping it into
OptcgAttrs.life); Characters: 2,033/2,185 have power, 1,697 have counter;
Events/Stages have neither. THE SCHEMA DRIFT THAT SHAPES THE FETCH PLAN:
per-pack files english/cards/<pack_id>/<CARD-ID>.json carry BASE cards only
(ST01 = 17 files) with the FULL field set (id, pack_id, name, rarity,
category, colors, cost, power, counter, attributes[], types[], effect,
trigger, block_number, img_url, img_full_url) while cards_by_id.json carries
base+parallels (ST01 = 59 keys) with a SLIM schema (no effect, no trigger,
no block_number; adds keywords[]) — so identities/card text come from the 60
per-pack files and parallel-art PRINTINGS come from the index (61 polite
requests total, real User-Agent); there is NO artist field anywhere in v2.0
— LATER.md row ~49 claims "punk-records carries artist too": CORRECT THAT
ROW, its OG-image trigger reasoning partly rests on it. Images: all 4,843
img urls point at https://en.onepiece-cardgame.com/images/cardlist/card/
<ID>.png with a ?YYMMDD cache-buster — official Bandai hosting, nothing
bundled. Cross-check source optcgapi.com is alive, free, English-only,
covers OP-01→OP-15 + starters (punk-records is AHEAD of it — OP16/OP17
exist in english/); use it to spot-check a handful of mapped cards, not as
a second pipeline. Codebase facts you inherit (verified, with lines): the
DB needs NO new tables — games row optcg id 2 seeded (seed-data.ts:6,11),
sets is game-generic (schema.ts:59–74, unique(game_id, code)),
card_identities/card_printings/legalities all game-scoped with the
conditional-legality jsonb already provisioned (schema.ts:229, partial
uniques :242–248 applied in drizzle/0000_schema_v1.sql:77,114) — expect a
ZERO-migration package (if one appears anyway, the eyeball rule applies);
the ONE seed gap is the fire drill's known 400: the stub declares format
"standard" (optcg/adapter.ts:25–41) but FORMATS (seed-data.ts:23) has no
row, so POST /api/decks {game:"optcg"} 400s today — add the FORMAT_ID entry
(append-only per the file's comment) and re-seed; search/resolve routes
already accept game=optcg (search/route.ts:29, resolve/route.ts:37) and
scope every query by game_id (search :91, resolve :118); translate.ts is
game-agnostic (jsonb GIN multiselect serves traits as-is) BUT its COLOR_BIT
map (:39) is MTG-letter-keyed — check how the adapter's colorset search
field maps OP color names to mask bits and put the mapping in ONE place;
the searchText GENERATED column (schema.ts:142–145) hardcodes
attrs->>'type_line' and attrs->>'oracle_text', so OP cards get no FTS
unless you either write those two keys into OP attrs (cheap, honest if the
content is the type/effect text) or accept trgm-name-only search (the stub's
searchFields already omit search_text — either is fine, decide and
disclose); card_printings.id is a caller-supplied uuid with NO default
(schema.ts:163–166 — Scryfall's id IS the uuid for MTG): OP has no upstream
uuid, so MINT DETERMINISTIC ids (uuidv5 over game+card_id+variant or
similar) so re-ingest and pg_dump/restore stay stable — decide, disclose,
test-pin; printingImageUrl (src/lib/cards/images.ts:33–42) is the single
image choke point and imageOverride jsonb is the no-code-change hook
(toSmallImage passes non-Scryfall URLs through untouched; next.config.ts
needs nothing — plain <img> everywhere). Design decisions to make
explicitly, each disclosed in code and test-pinned: (1) identity =
external_key base card id, name from the per-pack file, attrs per the
stub's OptcgAttrs (optcg/adapter.ts:13–20: category, effect_text, traits ←
types[], power_num, counter_num, life ← verified leader-cost rule) plus
where attributes[] (Strike/Slash/…) and trigger live; primaryType ←
category; cost_value ← cost for non-leaders (NULL or life for leaders —
decide what the browse index should sort by); is_leader_candidate ← category
=== "Leader" (the slug post-pass is GAME_ID.mtg-pinned at
assign-leader-slugs.ts:24,33 so NO OP slugs get minted — correct for now,
note it with the hubs LATER row); colors_mask/ci_mask ← the finalized
six-color mapping (OP has no color identity distinct from colors — same
mask in both, say so). (2) printings: one row per base id + one per _pN
(finishes/rarity from the slim index entry; has_back false; set from the id
prefix via packs.json title_parts.label — decide the sets.code vocabulary
(ST-01 vs ST01, promos' pseudo-set) and keep it stable, it's a unique key);
content_hash over the mapped fields (the scryfall.ts:291–309 hash-guard
pattern) so unchanged cards skip. (3) The ingest script
scripts/ingest/punk-records.ts on the house skeleton — loadEnv first,
directUrl() (-pooler strip), the SHARED advisory lock key 7234015309 (all
ingests serialize), ingest_runs source 'punk-records' (the name is already
reserved in schema.ts:848's comment), temp-table staging + tuple-compare
merge + soft-delete sweep (scryfall.ts:263–337 is the template), stats with
counts reconciled against the numbers above, pg_database_size + duration.
Nightly wiring: a step between spellbook and topdeck (order doesn't matter —
no cross-dependency; keep the !cancelled() safety-step structure intact),
proven by ONE manual workflow_dispatch. (4) Legality: rows for the new
format — MTG writes explicit per-card rows from Scryfall's legalities
(scryfall.ts:387–410); OP has no upstream legality feed, so decide between
defaultLegality:"legal" on the format row carrying the load (zero rows,
validate treats missing as default — check how fetchLegalityMap consumers
handle absence) versus explicit 'legal' rows (2,785 rows of noise) — lean
default; the REAL banlist (Bandai bans exist) plus banned-pairs overlay is
the adapter package's job with the conditional rows already waiting
(LegalityEntry types banned_with_leader at types.ts:54–62). (5) Images —
the plan says MIRROR TO R2, and the gray-zone posture is the reason
(serving from our own bucket with disclaimers beats hotlinking Bandai's
site, which is both fragile — the ?YYMMDD buster implies cache churn — and
ruder): ~4,843 PNGs, likely well under R2's 10GB free tier; aws CLI exists
ONLY in Actions (archive-topdeck-raw.sh:24–25 is the credential pattern;
aws is NOT installed locally, R2 secrets live in Actions not .env.local) —
so the mirror is an Actions job (one-time backfill + nightly delta for new
cards) writing s3://$R2_BUCKET/optcg/images/<ID>.png, and the ingest writes
the R2 public-bucket URL into image_override {front} (decide: is the bucket
public? if no public domain is configured yet, that's an OWNER step —
surface it early in the session, don't discover it at deploy time; interim
hotlink of img_full_url is the honest fallback if R2 publishing blocks, as
its own disclosed decision + LATER row). Whichever serves, the card page
must show the © line ("©Eiichiro Oda/Shueisha, Toei Animation, ©Bandai
Namco" — pull exact wording from Bandai's site) near the image, same spirit
as the Scryfall artist/© rule. (6) The gray-zone statement (plan bullet
verbatim: "no official API exists… posture = disclaimers, no paywalling
card content, immediate takedown compliance, permission email already
sent") — a short honest section on an /about or legal page + the OP card
surfaces' © line; the permission email to Bandai went out 2026-08-30, no
reply — the posture doesn't wait for one. (7) Sitemap game leaks — fix
BEFORE the corpus lands, in the same package: src/app/cards/sitemap.ts
(:39,:53) has NO game filter (4,843 OP ids would silently join the MTG card
sitemap — decide whether OP cards belong in a sitemap at all pre-beta) and
src/app/c/sitemap.ts (:26–32) filters leader candidates WITHOUT game_id (OP
leaders would advertise /c/ URLs that don't resolve). (8) OP decklist
import resolution is by CARD ID, not name (the stub's parseDecklist emits
"OP01-025" tokens) — the resolve route's three passes are all name-based;
an external_key exact pass for game=optcg is probably a few honest lines
and makes import real, but if it grows, LATER with the adapter package
(this half-fires LATER row ~42's "M4's One Piece import (set codes are its
primary key)" — update that row either way). Deployable outcome = the
corpus in prod with counts logged and reconciled (packs 60, identities
2,785, printings 4,843, leaders 142); nightly step wired and proven by one
manual workflow_dispatch (unchanged-sha second run = clean no-op skip);
/api/cards/search?game=optcg returning real cards on prod;
/cards/<uuid> rendering an OP card with image + © + disclaimer; sitemaps
game-scoped; the statement page live; smoke coverage for the new surface
(a small optcg search+card-page smoke following the house BASE_URL
pattern); pnpm check green; deployed; db:size stated before/after (245MB /
257,105,920 bytes on 2026-09-03, alert 350 — the OP rows are single-digit
MB); LATER updated: row ~49 artist claim corrected, row ~42 half-fired or
deferred with facts, new rows for OP hubs + /c/ routing (trigger: the
adapter package), the real banlist + overlay file (trigger: adapter
package), en-asia/other languages (trigger: a user asks), Limitless
(trigger: key granted), image-mirror remainder if hotlink shipped.

Session notes:

* Toolchain update (2026-09-03): `gh` IS installed now (~/.local/bin/gh) —
  P3.8's "GitHub REST via curl" note is stale; `psql` and `aws` remain NOT
  installed (ad-hoc SQL via a tsx one-off with the repo's `postgres`
  package; scratch tsx scripts in scripts/.tmp/ — gitignored, inside the
  repo so node_modules resolve). node/pnpm at ~/.local/node22/bin —
  non-login shells need export PATH="$HOME/.local/node22/bin:$PATH".
  `source .env.local` fails in zsh — pull single vars via grep/cut. Deploy
  verification: Vercel posts a commit status (context "Vercel") on the
  pushed sha; actions/runs?head_sha= needs the full 40-char sha.
* Pre-flight: verify the 2026-09-03 10:37 UTC scheduled nightly ran green —
  it's the FIRST SCHEDULED run with (a) the Vehicle/Spacecraft leader
  predicate (2eb2351; the 2026-09-03 manual dispatch already assigned 37
  leader slugs and dropped the window's unresolved_commander to 8/1498) and
  (b) the aggregate alias fix (7edd714 — the first night a settled event
  aggregates exercises it; the manual dispatch aggregated 0). A red step is
  triage BEFORE P4.1 work.
* What the 2026-09-03 chore session shipped (not in the plan doc): CR 903.3
  verified verbatim (legendary Vehicles unconditionally + Spacecraft WITH a
  P/T box are commanders — The Eternity Elevator correctly stays excluded);
  isLeaderCandidate + tests (scryfall-map.ts/.test.ts); LATER row ~39 gap
  (a) rewritten Done; "Kavaero, Mind-Bitten" (4 unresolved standings) does
  not exist on Scryfall — unknown cause, left unresolved; gaps (b) DFC
  front-face leader names and (c) Background pairs still open in that row.
* Corpus measurement provenance: the punk-records numbers above came from a
  scratch analysis of english/index/cards_by_id.json + spot fetches of
  per-pack files, 2026-09-03, not committed — re-derive against the sha you
  pin; reconcile your ingest counts against them and investigate any gap
  (e.g. index minus per-pack-file union should be exactly the _pN set).
* Politeness: punk-records is one person's free repo — fetch via
  raw.githubusercontent at a pinned sha (61 small files + index), send a
  real User-Agent (Deckwarden/1.0), never hammer; the weekly-commit cadence
  means the nightly's sha check is a single API call
  (repos/buhbbl/punk-records/commits?per_page=1) on most nights.
* Neon: 245MB on 2026-09-03 (includes the P3.8 aggregate), alert 350, free
  ~512. The OP corpus is a few MB of rows; images go to R2, NEVER into
  Postgres. db:size before/after as always.
* R2 state: secrets (R2_ENDPOINT/ACCESS_KEY_ID/SECRET_ACCESS_KEY/BUCKET)
  exist in GitHub Actions only; .env.example documents none of them; bucket
  currently holds topdeck/raw/<date>/ nightlies and pg/ backups. Whether
  the bucket has a PUBLIC serving domain is UNVERIFIED — an owner question
  to surface in the session's first hour if mirroring ships.
* Prod smoke budget: deck-create is 10/h + 30/day per IP (fixed windows :00
  UTC) — P4.1's smoke shouldn't need deck creation (search + card page are
  read-only); if one sneaks in, read rate_limit_counters first.
  engagement-smoke.ts and versions-forks-smoke.ts still push unchecked ids
  (the requireDeckId guard is only on the profile smoke) — standalone chore
  if a 429 bites.
* Browser pane: hidden pane swallows computer clicks silently — drive via
  javascript_tool, verify via DOM/API reads.
* Context, not tasks: beta announcement still deliberately deferred (owner
  call); Bandai + Azuki permission emails out 2026-08-30, no replies; the
  cold-start rule stands — OP hubs/recommendations wait for real data, and
  deriving anything from seeded or simulated decks stays banned; premium
  may never gate card data (applies to OP cards identically).
* Protocol as usual: one package, ends deployed and pnpm check-green; scope
  leaks to LATER.md with a trigger; every drizzle migration eyeballed
  (expect zero this package — a surprise migration is a design smell worth
  a second look).
