# R5a session prompt — Public surfaces I (the equal two-game homepage with real leader shelves, deck tiles with the identity strip, `ColorChip`, the commander index image view)

Pull latest, then run R5a — the fifth R-series package. **`REDESIGN.md` is the contract (read §1 Mechanics — game accent, tokens, motion, containers; §2 "Site shell and homepage" in full — the header spec's "My decks" line and the five-item homepage order; §2 "Browsing, sharing, and account pages" — the index image view under `deckwarden:index-view`, the one `ColorChip`, the deck-collection tiles; §3 only for what R2 handed you (`colorSwatches` is the natural paint for an image slot that has no image — see the R2 block in §6); §4 G4 G5 G8 C14 and the deferral list (card-stacks grid, ⌘K — LATER rows only when a user asks); the R5a row in §5; the ledgers in §6 — the smoke-pin row for R5a (the hero string, "Curve of these staples") and LATER row 51's gate on One Piece images; §7 Visual checks)**; build plan §6b makes R-packages legal sessions under the CLAUDE.md protocol. R5a is the homepage and the browsing surfaces: the hero **"Your next great deck starts here."** with the `seo-smoke` pin updated in the same commit; two equally prominent game cards, each with Build and Browse actions and a shelf of REAL leader cards (Magic: the most-played commanders by `edhrec_rank` through `loadLeaderIndex` grown with a limit and a batched default-printing lookup; One Piece: leaders with recent Top finishes from the tournament-standings index — 74 leaders carry finishes today, so the shelf renders); Continue building (the visitor's own decks as tiles — claim tokens or account); Recent public decks as tiles with leader imagery and the identity strip; a capabilities strip with copy only for shipped features; the `DeckTile` (G5) with the 3 px identity strip (G8) on home, the `/c/` deck shelf and `/account`; `ColorChip` (C14) replacing the three hand-rolled color controls; and the `/commanders` image view persisted under `deckwarden:index-view`. Expect ZERO migrations, ZERO new dependencies, ZERO new URLs (the route table diff is EMPTY — home stays `ƒ`, the indexes stay `ƒ`, the hubs stay `●`), no change to any existing wire shape except one ADDITIVE field on `/api/decks/mine` if you take that decision, no ingest or data work, the home query budget of at most three extra unconditional indexed queries, One Piece shelf and tile IMAGES gated on LATER row 51 (no r2.dev grids anywhere), and nothing invented (the cold-start rule: no fake popularity, no padded shelves, no testimonials). The owner's decisions are settled (§0 and §6): the sequence R5a → R5b → R4 → R6, the restrained fun ceiling, One Piece ambient art off, the hero wording "stands until R5a proves it in place" (this session proves it or records why not), mascot = shield mark only. Do not relitigate them.

Pre-flight, in order. (1) Nightlies since 2026-09-10 all green (`gh run list --workflow=nightly-ingest.yml`; cron 10:37 UTC drifting ~13:30–17:40Z; 2026-09-09's ran 14:47Z green — the first on R1a + R1b + R3 code; 2026-09-10's is the FIRST nightly on R2 code and had not fired at 02:50Z on 09-10 when this prompt was written). A red run is P4.7 branch F and takes precedence — fix or report it as P4.7, and R5a waits for its own session. (2) Working tree clean at or after `8789499` (the R2 docs commit; R2's code is `096df3b`) plus this prompt's commit. (3) db:size baseline stated (254.4 MB on 2026-09-10 after R2's dev smokes; Neon alert 350 MB) — R5a adds no rows by hand; the dev smokes mint and delete their own fixtures, and if the guest Continue-building check needs a draft in the pane, mint exactly one through the editor and delete it before the session ends (state its id and its deletion). (4) QA fixtures unchanged, read from the DB: the FIVE public decks are the whole home rail — `/d/q9j9uxphwp9n` Queza (MTG, ci 7), `/d/uwvrnv2pv4t6` Atraxa Superfriends (MTG, ci 23), `/d/4c5_9yd9ewdy` Boros Equipment Burn (MTG, ci 9), `/d/end97r37znuv` Nelson & Murdock (MTG, ci 9), `/d/jhr5ax43ewx7` "Six DON!!, Endless Thunder" (One Piece, ci 32 = Purple, the only deck with a like: 1) — all on the owner's account, which has a username (the rail's byline reads "by Bobandis6"); the three "Untitled" decks on the owner's account (`28vsuc6j54pf`, `avzq42djrda7`, `2un8xrmq5wrs`) are the owner's, never yours to touch. The owner's open looks — the `/account` Danger zone, the signed-in editor on `/d/q9j9uxphwp9n` (Share primary, History in More), and the Background art toggle in the signed-in header (R2) — leave no trace; state whether the owner did them (R2 could not tell: a signed-in visit shows in `rate_limit_counters` as `auth:…|/get-session` and `decks-mine:ip:…` rows, nothing more). The browser pane is signed out on prod AND on dev (OAuth cannot be typed), so every signed-in surface R5a touches (`/account` tiles, Continue building for accounts) is proven by `smoke:engagement` / `smoke:profile` — they mint users and session rows straight into the DB and fetch the HTML with a signed cookie — and by RTL with the auth client mocked (`site-header.test.tsx`), never by eye. (5) `pnpm check` green at the start (627 tests at `8789499`, the same 6 pre-existing `no-unused-vars` warnings, 0 errors) and `pnpm build`'s route table saved before and after (grep the glyph lines, strip the sizes — the R2 method): the diff must be EMPTY. No `shadcn add` is expected (Badge, Tooltip, Skeleton, Select, Toggle-group, Separator all exist under `src/components/ui/`); never `shadcn init`.

What R5a is NOT (scope fence)

NOT the hub art-crop banners and accent-gradient headers (G3, R5b — they consume R2's resolver), NOT the share page's artwork header, NOT F5 hover previews, F8 Warden pages, F12 (R5b). NOT the `/c/` "Build with this commander" CTA (R5b, fires LATER row 63). NOT `/u/[username]` and `/f/[publicId]` tiles — §5 names them under R5b beside the account Decks / Collection import / Settings restructure; R5a swaps the `/account` deck ROWS for tiles inside today's folder sections and Unfiled bucket and changes nothing else about that page. NOT the `/cards` page beyond `ColorChip` (its grouped filters, selected-filter row and loading state are listed in §2 without a package — record that gap as a §6 open item assigned to R5b or R6, do not build it). NOT the `/leaders` image view until LATER row 51 fires (a design decision below — 142 r2.dev images on one page is exactly the throttling the row warns about). NOT One Piece tile or shelf images (row 51; the `/l/` hub hero and the editor grid keep rendering r2.dev as today). NOT the editor or the share page (R3, R2, R4 own them; the ambient layer stays as it is). NOT an `artist` column, stored image URLs, `next/image` optimization, or any ingest change. NOT sitemaps, robots, OG images, JSON-LD beyond keeping today's. NOT a change to `loadHubDecks`' ordering or limit, to ISR (`/c/`, `/l/`, `/cards/[id]` stay `revalidate 3600` + `generateStaticParams []`), or to `decks_recent_public`'s 12-row cross-game rail semantics. NOT invented ordering: "most liked" over a corpus with one like is not a shelf; keep recency. NOT a "featured brews" curation (build plan §10's seeding checklist is the owner's). NOT ⌘K, card-stacks, draggable panels, extra theme families. NOT a Next, React, Tailwind or Base UI upgrade. NOT any P4.7 branch unless pre-flight (1) forces it, and then R5a is not this session.

Facts you inherit (verified 2026-09-10 against `8789499` — trust these, spot-check nothing)

* Home. `src/app/(site)/page.tsx` (143 lines): `force-dynamic` (the rail must drop a deck flipped private on the next request); `metadata.alternates.canonical "/"`; `JsonLd websiteJsonLd()`; the hero `<h1>` "Build a legal deck, fast." (line 61) over a two-game paragraph, "Start building" → `/decks/new` and "Browse commanders" → `/commanders`, the Moxfield/import line with the `/account` "sign in" link; `FEATURES` (lines 30–49, three cards: "The Warden checks your work" → `/decks/new`, "Combos, surfaced" → `/commanders`, "Share pages that sell the deck" → `/cards`); `<YourDecks />` then `<RecentPublicDecks />`; the "Also speaks One Piece" section (lines 107–128) carrying the literal `/leaders`, `/cards?game=optcg` and `/decks/new?game=optcg` links; the GitHub line. Container `max-w-browse`, `gap-12 px-4 py-16`, everything centered. The root layout's title and description are the two-game strings `smoke:seo` checks (do not touch them).
* Smoke pins on R5a's surfaces. `scripts/seo-smoke.ts` 476–501: home canonical, the WebSite + SearchAction JSON-LD, `/leaders` AND `/cards?game=optcg` present in the home HTML, the two-game title, the description, and line 501 `check("home hero is game-neutral", home.text.includes("Build a legal deck, fast."))` — change that string to the new hero IN THE SAME COMMIT; 318–326: `/leaders` 200, canonical, links the fixture hub `/l/<slug>`, carries `©BANDAI`. `scripts/hubs-smoke.ts` 63–86: `/commanders` HTML contains the most-played leader's name and `/c/<slug>`; `/commanders?colors=w` contains the top mono-white leader and does NOT contain `/c/<top slug>"` (note the closing quote — the href attribute must end right after the slug in every view); `/commanders?page=2` drops the page-1 leader; 89–95: the hub's "Curve of these staples", the role template, Scryfall + EDHREC credits. `scripts/engagement-smoke.ts` 242–248: a public fixture deck's name appears on the home rail and `♥` is in the home HTML once a like exists (keep `♥` as tile content); 213–215: `/account` shows "No bookmarks yet". `scripts/profile-folders-smoke.ts`: `/u` and `/f` pins ("This folder is private", 404s) — R5b's surfaces, untouched by you. `scripts/optcg-smoke.ts` pins "Card image coming soon" on the card page fallback (`CardImage`'s default text stays).
* Rails today. `RecentPublicDecks` (`src/components/deck/recent-public-decks.tsx`, server): ONE query — `decks` left-joined to `users`, `visibility = 'public'`, `updated_at DESC`, limit 12 (the `decks_recent_public` partial index) — fields id/publicId/name/gameId/formatId/likesCount/updatedAt + author name/username; a `divide-y` list; byline only when the author chose a username; `♥ N` only when N > 0; the empty copy "No public decks yet — build one and set it to public…". `YourDecks` (`src/components/deck/your-decks.tsx`, client): `listDeckTokens()` → `POST /api/decks/mine` (≤ 100 ids, limiter 30/min per IP) → rows shaped by `deckMetaJson(deck, { isOwner: true })` (`src/app/api/decks/mine/route.ts:60`), so `leaderIds` and `ciMask` are ALREADY on that wire — no printing id, no image; renders nothing without tokens; `id="your-decks"` is the header's guest "My decks" target (`/#your-decks`); "Share page" link per row; `formatLabel` computed client-side through the adapter.
* Indexes. `/commanders` (`src/app/(site)/commanders/page.tsx`, 163 lines): `force-dynamic`; `loadLeaderIndex({ ciMask, page })`; the color filter is a `<nav aria-label="Color identity filter">` of text `Link`s ("All" + `COLOR_LABEL` White/Blue/Black/Red/Green/Colorless over `COLOR_ORDER`) with `aria-current="page"` and exact-identity toggling in `filterHref` (lines 74–104); rows: rank number `(page-1)*60+i+1`, name, `ciPipsHtml` via `dangerouslySetInnerHTML`, each a `Link` to `/c/<slug>` (lines 114–135); `GameSwitch active="mtg"`; Previous / Next; the Scryfall credit line. `/leaders` (`src/app/(site)/leaders/page.tsx`, 177 lines): `force-dynamic`; `loadOpLeaderIndex({ colorsMask })` — all 142 rows, name order, no pagination; a local `ColorDots` (lines 46–63: `size-2.5` hex dots from `OPTCG_COLORS` with an `sr-only` "Red/Green" text); the filter nav ("All" + the six color names, `maskLetter` toggling); rows: name, `externalKey` (uppercase mono), `attrs.life` "N Life", each a `Link` to `/l/<slug>`; the "N leaders" live count; `EmptyState` "No leaders match that exact color pairing."; the "Start a deck" button → `/decks/new?game=optcg`; the Bandai posture paragraph. `/cards`: `CardSearch` (`src/components/cards/card-search.tsx`, client): `COLOR_TOGGLES` (MTG letters W U B R G C rendered `size="icon-xs"`; OP color names `size="xs"`) as `Button`s `variant default|outline` with `aria-pressed` inside `role="group"` labelled "Color identity (within)" / "Color (within)" (lines 176–190); the param is `ci=within:` for MTG and `color=within:` for OP.
* Queries. `src/lib/hub/queries.ts`: `loadLeaderIndex` (~181–208, `LeaderIndexRow { id, name, slug, ciMask, costValue, popularity, cheapestUsd }`, `LEADERS_PAGE_SIZE = 60`, `popularity ASC NULLS LAST, name`, MTG-only, slugged, not removed); `loadOpLeaderIndex` (~223–246, `{ id, name, slug, externalKey, colorsMask, attrs }`); `loadHubDecks(leaderId)` (~148–170, `HubDeckRow { publicId, name, likesCount, updatedAt }`, `HUB_DECKS_LIMIT = 10`, public only, `leader_ids @>` on the `decks_hub` GIN, most-liked then recency) — `/c/[slug]/page.tsx` 333–362 renders "Decks with this commander" only with rows; `loadDefaultPrinting(cardIdentityId) → { id, imageOverride } | null` is single-row and no batched variant exists (batching precedents: `inArray` in `src/lib/collection/owned.ts:47`, `src/lib/recommend/queries.ts:187`; `deck-cards-wire.ts` joins `is_default` in one query). `src/lib/tournaments/queries.ts`: `loadTopFinishes(gameId, leaderId)` is per-leader (`TOP_FINISHES_SHOWN = 8`, `leader_ids @>` on `ts_by_leader`, newest event first); the tables (`schema.ts` 684–760): `tournaments (game_id, format_id, source, external_key, name, start_date, player_count)` with `tournaments_game_date (game_id, start_date desc)`, `tournament_standings (tournament_id, placement ≤ 16, player_name, leader_ids uuid[] GIN, decklist_url, wins/draws/losses)`. Live One Piece corpus 2026-09-10: 122 events, 1,939 standings, 74 distinct leaders with finishes; by latest event (2026-09-07) then count: Enel OP15-058 (231), Dracule Mihawk OP14-020 (173), Boa Hancock OP14-041 (49), Sabo OP13-004 (48), Nico Robin OP09-062 (32), Kaido OP17-058 (28), Monkey.D.Luffy OP17-079 (16). Magic page-1 top six by `popularity`: Syr Konrad, the Grim (258), Etali, Primal Storm (269), Ragavan, Nimble Pilferer (279), Braids, Arisen Nightmare (295), Azusa, Lost but Seeking (326), Loran of the Third Path (344) — `edhrec_rank` is CARD play-rate, which is why Ragavan sits there; the index's own subtitle "Ranked by how much each commander is actually played (EDHREC data via Scryfall)" is the honest label, reuse its sense on the shelf. Corpus: 4,023 slugged MTG leaders, 142 OP leaders, 5 public decks, 16 decks in total.
* Images. `src/lib/cards/images.ts`: `scryfallImageUrl(id, version, face)`, `printingImageUrl(printing, version)` (override-aware; overrides are stored at `normal` and served as-is for every version), `isEmbeddableImageUrl` (Bandai hosts refused; `pub-….r2.dev` passes), `embeddablePrintingImageUrl`, `toSmallImage(url)`, `thumbnailUrl(image)` (R3: the `small` rendition ONLY for `cards.scryfall.io/normal/` URLs, null otherwise — the gate to reuse for tiles and shelves: One Piece → null → no image, the flip changes no layout). No MTG default printing carries an `image_override`; every OP printing does (4,843, e.g. Enel `…r2.dev/optcg/images/OP15-058.png`). `CardImage` (`src/components/cards/card-image.tsx`): `src | null`, `alt`, explicit `width`/`height`, `priority`, `frame` (G2: card radius, accent ring on hover, 2 px lift), `fallback`, `title`; renders a bare `<img>` (lazy + fade unless `priority`) or the fallback box at the same aspect ratio; the `small` rendition is 146×204. LATER row 51: r2.dev is rate-limited and "not for production" — One Piece thumbnails (R3), tiles and home shelf images (R5a) wait for `img.deckwarden.gg`; hub and grid renders continue as today. R2's `CardArt`/`resolveCardArt` are art_crop — NOT what tiles want (tiles want `small` full-card renditions, no attribution question).
* Colors. `src/lib/games/colors.ts`: `COLOR_BIT` (W1 U2 B4 R8 G16 C32), `COLOR_ORDER`, `maskToLetters`, `lettersToMask`, `ciPipsHtml(mask)` (an HTML string of `.pip.pip-x` spans styled in `globals.css`, mask 0 → one C pip). `src/lib/games/optcg/colors.ts`: `OPTCG_COLORS` (Red #d32f2f, Green #2e7d32, Blue #1565c0, Purple #6a1b9a, Black #37474f, Yellow #f9a825 in Bandai order; bits Yellow 1 · Blue 2 · Black 4 · Red 8 · Green 16 · Purple 32), `maskToOptcgColorNames`, `maskToOptcgLetters`, `OPTCG_COLORLESS_HEX` (#9e9e9e, R2). Adapters: `display.colorSwatches(mask)` (R2) — MTG `var(--mana-x)` in WUBRG order with `["var(--mana-c)"]` for colorless, OP the frame hexes — and `ambientGradient(swatches)` in `src/components/deck/ambient-art.tsx` (exported) paints them. `--mana-*` live in `globals.css` ~187, identical in both themes.
* Decks. `decks` columns (`schema.ts` 396–460): `public_id`, `game_id`, `format_id`, `user_id`, `name` (default "Untitled"), `visibility` (default unlisted), `leader_ids uuid[]` (entries order, R2 keeps it stable), `ci_mask`, `folder_id`, `forked_from_deck_id`, `likes_count`, `created_at`, `updated_at`; indexes `decks_hub` (GIN), `decks_browse (game_id, format_id, visibility, updated_at desc)`, `decks_owner (user_id, updated_at desc)`, `decks_folder`, `decks_recent_public` (partial, public). `src/lib/decks/display.ts`: `formatLabel(gameId, formatId)`, `updatedLabel(date)` (UTC-pinned — the hydration rule), `joinedLabel`. `deckMetaJson` (`src/lib/decks/serialize.ts`) — untouched by you. `/account` (`src/app/(site)/account/page.tsx`): `DeckItem` (lines 47–75: edit link titled "Edit {name}", "format · visibility · Updated …", `DeckFolderSelect`, a "Share page" link) rendered inside each folder section and the Unfiled bucket; `EmptyState "No decks in this account yet"` + "Build one"; the bookmarks list; `ClaimDecks`; `max-w-reading`. `GAME_ID` / `gameCodeById` / `findFormatById` in `src/db/seed-data.ts`.
* Primitives and precedents. `Badge` (`ui/badge.tsx`: default / secondary / destructive / outline / ghost / link — the editor's game/format chip is `outline`), `EmptyState` (`src/components/empty-state.tsx`: title, hint, action, `mark`), `GameSwitch` (`src/components/game-switch.tsx`: pills with `aria-current`, hrefs per game), `Segmented` (`src/components/deck/segmented.tsx`: ToggleGroup, `label / options / value / onChange`, sliding thumb — the obvious index-view toggle), `BrandMark`. Persistence precedents: `src/lib/decks/view-prefs.ts` (`deckwarden:deck-view`, `parseViewPrefs` field by field, `loadViewPrefs` / `saveViewPrefs`, read in a client component after mount) and R2's `src/lib/theme/appearance.ts` (`useSyncExternalStore`, null server snapshot). Installed and unwired: tooltip, skeleton, input, select, toggle, scroll-area, hover-card, sheet, drawer, separator, progress, popover, navigation-menu. The `(site)` layout is synchronous and reads no request data (ISR guard) — a PAGE that is already `force-dynamic` (home, `/account`) may read `headers()`.
* Tests: 627 green at `8789499`. Precedents: `site-header.test.tsx` (auth client mocked with `vi.mock("@/lib/auth-client")`), `game-switch.test.tsx`, `empty-state.test.tsx`, `cards/card-image.test.tsx`, `deck/segmented.test.tsx`, `lib/theme/appearance.test.ts`, `lib/decks/ambient-art.test.ts` (pure helpers), `lib/cards/images.test.ts`. Async server components with DB reads are not RTL-renderable: put the logic in pure, tested helpers (row → tile props, image gating, strip colors, shelf mappers, cold-start branches) and let the smokes read the HTML. Lint: `react-hooks/refs` and `react-hooks/set-state-in-effect` are errors; every raw `<img>` needs `CardImage` or the eslint-disable line with the CDN reason.
* Environment facts from R2: the dev deck-create limiter DID count the pane's create (`deck-create:ip:::1`, 10/h + 30/day) — `smoke:seo`, `smoke:engagement`, `smoke:profile` and `smoke:collection` all mint decks; read `rate_limit_counters` before a smoke run and space them; a `computer` `ctrl+Return` in the pane does not register as Ctrl+Enter — dispatch a `KeyboardEvent('keydown', { key: 'Enter', ctrlKey: true })` on the combobox; under `resize_window` emulation the hidden pane's screenshots squeeze the page into the top ~58 % of the frame — trust `getBoundingClientRect` over pixels; prod's `og:image` URLs carry a hash suffix (`opengraph-image-u0d1r4`), extract them from the page HTML.

Verify-first list (never from memory)

1. The hero pin: `smoke:seo` green on dev AFTER the string at `seo-smoke.ts:501` reads the new hero, with `/leaders` and `/cards?game=optcg` still literally in the home HTML (the game cards' Browse / Search links carry them) and the JSON-LD, canonical, title and description checks untouched. `smoke:hubs` green: the `/commanders` HTML still contains the most-played leader's name and `/c/<slug>` links in the DEFAULT view; `?colors=w` and `?page=2` behave exactly as before in both views (the grid's `href` must end right after the slug — `/c/<slug>"`).
2. The home query budget, counted, not estimated: list every query the home render awaits — today one (the rail) plus the guest rail's client POST — and the three extras you add (the Magic shelf with its printings in ONE query, the One Piece finish-leaders query, the recent rail's batched leader printings). Prove the count on dev with an env-gated postgres.js `debug` hook in `createDb` (`src/db/index.ts`) that logs one line per statement — keep it env-gated (`DB_LOG=1`) as a small tooling addition or remove it before commit; either way disclose. A signed-in visitor's Continue-building query (the decision below) is conditional and reported separately.
3. The rail as tiles: exactly the five public decks, newest activity first, `♥ 1` only on the One Piece deck, "by Bobandis6" on every one (the owner has a username), each tile ONE link to `/d/<publicId>` named by the deck name, the identity strip painted from `ci_mask` (Queza WUB, Atraxa WUBG, the Boros pair RW, Enel Purple), the Magic tiles with a `small` leader image via the batched default-printing lookup and the One Piece tile with NO image request (grep the HTML: no `r2.dev` in a tile), the game chip, the format label, `updatedLabel` UTC-pinned, and no hydration warning in the console.
4. The two shelves: the Magic shelf shows page 1's top six (names above) with `small` images and its honest label; the One Piece shelf shows leaders ordered by their latest finish (Enel, Mihawk, Boa Hancock, Sabo, Nico Robin, Kaido today) with name, `ColorChip` dots, "N Life", the printed id, NO images, and the Limitless credit near the shelf (the tournament-data attribution rule holds on every surface that shows those results); the cold-start branch — zero finishes → names, chips and life with NO shelf label and NO popularity wording — is unit-tested against the pure shelf builder, since the live corpus never exercises it. Every shelf card links its hub.
5. Continue building: guest → tiles from this browser's tokens (mint one draft in the pane to see it, then delete it; state the id); signed-in → the account's decks, newest first, capped — proven through `smoke:engagement`'s signed-in HTML fetch of `/` (extend it by one check) or a new check in `smoke:profile`; hidden entirely with nothing to continue (no empty state on home for this section — it is not a discovery surface).
6. `ColorChip` at all three sites: `/commanders` filter (Links, `aria-current`, exact-identity toggling unchanged, "Colorless" still a distinct chip), `/leaders` rows (the `sr-only` color names preserved) and filter, `/cards` toggles in both games (`aria-pressed`, the `role="group"` labels unchanged, `ci=within:` / `color=within:` params unchanged); one component, test-pinned for both games including the colorless case; keyboard reachable; both themes.
7. The `/commanders` image view: SSR renders the list; the toggle (List / Grid) persists under `deckwarden:index-view` and survives a reload; the grid keeps the rank numbers, the names, the pips and the exact hrefs; `?colors=` and `?page=` are untouched by the toggle; a corrupt stored value → list; the flash-to-grid after hydration for grid readers is disclosed (the deck-view precedent); `/leaders` shows no toggle (or renders its grid without images — your decision, pinned).
8. The `/c/` deck shelf as tiles: `loadHubDecks` grown with `gameId`, `formatId`, `leaderIds`, `ciMask` and a batched default-printing lookup (no per-viewer state, so ISR holds — prove `x-nextjs-cache: HIT` on the second dev hit and `x-vercel-cache: HIT` on prod after the hour); `/c/atraxa-praetors-voice` renders the Atraxa Superfriends tile; a hub with no public decks renders no shelf (unchanged).
9. `/account` tiles: every deck row becomes a tile that KEEPS the edit link titled "Edit {name}", the visibility word, the `DeckFolderSelect` and the "Share page" link; folder sections, Unfiled, "No decks in this account yet", "No bookmarks yet" and the collection section untouched — `smoke:engagement` and `smoke:profile` green on dev are the evidence.
10. G8: the strip is 3 px tall, painted from `ci_mask` before any image loads (an inline `background` on a server-rendered element, never JavaScript), segments in WUBRG order for Magic and Bandai order for One Piece, mask 0 → the neutral swatch, partners OR'd (unit test over a two-leader mask); it never carries text and is `aria-hidden`.
11. Layout at 390, 768, 1024 and 1440 px in both themes: home (hero, the two game cards side by side from `md`, shelves scrolling or wrapping without overflow, tiles in a grid that collapses to one column), `/commanders` list and grid, `/c/` with the tile shelf, `/account` tiles; tile images never shift content (explicit width and height); the hero stays the LCP (text), shelf images lazy except your decision on the first row; the `♥` count and the author byline never wrap into the strip.
12. Accessibility: each tile is exactly one link whose accessible name is the deck name (the strip, the chips and the image are decoration — `alt=""`, `aria-hidden`); the shelves are `<section aria-label>`ed like today's rail; the index toggle is a labelled group with `aria-pressed`; focus rings in the game accent on `data-game` surfaces.
13. Deploy verification via the Vercel commit status on the full 40-char sha; then the prod pass in §Deployable outcome.

Design decisions to make explicitly (disclose + test-pin each)

* `DeckTile` (`src/components/deck/deck-tile.tsx`): one server-renderable component, props built by a pure `deckTileProps(row, printing | null, opts)` helper in `src/lib/decks/tiles.ts` (unit-tested): `href`, `name`, `gameId` + `formatLabel`, `visibility` (owner surfaces only), `updatedAt`, `likesCount` (rendered as `♥ N` only when > 0), `ciMask` → the strip and the pips/dots, `leaderImage: string | null` (the `small` rendition through the `thumbnailUrl` gate — One Piece stays null until row 51), `author` (byline only with a username), optional `actions` (the account's folder select and Share link) and an optional `swatches` for the image slot. The One Piece image slot paints `ambientGradient(adapter.display.colorSwatches(ciMask))` — R2's paint, reused — so a One Piece tile is never a grey box and the row-51 flip only swaps the slot's contents. The compact list option: one persisted key `deckwarden:collection-view` ("tiles" | "list") shared by the home rail, `/account` and the hub shelf through a small client toggle (`Segmented`), SSR default tiles — or no persistence at all (the share page's toggles are not persisted); decide and say why.
* Batched printings: `loadDefaultPrintings(cardIdentityIds) → Map<id, { id, imageOverride }>` beside `loadDefaultPrinting` in `hub/queries.ts` (one `inArray` query on `is_default`), used by the recent rail, the hub shelf, `/account` and the mine route; the Magic shelf joins `card_printings` in its own single query instead (`loadTopCommanders(limit)` — grow `loadLeaderIndex` with a `limit` and a `withPrinting` join, or add the sibling; either way one query).
* Continue building for accounts: home is `force-dynamic`, so the PAGE may read `headers()` and run ONE `decks_owner` query only when a session exists (the guest branch stays the client `YourDecks`); that conditional query sits outside the three-extra budget and is disclosed against §2's line — or the section stays guest-only with "My decks" in the header as the account path. Recommend the conditional query: §2 says "guest claim tokens or account".
* The mine route: an ADDITIVE `leaderImage: string | null` per deck (computed in the route from the batched printings; `deckMetaJson` untouched, every existing field in place) so guest tiles carry images — or guest tiles render the gradient slot only. Recommend the additive field; state it as the one wire change.
* The One Piece shelf query: `loadRecentFinishLeaders(gameId, limit)` in `src/lib/tournaments/queries.ts` — leaders unnested from `tournament_standings.leader_ids` joined to `tournaments` (`game_id`, `start_date`), grouped per leader, ordered by the latest `start_date` then the count, limited, then joined to `card_identities` for name / `external_key` / `colors_mask` / `attrs.life` / `slug` — ONE statement (a CTE or subquery), indexed by `tournaments_game_date` + `ts_by_leader`; its shelf label states the source ("Recent Top finishes · Limitless") because the ordering IS tournament data; the cold-start branch (no rows) renders the leaders in name order with no label, exactly §2's words, and the builder is pure and tested.
* The hero and the game cards: "Your next great deck starts here." as the `<h1>`; the sub-line shortened to one sentence; the "Open beta" pill stays; two `<section aria-label>` cards — "Magic: The Gathering · Commander" (Build → `/decks/new?game=mtg`, Browse → `/commanders`, Search → `/cards`) and "One Piece Card Game" (Build → `/decks/new?game=optcg`, Browse → `/leaders`, Search → `/cards?game=optcg`) — equal width from `md`, each with its shelf; the capabilities strip lists validation, combos (Magic), share pages, sample hands, tournament finishes (both games) — copy only for shipped features, reuse the three `FEATURES` bodies where they still read true; the GitHub line stays. Order per §2: hero → game cards → Continue building → Recent public decks → capabilities strip.
* `ColorChip` (`src/components/color-chip.tsx`): a swatch (Magic: the `--mana-*` pastel with the letter inside, like a pip; One Piece: the frame hex dot) plus an optional visible label, rendered as a `Link` (indexes) or a `button` with `aria-pressed` (`/cards`) through Base UI's `render` prop or two thin wrappers; colorless = `--mana-c` / `OPTCG_COLORLESS_HEX`; the accessible name is always the color's name (`sr-only` when the label is hidden); active state = filled swatch + ring in the game accent; pinned by `color-chip.test.tsx` for both games. The `/leaders` `ColorDots` and the two filter navs are deleted in favor of it.
* The index image view: an `IndexViewToggle` client island (`Segmented`, "List" / "Grid", `aria-label="Index view"`) with `parseIndexView` / `loadIndexView` / `saveIndexView` under `deckwarden:index-view` (view-prefs precedent — a `useSyncExternalStore` null-server-snapshot store like R2's, or the after-mount read; decide by whether the list must be in the SSR HTML — it must, for the smokes and SEO — so the store returns "list" on the server and the grid appears after hydration); the grid: `CardImage` `small` renditions with `frame`, the rank badge in a corner that never covers the artist/© line (top-left, like the editor's id badge — C13's rule), the name below, `ciPipsHtml` — 60 lazy images per page. `/leaders`: hide the toggle until row 51 fires (recommended — 142 r2.dev images is the throttling the row names) and say so in §6; extend row 51's text ("tiles, home shelf images and the /leaders image view") rather than writing a new row.
* `/account` tiles: `DeckTile` with the owner `actions` slot (the folder select and Share link keep their exact behavior and labels); tiles inside the folder sections and Unfiled; the bookmarks list may stay rows (R5b restructures the page) — decide and pin the "No bookmarks yet" string either way.
* Query logging: the env-gated `debug` hook in `createDb` stays only if it costs nothing when unset and is documented in the file's header; otherwise it is a scratch that never lands.
* LATER rows to write only if you defer something real: the `/cards` restyle is a §6 open item (a contract gap, not a LATER row); the `/leaders` grid rides row 51; a "most liked" rail needs likes to exist (no row — the cold-start rule already covers it).

Deployable outcome
`pnpm check` green and deployed; verified on DEV: home in both themes at 390 / 768 / 1024 / 1440 with the five real decks as tiles (strip colors named), the Magic shelf's six names and the One Piece shelf's six names as listed above, Continue building for a guest (one draft minted through the editor and deleted, id stated) and for an account (through the smoke's signed-in fetch), the capabilities strip, the query count proven (three unconditional extras, plus the conditional owner query if taken); `smoke:seo` green after the hero pin update, `smoke:hubs`, `smoke:engagement`, `smoke:profile` and `smoke:collection` green on dev (spaced against the 10/h deck-create limiter — read the counters first); `/commanders` in both views with the preference persisting and the smokes' negative checks holding; `/leaders` with `ColorChip` and the toggle decision applied; `/cards` chips in both games; `/c/atraxa-praetors-voice`'s shelf as a tile with ISR intact; `/account` tiles keeping the folder select and Share link; the route-table diff empty; verified ON PROD signed out with no edits: home (both themes, the five tiles, both shelves, `♥ 1` on the One Piece tile), `/commanders` list + grid, `/leaders`, `/cards` and `/cards?game=optcg` chips, `/c/atraxa-praetors-voice` (ISR — assert existence, freshness on dev). Docs in the same package: `REDESIGN.md` progress tracker R5a ticked with the sha; §6 open items — the hero wording resolved ("proven in place" or why not), the `/cards` restyle gap assigned; the R5a decisions block under §6 (every decision above that deviated from §2, with the measured query count and the shelf orderings); the smoke-pin ledger's R5a row marked done; LATER row 51's text extended; the unwired-primitives list updated if you wired tooltip, skeleton, select or separator (say if not); db:size stated; any minted deck's deletion stated. Nothing posted, seeded, or simulated; no prod deck created; no shelf padded.

Session notes (environment, unchanged from R2 unless noted)

* PATH: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node / pnpm + gh); psql / aws absent — ad-hoc SQL via tsx in `scripts/.tmp/` (gitignored but LINTED; delete only what you add). zsh: quote `--include='*.ts'` in `grep -r`, and a `for` loop that loses `curl` needs `/usr/bin/curl` or an exported PATH.
* `.env.local`'s `DATABASE_URL` is the Neon pooler prod uses: dev fixtures are real rows; the five public decks ARE the home rail on prod too. Never mint on prod. The deck-create limiter counts the pane's creates now (R2) — `deck-create:ip:::1` 10/h and 30/day; the smokes mint several each, so read `rate_limit_counters` before running them and spread them over the hour if the counter is high.
* A dev server may already be listening on 3000 from ANOTHER chat rooted in this checkout (`lsof -nP -iTCP:3000 -sTCP:LISTEN`); reuse it via `preview_start {url: "http://localhost:3000"}` or start `preview_start {name: "dev"}` (`.claude/launch.json`); first hits compile routes (seconds) — poll `/api/health`. `pnpm build` coexists with `next dev`; run `pnpm check` before `pnpm build` (both write `.next`).
* Browser pane: `document.visibilityState` stays "hidden" even after `tabs_select` — Base UI exit animations and toast timers stall, streamed Suspense HTML can sit un-revealed, long scripts hit the 45 s tool timeout (keep pane scripts short); menus open from a PLAIN `click`; React state after a dispatched `KeyboardEvent` needs ~50 ms; lazy images may never load in the hidden pane (assert `complete && naturalWidth > 0`, or scroll them into view); `read_network_requests` buffers ~250 requests; `resize_window` for 390 / 768 / 1024 / 1440 (mobile preset switches the UA — reload after) and reset with `preset: "desktop"`; screenshot geometry under emulation is unreliable in the hidden pane — measure with `getBoundingClientRect`; clear `localStorage.theme`, `deckwarden:appearance`, `deckwarden:index-view` and `deckwarden:collection-view` on both origins when done.
* Prettier reflows scripted-edit anchors: re-grep after `pnpm format`. `*.md` is prettier-ignored.
* Deploy verification: `gh api repos/Bobandis6/deckwarden/commits/<full sha>/status` → the "Vercel" context (R2's deploy took about a minute); `gh run list --workflow=nightly-ingest.yml` for nightlies. ISR is real: `/c/`, `/l/` and `/cards/[id]` lag up to an hour on prod — assert existence on prod, freshness on dev; home, the indexes and `/account` are `force-dynamic`, so their prod checks are immediate. Vercel rewrites the browser-facing `Cache-Control` of cached routes to `public`; the edge state is `x-vercel-cache`.
* `pnpm check` takes about a minute; `smoke:collection` has a budget note (3 users, 1 deck — run it first among the smokes); `smoke:seo` mints fixture decks (dev only).

Context, not tasks
Sequence after R5a: R5b (G3 art-crop banners on R2's resolver, the share artwork header, F5 F8 F12, the `/c/` build CTA firing LATER row 63, the account sections, `/u` and `/f` tiles on your `DeckTile`) → R4 (the responsive builder on R3's panes; R2's ambient layer and chip must survive pane stacking) → R6 (skeletons, the analytics grow-in, the dealt hand, the audit at five widths), each with its own prompt written the way this one was, after the previous package ships. P4.7's standing triggers still exist beside the R-series (the `img.deckwarden.gg` flip — LATER row 51, which also unlocks the One Piece images this package gates —, the Limitless key, feedback, red nightlies) — their own P4.7 round, never a slice of an R-session. Bandai and Azuki permission emails remain unanswered — posture unchanged; One Piece ambient art stays off (REDESIGN.md §3) and the posture line stays on every One Piece surface. Cold-start rule holds everywhere: no simulated decks, posts, or metrics, ever. Premium never gates card data. The owner's open items: the Danger-zone look, the signed-in editor look on `/d/q9j9uxphwp9n`, the Background art toggle in the signed-in header, and — new after R5a — the signed-in home with Continue building and the `/account` tiles. R2's observations stand: a search list scrolled to its very end passes its last row under the credit chip (R4 re-lays the panes), the OP promo Nami P-117 has a null cost (row 61), OP quick-add by card number is name-only (row 72), the 1024 px deck-pane squeeze is R4's. The smoke-pin and LATER-row ledgers in REDESIGN.md §6 remain the map.
