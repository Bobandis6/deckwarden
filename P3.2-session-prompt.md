# P3.2 session prompt

Pull latest, then kick off P3.2 from deckwarden-build-plan.md — M3's second
package (~line 203): the Recommendations panel in the builder's right pane,
rendering the evidence payload — **never a bare "add this"**. The engine and
its dark route shipped in P3.1 (GET /api/decks/[id]/recommendations, zod'd
?budget/?limit, force-dynamic + no-store); this package is a CONSUMER of
that payload — no engine changes unless rendering proves the payload wrong,
and any payload change updates smoke:recommend + the rank tests in the same
commit. Render the full contract per suggestion: why, with (partner card
names), how often, source, confidence — sources visible and confidence shown
honestly (a low is displayed as low, not hidden; that honesty is the
differentiator, so resist the urge to prettify it away). Keep it scannable:
collapsed detail is fine, hidden sources are not. Gate the panel on a
command-zone card — no commander means ciMask 0 and colorless-only noise, so
the honest empty state is "add a commander", and that same gate covers draft
mode (lazy creation: no deck row exists before the first real edit — never
fetch recommendations for a deck that doesn't exist). The add action rides
the existing editor state/autosave path (draft-safe ensureDeck and all) —
no parallel write path. Read the editor's current right-pane layout before
designing; the panel joins what's there (analytics, sample hand), it doesn't
collide with it. Decide and state the refetch policy (debounced after
autosave settles / explicit refresh — not per keystroke), and decide whether
the route needs a rate limit now that UI drives it (it shipped without one;
reads are ~4 batched queries and Neon compute hours are the budget) — a
decision either way, not drift. Budget filter UI: the engine already takes
?budget — ship a minimal control only if it's cheap inside this package,
else LATER with a trigger. Deployable outcome = the panel live in the prod
builder rendering evidence, deployed and check-green, smoke:recommend still
green against prod.

Session notes:
- Toolchain: node/pnpm at ~/.local/node22/bin — non-login shells need
  export PATH="$HOME/.local/node22/bin:$PATH". .claude/launch.json
  (gitignored) wraps pnpm dev for the preview runner.
- Before starting: pnpm check on main AND the nightly Action's recent runs
  (two consecutive greens). gh is NOT installed — GitHub REST API via curl.
- What P3.1 shipped (2026-08-31, commit 8c36524 — not in the plan doc):
  - The seam: scoring machine is core (src/lib/recommend/ — rank.ts pure,
    queries.ts the one batched IO layer, engine.ts orchestrator); game
    knowledge is a declarative `recommend` section on the adapter
    (games/types.ts RecommendMeta; MTG in games/mtg/recommend.ts — its curve
    template ties to the hub roles' 62 nonland slots, test-enforced).
  - Payload: Recommendation { cardId, name, primaryType, costValue, ciMask,
    cheapestUsd, popularity, score, confidence, evidence[] }; evidence
    { source: "edhrec_rank" | "spellbook" | "curve-template", why, with[]
    (cardId+name), howOften | null, confidence: high|medium|low }. Default
    25 results, max 50. Weights 0.45 popularity / 0.35 combos / 0.20 curve.
  - Engine behaviors the UI must reflect, not fight: curve evidence goes
    low-confidence under 10 nonland cards (expected on young decks — render
    it honestly); template-requirement combos say "also needs …" and never
    "Completes"; unknown price never passes a budget; basics and cards
    already in the deck never come back; owned-cards hook stays inert until
    P3.7. Latency is sub-second — a loading state, not a spinner festival.
  - If live-draft recommendations are ever wanted, the engine's own docs
    say: feed a DeckSnapshot through a POST into the SAME engine — never a
    second engine. The commander gate should make that moot this package.
- LATER triggers: the producedMask/role-tags row now names "the first
  package that renders deck-vs-template analytics in the builder — P3.2's
  panel or P3.4 Cut Coach." Per-candidate evidence is NOT deck-vs-template
  analytics — so it fires only if you choose to render a template-comparison
  block; fire it or re-record with reasoning, same discipline as always.
  The Spellbook popularity-floor / findForDeck template-semantics row stays
  scoped to P3.3 Combo Radar — leave it.
- smoke rules against prod: smoke:recommend is new (17 checks; creates and
  deletes its own guest deck; BASE_URL=https://deckwarden.gg pattern);
  smoke:seo 42/43 is a pass (sitemap revalidate lag — any OTHER failure is
  real); smoke:decks includes the notes round-trip; engagement/account
  smokes mint signed sessions directly (BETTER_AUTH_SECRET pattern in
  scripts/engagement-smoke.ts).
- Browser pane (memory file has details): hidden pane swallows computer
  clicks silently — drive via javascript_tool (.click(), native value
  setter + input event), verify via DOM/API reads; tall emulated viewport
  via resize_window for full-page screenshots. This package is UI — expect
  to actually use this.
- Context, not tasks: owner is mid-seeding (featured brews with primers);
  the beta announcement is deliberately deferred; permission emails to
  Bandai + Azuki Labs went out 2026-08-30. Cold-start rule stands — no
  simulated activity, no waiting on content.
- Protocol as usual: one package, ends deployed and check-green; scope
  leaks to LATER.md with a trigger.
