# W1 session prompt — Brand foundation (contract, tokens, the crest, Literata titles)

Pull latest, then run W1 — the first Wave-2 package. **`WAVE2.md` is the contract** (the owner-approved plan, landed 2026-09-19 in the same commit as this prompt). Read it before anything else — all of A, B's "Strengths to preserve", **D0** (tokens, button, type, the mark), the W1 rows of the appendix to E (pin matrix, rollback), F, and **G** (this package). Step 0 of this session adds the docs that make W-packages legal sessions under the CLAUDE.md protocol — build plan §6c, the CLAUDE.md line, the REDESIGN.md addendum, the LATER rows — the way `521dac9` did for the R-series. W1 is the layer every later package stands on: the crest's palette replacing indigo in both themes, the mark redrawn as a vector with two variants, the favicon, and one serif for the wordmark and titles. No layout moves.

Pre-flight, in order. (1) **Nightlies green** (`gh run list --workflow=nightly-ingest.yml`). A red run is P4.7 branch F and preempts everything — W1 waits for its own session. (2) **A warm beta signal outranks a package**: owner posted / feedback / a stranger's 429 / a new issue → run the `P2.9-session-prompt.md` round (MTG) or `P4.7-session-prompt.md` round 3 (OP) as its own session first. (3) Working tree clean at or after the commit that landed `WAVE2.md` and this prompt (`git log --oneline -1 -- WAVE2.md`; its parent is `b67ee0b`). (4) State your baseline: `pnpm check` test/file counts and warnings (781 tests / 105 files / 6 pre-existing `no-unused-vars` warnings / 0 errors, measured 2026-09-19 on `b67ee0b` + these two docs), and `pnpm db:size` (261.2 MB on 2026-09-17, alert 350). (5) If the owner's session message names a serif other than Literata, use it everywhere this prompt says Literata — the swap is one `next/font` line now and much more after W2 builds OG images on it.

## What W1 is NOT (scope fence)

NOT the OG images — no wordmark, crest, or font work in `src/lib/og/*` or the four `opengraph-image.tsx` routes, and **`og.accentGeneric` stays `#a5b4fc`** (`src/lib/theme/tokens.ts:99`) with its two test literals until W2. (The OG *canvas* repaints on its own because `og.bg/fg/muted/panel/raised` are asserted equal to the dark tokens, not to literals — that is expected; spot-check one route.) NOT `manifest.ts`, `apple-icon`, `themeColor`, the `SurfaceHeader` gold rule or the generic band gradient, or the both-theme contrast **audit** — all W2; W1 ships the measured table and fixes only what it breaks. NOT `--font-heading`: it stays `var(--font-sans)` because it feeds `ui/dialog.tsx:108`, `sheet.tsx:100`, `drawer.tsx:188` and `alert-dialog.tsx:104`, which the builder opens, and the builder stays Geist; whether modal titles go serif is W2's recorded decision. NOT the One Piece **dark** accent — `#62d6c5` stays (ocean blue collides with One Piece's own Blue card color `#1565c0` in `src/lib/games/optcg/colors.ts:23-27`, and keeping it means no OG or token-test churn); only the light value moves. NOT any layout, route, schema, API, or feature work — the account menu is W3, the choose-commander flow W4, printings W5. NOT `shadcn add`, NOT a new dependency (Literata arrives through the built-in `next/font/google`; a one-off `pnpm dlx png-to-ico` is a tool run, not a dependency). NOT One Piece ambient art — off until Bandai answers, unchanged. Anything else you notice goes to `LATER.md` with a trigger.

## Facts you inherit (verified 2026-09-19 against `b67ee0b` — re-grep lines, trust the shapes)

- **Tokens.** `src/app/globals.css` is the only CSS file: `@theme inline` at line 7 (`--font-sans` 10, `--font-mono` 11, `--font-heading: var(--font-sans)` 12, `--color-brand` 35, `--color-accent-game` 38), `:root` (light) at 78, `.dark` at 108, the accent rules at 143–154 (`:root { --accent-game: var(--brand) }`, `[data-game="mtg"|"optcg"]`, `[data-game] { --ring: var(--accent-game) }`), the mana block at 198. `src/lib/theme/tokens.ts` mirrors it (`light` 23, `dark` 53, `radius` 85, `og` 91, `accentGeneric` 99, `ogAccent` 106). `src/lib/theme/tokens.test.ts` reads `globals.css` from disk: both blocks **deep-equal** the token objects (exact key set — an extra or missing property fails by name), every value must be lowercase `#rrggbb` (no alpha, no `oklch`, no `color-mix` inside the two blocks), the four accent utilities must be exposed as `--color-*`, and the OG test at 87–91 holds the literals `#b5a2ff` / `#62d6c5` / `#a5b4fc` (its name says "One Piece teal, generic indigo"). `src/lib/og/labels.test.ts:12,21,30` holds the same three. **Edit `globals.css` and `tokens.ts` in the same commit.**
- **Indigo literals live in exactly six files:** `src/app/icon.svg`, `src/app/globals.css`, `src/components/brand-mark.tsx`, `src/lib/theme/tokens.ts`, `src/lib/og/elements.tsx`, `src/lib/og/labels.test.ts`. The last two (and `og.accentGeneric`) are W2's.
- **`primary` as a utility:** `text-primary` only in the `link` variants (`src/components/ui/button.tsx:20`, `src/components/ui/badge.tsx:17`). `bg-primary` at `ui/button.tsx:11` (default variant, `hover:bg-primary/80`), `ui/badge.tsx:11` (default variant, `[a]:hover:bg-primary/80`), `ui/meter.tsx:43`, `ui/progress.tsx:39`, `ui/avatar.tsx:55` (the badge dot), `src/app/global-error.tsx:43` (a hand-styled button). **No component uses a `*-brand` utility today** — brand reaches the UI through `--accent-game` and `--ring`.
- **Button** (`src/components/ui/button.tsx`): the cva base string already carries `border border-transparent … focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50`; `secondary` hovers with `hover:bg-[color-mix(in_oklch,var(--secondary),var(--foreground)_5%)]` — the house idiom for a themed hover. `src/components/ui/button.test.tsx` exists.
- **Fonts:** `src/app/layout.tsx:2,8-16` loads `Geist` and `Geist_Mono` through `next/font/google`, applied at line 50. No local fonts, no `@font-face`, no serif anywhere. The installed docs (`node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md`) say a **variable** Google font takes no `weight` (an array of weights is for non-variable fonts) and extra axes go in `axes`.
- **The mark:** `src/app/icon.svg` (64×64: an indigo shield holding one card) and its hand-inlined twin `src/components/brand-mark.tsx`, whose docblock says the colors are literal on purpose and "Edit icon.svg and this file together". Seven render sites: `site-header.tsx:37` and `editor/editor-header.tsx:88` (`size-7` with the F13 `-rotate-3` hover tilt), `warden-not-found.tsx:22`, `src/app/error.tsx:39`, `src/app/global-error.tsx:34` (`size-14`), `empty-state.tsx:30` (`size-8 opacity-80`), and `deck/validation-panel.tsx:73` (`size-4`, keyed zoom-in — the Warden-approves moment; line 87 is the sentence). `src/app/favicon.ico` is 16 + 32 px. No apple icon, no manifest (W2).
- **Wordmark:** `src/components/site-header.tsx:38` — `<span className="text-sm font-semibold tracking-tight">Deckwarden</span>` inside the home link whose `aria-label="Deckwarden"` (line 34) is pinned by `scripts/hubs-smoke.ts` on the 404 and by `site-header.test.tsx:43` (link named "Deckwarden" → `/`). The header row is `h-14`.
- **Titles:** 19 `<h1>` sites in 17 files (`grep -rn "<h1" src --include='*.tsx'`); `EmptyState`'s title is `src/components/empty-state.tsx:31` (`<p className="text-foreground font-medium">`); the 404/error lines come from `src/lib/warden-copy.ts`, consumed by `warden-not-found.tsx` and `src/app/error.tsx`.
- **Dead files:** `public/` holds exactly `file.svg globe.svg next.svg vercel.svg window.svg` — Create-Next-App leftovers with zero references in `src/` or `scripts/`.
- **The crest is raster only:** `/Users/danielson/Documents/codex/Deckwarden/assets/brand/deckwarden-crest-v2.png` (ivory background; `deckwarden-crest-v2-prompt.md` beside it records the palette: green `#23483c`, gold `#ba9b61`, charcoal `#242c2f`, ivory `#f7f4ee`). `deckwarden-mark.svg` in that folder is the OLD indigo mark. Measured from the PNG (emblem ≈ 575×570 px, origin 478,128): shield peak (766,292), shoulders (590,372) / (943,372), point (766,697); centre card x 643–890 from y 130; side cards ≈ ±20° with outer top corners (480,232) / (1052,232); sword pommel y 340, guard y ≈ 432 spanning x 706–828, blade x 750–782, tip y 652. `WAVE2.md` D0 carries these scaled into a `0 0 96 96` SVG scaffold — a starting point to tune by eye, not final art.
- **Measured contrast (WCAG formula, 2026-09-19):** logo green `#23483c` is 1.84:1 on `#0f1314` (1.69 on card, 1.49 on popover) — it cannot carry text or stand alone as a fill; ivory on it is 9.3:1. Gold `#c9a96a` is 8.34:1 on the dark page, 6.8 on popover, 5.8 on hover, and 4.54:1 on the green; the crest's own gold `#ba9b61` is 7.07 on the dark page and 2.41 on ivory (decorative only there). `#f3f1ea` 16.5:1; dark `muted-foreground` `#a9b0ad` 8.5 (6.9 popover / 5.9 hover); light `#242c2f` 13.0 and `#565e5b` 6.1 (5.2 on hover `#e9e3d6`); light green text 9.3; `#5246cf` 5.6 on muted; `#0a6587` 5.9 on ivory / 5.5 on muted; `#62d6c5` 10.6 on the new dark page; amber-700 on ivory 4.57 (passes, thin). Focus rings at 50 %: gold ≈ 2.95:1, green ≈ 2.57:1 — both beat today's 2.46 / 2.24.

## Verify-first list (never from memory)

1. **The font API** in `node_modules/next/dist/docs/` before writing the loader (AGENTS.md): `Literata({ variable: "--font-literata", subsets: ["latin"], display: "swap", axes: ["opsz"] })`, no `weight`; 600/700 set in CSS. Confirm the build self-hosts the files (no runtime request to Google) and that the header is still 56 px tall with no shift when the font swaps in (`adjustFontFallback` default on).
2. **tailwind-merge order:** the default variant's hairline (`border-gold/70`) must beat the base string's `border-transparent` through `cn(buttonVariants(…))`. Pin it in `button.test.tsx`: the default variant carries the hairline class, `outline` / `ghost` / `secondary` do not, and the hairline survives hover. A primary button that loses its border is invisible on dark.
3. **Token plumbing:** `gold` added to BOTH objects in `tokens.ts` and BOTH blocks in `globals.css`; `--color-gold: var(--gold)` and `--font-display: var(--font-literata)` in `@theme inline`; extend the "accents are exposed as Tailwind colors" test for gold. Rename the OG test at `tokens.test.ts:87` ("teal/indigo") — its One Piece literal is unchanged and the generic literal waits for W2.
4. **Every `primary` utility, re-grepped at your HEAD** (`bg-primary|text-primary|border-primary|ring-primary|fill-primary|stroke-primary`): fills with no label or border move to `bg-brand` (`meter.tsx:43`, `progress.tsx:39`, `avatar.tsx:55`), the two `link` variants move to `text-brand`, `global-error.tsx:43` gets the hairline by hand, and **Badge's default variant** (`badge.tsx:11`) needs a decision — grep its call sites first (the editor's game/format chip uses Badge) and either give it the hairline or move the callers to `secondary`/`outline`.
5. **The crest on real pixels:** `Read` the PNG, then render `BrandMark` on dev at 16 / 28 / 32 / 56 px in both themes and compare. At `size-8` a 2.4-unit stroke is ≈ 0.8 px — thicken until it stops shimmering. The charcoal shield is 1.3:1 on the dark page by design: its gold border is the silhouette. The `shield` variant must read at 16 px (browser tab and the Warden line).
6. **The favicon:** `/icon.svg` and `/favicon.ico` both answer on dev with the new shield; check a Chromium tab and Safari.
7. **The four light-theme fixes R6 made** (placeholders, inactive tabs, the banner credit, tinted chips — `REDESIGN.md` §6, R6 decisions): re-measure each on ivory; they were tuned against `#f7f7fb` and may hold hard-coded shades.
8. **OG spot-check on dev:** one deck OG and one hub OG repaint on the new dark canvas through `og.bg`; the One Piece unfurl stays artless; accents are unchanged (lavender / teal / indigo-generic until W2).
9. **Smokes against DEV only:** `smoke:seo` (mints fixtures — never prod), `smoke:hubs` (the `aria-label="Deckwarden"` 404 pin), `smoke:optcg`, `smoke:engagement` (the `♥` pin). `pnpm counters:reset` clears local loopback deck-create counters if the battery fills them; never retry into a 429.
10. **Route table:** `pnpm build` — `/c/[slug]`, `/l/[slug]`, `/cards/[id]` and their OG routes still `●`; the diff is empty.
11. **Deploy verification** via `gh api repos/Bobandis6/deckwarden/commits/<full sha>/statuses` → context "Vercel"; then the prod pass below (read-only, zero deck creates).

## Design decisions to make explicitly (disclose + test-pin each)

- **Token values — use `WAVE2.md`'s D0 table verbatim.** Dark: background `#0f1314`, card `#171c1e`, popover `#20272a`, foreground `#f3f1ea`, muted-foreground `#a9b0ad`, secondary `#252d30`, muted `#20272a`, accent (hover) `#2a3336`, border/input `#2b3437`, primary `#23483c` on `#f7f4ee`, brand `#c9a96a`, ring `#c9a96a`, gold `#c9a96a`, accent-mtg `#b5a2ff`, accent-optcg `#62d6c5`, destructive `#f87171`, charts `#c9a96a #8b9391 #62d6c5 #b5a2ff #a9b0ad`. Light: background `#f7f4ee`, card `#fffdf8`, popover `#ffffff`, foreground `#242c2f`, muted-foreground `#565e5b`, secondary `#ece7dc`, muted `#f0ebe1`, accent `#e9e3d6`, border `#e0d9ca`, input `#cfc6b2`, primary `#23483c` on `#f7f4ee`, brand `#23483c`, ring `#23483c`, gold `#ba9b61`, accent-mtg `#5246cf`, accent-optcg `#0a6587`, destructive `#b91c1c`, charts `#23483c #6f746f #0a6587 #5246cf #565e5b`. Keep any shadcn token the blocks carry today that the table does not name (card/popover/secondary/accent *-foreground) on the matching foreground. Record any value you change, with its measured ratio, in the REDESIGN.md addendum.
- **Color roles:** gold = brand voice on dark (links, focus, hairlines); green = action (primary buttons; light-theme links); game accents = context only, never card data, never brand. Status hues with no token (amber / emerald) keep their `dark:` pairs.
- **Primary button:** `bg-primary text-primary-foreground border-gold/70`, `focus-visible:border-ring` kept. Decide the hover: today's `hover:bg-primary/80` fades toward the page (dimmer on dark); the house `color-mix(in_oklch,var(--primary),var(--foreground)_12%)` idiom lightens on dark and darkens on light. Pick one, say why, pin it.
- **The mark API:** `BrandMark({ variant = "crest" | "shield", … })`, `aria-hidden` default unchanged, colors literal (`#23483c`, `#ba9b61`, `#242c2f`) so it is identical in both themes; rewrite the docblock (the indigo shield is no longer "the brand"). `crest` at `size-8` in both headers (up from `size-7`; the row stays `h-14`), `size-14` on the 404/error pages, `size-8 opacity-80` in empty states; `shield` at `validation-panel.tsx:73`, in `icon.svg`, and in `favicon.ico`. Keep F13's hover tilt. Edit `icon.svg` and the component together.
- **The favicon path:** regenerate `favicon.ico` (16 + 32) once from the shield SVG with `pnpm dlx png-to-ico` (a tool run, no dependency), or — if you would rather keep binaries out — delete it and add an `icon.tsx` PNG through `ImageResponse`. Disclose which.
- **Where serif goes:** the `font-display` utility on the wordmark (18 px / 600), the 19 `<h1>` sites and the site-page section `<h2>`s at their existing R1b sizes (30/36 and 18 — nothing shifts), `empty-state.tsx:31`, and the Warden lines (`validation-panel.tsx:87`, `warden-not-found.tsx`, `error.tsx`). Nothing inside the builder panes; not the editable deck name; not tables, chips or buttons. Keep `tracking-tight` unless a title looks cramped in the dev pass — say where you loosened it.
- **Small details:** `::selection` = gold at 30 %. Delete the five dead `public/*.svg` files (re-verify zero references first). Nothing else.

## Deployable outcome

Step 0 first, in its own commit (`WAVE2.md` is already in the repo with its progress tracker — do not re-land it): build plan §6c (the W-series table, mirroring §6b); one CLAUDE.md line under Session protocol — "Wave-2 packages (Wn from `WAVE2.md`, build plan §6c) count as work packages under the same rules"; a REDESIGN.md addendum recording that **W1 supersedes** §1's "the brand is the indigo" and "Typography stays Geist and Geist Mono" and the `BrandMark` docblock, and that **W4 will supersede** §2's "Choose commander / leader, which focuses search"; and `WAVE2.md`'s section F as `LATER.md` rows with their triggers (rows 22 / 66 / 80 get cross-references, not rewrites).

Then W1: `pnpm check` green and deployed; verified ON PROD in **both themes**, read-only, zero creates: the header (crest beside a serif "Deckwarden"), the browser tab (shield), home, one `/c/` hub and `/l/enel-op15-058` (focus rings lavender / teal by surface; gold on generic pages), a card page, a deck share page, the editor drafts `/decks/new?game=mtg` and `?game=optcg` (primary buttons readable with the hairline, the completion ring and avatars visible, the Warden line's 16 px shield on a legal deck's share page, the `?` shortcut sheet still Geist-titled), and the 404. A grep for the eight indigo hex values plus `indigo` over `src/` returns hits only in OG code reserved for W2 (`og.accentGeneric` with its two test literals, `src/lib/og/*`). Docs in the same package: `WAVE2.md` tracker W1 ticked with the sha; a dated ship-note; any deviation from D0 recorded in the REDESIGN.md addendum with measured ratios; memory updated; **`W2-session-prompt.md` written** the way this one was. Route-table diff stated (must be empty). db:size stated. Nothing posted, seeded, or simulated.

## Session notes (environment, unchanged from P3.11 unless noted)

- PATH per command: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node/pnpm + gh); psql/aws absent — ad-hoc SQL via `pnpm exec tsx` in `scripts/.tmp/` (tsconfig-excluded but LINTED; `.env.local`'s DATABASE_URL is prod's pooler; delete scratch when done). W1 needs no SQL.
- Dev server: `preview_start {name: "dev-log"}` after `lsof -nP -iTCP:3000 -sTCP:LISTEN` (a server on 3000 is reused). `next dev` re-adds the AGENTS.md block — commit it with your work.
- Browser pane: a hidden pane pauses rAF, never fires `matchMedia` change events, stalls Base UI exits/toasts, and tabs do not hydrate until painted — verify via DOM reads (`getComputedStyle`, `document.documentElement.classList`) rather than screenshots; 1024 / 1440 emulation screenshots fail (measure instead); reload after resizing. The pane is signed out on prod; the guest editor pass is `/decks/new?game=`, which mints nothing until a first edit.
- `react-hooks/refs` and `react-hooks/set-state-in-effect` are lint ERRORS in this tree (W1 should not need either).
- Prettier reflows scripted-edit anchors: after `pnpm format`, re-grep before exact-string edits. `*.md` is prettier-ignored, so docs cannot fail the format check.
- Prod: curls need `--compressed`; ISR hubs read MISS twice post-deploy before HIT; OG routes revalidate at 86400, so old indigo-canvas unfurls linger up to a day — assert OG freshness on dev, existence on prod. Deck-create budget 10/h + 30/day per IP: this session creates no prod decks; only `smoke:optcg` may ever run against prod.
- Deploy verification = the GitHub commit status (context "Vercel") on the full 40-char sha.

## Context, not tasks

Sequence after W1: W2 brand completion (OG wordmark + font, manifest, apple icon, `themeColor`, the both-theme audit, the serif-modal-titles decision) → W3 account menu + deck tile actions → W4 choose-commander flow → W5 card-page printings + the printings API → W6 editor printings → W7 buy links → W8a/b precons → W9a/b/c autofill → W10 tournaments; each gets its own prompt written the way this one was, after the previous package ships. Owner decisions recorded 2026-09-19 and not to be relitigated: buy links ship plain (the affiliate env var stays EMPTY on Vercel Hobby); precons are a separate, ownerless official library kept out of community rails; "random deck" is an evidence-based starter shell with no role inference; the brand is a full re-key with dark still the default. P2.9's and P4.7's standing triggers still exist beside the W-series (the owner posting, feedback, `img.deckwarden.gg`, the Limitless key, red nightlies) — they get their own round, never a slice of a W-session. Bandai and Azuki permission emails remain unanswered — posture unchanged. Cold-start rule holds everywhere: no simulated decks, posts, or metrics, ever. Premium never gates card data. The pin matrix and the `kind` inventory in `WAVE2.md`'s appendix to E are the map for every later package.
