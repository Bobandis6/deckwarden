# W7 session prompt — Buy links (plain; affiliate env stays empty on Hobby)

Pull latest, then run W7 — the seventh Wave-2 package. **`WAVE2.md` is the contract**; read its W7 section (steps 1–4 are the build order), D6 (the ASCII spec — it IS the design), the W7 row of the pin matrix. W1–W6 are live (`e40342d`, `ce9d7c2`, `0da07cc`, `077401a`, `34a5d41`, `2c8a755`). W7 turns "Copy decklist, open TCGplayer, find Mass Entry, paste" into one menu: real links to TCGplayer Mass Entry from the share page, the editor, and the card pages — **plain links only** (Vercel Hobby is non-commercial; `NEXT_PUBLIC_TCGPLAYER_PARTNER_BASE` stays empty, and everything affiliate-shaped is dark until the env var exists).

Pre-flight, in order. (1) **Nightlies green** (`gh run list --workflow=nightly-ingest.yml`). The REC-1 backfill already ran (09-20 nightly, `defaults_cleared`/`set` 2,825) — a rank-rule marker in later stats is steady state, not drift. A red run preempts everything. (2) **A warm beta signal outranks a package**: owner posted / feedback / a stranger's 429 / a new issue → run the `P2.9-session-prompt.md` round (MTG) or `P4.7-session-prompt.md` round 3 (OP) as its own session first (2026-09-20 census baseline: 25 decks — 24 MTG + 1 OP, the owner's — 1 user, likes 1 · bm 0 · folders 1). (3) Working tree clean at or after W6's docs commit (feat is `2c8a755`). (4) State your baseline: **843 tests / 111 files / 6 pre-existing `no-unused-vars` warnings / 0 errors** (measured 2026-09-20 on `2c8a755`), and `pnpm db:size` (263.2 MB on 2026-09-20, alert 350).

## What W7 is NOT (scope fence)

NOT affiliate anything (the env var stays EMPTY in every Vercel env — plain links, no `rel="sponsored"`, no disclosure lines render; the wiring exists but is dark), NOT a server-side TCGplayer call (no Deckwarden server ever contacts TCGplayer — links and copy only), NOT exact-printing buy links (`tcgplayer_id` is a LATER row with its own trigger), NOT a Card Kingdom integration (LATER row 22), NOT a shopping-list CSV export (that's the Collection Mode row; W7's "Only cards I'm missing" menu item partially fires it and the LATER row already says so), NOT One Piece buy links (the adapter declares no `capabilities.buy` → the feature is invisible there, with no apology copy). Anything else → `LATER.md` with a trigger.

## Facts you inherit (verified 2026-09-20 against `2c8a755` — re-grep lines, trust the shapes)

- **The capability seam**: `GameAdapter.capabilities` at `src/lib/games/types.ts:508` currently declares `ambientArt?` and `combos?` — add optional `buy?: { vendor: string; productLine: string; massEntryLine(card, qty): string; cardUrl(card): string; skipByDefault?(card): boolean }` beside them (declarative + pure functions, the adapter contract; core renders only off the declaration). One Piece declares nothing.
- **MTG specifics** (**(new)** `src/lib/games/mtg/buy.ts`): `MtgAttrs.mana_cost` is optional top-level (`src/lib/games/mtg/attrs.ts:12`) and there is NO `layout` field — the split-card test is `mana_cost` containing `" // "` (keep the FULL name then; else the front-face name, i.e. everything before `" // "` in `card.name`). `skipByDefault` = `type_line` contains "Basic" (D6's "Without basic lands").
- **Link builder** (**(new)** `src/lib/buy/links.ts` + tests): `massEntryUrl(lines)` → `https://www.tcgplayer.com/massentry?productline=Magic&c=<encoded "1 Sol Ring||33 Mountain">` (verify the live URL shape in a browser FIRST — don't trust this line); `deckBuyOptions(entries, cards, owned?)` returns the three D6 items with live counts ("Only cards I'm missing" only when `owned` is present); `withPartner(url)` reads `NEXT_PUBLIC_TCGPLAYER_PARTNER_BASE` (add to `.env.example` with the MUST-STAY-EMPTY-on-Hobby comment). The ≈6,000-char limit is measured on the FINAL url (after any partner wrap); over it → copy the list to the clipboard, open bare Mass Entry, toast "List copied — paste it into Mass Entry" — a first-class path, not an error.
- **Share page slot**: the action row is `deck-share-view.tsx:304-309` (`EngagementButtons` + `ForkButton` + Copy decklist + the owner's Open in editor); `DeckShareView` already receives `owned` (`:128`, prop doc `:140-141`). **(new)** `src/components/deck/buy-deck-menu.tsx` lands in that row. D6 menu footer label: "Opens TCGplayer Mass Entry in a new tab · prices via Scryfall, updated daily."
- **Editor slots**: the More menu is `editor-header.tsx:126-148` — "Buy this deck…" goes in it, and **the exact menu array is pinned at `editor-header.test.tsx:129`** (`["Details", "Import", "Export", "Keyboard shortcuts?"]` — the pin matrix says update it). The card pane footer (`card-detail-pane.tsx:159-165` after W6, the "from $X.XX · Card page →" line) takes "Buy ↗" — D6 placed the slot between the price and the card-page link; W6's Printings collapsible sits directly below, don't disturb its trigger.
- **Card page slot**: "Buy on TCGplayer ↗" under the hero caption — the caption block is `PrintingsHero` in `src/app/(site)/cards/[id]/printings-gallery.tsx:353-377` (client), but D6 wants the link built server-side from the build-time env so `/cards/[id]` stays ● (ISR-safe: the env is inlined at build). Simplest honest shape: pass the ready URL (or null) through the page → provider/hero as a prop, or render it in the server caption area beside the gallery — decide and disclose. The phone drawer's empty buy-slot footer (`printings-gallery.tsx:609`, the W5 comment "W7's buy slot lands here") NOW gets its real link.
- **Legal + LATER**: `/legal` is `src/app/(site)/legal/page.tsx` — add the "Buying cards" paragraph (plain-links version; the affiliate disclosure sentence only renders when the env var is set). LATER: the affiliate row (currently line 78, "Affiliate buy links (TCGplayer via Impact…)") has its trigger; the buy-half of the Collection-Mode row (line 66) partially fires via "Only cards I'm missing" — update both rows' text, referencing by CONTENT not line number (rows shift).
- **Pins that stay green**: `seo-smoke:374` (OP card pages carry no `>USD<`) — OP surfaces must not grow buy links or price text; the © line near art untouched.
- **W6 lessons that bite here**: the RTL fetch mock returns plain objects — `await`-style fetch code works, `.then()` chains crash; Base UI menu triggers in RTL open on the pointer sequence (`editor-header.test.tsx:37`'s `open()` helper); the pane's footer line is inside the same `<p>` flex — keep the row's justify-between balance.

## Verify-first list (never from memory)

1. **The Mass Entry URL shape against the real site** in the browser pane BEFORE writing `links.ts`: paste a two-line list (`1 Sol Ring`, `33 Mountain`) into tcgplayer.com/massentry manually, then confirm a constructed URL round-trips to a pre-filled form. Record the exact param names in the ship note. If the URL scheme differs from the contract's sketch, the LIVE site wins.
2. **`/d/db5d4d5843ps` acceptance** (the owner's 100-card deck): Whole deck lists 100 with "33 Mountain" as ONE line; Without basic lands omits it and the count matches; a mono-color deck, a five-color DFC deck, and a split/adventure deck all parse ≥ 95% of lines on the real form (record misses; adjust `massEntryLine`).
3. **"Only cards I'm missing (N)"**: renders ONLY for a signed-in viewer with a collection; N = deck size − the page's "You own N/100" count (share view math, not a new query). Signed-in flows are RTL-only (the pane is signed out everywhere — W3 precedent); the RTL test drives `owned`.
4. **Long-deck path**: force a URL over the limit (99 copies × long names in a scratch deck list — pure function test, no deck row needed) → copy-list + bare Mass Entry + toast. The length check runs on the FINAL url.
5. **Env-dark proof**: with the var unset (every real env), grep the built HTML/JS output for `sponsored` and the disclosure line → absent; set it locally → links wrap, `rel="sponsored"` appears, disclosure renders in menu footer + site footer + /legal. `.env.example` documents MUST STAY EMPTY on Hobby.
6. **`/cards/[id]` stays ●** in the build route table after the buy link lands (build-time env, no runtime read on the ● path); the editor and share client bundles don't grow a TCGplayer fetch (links only — check the network tab: zero requests to tcgplayer.com from page load).
7. **OP surfaces unchanged**: no Buy anywhere on OP pages/editor (`capabilities.buy` undeclared), `smoke:optcg` + `seo-smoke:374` green.
8. **The More-menu pin**: `editor-header.test.tsx:129`'s array updated to include "Buy this deck…" exactly where it lands; create-count tests untouched.
9. **Smokes on dev**: `smoke:seo`, `smoke:optcg`, `smoke:hubs` (+ `counters:reset` if the battery fills local counters). Deploy via the GitHub commit status — context "Vercel", **the sha from `git rev-parse HEAD`, never retyped**. Prod pass read-only.

## Design decisions to make explicitly (disclose + pin each)

- **Where the card-page link is built** (server caption vs prop into the client hero) and how the phone drawer's footer gets it.
- **The menu component** (reuse `ui/dropdown-menu.tsx` in the share row? The share page is an RSC with client islands — the buy menu is a new client leaf) and its a11y name.
- **Clipboard fallback** when `navigator.clipboard` is unavailable (the copy-list path must still say something honest).
- **What "Buy ↗" opens in the editor pane** (the single-card TCGplayer page via `cardUrl` — name search vs product line; disclose the URL shape).

## Deployable outcome

`pnpm check` green and deployed; verified ON PROD read-only (no deck writes on prod; the acceptance clicks run against prod's PUBLIC share page — links open TCGplayer, nothing writes). D6 complete: share-row menu with three real items + footer label, editor More item + pane "Buy ↗", card-page + drawer links, over-long copy-list path, /legal paragraph; everything affiliate-shaped dark with the env var absent. `editor-header.test.tsx:129` updated; `links.ts` + `mtg/buy.ts` fully unit-tested (split cards, basics skip, the length cliff, partner wrap). Docs in the same package: `WAVE2.md` tracker W7 ticked with the sha; a dated ship-note in this file; LATER affiliate + collection rows updated; memory updated; **`W8a-session-prompt.md` written** the way this one was (it's the first W-package with a migration — carry the eyeball-the-SQL rule forward). db:size stated. Nothing posted, seeded, or simulated.

## Session notes (environment, updated by W6)

- PATH per command: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node/pnpm + gh); psql/aws absent. Census via a scripts/.tmp tsx probe (dotenv + postgres, the db-size.ts pattern; `decks.game_id` is a SMALLINT — GAME_ID.mtg=1/optcg=2 from `src/db/seed-data.ts`; the users table is `users`, not `user`); DELETE any probe after.
- **Dev shares the ONE Neon DB with prod.** If a QA deck is needed (W7 likely needs none — share-page clicks run on prod read-only, mono/five-color test decks can be the OWNER's existing public decks), mint ONE guest deck on dev, delete it by id (`user_id is null` guard), re-prove census 25 before closing.
- Dev server: `preview_start {name: "dev-log"}` after `lsof -nP -iTCP:3000 -sTCP:LISTEN`; stop it before `pnpm build` (shared `.next`). `next dev` re-adds the AGENTS.md block — commit it with your work if it appears.
- Pane quirks (W6-confirmed): the native-value-setter + `dispatchEvent(new Event('input', {bubbles:true}))` trick reaches React state; element `.click()` from `javascript_tool` drives React fine for buttons/rows; `navigate` may land on `/` — assert `location.pathname` before driving; the browser pane defaults to the md tier (Tools drawer), so pane checks need the drawer opened via an explicit card click first.
- Theme flips for dev screenshots: `localStorage.theme = "light"` (next-themes' key; `deckwarden:appearance` is backgroundArt only). Clean it up after.
- Prod curls need `--compressed`; deck-create budget 10/h + 30/day per IP. `x-vercel-cache: HIT` proves edge caching (client Cache-Control is rewritten to `public`).
- Prettier reflows scripted-edit anchors after `pnpm format`; `*.md` is prettier-ignored.

## Context, not tasks

Sequence after W7: W8a/b precons → W9a/b/c autofill → W10 tournaments. P2.9's and P4.7's standing triggers still exist beside the W-series and get their own round, never a slice of a W-session. Owner decisions of 2026-09-19 stand (plain buy links was one of them). Bandai and Azuki emails unanswered — posture unchanged. Cold-start rule holds: no simulated decks, posts, or metrics. Premium never gates card data or prices (the buy menu links OUT; it never gates). The affiliate day belongs to the VPS move (LATER), not to W7.
