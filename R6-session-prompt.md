# R6 session prompt — Polish and accessibility (C9 `loading.tsx` skeletons, G6 the analytics bars, G9 the curve ghost, F4 the dealt hand, the `/cards` restyle, the six-width audit in both themes, reduced motion, contrast, focus order, touch targets, status never color-only, no layout shift from images, home LCP unchanged)

Pull latest, then run R6 — the eighth and LAST R-series package: the closing pass that audits everything the seven before it built. **`REDESIGN.md` is the contract (read §1 Mechanics — the Motion policy line 73 and the Breakpoints line 75; §2 "Card search" under "Browsing, sharing, and account pages" — the `/cards` restyle R5a assigned here; §4 in full — "Interaction language" (207), "Signature fun moments" (213: the dealt hand's stagger, Keep and Mulligan as a game prompt, no draw-next) and the Polish catalog (223) rows C9 (237), G6 (249), G9 (252) and F4 (256); the R6 row in §5 (284) with its "done when" — every check passes; home LCP unchanged or better; §6 — the R4 block (418) for the pane's limits and the `pointer-coarse:` rules already in place, the R2 block for the ambient layer, the R3 block for the count-up and the Warden line, the smoke-pin ledger (the R4 row 319) and the LATER-row ledger (the R4 row 308; "row numbers are historical identifiers" applies); §7 in full (441) — the Functional checks (443) and the Visual and accessibility checks (458) ARE this package's checklist)**; build plan §6b makes R-packages legal sessions under the CLAUDE.md protocol. R6 delivers four catalog items and one restyle, then measures the whole product: `loading.tsx` skeletons for the hub, leader-hub, card and deck pages (C9); analytics bars with a grow-in and their value labels (G6); the editorial target-curve ghost behind the Magic curve (G9); the sample hand dealt from a stack with a stagger and a Keep / Mulligan prompt (F4); `/cards` with grouped filters, visible selected filters and a loading state; and the audit at 390 / 768 / 1024 / 1200 / 1440 / 1920 px in both themes with long names, populated and empty decks — keyboard access and focus order, dialog focus, touch targets of at least 44 px on every interactive control, reduced motion disabling every decorative animation, text contrast per §1, status never color-only, artwork and thumbnails never shifting content, home LCP unchanged. Expect ZERO migrations, ZERO new dependencies, ZERO new URLs (a `loading.tsx` is a segment file, not a route — the route table diff must be EMPTY: 55 glyph lines, `/c/[slug]`, `/l/[slug]`, `/cards/[id]` still `●`, `/d/[publicId]` still `ƒ`), ZERO API or wire changes (the analytics blocks never cross the wire — they are computed in-process by the adapter on both surfaces — so G9's one additive optional field on the histogram block is an adapter-contract addition, not a wire change; say so), no ingest or data work, the keyboard script unchanged, the first real edit still creating exactly ONE deck. The owner's decisions are settled: the sequence ends with R6; the restrained fun ceiling (no confetti, sounds, streaks); One Piece ambient art off until Bandai answers; mascot = shield mark only; no draw-next (LATER's goldfish row). Do not relitigate them.

Pre-flight, in order. (1) Nightlies since 2026-09-12 all green (`gh run list --workflow=nightly-ingest.yml` from the shell — `gh` needs HOME; cron 10:37 UTC drifting ~13:30–17:40Z; 2026-09-11's ran 14:35Z green, 7m0s — the first on R5b code; 2026-09-12's is the FIRST on R4 code, which changed no ingest and reads nothing the ingest writes differently, so a red run is P4.7 branch F and takes precedence: fix or report it as P4.7, and R6 waits). (2) Working tree clean at or after `d4ccf1f` (the R4 docs commit; R4's code is `e77ff0f`) plus this prompt's commit. (3) db:size baseline stated (254.7 MB on 2026-09-12 after R4's pass; Neon alert 350 MB) — R6 adds no rows by hand; the pane's drafts are the only rows you mint, every one deleted WITHIN the session (the pane's browser profile does not survive a session gap — R4 lost a claim token that way and had to delete its draft by SQL; do not repeat that: delete before you stop) and confirmed gone by query. (4) QA fixtures, read from the DB (R4's readout at `d4ccf1f`): 17 decks — the FIVE public decks (`/d/q9j9uxphwp9n` Queza, MTG, 95 rows; `/d/uwvrnv2pv4t6` Atraxa Superfriends, 86; `/d/4c5_9yd9ewdy` BOROS EQUIPMENT BURN, 87; `/d/end97r37znuv` Nelson & Murdock, 84, the one FILED deck in folder `veunykz3yuje` "custom decks"; `/d/jhr5ax43ewx7` "Six DON!!, Endless Thunder", One Piece, Enel OP15-058, 16 rows, the only like), the owner's FOUR unlisted "Untitled" decks (`2un8xrmq5wrs` 3 rows, `avzq42djrda7` 1, `28vsuc6j54pf` 1, and `84z28acfydce` 1 — NEW, created signed-in on 2026-09-12 01:58Z from a residential IP: the owner's own use of the editor; never yours), EIGHT anonymous walk decks (four private — `mb5qj7b2z_4b`, `vxe4uxvn_beq`, `ek66d5trhsgc` "P1.3 Eyeball Deck" 82 rows, `_zy3sf8bvdr_` — and four unlisted — `7sm_66hjm3g4` "Atraxa Superfriends" 14 rows, `pshzt7a6pwjb`, `86vxk_qvpaj3`, `_z2s3e78y8w8`; R4's prompt miscounted them as seven), 1 user (`bobandis6`), 1 like, 0 bookmarks, 1 folder, 0 collection rows — never yours to touch. The pane is signed out on dev AND prod and holds no token for any of them, so **its editor fixtures are the drafts it mints itself** (`/decks/new?game=mtg`, `/decks/new?game=optcg&leader=OP15-058`, and — for a Magic draft with ambient art and a leader WITHOUT a create — `/decks/new?game=mtg&leader=7e6b9b59-cd68-4e3c-827b-38833c92d6eb`, Atraxa's oracle id); the History and Share dialogs need a server row, so one draft gets one edit (one POST) and is deleted through Details → Delete deck… when you are done with it. State whether the owner did any of the open looks (the phone builder on a real device is R4's; R5b's list stands): the only evidence is `auth:…|/get-session` rows and the sessions table — the owner's session refreshed 2026-09-12 01:56Z and the deck above followed two minutes later, so they used the signed-in editor on R5b code; which pages, unknown. (5) The deck-create limiter (10/h + 30/day per IP; `deck-create:ip:::1` on dev; read `rate_limit_counters` BEFORE the pass — at `d4ccf1f` the local hour window 2026-09-12T03:00Z read 2 and the day 2, both `smoke:seo`'s; the owner's own `24.10.x.x` rows are theirs) and the mints planned: your drafts (one create at most), `smoke:collection` 1 (run it first — its budget note), `smoke:decks` 2, `smoke:optcg-deck` 1, `smoke:versions` 2 + 5 forks (7 — give it a fresh hour window: R4 waited for the top of the hour), `smoke:seo` 2 (LAST — it DELETES the local `deck-create:*:::1` counters at its start). (6) `pnpm check` green at the start (704 tests in 97 files at `d4ccf1f`, the same 6 pre-existing `no-unused-vars` warnings, 0 errors) and `pnpm build`'s route table saved before and after (python over the build log, the R5a → R4 method; 55 lines including the legend): the diff must be EMPTY. No `shadcn add` (`skeleton.tsx` exists); never `shadcn init`.

What R6 is NOT (scope fence)

NOT a feature package: NOT card-stacks (LATER's card-stacks row, line 61 today), NOT multi-step undo (60), NOT quick-add by One Piece card number (62), NOT the goldfish playtester or draw-next (14 — F4 keeps the hand a static widget), NOT ⌘K, draggable panels, extra theme families, a Warden illustration, animated ambient backgrounds. NOT a change to the keyboard script, the search reducer, the autosave engine, `ensureDeck`, `applyEdit`, the toast + Undo, the hotkey guard, the tier model (`use-tier.ts`), the pane structure (`editor-layout.tsx`'s tree — you may add classes and sr-only text, not move panes), or any API (`git diff --stat src/app/api src/lib/decks/editor-state.ts src/lib/decks/search-state.ts src/components/editor/use-autosave.ts src/components/editor/use-editor-hotkeys.ts src/components/editor/use-tier.ts` is empty). NOT the OG images (`src/lib/og/*` and the three `opengraph-image` routes change by zero lines; the deck / hub / card OG byte counts on dev are your yardstick — R5b measured 987,068 / 1,029,991 / 1,025,164 for `/d/q9j9uxphwp9n`, `/c/atraxa-praetors-voice`, `/cards/54125e08-12ec-435c-b147-91768ee5007d` at its code; measure yours before and after). NOT a new persisted preference. NOT the `md` drawer's width (LATER's md-drawer row, line 72 — R4's, trigger-gated). NOT `global-error.tsx`. NOT a Next, React, Tailwind or Base UI upgrade. NOT any P4.7 branch unless pre-flight (1) forces it, and then R6 is not this session. NOT new smoke pins on the editor (it is client-rendered) — and every existing pin stays (the list under Facts).

Facts you inherit (verified 2026-09-12 against `d4ccf1f` — trust these, spot-check nothing)

* LATER row numbering. "Row N" labels are HISTORICAL identifiers; today's lines: the goldfish row 14, the r2.dev row 51, multi-step undo 60, card-stacks 61, OP quick-add 62, OP null-cost 63, the claim nudge 64, the `/c/` CTA row (FIRED in R5b) 65, the R1b ISR row 71, the md-drawer row (R4) 72. Name rows by content. R6 fires nothing; it may add a row only for something real it defers with a trigger.
* C9's premise is WRONG in one word: "the `shimmer` utility already ships" — no `shimmer` exists anywhere (`grep -rn shimmer src node_modules/shadcn` is empty). What ships is `src/components/ui/skeleton.tsx` (13 lines): `<div data-slot="skeleton" className="animate-pulse rounded-md bg-muted">` — `animate-pulse` is NOT `motion-safe:` (the globals backstop stops it under reduced motion by collapsing its duration; make it `motion-safe:animate-pulse` in the primitive — one word — and say so). NO `loading.tsx` exists under `src/app`. The four pages that get one: `src/app/(site)/c/[slug]/page.tsx` (421 lines; `revalidate = 3600` 77, `generateStaticParams` → `[]` 84; `<main className="max-w-browse mx-auto w-full flex-1 px-4 py-8" data-game="mtg">` 144; `<SurfaceHeader art={art} className="mt-4" />` 158; the hero `CardImage … priority` 166), `src/app/(site)/l/[slug]/page.tsx` (384; 79 / 86; main 151; header 164; priority 172), `src/app/(site)/cards/[id]/page.tsx` (335; 36 / 43; main 157; `<SurfaceHeader className="mt-4 h-28 sm:h-36 md:h-44" />` 170; priority 178) and `src/app/(site)/d/[publicId]/page.tsx` (157; `force-dynamic` 39; the client view is `src/components/deck/deck-share-view.tsx`, whose `<main` is at 257 and whose loading main — "Loading…" — is at 211). `SurfaceHeader` (`src/components/surface-header.tsx`): the band `relative h-36 overflow-hidden rounded-2xl sm:h-44 md:h-56` (50) with the banner `loading="eager" fetchPriority="low" … object-cover` (64–67); the deck page's band is `h-32 sm:h-40 md:h-48` (R5b block). A `loading.tsx` is a segment-level Suspense fallback: on a client navigation it shows instantly while the RSC payload loads; on a dynamic render (`/d/`) it streams first and the content follows in a later chunk; on an ISR HIT it never shows. Because the streamed HTML then carries the fallback AND the content, every HTML-reading smoke keeps working only if its pins are substring checks — they are (below) — but you re-run them all. The editor routes get NO skeleton (client-rendered; the "Loading deck…" main in `deck-editor.tsx` stays).
* G6 / G9 — the renderer is `src/components/deck/analytics-blocks.tsx` (180 lines), "analytics are data, not components": `Histogram` (22–49) draws each bucket as a column — the value label ABOVE the bar (30–32, blank when 0), an `h-16` track with `border-b` (33), the bar `w-full rounded-t-sm` (35) with an inline `height` at `max(value / max × 100 %, 5 %)` and `background: var(colorVar ?? --chart-2)` (36–40), the bucket label below (43); `Breakdown` (51–75); `STAT_TONE` (77–81: `ok` emerald / `warn` amber / `bad` destructive — the tone is COLOR ONLY on the stat's number, 87–91); `DataTable` (97–130); `AnalyticsBlocks` (133–156 — the share page's direct consumer, `deck-share-view.tsx` 44); `AnalyticsPanel` (164–180, the editor's Collapsible, `pointer-coarse:min-h-11` on its trigger since R4). The block type (`src/lib/games/types.ts` 130–151): histogram `{ kind; id; title; buckets: { label; value; colorVar? }[] }` (132–135), stat `tone?: "ok" | "warn" | "bad"` (149). The Magic curve block: `src/lib/games/mtg/analyze.ts` counts nonland cards by mana value into eight buckets 0…7+ (50–82) and emits `{ kind: "histogram", id: "mana-curve", title: "Mana curve", buckets }` (90–95); One Piece emits `id: "don-curve"` (`src/lib/games/optcg/analyze.ts` 273–274). The editorial target: `MTG_CURVE_TEMPLATE = [2, 8, 13, 13, 10, 7, 5, 4]` (`src/lib/games/mtg/recommend.ts` 22 — nonland, sums to 62 = the hub template's 99 slots minus 37 lands, enforced by a test against `hub.roles`), `mtgCurveBucketOf` (25–28), exposed on the adapter as `recommend.curve = { source: "curve-template", buckets, bucketOf, evidence }` (56–65; the type at `types.ts` 289–295; the source label "Curve template" at 188). Its ONLY consumer today is the Cut Coach (`src/lib/recommend/cuts.ts` 180–280); no component reads it, and the One Piece adapter declares no `recommend` at all (`tabbed` false in the editor).
* F4 — `src/components/deck/sample-hand.tsx` (121 lines; shared by the editor's deck pane / the phone's Tools tab and the share page): a Collapsible open by default (51–60); the controls `Button variant="outline" size="sm"` — "Draw sample hand" (64–66) before a draw, "New hand" and "Mulligan" (69–74) after, the `aria-live` counter "After N mulligan(s)" and the short-library note (75–79); the hand `ul.mt-3 grid grid-cols-4 gap-1.5 sm:grid-cols-7` (84) of `CardImage` 146 × 204 `rounded-[4.75%/3.5%]` (89–96) inside preview buttons (102–109) keyed by slot (100); `drawHand` / `buildLibrary` in `src/lib/decks/sample-hand.ts` (pure — do not touch the draw). No stagger, no prompt, no Keep exists. R4 left its three buttons at 28 px on coarse pointers (deferred to you), and R4's phone Tools tab remounts the widget on a 768 px crossing (a dealt hand resets — accepted).
* The `/cards` page: `src/app/(site)/cards/page.tsx` (85 lines; dynamic — reads `searchParams` for `game`, `q` / `name`, `type`, `ci` / `color`, `traits`; canonicals `/cards` and `/cards?game=optcg`; `<main className="max-w-wide … data-game={game}>` 51, `<h1>Card search</h1>` 52, `GameSwitch` 55–59, `CardSearch` 61–70, the Scryfall / posture line 72–82) and `src/components/cards/card-search.tsx` (242 lines): state 66–86 (`loading` 85, `error` 86), the search fetch 112 and the options fetch 138, ONE flat filter row `flex flex-wrap items-center gap-3` (150) holding the name `<input type="search" … autoFocus … h-9 … max-w-sm>` (151–158), the type `<select … h-9>` (159–169), the color `role="group"` of `ColorChipButton`s (170–187, `showLabel` for One Piece only), the trait input + `<datalist>` (188–204); the live line "Loading…" / "N cards" / "Error: …" (208–213); the results `ul.mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5` (216) of `CardImage src alt width={488} height={680} frame` (223); "Load more" (233–236, reads "Loading…" while fetching). No grouped filters, no selected-filter chips, no skeleton — "Loading…" text is the only loading state.
* Motion today: everything decorative in `src/` is `motion-safe:` except the shadcn primitives' enter / exit classes (dialog, alert-dialog, popover, hover-card, tooltip, dropdown-menu, select, navigation-menu — the backstop's job), `Skeleton`'s `animate-pulse` and the toast's `animate-spin` loader (`toast.tsx` 157). The backstop (`src/app/globals.css` 176–184) collapses every animation and transition to 0.01 ms under `prefers-reduced-motion: reduce` (iteration count 1, `scroll-behavior: auto`). JavaScript motion respects it too: `use-count-up.ts` reads the media query (22–28) and jumps; `ambient-art.tsx` drops the previous crop at once (83–93); `CardImage`'s fade and lift are `motion-safe:` (`card-image.tsx` 47, 100). The pane cannot emulate the media query; prove reduced motion by (a) the class audit — every `animate-*` / `transition-*` outside `src/components/ui/*` carries `motion-safe:` (a grep you print), (b) the compiled CSS carrying the backstop, and (c) jsdom tests that stub `matchMedia` to `reduce` for the JS paths (the count-up test does this already; add the dealt hand).
* Contrast: `src/lib/theme/tokens.ts` is the one source (light 19–46, dark 49–76; its docblock 11–14 records the measured ratios), pinned against `globals.css` by `tokens.test.ts`. One number disagrees: §1's table says secondary text `#5F6775` on `#F7F7FB` is 5.7:1; the file says 5.3:1, and the arithmetic gives 5.33:1 — measure everything yourself and correct §1 in the docs commit. Colors used OUTSIDE the tokens: the Warden line `text-emerald-700 dark:text-emerald-400` (`validation-panel.tsx` 71; `analytics-blocks.tsx` 78; `deck-text-view.tsx` 111), the warning `text-amber-700 dark:text-amber-400` (`validation-panel.tsx` 107; `analytics-blocks.tsx` 79) and `bg-amber-500` dots / rings, the inactive tab triggers at `text-foreground/60` (`tabs.tsx`), the credit chip and the attribution at `text-[0.65rem] text-muted-foreground` (10.4 px still needs 4.5:1), placeholders, `disabled:opacity-50`. Measure with a WCAG contrast function over `getComputedStyle` colors in the pane (resolve alpha over the real background) on every surface in both themes and print the table.
* Status by color only, found at `d4ccf1f`: the grid view's validation rings (`deck-grid-view.tsx` 63–69 — the button's only name is `Show {name}`, 62) and the leader zone's (`leader-zone.tsx` 96–101; 95); the `STAT_TONE` number (above); the text view's severity dots are fine (`aria-label` "Has a problem" / "Warning", `deck-text-view.tsx` 96–101), so are the validation summary (text), the completion ring (a Meter with value text), the save slot (text), the owned mark (✓ + label), `ColorChip` (an sr-only name, `color-chip.tsx` 142), the bar's active tab (`aria-selected` + the indicator + text color), the toast types (icon + title).
* Touch targets at `d4ccf1f`: R4's `pointer-coarse:` rules cover the editor's bar, search rows and their actions, deck rows / steppers / remove, the segmented items (`touch` prop, editor only), Add cards, the strip, the Analytics trigger, the sheet and drawer close controls. Still 28 px (`size="sm"`) or 24 px (`xs`, `icon-xs`) on a phone: the editor header's Share (`editor-header.tsx` 111), Tools (118), More `icon-sm` (128), Retry `xs` (173); the appearance trigger (`appearance-menu.tsx` 44, `sm`); the site header's links and the phone menu (`site-header.tsx` 30–80); the sample hand's three buttons; the validation chips (`validation-panel.tsx` 146–153, `px-1.5 py-0.5`); the tag editor's remove (`card-detail-pane.tsx` 111–118 — a 12 px icon), presets (145–153) and Add tag input (124–139); `GameSwitch` pills (`game-switch.tsx` 43, `px-2 py-1`); `ColorChipButton`s on `/cards`; the share page's Copy (`deck-share-view.tsx` 305 / 312), Like / Bookmark (`engagement-buttons.tsx` 53 / 62 / 107 / 112 / 145), Fork (`fork-button.tsx` 33 / 62) and its `Segmented`s without `touch` (364–366); the panels' `xs` buttons (recommendations-panel ×3, combo-radar-panel ×3, search-pane's Retry); the dialogs' `xs` / `sm` (history-dialog ×5, import-export ×6, details-dialog, share-dialog ×2); the account / folder / collection forms (`folder-controls.tsx` ×4, `new-folder-form.tsx` ×3, `collection-import.tsx` ×7, `delete-account.tsx` ×2, `username-form.tsx`) — signed-out pane, so RTL / class pins carry those; the toast's action and close; the dialog close. The Button base (`src/components/ui/button.tsx` 7) has no coarse rule. Inline links inside prose (the footer's Legal / Privacy, "Card page") are exempt (WCAG 2.5.8's inline exception) — say so rather than inflating them.
* Layout shift: every image goes through `CardImage` (explicit `width` / `height`, an aspect box for the `null` fallback), the shelves and tiles size by `TILE_IMAGE` (`leader-shelves.tsx` 56–60, `deck-tile.tsx` 64–71), the banner has fixed band heights, the ambient crop is absolute; `document.querySelectorAll("img:not([width])")` on every surface should be empty — print it. `layout-shift` `PerformanceObserver` entries arrive only while the document renders (front the tab; the hidden pane may report none — say so).
* Home LCP: `src/app/(site)/page.tsx` — the hero section (60), `MagicShelf` (85) and `OpShelf` (99) of `CardImage`s WITHOUT `priority` (the only `priority` images are the hub / leader-hub / card-page heroes), `RecentPublicDecks` (104); a guest home costs 3 `[db]` statements (R5a). "Unchanged or better" is proven by: the same LCP candidate and its attributes before and after (the first shelf image's `loading` / `fetchpriority`, the hero text), the HTML byte size and TTFB (curl, `--compressed`), the statement count under `DB_LOG`, and a `largest-contentful-paint` `PerformanceObserver` reading if the fronted tab reports one — the hidden pane emits no LCP entries; disclose which you got.
* Dialogs on a phone: `Modal` (`src/components/ui/modal.tsx` 63 lines) = `DialogContent` `flex max-h-[85dvh] flex-col gap-3 overflow-y-auto text-base` + `sm:max-w-lg` (or `sm:max-w-2xl` when `wide`) (52–53) over the primitive's `max-w-[calc(100%-2rem)] … sm:max-w-sm` popup (`dialog.tsx` 53) — 343 px wide at 375. The six editor dialogs: Details (`details-dialog.tsx` 60), Import (`import-export.tsx` 98 / 125 / 201 — a `rows={12}` textarea at 202–206), Export (228 — `rows={16}` at 229–232), History (`history-dialog.tsx` 280, `wide`, `DeckDiffView` at 391 / 426 / 438, `xs` buttons 349–386), Share (`share-dialog.tsx` 52), Shortcuts (`shortcuts-sheet.tsx` 59); plus the collection wipe (`collection-import.tsx` 443) and the account AlertDialog (RTL only). R4 measured none of them on a phone — that is yours: no horizontal overflow at 375 / 390, the close control reachable, the textareas usable, the diff tables scrolling inside their own container.
* Surfaces and widths for the audit: home, `/commanders`, `/leaders` (both `force-dynamic`, with the List / Grid `Segmented` under `deckwarden:index-view`), `/cards` (both games), `/c/atraxa-praetors-voice` (banner) and `/c/birgi-god-of-storytelling` (front-face art) — and a banned hub if the smoke's pick exists, `/l/enel-op15-058` (gradient), `/cards/54125e08-12ec-435c-b147-91768ee5007d` (Atraxa's card page) and a One Piece card page, `/d/q9j9uxphwp9n` (Magic, art, 95 rows) and `/d/jhr5ax43ewx7` (One Piece, 16 rows), `/u/bobandis6`, `/f/veunykz3yuje`, `/account` (signed out = the sign-in page), `/decks/new` (the picker), the two editors (a Magic draft with the Atraxa seed; the One Piece draft) at every tier with an empty deck and with the cards you add, `/nonsense` and `/c/zz-no-such-commander-zz`, the error page (RTL). Long names: Queza's "Nelson & Murdock: Avocados at Law" and the One Piece "Six DON!!, Endless Thunder"; the widest costs are R3's pins (Prime Speaker Zegana, 5 pips).
* Smoke pins R6 must keep (every one substring-based, so a streamed skeleton does not break them — re-run them all after C9): `seo-smoke` — the hero "Your next great deck starts here.", home's literal `/leaders` and `/cards?game=optcg` links, the canonicals `/cards` / `/cards?game=optcg` / card pages, the sitemaps and the robots chunk count; `hubs-smoke` (64–132) — the index and its slugs, "A typical Commander deck" + "Lands", "Curve of these staples", "Scryfall" + "EDHREC", the CTA href with the oracle id or "Banned in Commander", `art_crop` ⇒ "Wizards of the Coast", no `>Plains<`, the 404; `engagement-smoke` — `♥` in the home HTML (248), "Continue building" signed-in only (256–261), "Like · 1" on the share page (278), "No bookmarks yet" (215); `optcg-smoke` — `/optcg/images/` or "Card image coming soon" (122), no `>USD<` on the One Piece card page; `profile-folders-smoke` — "This folder is private" (312); `account-delete-smoke` — "Danger zone" (135); `optcg-deck-smoke`, `deck-api-smoke`, `versions-forks-smoke`, `collection-smoke` — API only. The editor's compact attribution and the R4 layout are not pinned (client-rendered).
* Tests: 704 green at `d4ccf1f` (97 files). Precedents: `analytics-blocks` has NO test yet; `sample-hand` has none; `card-search` has none — you add them. `use-count-up.test.tsx` stubs `matchMedia` for reduced motion; `editor-layout.test.tsx` stubs `matchMedia` + `ResizeObserver`; `deck-editor.test.tsx` renders the whole editor in draft mode under fake timers (`toFake: ["setTimeout", "clearTimeout"]` only — `findBy*` would spin on a faked clock); `deck-share-view.test.tsx` renders a full 100-card deck with `next/navigation` mocked; `not-found.test.tsx` / `error.test.tsx` pin the shells. Lint: `react-hooks/refs` and `react-hooks/set-state-in-effect` are errors (the previous-render pattern; setState in a timer or subscription callback is fine); every raw `<img>` needs `CardImage` or the eslint-disable line; a raw `<a>` to an internal path fails `@next/next/no-html-link-for-pages`.
* Corpus and environment: 17 decks / 1 user / 1 folder (above); 4,023 slugged Magic leaders, 142 One Piece leaders. `.claude/launch.json` (gitignored) has `dev-log` (`DB_LOG=1`; `preview_logs search:"[db]"` counts statements); dev output is isolated in `.next/dev`, so `pnpm build` runs beside `next dev`; the dev server DIES with a session gap (R4 found port 3000 free the next day) and a stale one from a finished session may still hold it (`lsof -nP -iTCP:3000 -sTCP:LISTEN`; kill it, then `preview_start {name: "dev-log"}`). Route table: 55 glyph lines at `d4ccf1f`.

Verify-first list (never from memory)

1. C9: the four `loading.tsx` files render a shell that matches each page's real one — the same container, the band at the page's own heights (`h-36 sm:h-44 md:h-56` on the hubs, `h-28 sm:h-36 md:h-44` on the card page, `h-32 sm:h-40 md:h-48` on the deck page), a hero-card box of the hub's aspect, two or three title bars — with `motion-safe:animate-pulse` skeletons and NO text; measure the `<h1>`'s y and the band's height with the skeleton and with the real page at 390 and 1440 (they must match — that is the layout-shift check for C9); a client-side navigation from `/commanders` to a hub shows the skeleton (poll `[data-slot=skeleton]` right after the click; if the hidden pane never catches the frame, prove it by the streamed HTML of a cold dynamic render: `curl` of `/d/q9j9uxphwp9n` carries `data-slot="skeleton"` before the content); ISR untouched (`/c/`, `/l/`, `/cards/[id]` still `●`; on prod still `HIT`); every HTML-reading smoke green after the change.
2. G6: the histogram bars keep `rounded-t-sm`, gain a `motion-safe:` grow-in from a `starting:` (or `animate-in`) state within 400 ms, and keep the value label; on the share page the same bars (the block is shared) — the OG images unchanged (byte counts before and after on dev); under reduced motion (jsdom) no animation classes exist.
3. G9: the Magic curve shows an OUTLINED ghost per bucket at the template's height, scaled on the same max as the bars (so a template bucket never overflows the track when the deck is small), labeled by a visible legend line naming "Curve template"; One Piece's `don-curve` shows none; the editor's Analytics collapsible and the share page's analytics both carry it; the `mana-curve` block's data carries the target (an adapter test) and the renderer knows nothing about Magic (the field is generic).
4. F4: a fresh hand's cards enter with a stagger (`animation-delay` climbing per slot, the whole deal under 400 ms, `fill-mode-backwards`, `motion-safe:` — under reduced motion they simply appear), a new hand or a mulligan replays it (keyed by a draw nonce), the prompt reads as a game prompt — "Keep this hand?" with **Keep** and **Mulligan** (Keep freezes the hand and says so in the live region; Mulligan redraws seven and counts; "New hand" restarts) — with no draw-next; the widget behaves the same on the share page; the phone's Tools tab (R4) still hosts it; the three buttons are 44 px on coarse pointers.
5. `/cards`: the filters in labelled groups (Name · Type · Colors · Traits for One Piece), a visible row of the selected filters with a remove control each and a Clear all, a skeleton grid (the same columns and aspect) while the first response is in flight and "Load more" keeping its own loading label, the URL params (`q` / `name`, `type`, `ci` / `color`, `traits`) still landing preset from hub browse links, the canonicals and the Scryfall / ©BANDAI lines intact, `seo-smoke` and `optcg-smoke` green; both games at 390 and 1440 in both themes.
6. The six widths in both themes, every surface above: screenshots at 390 (custom 390 × 844) and 768 (the tablet preset), measurements at 1024 / 1200 / 1440 / 1920 (`getBoundingClientRect`, `scrollWidth === innerWidth` everywhere; the pane's screenshots at 1024 and above come back empty — R5a); long names truncate or wrap without overflow; empty decks (the One Piece draft, a Magic draft before its first add) and populated ones (Queza's share page, your draft after adds).
7. Keyboard and focus order: Tab through the site header (mark, nav, My decks, Sign in, appearance), a hub, the share page (the actions row, the hover previews open on focus — R5b), the editor at wide (header → search → deck → tools) and on the phone (header → the active pane → the bar; hidden panes contribute nothing — count the tab stops), each dialog (trapped, Escape, focus returned — R1a / R3), the md drawer (non-modal: Tab leaves it; Escape closes; the Close control focusable), the phone sheet (trapped); every control shows a `:focus-visible` ring in the game accent — the hidden pane never matches `:focus-visible`, so pin the classes (`focus-visible:ring-*` present on every new control) and read `--ring` per surface.
8. Touch targets ≥ 44 px on coarse pointers on EVERY interactive control listed under Facts (measure a sample of each on the mobile preset: the editor header's buttons, the sample hand's, the validation chips, the tag editor, GameSwitch, ColorChip buttons, Like / Bookmark / Fork / Copy on the share page, the share page's segmented items, the panels' buttons, a dialog's buttons); desktop density unchanged (measure the same controls at 1440: 28 / 24 px as today).
9. Reduced motion: the class audit printed (zero decorative animations without `motion-safe:` outside `src/components/ui/*`), the backstop in the compiled CSS, the jsdom paths (count-up jumps, the dealt hand renders complete with no delay classes) — and nothing "decorative" hides content: a reader with reduced motion sees every bar, card and toast.
10. Contrast: the measured table (both themes: foreground, muted-foreground, brand text, both accents, destructive, the emerald and amber pairs, the inactive tab trigger, `text-[0.65rem]` credits, placeholders) — every text pair ≥ 4.5:1 (or ≥ 3:1 for ≥ 24 px / bold ≥ 18.7 px); fix anything that fails in tokens.ts AND globals.css (the tokens test forces both) and record the new numbers in §1.
11. Status never color-only: the grid and leader rings gain sr-only severity text (or `aria-describedby`), the stat tone gains a visible or sr-only word, and a screen-reader walk (the accessibility tree via `read_page`) of the deck pane, the validation panel, the analytics and the share page names every state without a color.
12. No layout shift: `img:not([width])` empty on every surface; the skeleton-vs-page heights (1); the `layout-shift` observer reading if the fronted tab yields one.
13. Home LCP: the before / after comparison described under Facts, on dev and prod.
14. The keyboard script and the first-edit contract unchanged (one draft, one POST, deleted after); the phone flow's repeated adds unchanged; `pnpm check` green; the route table diff EMPTY; the OG byte counts unchanged; the API fence diff empty.
15. Deploy verification via the Vercel commit status on the full 40-char sha (from the shell); then the prod pass in §Deployable outcome.

Design decisions to make explicitly (disclose + test-pin each)

* Skeleton anatomy per route: a shared `src/components/surface-skeleton.tsx` (server-safe, no hooks) parameterized by the band height class and whether a hero box exists, used by the four `loading.tsx` files, so the shells cannot drift from `SurfaceHeader`; `Skeleton` becomes `motion-safe:animate-pulse`. Say why the editor routes and the index pages get none (client-rendered / list pages whose content IS the shell).
* G6's grow-in mechanism: Tailwind's `starting:` variant (`@starting-style`, plays on mount without JavaScript, works for server-rendered bars on the share page) versus `animate-in` from tw-animate-css; both `motion-safe:`. Pick one, say why, and pin the class.
* G9's data shape: `target?: { label: string; values: readonly number[] }` on the histogram block (types.ts 132–135), attached by `mtg/analyze.ts` from `MTG_CURVE_TEMPLATE` (one import between two pure adapter modules), rendered generically as outlines behind the bars plus one legend line; the scale includes the target so nothing overflows; `aria-hidden` outlines, a visible legend. Consider and reject drawing it from `recommend.curve` inside the component (the renderer would then know the adapter — "data, not components").
* F4's Keep / Mulligan prompt: the state machine (`none → dealt → kept`), the copy, what Keep does (freeze + announce; no game state beyond that), the stagger's per-slot delay and total (≤ 400 ms), the replay key; the same widget on the share page.
* `/cards` filter groups: `<fieldset>` + `<legend>` (or `role="group"` + `aria-label`) per group, the selected-filter chips (each a button "Remove filter: Type Creature"), Clear all, the skeleton grid; keep every URL param and the `ColorChipButton`s; the `autoFocus` on the name input stays (a page whose purpose is search) — or drop it on phones like R4 did for the editor: decide and say.
* Touch targets: ONE base rule in `buttonVariants` (`pointer-coarse:min-h-11` on the text sizes, `pointer-coarse:size-11` on the icon sizes — every Button on every surface, including the dialogs and forms the signed-out pane cannot open) versus per-site rules; recommend the base rule, keep R4's per-site rules (harmless), and add `pointer-coarse:` to the non-Button controls (chips, the tag editor, GameSwitch, ColorChip, the share page's `Segmented`s via `touch`). Pin the base rule in a button test and measure.
* Color-only status: sr-only text inside the grid and leader buttons ("Has a problem" / "Warning", the same words as the text view's dots) and a word for the stat tone; no new icons unless a tone glyph reads better — say.
* The §1 contrast correction (5.3:1) and any token you change (both files, the test).
* Budget statement for §6: 0 DB, 0 API, 0 wire; C9 adds a streamed fallback per navigation and nothing else; G9 one optional in-process field; no new requests anywhere; route table diff empty; OG bytes identical.
* LATER rows: none fire; add a row only for something real you defer (e.g. an `/cards` filter you could not group without a wire change), with a trigger.

Deployable outcome
`pnpm check` green and deployed; verified on DEV: the four skeletons with matched heights and the streamed shell on the deck page; the bars' grow-in and labels, the Magic curve ghost with its legend and none on One Piece; the dealt hand's stagger and the Keep / Mulligan prompt on the editor (wide and phone) and on the share page; `/cards` with groups, selected-filter chips and the skeleton grid in both games; the six widths in both themes with no horizontal overflow anywhere; the focus-order walks; every listed control ≥ 44 px on the mobile preset with desktop density unchanged; the reduced-motion audit; the contrast table; the sr-only statuses; `img:not([width])` empty everywhere; the home LCP comparison; the keyboard script and one-POST draft contract unchanged (the draft deleted within the session, confirmed by query); every smoke green on dev (collection first, seo last, versions in its own hour); the route table diff empty; the OG byte counts unchanged; verified ON PROD signed out with no edits and no creates: the skeleton shell in the deck page's streamed HTML, the hubs still `HIT` with `x-nextjs-prerender: 1`, `/cards` in both games, `/d/q9j9uxphwp9n`'s ghost curve and dealt hand, `/d/jhr5ax43ewx7` without a ghost, both drafts at 390 and 1440, the home LCP candidate unchanged. Docs in the same package: `REDESIGN.md` progress tracker R6 ticked with the sha and the status line reading R-series complete; the R6 decisions block under §6 (the skeleton anatomy, the grow-in, the ghost's data shape, the prompt's state machine, the `/cards` groups, the touch rule, the color-only fixes, the contrast table and the §1 correction, the reduced-motion audit, the focus walks, the LCP method and numbers, the dev and prod passes, the mints and their deletion); the smoke-pin ledger's R6 row; the unwired-primitives list updated (skeleton wired; what remains); C9's "shimmer" note corrected in the catalog; db:size stated; nothing minted beyond the drafts. Nothing posted, seeded, or simulated; no prod deck created.

Session notes (environment, unchanged from R4 unless noted)

* PATH: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node / pnpm + gh); psql / aws absent — ad-hoc SQL via tsx in `scripts/.tmp/` (gitignored but LINTED; delete only what you add — R4 deleted its four). **A zsh `for` loop in the Bash tool loses PATH after its first iteration** — python with absolute paths (`/usr/bin/git`, `/usr/bin/curl`, `/bin/cat`) or single commands; a bounded `for i in $(seq …); do …; sleep 15; done` polling loop with absolute paths is fine (R4's deploy poll); `timeout` does not exist on macOS. `sed` / `cat -A` mangle the build log's glyphs — python for the route table. Prod curl bodies arrive gzip-encoded: `--compressed`.
* `.env.local`'s `DATABASE_URL` is the Neon pooler prod uses: dev fixtures are real rows. Never mint on prod. Read `rate_limit_counters` before the pass; `smoke:seo` deletes the local counters at its start.
* Dev server: `preview_start {name: "dev-log"}`; first hits compile routes (search 2.2 s, recommendations 3.8 s in R4 — wait past the 200 ms debounce); `pnpm check` before `pnpm build`. In dev, `/decks/new?game=mtg` SSRs the editor with the `wide` server snapshot (prod serves the static fallback) — a dev-only artifact.
* Browser pane: `document.visibilityState` stays "hidden" — rAF never runs, so Base UI exits stall, toasts never auto-dismiss, Tabs panels keep an unsettled `hidden`, `matchMedia` `change` events NEVER fire (R4's probe; `useTier` reads only on load — reload after a resize) and a Drawer's ENTER frame parks the popup at its closed transform (remove `data-starting-style` to read the settled box); `window.BASE_UI_ANIMATIONS_DISABLED = true` makes Base UI treat exit animations as finished; menus and toggle items open from a PLAIN JS `click`; the combobox needs a dispatched `KeyboardEvent("keydown", { key: "Enter", bubbles: true })` (`ctrlKey: true` for the leader add); read React state a tick after dispatching, not synchronously; hover needs dispatched `pointerover` / `pointerenter` / `mousemove` (`pointerType: "mouse"`); programmatic `focus()` never matches `:focus-visible`; `navigator.clipboard` rejects; lazy images may never load (assert `complete && naturalWidth > 0`); `performance.setResourceTimingBufferSize(2000)` early, and the resource list can still miss requests (R4 saw PUTs missing while the POST was there) — the dev server log (`preview_logs search:"PUT /api/decks"`) is the authority for write counts; `resize_window` `preset: "mobile"` (375 × 812, coarse pointer, Android UA — reload after switching), `preset: "tablet"` (768 × 1024, fine pointer), custom `width` / `height` (390 × 844, 1024, 1200, 1440, 1920 — measure with `getBoundingClientRect`; screenshots at 375 / 390 / 768 work, 1024 and above come back empty), then `preset: "desktop"` to reset; the pane's browser profile does NOT survive a session gap (localStorage — claim tokens, `theme`, `deckwarden:*` — came back empty after R4's 20-hour gap): delete drafts and clear keys before you stop; in the deck list `li button` picks a STEPPER (they carry `aria-label`s; the name button does not) — R4 removed two cards that way; the theme is `localStorage.theme` (`"light"` / remove for the dark default) or the appearance menu's radio items (a plain click).
* Prettier reflows scripted-edit anchors: re-grep after `pnpm format`. `*.md` is prettier-ignored. React splits `{"text "}{n}` into two text nodes — render one template string wherever a test or smoke reads the HTML.
* Deploy verification: `gh api repos/Bobandis6/deckwarden/commits/<full sha>/status` from the shell → the "Vercel" context (R4's went green within two minutes); `gh run list --workflow=nightly-ingest.yml` for nightlies. ISR is real on prod only; right after a deploy the hubs read MISS twice before the first HIT (a cold cache, not a regression — R4).
* `pnpm check` takes about a minute; `smoke:seo` mints fixture decks (dev only); `smoke:versions` needs 7 creates in one hour window.

Context, not tasks
R6 closes the R-series (R1a → R1b → R3 → R2 → R5a → R5b → R4 → R6). After it, the standing P4.7 triggers are what remain beside the product (the `img.deckwarden.gg` flip — LATER's r2.dev row, line 51, which unlocks the One Piece thumbnails, tiles, shelf images and the `/leaders` grid —, the Limitless key, feedback, red nightlies) — their own P4.7 round, never a slice of an R-session; and whatever the owner asks for next gets its own plan. Bandai and Azuki permission emails remain unanswered — posture unchanged; One Piece ambient art stays off (REDESIGN.md §3) and the posture line stays on every One Piece surface (the editor joined the list in R4). Cold-start rule holds everywhere: no simulated decks, posts, or metrics, ever. Premium never gates card data. The owner's open items: the phone builder on a real device (R4 — the software keyboard under `Drawer.VirtualKeyboardProvider` and a real safe-area inset are proven by structure only), the Danger-zone look, the signed-in editor look, the Background art toggle, the signed-in home and `/account` tiles, the signed-in deck header and the owner's `/f/` view, the `/account` sections; the owner's own use of the signed-in editor is now evidenced (the 2026-09-12 01:58Z account deck). R4's observations stand: the md drawer covers most of the deck pane at 768 (LATER row 72); the OP promo Nami P-117 has a null cost (row 63); OP quick-add by card number is name-only (row 62); the seo smoke resets the local deck-create counters; the in-group 404 hydrates client-side. The smoke-pin and LATER-row ledgers in REDESIGN.md §6 remain the map.
