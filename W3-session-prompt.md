# W3 session prompt — Account menu + deck quick actions (sign out anywhere, ⋯ + right-click on tiles)

Pull latest, then run W3 — the third Wave-2 package. **`WAVE2.md` is the contract**; read its W3 section (steps 1–6 are the build order), D1 + D2 (the two ASCII specs — they ARE the design), the W3 row of the pin matrix, and REC-3. W1/W2 finished the brand (`e40342d`, `ce9d7c2`); W3 is the first pure-feature package on final tokens: the header account slot becomes a real menu with **Sign out**, and every account deck tile gets ONE shared action list on both a visible ⋯ button and right-click — visibility as one-click radios, folder as a radio submenu (replacing the `<select>`), AlertDialog delete. **No API or schema work**: `PATCH` (visibility/folder) and `DELETE` already exist, owner-gated, at `src/app/api/decks/[id]/route.ts:88,140`.

Pre-flight, in order. (1) **Nightlies green** (`gh run list --workflow=nightly-ingest.yml`). A red run preempts everything. (2) **A warm beta signal outranks a package**: owner posted / feedback / a stranger's 429 / a new issue → run the `P2.9-session-prompt.md` round (MTG) or `P4.7-session-prompt.md` round 3 (OP) as its own session first (2026-09-19 census baseline: 25 decks — 24 MTG + 1 OP, the owner's — 1 user, likes 1 · bm 0 · folders 1). (3) Working tree clean at or after W2's docs commit (feat is `ce9d7c2`). (4) State your baseline: **790 tests / 105 files / 6 pre-existing `no-unused-vars` warnings / 0 errors** (measured 2026-09-19 on `ce9d7c2`), and `pnpm db:size` (262.9 MB on 2026-09-19, alert 350).

## What W3 is NOT (scope fence)

NOT the choose-commander flow (W4), NOT printings (W5/6), NOT "Public profile ↗" in the menu (the client session has no `username` — already a LATER row), NOT bulk multi-select on /account (LATER), NOT any change to the PATCH/DELETE routes, NOT new deps (`@base-ui/react/context-menu` is an export of the installed package — verified). The editor's own delete (`details-dialog.tsx:46`, window.confirm) stays as is — W3 touches /account and the header only. Anything else → `LATER.md` with a trigger.

## Facts you inherit (verified 2026-09-19 against `ce9d7c2` — re-grep lines, trust the shapes)

- **Account slot**: `src/components/auth/account-slot.tsx` — guest branch = styled `<Link>` "Sign in" (:24–31, keep byte-identical: account-delete-smoke pins "Sign in" on signed-out /account); signed-in branch = avatar + name `<Link>` to /account (:36–48). The whole file is client-side ON PURPOSE (server session read = `headers()` in the (site) layout = every ISR page goes dynamic). The menu trigger must stay a real `<button>` (avatar + name; name `sr-only` below `sm`), 44 px coarse.
- **Sign-out logic** to share via one hook: `src/components/auth/sign-out-button.tsx` (pending flag → `authClient.signOut()` → `router.refresh()`). D1 adds: on failure the menu stays open, item reads "Couldn't sign out — try again".
- **Primitives**: `ui/dropdown-menu.tsx` has `DropdownMenuLinkItem` (:105) — the `site-nav.tsx:66-70` render={<Link/>} closeOnClick pattern; `ui/alert-dialog.tsx` EXISTS (the delete dialog is new usage, not a new primitive); `ui/toast.tsx` exports `Toaster` (:222) and the module-level `toast` manager (`createToastManager()`, :17). **No `ui/context-menu.tsx` yet** — write it by hand (~40 lines: Root/Trigger/Content reusing the dropdown popup classes; `@base-ui/react/context-menu` re-exports the Menu parts so the existing `DropdownMenu*` item wrappers render inside it — package export verified in its package.json).
- **DeckTile**: `src/components/deck/deck-tile.tsx` — `<li>` root at :45 (spread `...rest` onto it for `ContextMenuTrigger render={<DeckTile/>}`; it stays directive-free), stretched link via `after:absolute after:inset-0` at :87 with `title={linkTitle}` (:85), `actions` slot at :112–113 (already `relative z-10`).
- **Account page**: `src/app/(site)/account/page.tsx` — `DeckItem` at :65 (server component; renders `DeckFolderSelect` at :86 and the "Share page" link at :93). Per the contract: DeckItem renders a **(new)** client `account-deck-tile.tsx` with `tile` computed server-side and passed as plain data; the folder `<select>` (`src/components/folders/deck-folder-select.tsx`) becomes the menu's radio submenu; "Share page" stays; add `id="bookmarks"`.
- **Visibility local state**: resync to props with the adjust-during-render pattern — precedent at `src/app/decks/new/new-deck-chooser.tsx:38`. **No `router.refresh()` after PATCH** (it bumps `updated_at` — a refresh reorders the grid under the pointer); Undo toast 5 s; failure reverts + toasts. Delete → `DELETE` → `router.refresh()` + "Deck deleted" toast, NO Undo (hard delete; the AlertDialog with Cancel-focused is the guard).
- **Test pins**: `site-header.test.tsx:77-84` is the signed-in slot test to REWRITE (slot becomes a `button` named after the user; add an open-menu test using the file's `open()` helper — pointerDown + mouseDown + click); the `:52-53` negative regexes stay safe because the popup is portaled. `deck-tile.test.tsx` must stay green untouched. Smokes: `smoke:account`, `smoke:profile`, `smoke:engagement`.
- **D1 menu contents** (label = user name, then): My decks → `/account#decks` · Bookmarks → `/account#bookmarks` (new id) · Collection → `/account#collection` · Profile & settings → `/account#settings` · separator · Sign out. Navigation items are LINKS (Enter, middle-click, new-tab all work). Guests unchanged ("Sign in" link; "My decks" stays in the top nav; Appearance stays separate).
- **D2 item list** (ONE shared `deck-action-items.tsx` for ⋯ and right-click): Open in builder · Open in new tab (replaces the native link menu ContextMenu suppresses) · View share page · Copy share link (private deck → "Copy share link (private — only you can open it)"; writes `https://deckwarden.gg/d/<publicId>`) · VISIBILITY radio group at top level (one click; hint "Unlisted: anyone with the link") · Move to folder ▸ (radio submenu: No folder + folders) · Delete deck… (destructive + Trash2). Keyboard: ⋯ is the tab stop after the tile link; Esc returns focus to ⋯. Touch: ⋯ at 44 px is the path — iOS long-press on an anchor opens Safari's preview, so ContextMenu is desktop-only by design. Icons per D0: MoreHorizontal/Trash2/Link2/Globe/Lock/Folder/LogOut; ⋯ gets `aria-label="Deck actions for {name}"`.

## Verify-first list (never from memory)

1. **Base UI ContextMenu parts** against the installed `node_modules/@base-ui/react/context-menu` types before writing the wrapper — what it re-exports from Menu, and the RadioGroup/RadioItem `closeOnClick` default (the contract's risk row: **RadioItem does NOT close on click by default — set it**).
2. **`react-hooks/refs`** is an error rule here: never read a ref during render (R3 lesson); the adjust-during-render resync is state-from-props, not refs.
3. **The AlertDialog focus contract**: Cancel holds initial focus; "Delete deck" is the destructive action; copy per D2 ("This permanently deletes the deck, its version history and its share page. Forks keep their cards.").
4. **Signed-out /account** unchanged in served HTML (account-delete-smoke pins), and the header's guest shape byte-identical (hydration match: server HTML and first client render both show the guest shape).
5. **Sign out end-to-end on dev**: from a site page AND from /account — header shows "Sign in" within one refresh; no ISR page went dynamic (`pnpm build` route table unchanged — zero expected additions this time; /account is already ƒ).
6. **Visibility flip on dev**: tile → Private → share page in a private window shows the denial shell immediately (the share page is force-dynamic for exactly this) and the grid did NOT reorder; Undo restores. Failure path: patch with the dev server's DB unreachable or a 4xx forced — state reverts.
7. **Delete with a fork**: deleting a deck that has forks succeeds and the fork keeps its cards (the route NULLs the self-FK in-transaction).
8. **Right-click never navigates**, the tile's stretched anchor still works and is titled "Edit {name}", and `ul > li` markup stays valid (the trigger renders the `<li>` itself).
9. **Browser-pane caveats** (unchanged): hidden pane freezes CSS transitions AND stalls Base UI exit animations — verify menus via DOM/jsdom, not screenshots; Base UI menus in RTL need pointerDown + mouseDown + click (the `open()` helper).
10. **Smokes on dev**: `smoke:account`, `smoke:profile`, `smoke:engagement` (+ `counters:reset` if the battery fills local counters). Deploy via the GitHub commit status (context "Vercel", full sha); prod pass read-only against the owner's real decks — look, don't touch: **zero PATCH/DELETE against prod rows**; verify menu rendering signed-out (guest "Sign in" + a `/u/` page's tiles show no ⋯ for non-owners… verify the ⋯ renders only where the viewer owns the deck).

## Design decisions to make explicitly (disclose + pin each)

- **Where ⋯ renders**: /account tiles only (D2), or also the home "Your decks" rail? The contract scopes W3 to /account; home rail tiles gain nothing until the same island wraps them — record the call.
- **Menu naming**: Base UI names the popup after its trigger — the account menu announces as the user's name (D1 says that's consistent); the ⋯ menu announces as "Deck actions for {name}".
- **Folder submenu vs keep `<select>`**: contract replaces the select (REC-3); the unresolved-decisions table offers keeping both if the owner files decks often — default is replace, record it.
- **Bookmarked precons note** (future-proofing): the account menu's Bookmarks anchor must not assume deck ownership semantics (W8 precons are bookmarkable, ownerless).

## Deployable outcome

`pnpm check` green and deployed; verified ON PROD read-only: signed-out header/account byte-stable, the new menu code ships dark for guests. Dev-verified (signed-in surfaces aren't reachable on the prod pane — same disclosure as R1b): D1 menu complete with working Sign out (both failure and success paths), D2 list complete on ⋯ AND right-click sharing one item file, visibility radios PATCH + Undo + no reorder, folder submenu moves decks, AlertDialog delete cascades and refreshes, keyboard + Esc-returns-focus, 44 px coarse targets. Route table: **zero additions** — every route keeps its type. Tests: `site-header.test.tsx` rewritten for the button trigger + menu; new RTL for `deck-action-items` (radio reflects visibility; PATCH body; rollback on failure; delete confirm → DELETE); `deck-tile.test.tsx` untouched and green. Docs in the same package: `WAVE2.md` tracker W3 ticked with the sha; a dated ship-note in this file; memory updated; **`W4-session-prompt.md` written** the way this one was. db:size stated. Nothing posted, seeded, or simulated.

## Session notes (environment, unchanged from W2 unless noted)

- PATH per command: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node/pnpm + gh); psql/aws absent. W3 needs no SQL (census via a scripts/.tmp tsx probe if signals need checking — tsconfig-excluded, delete after).
- Dev server: `preview_start {name: "dev-log"}` after `lsof -nP -iTCP:3000 -sTCP:LISTEN`; stop it before `pnpm build` (shared `.next`). `next dev` re-adds the AGENTS.md block — commit it with your work.
- Signed-in testing: the pane is signed out everywhere. RTL covers the signed-in menu (the `session.current` mock in site-header.test.tsx); dev-pane signed-in checks work only if a dev session exists — otherwise disclose, as R1b did. The deck-action flows are testable signed-OUT on dev against a claimed-by-token draft? No — /account tiles need a session; rely on RTL + the API's existing owner-gate tests, and drive the ⋯ menu on a dev-seeded signed-in session only if one already exists. Never mint prod rows.
- Prettier reflows scripted-edit anchors after `pnpm format`; `*.md` is prettier-ignored.
- Prod: curls need `--compressed`; ISR reads MISS twice post-deploy before HIT; deck-create budget 10/h + 30/day per IP — this session creates no prod decks.

## Context, not tasks

Sequence after W3: W4 choose-commander (+ `/commanders?q=`) → W5 printings (+ API + REC-1) → W6 editor printings → W7 buy links → W8a/b precons → W9a/b/c autofill → W10 tournaments. P2.9's and P4.7's standing triggers still exist beside the W-series and get their own round, never a slice of a W-session. Owner decisions of 2026-09-19 stand. Bandai and Azuki emails unanswered — posture unchanged. Cold-start rule holds: no simulated decks, posts, or metrics. Premium never gates card data.
