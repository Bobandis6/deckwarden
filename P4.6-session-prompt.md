# P4.6 session prompt — OP beta launch package (readiness code + the human half)

Pull latest, then run P4.6 — the LAST M4 bullet (plan line 217): "OP beta launch into the fragmented-market opening." Split it exactly the way P2.8 split the MTG beta — **P2.8-beta-launch.md is the precedent, on disk, read it first**: a code/readiness half this session ships and deploys, and a human half committed as `P4.6-op-beta-launch.md` (ordered owner checklist + announcement drafts), because posting, seeding decks, and DNS are owner actions with owner timing. Expect ZERO migrations, a small two-game meta/home widening, funnel paper-cut fixes from one real guest walk, the img.deckwarden.gg branch (below — owner-gated, must not block), the human-half doc, smoke extensions. Nothing gets posted, sent, or seeded by the session — drafts and checklists only; no simulated decks, posts, or emails, ever.

Pre-flight, in order. (1) Last scheduled nightly green (cron 10:37 UTC, drifts to ~13:30–17:40). Context: 2026-09-06's ran green at 13:47 UTC and was the FIRST unattended run with the Limitless step — `window …(15d), keyless`, 12 tournaments re-fetched, merged inserted 0 — that shape is steady state (small windows mostly merge 0/0; occasional small inserts as new events settle are normal, so is a quiet Friday). (2) Limitless key status — the owner's reminder to check was ~2026-09-08, so ASK. Granted → `.env.local` + `gh secret set LIMITLESS_API_KEY` and done (workflow env already wired, sent as X-Access-Key, zero code change); LATER row 58 gains "granted <date>". Not granted → nothing blocks, note it. (3) db:size baseline (248.0MB on 2026-09-05 post-backfill; Neon alert 350MB). (4) `dig NS deckwarden.gg +short` — on 2026-09-06 all four nameservers were still Porkbun; the img.deckwarden.gg branch reads off this.

## What P4.6 is NOT (scope fence)

NOT posting or sending ANYTHING — the announcement, permission follow-ups, Discord messages: drafts land in the doc, the owner sends them (P2.8's rule, unchanged). NOT seeding OP featured brews — owner content, real account, real primers. NOT the OP leader×card aggregate (LATER row + volume trigger stand). NOT deck shelves on OP hubs (the cold-start counter still runs; Limitless finishes are the only community shelf, already shipped). NOT premium/affiliate/monetization work and NOT the Vercel→VPS move (first-affiliate-dollar trigger). NOT Azuki anything (permission unanswered). NOT a home redesign or rebrand — the two-game widening is copy/meta-level, smallest honest diff; /c/'s ~4k indexed URLs and their titles do not move. NOT Cloudflare Web Analytics setup (rides free with the DNS move; owner-optional checklist line, nothing more). NOT the /games deck-rules endpoint even if the key lands (its own later conversation).

## Facts you inherit (verified live 2026-09-05/06 — trust these, spot-check nothing)

- P4.5 shipped (dfa41f9), keyless: nightly Limitless ingest after punk-records; /l/ hubs render the attributed "Top finishes" shelf; corpus 120 tournaments / 1,907 standings over 180d; idempotency proven three ways (local re-run, workflow_dispatch, first scheduled nightly — all merged 0/0/0). `loadTopFinishes(gameId, leaderId)`; optcg `capabilities.tournaments` test-pinned. Enel (`/l/enel-op15-058`, 230 finishes) is the busiest hub — a "genuinely useful hub page" in the playbook's sense.
- The site's default title AND og:title are **"Deckwarden — Commander deck builder"** (src/app/layout.tsx:26, template `%s · Deckwarden`; shared DESCRIPTION const nearby). An OP-beta visitor's first impression is MTG-only. The home hero is static content in src/app/page.tsx (deck rail rides along).
- DNS: Porkbun nameservers as of 2026-09-06. Launch Playbook §10 prescribes the Cloudflare move; P2.8 recorded "stays on Porkbun — not a blocker" for the MTG beta. The OP beta is the LATER trigger for **img.deckwarden.gg**: ALL OP images ride `pub-…r2.dev` (Bandai hotlinks are impossible — CORP same-site), and r2.dev is rate-limited, "not for production."
- The flip mechanism is proven and cheap: punk-records' skip-check tuple includes `image_base` (punk-records.ts ~line 142) — change R2_PUBLIC_IMAGE_BASE (.env.local + Actions secret) and ONE dispatch re-points every image_override (~4.8k), exactly like the P4.1 r2.dev flip. Custom domain attaches to bucket `deckwarden-public` only; `deckwarden-backups` stays private forever (pg/ dumps).
- The OP guest funnel exists end-to-end, each piece smoke-covered separately, never walked as ONE journey: /leaders → /l/[slug] → "Build with this leader" → /decks/new?game=optcg (server-side anon deck + claim_token) → sim-style paste import ("4xOP01-025" / "4 OP01-025", optcg parseDecklist) → exact external_key resolve pass (P4.2) → real validate incl. Bandai bans + symmetric pair bans → DON dashboard (P4.3) → share page.
- seo-smoke pins OP hub OG as "200, png — artless frame by design." OP **deck** share OG behavior is unverified (verify-first 3).
- P2.8-beta-launch.md carries still-live content: the Bandai + Azuki permission letters (SENT, unanswered — posture unchanged), the MTG drafts' voice and disclosure style, the checklist structure, the beta invariant ("honestly useful to one person building one deck before any promotion").
- Prod smoke budget (hardened 2026-09-02): deck-create 10/h + 30/day per IP — read `rate_limit_counters` BEFORE any prod walk that creates a deck; engagement/versions smokes still push unchecked ids — never run those against prod.
- Env state: R2_PUBLIC_IMAGE_BASE in .env.local + .env.example + Actions secret (r2.dev value today); TOPDECK_API_KEY local+Actions; LIMITLESS_API_KEY nowhere (pending); DATABASE_URL everywhere.

## Verify-first list (never from memory)

1. Home + root metadata reality: read page.tsx's hero copy and layout.tsx's full metadata (title/description/OG/DESCRIPTION const) before deciding the two-game phrasing — work from the actual strings, and find every mirror of the old title (OG images? manifest? README?) with a grep, not recall.
2. The OP funnel as ONE guest journey against PROD, budget-aware (read the counters; one deck, reuse it): /leaders → the CTA hub → build CTA → editor in game=optcg → paste a REAL sim-style list (lift codes from a live Limitless decklist page) → resolve/review → save → validate verdicts sane → share page → claim path visible signed-out → mobile viewport pass. File every paper cut; fix the in-scope ones; LATER the rest with triggers.
3. OP deck share OG: fetch a real OP deck's OG image — status, and artless frame vs leader art? Then DECIDE the posture explicitly (artless mirrors the /l/ choice; leader art from our own R2 is arguable but is a posture extension) — record + pin whichever, never drift into one.
4. Custom-domain-on-bucket mechanics for the branch: R2 custom domains require the zone on the same Cloudflare account — enumerate the owner's exact console steps (move nameservers per Playbook §10; attach img.deckwarden.gg to deckwarden-public; wait active; confirm an image serves on the new domain) BEFORE writing the checklist, and only flip env/secret after a test URL serves.
5. Announcement-target rules, current stickies: the OP TCG subreddit of record's self-promo rules + the 2–3 OP Discords' rules — drafts must fit them; r/EDH norms don't transfer.
6. What the drafts can HONESTLY claim about data: read /legal#one-piece and the live footers first; posture = unofficial fan content, no official API exists, community dataset, takedown-compliant, permission outreach sent (never "permitted"); Limitless credited for tournament data.
7. The CTA hub choice from data, not vibes: most finishes × good art × recognizable leader (Enel op15-058 at 230 is the baseline; check whether a Luffy/Shanks-tier name beats it on recognizability with enough finishes).

## Design decisions to make explicitly (disclose + test-pin each)

- Two-game default title/meta (e.g. "Deckwarden — MTG & One Piece deck builder", or widen only home + OG and keep per-page titles game-true): smallest honest diff, MTG page titles untouched via the template, home widening pinned in seo-smoke's home checks.
- Hero copy widening: both games present without burying the indexed Commander identity; the P4.4 home links to /leaders + scoped card search are already smoke-pinned — build on them.
- The img.deckwarden.gg BRANCH: owner does the DNS move before/during the session → complete the flip (console steps + env + secret + one dispatch + site-wide OP image verification on the new domain + LATER row fired). Owner doesn't → ship the runbook as the human-half checklist's TOP pre-announcement item, sharpen the LATER row to "announce only after the flip, or knowingly accept r2.dev throttling," and do not block. The code half must not depend on the branch either way.
- OP deck OG posture (from verify-first 3).
- The human-half doc `P4.6-op-beta-launch.md`, mirroring P2.8's structure: ordered owner checklist (DNS + domain flip; key check; claim/seed real OP decks with primers; dry-run the paste-import promise signed-out on a phone; posting plan per the verified rules; launch-week watch: Sentry, db:size, nightly green, r2.dev behavior, home rail) then the drafts (subreddit long-form with disclosure, Discord blurb, short social) — honest claims only, Deckwarden's actual OP feature set: no account needed, sim-style paste import, legality with Bandai bans + pair bans explained, DON curve dashboard, leader hubs with attributed Limitless top finishes.

## Deployable outcome

Code half deployed and `pnpm check` green; two-game meta live and verified on prod (title + OG actually changed, MTG page titles unchanged); the funnel walk done with paper cuts fixed or LATER'd; smoke extended where the walk found a gap and green against dev; `P4.6-op-beta-launch.md` committed with the checklist + drafts; LATER.md updated (img.deckwarden.gg fired or sharpened; row 58 key status; any new paper-cut rows); db:size stated; browser-verified ON PROD (home, the CTA hub, an OP deck share page — hidden-pane rule). The announcement itself: NOT sent — the package ends where P2.8 ended, with everything ready and the owner at the wheel. M4 exits when the owner posts.

## Session notes (environment, unchanged from P4.5 unless noted)

- PATH: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node/pnpm + gh); psql/aws absent — ad-hoc SQL via tsx in scripts/.tmp/ (tsconfig-excluded but LINTED; delete scratch when done).
- Deploy verification: Vercel commit status (context "Vercel") on the full 40-char sha; `gh run list --workflow=nightly-ingest.yml` for nightlies.
- Browser pane: hidden pane swallows computer clicks AND read_page AND scroll/screenshot state — drive via javascript_tool, verify via DOM/API reads.
- Prettier reflows scripted-edit anchors: after `pnpm format`, re-grep before exact-string edits.
- ISR surfaces lag revalidate windows in prod — assert fresh behavior against dev, existence/reachability against prod; check a route's own `revalidate` before asserting freshness anywhere.
- PROD deck-create budget 10/h + 30/day per IP: read `rate_limit_counters` before the funnel walk; never run engagement/versions smokes against prod.

## Context, not tasks

Bandai and Azuki permission emails remain unanswered — posture unchanged, and no draft may imply permission. The Limitless key reminder (~09-08) is the owner's; pre-flight (2) covers both outcomes. After P4.6, M4 exits with the owner's announcement; M5 (Azuki) stays permission-gated, so the next engineering conversation is feedback-driven post-beta work plus whatever standing LATER trigger fires first (img.deckwarden.gg if deferred; the OP aggregate on volume; collection-mode rows on real users). Cold-start rule holds everywhere. Premium never gates card data. No simulated or seeded anything, ever.
