# R1b session prompt — Shell (site header, `(site)` route group, CardImage, EmptyState, type scale)

Pull latest, then run R1b — the second R-series package. **`REDESIGN.md` is the contract (read §1 Mechanics — containers, typography, icons, motion; §2 "Site shell and homepage" for the header spec; the R1b row in §5; the ledgers, the R1a decisions and the R1b premise correction in §6; and §7 first)**; build plan §6b makes R-packages legal sessions under the CLAUDE.md protocol. R1b is the shell every public page stands in: the `(site)` route group with `SiteHeader` (mark, Build, Browse, My decks, account slot, and the appearance menu moved out of the footer), the contextual game switch on the browse indexes, deletion of the per-page `← Deckwarden` links, `CardImage` replacing the eight raw `<img>` sites with the G2 frame treatment, `EmptyState` plus the one copy fix, the type scale and the three container widths, the F13 logo micro-tilt — and, folded in because the route move is exactly where it could regress, making the three "ISR" pages ISR for real (LATER row 69). Expect ZERO migrations, ZERO new dependencies, no new URLs (a route group is invisible), no data or ingest work, and no visible change to the editor beyond `CardImage` swapping in under the same sizes. The owner's decisions are settled (REDESIGN.md §0 and §6): dark by default, OP ambient art off until Bandai answers, restrained fun, and the header's auth strategy — a client-side session slot, never `headers()` or `cookies()` in a layout. Do not relitigate them.

Pre-flight, in order. (1) **Nightlies since 2026-09-09 all green** (`gh run list --workflow=nightly-ingest.yml`; cron 10:37 UTC drifting ~13:30–17:40; 2026-09-08's ran 14:45Z green on `d4c21e5`, so 09-09 is the first nightly on R1a's code). A red run is P4.7 branch F and takes precedence — fix or report it as P4.7, and R1b waits for its own session (one package per session). (2) Working tree clean at or after `b0e1fcb` (the R1a docs commit) plus this prompt's commit. (3) db:size baseline stated (253.3MB on 2026-09-08; Neon alert 350MB) — R1b adds no rows, state it anyway. (4) QA fixtures unchanged and now fully attributed: the OP deck **"Six DON!!, Endless Thunder" at `/d/jhr5ax43ewx7`** and ALL FOUR public MTG decks (`/d/q9j9uxphwp9n` Queza — most recently updated, `/d/uwvrnv2pv4t6` Atraxa Superfriends, `/d/4c5_9yd9ewdy` Boros Equipment Burn, `/d/end97r37znuv` Nelson & Murdock) belong to the owner's account (read from the DB 2026-09-08 — R1a's "confirm which" is answered). R1a's open gap: the owner-deck editor and the live `/account` Danger-zone dialog were never seen signed-in, because the browser pane is signed out on prod and OAuth cannot be typed; state whether the owner did the two-minute signed-in look, and if not, R1b's prod pass repeats the guest pattern (`/decks/new?game=mtg|optcg`, draft mode, no edits — nothing minted). (5) `pnpm build` runs locally and its route table matches the baseline under Facts — that table is the ISR yardstick (verify-first 1). **No `shadcn add` is expected this session** (everything R1b needs is installed); never `shadcn init`; if an add turns out necessary, the R1a notes in REDESIGN.md §6 tell you what the CLI does to `cn` and `button.tsx`.

## What R1b is NOT (scope fence)

NOT the editor: its header row (mark, game/format chip, name, save slot, Share primary, More menu), panes, search states, thumbnails, sticky headers, Tabs, toasts, Warden moment, completion ring, `idBadge` — all R3. `deck-editor.tsx` keeps its own `<header>` with the `← Deckwarden` text link and stays OUTSIDE the group; R1b touches editor files only where `CardImage` replaces an `<img>` (`card-detail-pane`, `leader-zone`, `deck-grid-view`, `sample-hand`) — same props, same rendered sizes, no behavior change. NOT the art resolver, ambient layers, capability flag, or the appearance menu's "Background art" entry (R2). NOT homepage content: the hero string "Build a legal deck, fast." stays pinned, no two-game cards, no deck tiles, no leader shelves, no `ColorChip`, no index image view (R5a); home only keeps its literal `/leaders` and `/cards?game=optcg` links (pin) and gets the header like every other page. NOT hub, deck, or card gradient/art headers, the `/c/` build CTA (row 63), hover previews, Warden-voiced 404/error/empty copy, account Decks/Collection/Settings sections (R5b) — `EmptyState` is the component plus ONE copy fix, not Warden voice. NOT responsive builder work or the `wide:` breakpoint token (R4). NOT `loading.tsx` skeletons (R6/C9). NOT ⌘K (a LATER row when the header lands and a user asks — write the row if you must, don't build it). NOT the claim nudge (row 62 stays gated). NOT `global-not-found` (experimental, off): the root `not-found.tsx` stays the global 404 and renders headerless this session — acceptable, R5b/F8 owns that page. NOT a Next, React, or Tailwind upgrade. NOT any P4.7 branch unless pre-flight (1) forces it, and then R1b is not this session.

## Facts you inherit (verified 2026-09-08 against `b0e1fcb` — trust these, spot-check nothing)

* **Layout today.** `src/app/layout.tsx`: `<html lang="en" suppressHydrationWarning …>` → `<body>` → `<ThemeProvider>` (`src/components/theme/theme-provider.tsx`, next-themes: class attribute, dark default, system, color-scheme, no transition on change) → `{children}` + `<SiteFooter />`. `src/components/site-footer.tsx` = Scryfall/WotC attribution + Legal / Privacy / Contact / GitHub + `<AppearanceMenu />` (`src/components/theme/appearance-menu.tsx`: Base UI DropdownMenu radio group Dark / Light / System with `closeOnClick`, trigger = ghost `xs` Button "Appearance" with `SunMoonIcon`). No header and no nav component exist. Typed routes: `LayoutRoutes` in `.next/types/routes.d.ts` is `"/"` only (typegen runs inside `pnpm typecheck`).
* **Route inventory (build table at `b0e1fcb`).** Static ○: `/_not-found`, `/decks/new`, `/legal`, `/privacy`, `/icon.svg`, `/robots.txt` (1d), `/sitemap.xml` (1h), `/c/sitemap.xml` (1d), `/l/sitemap.xml` (1d); SSG ●: `/cards/sitemap/0–3.xml` (1d); everything else ƒ Dynamic. **`/c/[slug]`, `/l/[slug]`, `/cards/[id]` and their `opengraph-image` routes export `revalidate = 3600` / `86400` but have NO `generateStaticParams`, so Next never treats them as ISR**: prod serves them `cache-control: private, no-cache, no-store` with `x-vercel-cache: MISS` on every hit, and `.next/prerender-manifest.json` lists only `/cards/sitemap/[__metadata_id__]` under `dynamicRoutes`. Verified at `d4c21e5` too (scratch production build of the pre-R1a tree): NOT an R1a regression — the P2.6 "ISR, revalidate hourly" comments were aspirational from day one. The fix is verified the same way: `export function generateStaticParams() { return []; }` on `/c/[slug]/page.tsx` put it in the manifest, flipped its build glyph to ●, and served `Cache-Control: s-maxage=3600, stale-while-revalidate=…` with `x-nextjs-cache: HIT` on the second request (its OG route stayed ƒ — each route file needs its own). Next docs (`03-api-reference/04-functions/generate-static-params.md`, line 57): "You must return an empty array from `generateStaticParams` or utilize `export const dynamic = 'force-static'` in order to revalidate (ISR) paths at runtime." LATER row 69 records it; R1b fires it.
* **Force-dynamic pages read request data** — none of it may move into a layout: `/` (home, force-dynamic since P2.3), `/account` (`auth.api.getSession({ headers })`), `/u/[username]`, `/f/[publicId]` and `/d/[publicId]` (`getSessionUserId(await headers())`), `/commanders`, `/leaders` and `/cards` (searchParams), `/decks/[id]/edit`.
* **Auth.** `src/lib/auth-client.ts` exports `authClient = createAuthClient()` from `better-auth/react`; it carries `useSession()` (verified in `node_modules/better-auth/dist/client/react/index.d.mts`), `signIn.social`, `signOut`. `SignOutButton` (client) calls `authClient.signOut()` then `router.refresh()`; `SignInButtons` (client) offers Discord and Google with `callbackURL: "/account"`. `/account` signed-out: h1 "Sign in", `SignInButtons`, "← Back to Deckwarden"; signed-in: avatar (`session.user.image` as a plain 48×48 `<img referrerPolicy="no-referrer">`, else an initial in a circle), name, email · providers, `SignOutButton`, sections aria-labelled "Public profile", "Your decks", "Collection", "Danger zone".
* **Back-links after R1a** (all `<ArrowLeftIcon aria-hidden className="mr-1 inline size-4 align-[-0.2em]" />` + text inside a `text-muted-foreground text-sm hover:underline` Link). DELETE — "Deckwarden": `/commanders` ~65, `/leaders` ~81, `/u/[username]` ~101, `/f/[publicId]` ~100 (and "Back to Deckwarden" on its private gate ~70), `/account` ~163 (and "Back to Deckwarden" signed-out ~87), `deck-share-view.tsx` ~186. KEEP — contextual: `/c/[slug]` "Commanders", `/l/[slug]` "One Piece Leaders", `/cards/[id]` "Card search", `/commanders` pagination "Previous"/"Next". KEEP — R3's: `deck-editor.tsx` ~677 "Deckwarden". Buttons reading "Back to Deckwarden" in `not-found.tsx`, `error.tsx`, `private-share-gate.tsx` and the share view's unknown-game state are actions, not chrome — leave them.
* **Containers today** (`<main>` roots): `max-w-2xl` (42rem) on `/account` (both states), `/u/`, `/f/`, `/legal`, `/privacy`, the `/decks/new` picker; `max-w-3xl` (48rem) on `/commanders`, `/leaders`, the share view (`deck-share-view.tsx` ~183) and the `/d/[publicId]` gate wrapper (~143); `max-w-4xl` (56rem) on home; `max-w-5xl` (64rem) on `/c/`, `/l/`, `/cards/[id]`; `max-w-6xl` (72rem) on `/cards`. §1's widths: reading 42rem (= today's 2xl), browse 64rem (= 5xl), wide 80rem (= 7xl). Inner `max-w-2xl` blocks in `recent-public-decks.tsx`, `/c/` (two), `/l/` (one) and the home hero are content widths, not containers — leave them.
* **Type today.** 11 page titles `text-3xl font-bold tracking-tight` (hubs, card page, `/commanders`, `/leaders`, `/legal`, `/privacy`, `/account` signed-out, `not-found`, `error`, `global-error`); 5 `text-2xl font-bold tracking-tight` (`/cards` "Card search", `/u/`, `/f/`, `/account` signed-in name, share view deck name); the picker `text-2xl font-semibold`; the `/f/` private gate `text-xl font-semibold`; the home hero `text-5xl font-bold tracking-tight text-balance` (R5a's — don't touch). Sections: 13 `text-xl font-semibold` and 13 `text-lg font-semibold`; 23 eyebrows already `text-xs font-medium tracking-wide uppercase`; body inherits the root 16px; 46 `tabular-nums` usages. §1 scale: page title 30/36 semibold, section 18 semibold, eyebrow as is, body 14, meta 12, `tabular-nums` on every count. R1a pinned `text-base` inside `Modal` pending this scale (REDESIGN.md §6).
* **The eight raw card `<img>` sites** (each `width={488} height={680}` except the sample hand at 146×204 `small`; `alt` = card or leader name; an `eslint-disable-next-line @next/next/no-img-element` on every one): `/cards/[id]/page.tsx` ~158 (`w-72 rounded-2xl shadow-lg`; fallback "Card image coming soon" when a default printing exists but no embeddable URL, else "No image"); `/c/[slug]/page.tsx` ~136 (`printingImageUrl(printing, "normal")`; fallback "No image"); `/l/[slug]/page.tsx` ~143 (R2 mirror URL via `embeddablePrintingImageUrl`; fallback "Card image coming soon"); `src/components/cards/card-search.tsx` ~227 (`loading="lazy"`, `rounded-[4.75%/3.5%] transition-transform hover:scale-[1.03]`; fallback = the name in an `aspect-[488/680]` box); `src/components/editor/card-detail-pane.tsx` ~51 (`mx-auto w-full max-w-72 rounded-[4.75%/3.5%] shadow-md`); `src/components/deck/leader-zone.tsx` ~56 (`w-full rounded-[4.75%/3.5%] shadow-md`; severity ring on the wrapping button); `src/components/deck/deck-grid-view.tsx` ~47 (`loading="lazy" decoding="async"`, `shadow-sm`); `src/components/deck/sample-hand.tsx` ~77 (`toSmallImage(card.image)`, `title`, lazy). Two more `<img>` are avatars (`/u/` ~110, `/account` ~173) — not card images, leave them. Helpers in `src/lib/cards/images.ts`: `scryfallImageUrl`, `printingImageUrl` (honors `image_override`), `isEmbeddableImageUrl` (Bandai's hosts send CORP same-site — never a `src`), `embeddablePrintingImageUrl` (null instead of a URL browsers refuse), `toSmallImage`. The wire's `card.image` is already a display URL (Scryfall CDN for MTG, the pub-…r2.dev mirror for OP).
* **Copy pins in smokes** (dev-only runs): `optcg-smoke.ts` ~122 — the OP card page HTML contains `/optcg/images/` OR "Card image coming soon"; `engagement-smoke.ts` ~215 "No bookmarks yet" on `/account`; `profile-folders-smoke.ts` ~312 "This folder is private" on a private `/f/`; `account-delete-smoke.ts` ~136 "Danger zone" and ~200 "Sign in" on `/account`; `seo-smoke.ts` ~488 home HTML contains `/leaders` AND `/cards?game=optcg`, ~501 hero "Build a legal deck, fast.", ~509 "Sign in to fork decks". **Negative pins a site-wide header must not trip:** the OP hub HTML must not contain "Staples" or "Budget" (`seo-smoke` ~353); the OP card page no `>USD<` (~374); share pages no "You own" for guests (`collection-smoke` ~517/522); `/c/` hubs no "Top finishes" when there are no rows (`hubs-smoke` ~143/183) and no "Decks with this commander" when empty (`combos-smoke` ~168); the color-filtered `/commanders` page must not contain the top overall commander's `/c/<slug>"` link (`hubs-smoke` ~78/84) — the header may link `/commanders`, never a specific hub. Every header string, menu item and aria-label must stay clear of all of those.
* **Empty states today:** `deck-list-pane.tsx` ~181 "No cards yet — search on the left and press Enter." (the C8 copy fix — a phone has no left; R4 puts Search in a tab), `leader-zone.tsx` ~36 "No {leaderNoun} yet" + " — Ctrl+Enter on a search result adds one." (the suffix only when `onRemove` is set, i.e. in the editor; the share page gets a plain period), `search-pane.tsx` ~253 "No cards match “{query}”.", `history-dialog.tsx` ~333 "No versions yet. …", `recommendations-panel.tsx` ~237 "No suggestions right now.", `/leaders` ~125 "No leaders match that exact color pairing.", `/account` ~262 "No decks in this account yet —" and ~284 "No bookmarks yet — the Bookmark button on any shared deck saves it here." (pinned substring), the `/f/` private gate h1 "This folder is private" (pinned).
* **Browse pages.** `/commanders` (force-dynamic): "Deckwarden" back-link, h1 "Commanders", intro line, `<nav aria-label="Color identity filter">` of Link pills `rounded-md border px-2 py-1 text-sm` (active = `bg-foreground text-background` + `aria-current="page"`), list, Previous/Next. `/leaders` (force-dynamic): back-link, h1 "One Piece Leaders" beside a "Start a deck" Button → `/decks/new?game=optcg`, intro, `<nav aria-label="Color filter">` pills. `/cards`: h1 "Card search" then `<nav aria-label="Game">` with two pills "Magic: The Gathering" → `/cards` and "One Piece" → `/cards?game=optcg` — the markup the game switch should mirror; `/cards` keeps its own (§2). `src/components/deck/segmented.tsx` is a client value/onChange control, not links — the browse switch is links.
* **Home** (`src/app/page.tsx`, force-dynamic): `<main className="… max-w-4xl …">` → hero (h1 hero string; Buttons "Start a deck" → `/decks/new` and browse → `/commanders`; a link → `/account`) → "What Deckwarden does" grid → `<YourDecks />` (`src/components/deck/your-decks.tsx`, client, `<section aria-label="Your decks">` listing the browser's claim-token decks — this is "the browser's guest-deck section"; it has no `id` today) → `<RecentPublicDecks />` (`aria-label="Recent public decks"`) → the "Also speaks One Piece" section with the three literal links → the GitHub line. Home has no back-link.
* **`/decks/new`**: `page.tsx` is a static shell (`Suspense` around `NewDeckChooser`, robots noindex). The chooser (client) reads `?game=` (latched) and renders `DeckEditor` in draft mode; without it, the picker: `<main className="mx-auto flex min-h-screen w-full max-w-2xl …">`, h1 "Start a new deck", one Link card per adapter (`/decks/new?game=<id>`, `replace`). Draft mode mints no row until the first edit; `/decks/[id]/edit` is force-dynamic and renders `<DeckEditor deckId>`.
* **Editor root**: `deck-editor.tsx` ~670 `<div className="flex min-h-dvh flex-col lg:h-dvh" data-game=…>` with its own `<header>` row (back-link, name input, Details / Import / Export / History / Share). The footer renders below it from the root layout ("below the fold, intentional" — P1.8 attribution gate).
* **Mark**: `src/app/icon.svg` — 64×64 viewBox; shield path `fill="#4338ca"` with a `#312e81` 2px stroke; a card group `rotate(-8 32 31)`: `#a5b4fc` back card at 0.85 opacity (`rotate(14 …)`), `#eef2ff` front card, `#6366f1` art block, two `#c7d2fe` text lines. Never rendered in the app today.
* **Primitives installed and unwired** (R1a): avatar, badge, collapsible, drawer, hover-card, input, meter, navigation-menu, popover, progress, scroll-area, select, separator, sheet, skeleton, tabs, toast, toggle, toggle-group, tooltip in `src/components/ui/`; dropdown-menu is wired (appearance menu) and is the Base UI Menu §2 names for the phone collapse; dialog/alert-dialog are wired. lucide is the icon set (`*Icon` names, `aria-hidden` beside text, `aria-label` when alone; Button sizes svgs, standalone `size-4`). Focus rings read `--ring` (brand outside `data-game` surfaces, the game accent inside).
* **Motion**: decorative motion is `motion-safe:` by convention; the reduced-motion backstop in `globals.css` zeroes everything else. 120 ms controls, 250 ms crossfades, ≤ 400 ms one-offs.
* **Tests**: RTL house pattern — `vitest.config.mts` has `setupFiles: ["src/test/setup.ts"]` (cleanup); precedents `src/components/ui/modal.test.tsx` and `src/components/auth/delete-account.test.tsx` (mocks `next/navigation`). 508 tests green at `b0e1fcb`; 6 pre-existing `no-unused-vars` warnings, 0 errors.
* **Next 16 route-group facts** (docs read 2026-09-08: `03-file-conventions/route-groups.md`, `not-found.md`, `layout.md`): a `(group)` folder is invisible in URLs; a group's `layout.tsx` nests inside the root layout (one root layout, so no full-page reloads); routes in different groups must not resolve to the same path; the root `not-found.tsx` handles unmatched URLs (a `(site)/not-found.tsx` would only serve `notFound()` calls inside the group); `global-not-found` is experimental and off. Metadata routes (`sitemap.ts`, `opengraph-image.tsx`) live with their segment and move with it.
* **Prod cache headers (2026-09-08)**: `/legal`, `/decks/new` → `x-vercel-cache: HIT` / `PRERENDER`, `x-nextjs-prerender: 1`; `/c/…`, `/l/…`, `/cards/<id>`, `/commanders` → `private, no-cache, no-store`, `MISS`. `pnpm build` writes `.next/` while `next dev` uses `.next/dev` — they coexist; `pnpm start` warns about `output: standalone` but serves (or run `node .next/standalone/server.js`).

## Verify-first list (never from memory)

1. **ISR, proven not assumed.** After the `generateStaticParams` additions and the move: `pnpm build`, then (a) `.next/prerender-manifest.json` → `dynamicRoutes` lists `/c/[slug]`, `/l/[slug]`, `/cards/[id]` (plus the OG routes if you give them one — decide, disclose); (b) the build table shows ● for them while `/legal`, `/privacy`, `/decks/new`, `/_not-found`, robots and every sitemap keep today's glyphs and revalidate columns; (c) served locally, the second hit is `x-nextjs-cache: HIT` with `s-maxage=3600`, and the force-dynamic pages still say `private, no-cache`. On prod after deploy: `curl -sI` twice → `x-vercel-cache: MISS` then `HIT` (or `STALE`), `cache-control: s-maxage=3600, stale-while-revalidate`. A group layout that touches `headers()`, `cookies()`, `searchParams`, `connection()` or `noStore` flips all of it back silently — the account slot is client-side for exactly this reason.
2. **Typed layout props for the group**: run `pnpm typecheck` (typegen) and read `LayoutRoutes` in `.next/types/routes.d.ts`; use the key it generates for `src/app/(site)/layout.tsx` (expect `"/(site)"` or `"/"` — don't guess; `layout.md` documents `LayoutProps`).
3. **Every URL unchanged after `git mv`**: diff the new route table against the Facts list line by line; `pnpm smoke:seo` (sitemaps, canonicals, robots, JSON-LD, OG endpoints travel with their segments) and `pnpm smoke:hubs` against dev.
4. **Header ≠ request data**: grep `src/app/(site)/layout.tsx` and the `SiteHeader` tree for `headers(`, `cookies(`, `searchParams`, `connection(`, `noStore` — zero hits. The server HTML of `/legal` contains the header markup and no user name (`curl`); the signed-in slot is a component test with `useSession` mocked (the pane is signed out on prod, smokes are curl-level).
5. **Static shells stay static with the header**: `/legal`, `/privacy`, `/decks/new` still ○ locally and `x-nextjs-prerender: 1` on prod.
6. **`CardImage` on all eight sites, both games, both themes**: explicit width/height so nothing shifts on load (measure the box before and after `load` via JS); lazy below the fold, `priority` (eager) for the hub hero and card page; fade-in `motion-safe:` only; the frame ring in the game accent on hover/focus-within and the 2 px lift `motion-safe:`; fallback text "Card image coming soon" exactly where it is today (OP card page, `/l/` hub — smoke pin) and the name box elsewhere; full-card renditions only, the artist/© line never cropped; the eight `eslint-disable` lines gone, one attribution comment in the component.
7. **`EmptyState`**: mark + title + hint + optional action; the deck-list copy no longer says "search on the left"; "No bookmarks yet", "This folder is private", "Danger zone" byte-identical (pins).
8. **Phone collapse**: at 390 px (`resize_window` mobile preset) the links live in the Base UI Menu behind a `MenuIcon` trigger with `aria-label`, Escape closes it and focus returns; at ≥ md the inline links show. Drive via javascript_tool (hidden-pane rule), screenshot the fronted tab.
9. **One appearance control per page**: in the header on `(site)` pages, still reachable on the editor routes (decision below); a theme switch in a draft editor still creates no deck (`read_network_requests` for `/api/decks` on `/decks/new?game=…`).
10. **Smokes against DEV only**, all green before deploy: `seo`, `hubs`, `engagement` (♥ + "No bookmarks yet"), `profile` ("This folder is private"), `account` ("Danger zone", "Sign in"), `optcg` ("Card image coming soon" / mirror). Never against prod (deck-create budget; fixture rows).
11. **Deploy verification** via the Vercel commit status on the full 40-char sha; then the prod pass in §Deployable outcome.

## Design decisions to make explicitly (disclose + test-pin each)

* **Route group shape**: `src/app/(site)/` gets `layout.tsx` (a plain sync component: `<SiteHeader />` then `{children}` — nothing async, no request data) and, via `git mv` so history follows, `page.tsx` (home), `account/`, `c/`, `cards/`, `commanders/`, `d/`, `f/`, `l/`, `leaders/`, `legal/`, `privacy/`, `u/` — whole folders, so each segment's `opengraph-image.tsx` and `sitemap.ts` travel with its page. Stays at the root: `layout.tsx`, `globals.css`, `icon.svg`, `favicon.ico`, `robots.ts`, `sitemap.ts`, `not-found.tsx`, `error.tsx`, `global-error.tsx`, `api/`, `decks/`. The editor routes stay outside the group; the `/decks/new` picker renders `<SiteHeader />` itself above the cards (§2), the editor branch of the chooser does not.
* **Footer and the appearance menu**: the root layout keeps `<SiteFooter />` on every page (attribution is site-wide — the P1.8 gate). `AppearanceMenu` leaves the footer for the header. To keep it reachable on the two editor routes until R3 builds the editor header, either (a) `src/app/decks/layout.tsx` wraps those routes and renders the footer WITH the menu while the root footer renders it without (a `showAppearance` prop), or (b) the editor's existing header row gets the menu now (crosses the fence into `deck-editor.tsx`). Recommend (a). The test: exactly one appearance control in the DOM of every page.
* **`SiteHeader` composition** (server shell, client islands): the mark linking `/` (inline SVG copied from `icon.svg` — keep its colors literal, they are the brand, not theme tokens; `aria-label="Deckwarden"` on the link; F13 micro-tilt `motion-safe:transition-transform motion-safe:duration-150 motion-safe:hover:-rotate-3` or similar, nothing under reduced motion) · "Build" → `/decks/new` · "Browse" as a DropdownMenu (Commanders `/commanders`, Leaders `/leaders`, Cards `/cards`) · "My decks" · `AccountSlot` (client) · `AppearanceMenu`; `role="banner"` landmark; on phones a `MenuIcon` trigger opening a Base UI Menu with the same items. "My decks": signed-in → `/account`; guest → home's guest-deck section — give `YourDecks`' section `id="your-decks"` and link `/#your-decks`. Since sign-in state is client-side, render the guest href in the server HTML and let the client slot swap it.
* **Account slot hydration**: the server HTML renders the signed-out shape (a "Sign in" link to `/account` — what a guest sees, and `account-delete-smoke`'s "Sign in" pin keeps passing); after `useSession` resolves, the client swaps to the Avatar primitive (`session.user.image`, initial fallback) + name linking `/account`. Render identical markup on both sides and update from `isPending`/the resolved session — `suppressHydrationWarning` is not the tool here.
* **Game switch on the browse indexes**: one `GameSwitch` component — `<nav aria-label="Game">` of two Link pills (Magic: The Gathering → `/commanders`, One Piece → `/leaders`) with `aria-current`, mirroring `/cards`' pill markup — placed above the filters on `/commanders` and `/leaders`; `/cards` keeps its own pills (adopt the component there only if the markup is identical).
* **Containers**: add `--container-reading: 42rem`, `--container-browse: 64rem`, `--container-wide: 80rem` to `@theme` and verify the utilities Tailwind 4 generates (`max-w-reading` …). Mapping: reading → `/account`, `/u/`, `/f/`, `/legal`, `/privacy`, the picker; browse → `/commanders`, `/leaders`, `/c/`, `/l/`, `/cards/[id]`, share pages, home; wide → `/cards`. Say which pages visibly change width (`/commanders`, `/leaders`, share 48→64; home 56→64; `/cards` 72→80) and look at each at 1024 and 1440 px in both themes.
* **Type scale**: page titles → `text-3xl font-semibold tracking-tight` (30/36 semibold); the five `text-2xl` titles decided per page (the share-page deck name and the `/account` name may stay smaller — disclose); sections → `text-lg font-semibold` (18); eyebrows unchanged; meta `text-xs`. Body 14: do NOT change the root font size — the editor and every unstyled paragraph would move; apply `text-sm` on prose in the reading containers where it isn't already, and leave `Modal`'s `text-base` pin for R3. Pins: a header test (`role="banner"`, the nav links, the guest "My decks" href) and a `CardImage` test (attributes, lazy default, fallback).
* **`CardImage` API**: `{ src: string | null; alt: string; width: number; height: number; className?; priority?: boolean; frame?: boolean; fallback?: ReactNode }` — `loading="lazy"` and `decoding="async"` unless `priority`; width/height always on the element; fade-in through an `onLoad` state with `motion-safe:transition-opacity`; `frame` opts into the G2 treatment (`rounded-[4.75%/3.5%]`, `ring-accent-game` on hover/focus-within, `motion-safe:hover:-translate-y-0.5`, a theme shadow) so the hub heroes (`rounded-2xl shadow-lg`) and the sample hand keep their look; the fallback is the caller's text in an aspect box. The attribution comment (Scryfall CDN / R2 mirror, full-card renditions keep the credit line) lives once in the docblock.
* **`EmptyState` API**: `{ title: string; hint?: string; action?: ReactNode; mark?: boolean }` — shield mark only, no character asset; deck-list copy → "No cards yet — add them from Search." (R3 keeps the keycap hint beside it). Adopt it on the deck list, the leader zone, `/account`'s two, `/leaders`' one — with the pinned strings intact.
* **ISR fix**: `export function generateStaticParams() { return []; }` on `/c/[slug]/page.tsx`, `/l/[slug]/page.tsx`, `/cards/[id]/page.tsx`; decide the three matching `opengraph-image.tsx` routes (`revalidate = 86400`, the same rule applies — recommend yes; `/d/[publicId]` and its OG stay force-dynamic by design). `dynamicParams` stays the default `true`. Comment the rule at every site so the next reader doesn't delete an "unused" export. Mark LATER row 69 FIRED with the commit.

## Deployable outcome

`pnpm check` green and deployed; then verified ON PROD in both themes: every non-editor route shows the header (home, `/commanders`, `/leaders`, `/cards`, `/cards/<id>`, `/c/syr-konrad-the-grim`, `/l/enel-op15-058`, both share pages, `/u/<owner username>`, a `/f/` page or its private gate, `/legal`, `/privacy`, `/account` signed-out, the `/decks/new` picker); the two editor routes show no site header and exactly one appearance control; the header collapses at 390 px and the menu traps and restores focus; the ISR trio serve HIT on the second request and the static shells stay prerendered; `CardImage` on the hub heroes, the card page, the `/cards` grid, the share-page leader and grid view, and the sample hand, with no layout shift on load; `EmptyState` visible in a guest draft editor's deck list (and on `/account` if the owner's session is available — else the component test stands in); every pinned string present; a theme switch in a draft editor creates no deck; smokes green on dev (verify-first 10). Docs in the same package: `REDESIGN.md` progress tracker R1b ticked, LATER row 69 marked FIRED with date + commit (keep the row for the note), any decision above that deviated from §1/§2 recorded under §6 open items, the unwired-primitives list updated. db:size stated. Nothing posted, seeded, or simulated.

## Session notes (environment, unchanged from R1a unless noted)

* PATH: `export PATH="$HOME/.local/node22/bin:$HOME/.local/bin:$PATH"` (node/pnpm + gh); psql/aws absent — ad-hoc SQL via tsx in `scripts/.tmp/` (tsconfig-excluded but LINTED; `banlist-check.ts` and `browser-fixture.ts` there predate you — delete only what you add).
* `.env.local`'s `DATABASE_URL` is the same Neon pooler prod uses: dev fixtures are real rows. Smokes clean up after themselves; you mint nothing by hand.
* `pnpm build` (Turbopack) coexists with `next dev`; `.next/prerender-manifest.json` is the ISR truth; a scratch worktree needs its own `pnpm install --offline` (Turbopack rejects a symlinked `node_modules`).
* Deploy verification: Vercel commit status (context "Vercel") on the full sha; `gh run list --workflow=nightly-ingest.yml` for nightlies.
* Browser pane: a hidden pane swallows computer clicks, read_page and scroll state — drive via javascript_tool, verify via DOM/API reads; screenshots work on the fronted tab (`tabs_select` first); `resize_window` presets for the 390 px check; all prod tabs share one origin, so a theme chosen in one tab shows in the others (clear `localStorage.theme` when done). The pane is SIGNED OUT on prod and OAuth cannot be typed: signed-in states are component tests or the owner's own look.
* Base UI Menu triggers open on pointerdown/mousedown — a bare `.click()` from JS does not; dispatch the pointer sequence (R1a's scripts show the pattern).
* Prettier reflows scripted-edit anchors: re-grep after `pnpm format`. `*.md` is prettier-ignored.
* ISR is real after R1b: hub and card pages lag up to an hour on prod after a deploy — assert existence on prod, freshness on dev.
* PROD deck-create budget 10/h + 30/day per IP: this session creates no prod decks; never run seo/engagement/profile/account/versions smokes against prod.

## Context, not tasks

Sequence after R1b: R3 (desktop builder — its header hierarchy, search state machine, Tabs, Toast, the Warden moment, the completion ring on Meter) → R2 → R5a → R5b → R4 → R6, each with its own prompt written the way this one was, after the previous package ships. P4.7's standing triggers still exist beside the R-series (img.deckwarden.gg flip, Limitless key, feedback, red nightlies) — their own P4.7 round, never a slice of an R-session. Bandai and Azuki permission emails remain unanswered — posture unchanged; OP ambient art stays off (REDESIGN.md §3). Cold-start rule holds everywhere: no simulated decks, posts, or metrics, ever. Premium never gates card data. R1a's open gap for the owner: a two-minute signed-in look at the editor on `/d/q9j9uxphwp9n` (dialogs, chips, focus rings) and `/account`'s Danger zone. The `wide:` breakpoint token belongs to R4 with the three-pane work; ⌘K stays a LATER row until the header exists and a user asks. The smoke-pin and LATER-row ledgers in REDESIGN.md §6 remain the map.
