# P3.1 session prompt

Pull latest, then kick off P3.1 from deckwarden-build-plan.md — M3's opener
(~line 200): the recommendation engine core. Deterministic candidate filter
(legality / color identity / budget, plus an owned-cards hook that stays
inert until collections land in P3.7) + weighted ranking (edhrec_rank, curve
fit vs the adapter's hub template, combo participation; co-occurrence stays
OUT until a deck corpus exists — record that in LATER with its trigger)
emitting the **evidence payload**: why, with what, how often, source,
confidence. The payload IS the product identity ("explainable deck lab") —
never a bare score, and evidence must name its real source (edhrec_rank /
spellbook / curve-template) with confidence that degrades honestly when a
signal is missing (cold-start rule applied to ranking). No UI this package —
P3.2 renders the panel. Deployable outcome = engine + tests (a lib-only
deploy is legal; site unchanged), plus a dark zod'd route only if it helps
prove the engine end-to-end — state caching intent on anything routed.
Architecture seam before writing code: read Appendix B and decide where
recommend() lives — scoring stays pure (game logic in adapters, core
consumes only the adapter interface), IO is one batched query layer in the
hub/queries.ts style. Expect NO migration: the engine reads existing tables
(edhrec_rank on card_identities, combos, current prices, legalities); if a
migration appears anyway, its reasoning gets eyeballed like its SQL.

Session notes:
- Toolchain: node/pnpm at ~/.local/node22/bin — non-login shells need
  export PATH="$HOME/.local/node22/bin:$PATH". .claude/launch.json
  (gitignored) wraps pnpm dev for the preview runner.
- Before starting: pnpm check on main AND the nightly Action's recent runs
  (two consecutive greens). gh is NOT installed — GitHub REST API via curl.
- Shipped since the P2.8 prompt (P2.8 + three same-day follow-ups — none of
  this is in the plan doc):
  - Real home page (hero + feature cards + rails); smoke:seo still asserts
    home canonical + WebSite/SearchAction JSON-LD.
  - Auth rate limiting rides better-auth's customStorage over the house
    rate_limit_counters table ("auth:" key prefix). Sign-in is 3/10s per
    ip+path — a smoke or script that hammers /api/auth/* will 429 by design.
  - Account deletion: DELETE /api/account + danger zone on /account;
    pnpm smoke:account covers the transaction (likes recount, fork-pointer
    detach, cascades). Backup ships an account-links CSV; restore drill
    checks it.
  - **Lazy deck creation — the big editor change**: DeckEditor now takes
    `deckId: string | null`; /decks/new renders draft mode and NO deck row
    exists until the first real edit (ensureDeck single-flight inside the
    autosave path, URL swapped via history.replaceState). Anything touching
    the editor must respect draft mode. Deck delete lives in the Details
    dialog. Deck name field has a placeholder now.
  - contact@deckwarden.gg is live (footer/legal/privacy) and forwarding is
    verified end-to-end.
- Ranking signals that exist TODAY: card_identities.edhrec_rank (nightly),
  Spellbook combos (P2.5 — loadCombosForCard in src/lib/combos/queries.ts;
  ingest keeps popularity ≥1 or NULL), current prices on printings, the
  adapter hub template's curve/role structure (P2.4), and deck_cards.tags
  (P2.7 tag editor; preset labels come from adapter.hub.roles — one
  editorial source of truth).
- LATER triggers: row "producedMask conditional producers + analytics that
  consume role tags" names the next analytics-engine session — that is THIS
  session; fire it or re-record with reasoning. The Spellbook
  popularity-floor / findForDeck template-semantics row is scoped to the
  Combo Radar package (P3.3), NOT this one — leave it, and score combo
  participation against the tables as they are.
- smoke rules against prod: smoke:seo 42/43 is a pass (sitemap revalidate
  lag — any OTHER failure is real); smoke:decks includes the notes
  round-trip; engagement/account smokes mint signed sessions directly
  (BETTER_AUTH_SECRET pattern in scripts/engagement-smoke.ts).
- Browser pane (memory file has details): hidden pane swallows computer
  clicks silently — drive via javascript_tool (.click(), native value
  setter + input event), verify via DOM/API reads; tall emulated viewport
  via resize_window for full-page screenshots.
- Context, not tasks: owner is mid-seeding (featured brews with primers);
  the beta announcement is deliberately deferred; permission emails to
  Bandai + Azuki Labs went out 2026-08-30. Cold-start rule stands — no
  simulated activity, no waiting on content.
- Protocol as usual: one package, ends deployed and check-green; scope
  leaks to LATER.md with a trigger.
